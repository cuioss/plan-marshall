# Verification — finalize-step-contract-ordering-and-refire-currency

**Verified against:** commit `e0764ab1`   **Landed as:** PR #1309, commit `7566efd95`   **Verdict:** implemented-with-gaps

## Method

What was actually done:

- Read `plan.md` (758 lines) and `report-01.md` (858 lines) in full; extracted the eight deliverables with their *Done when* conditions, the seven Out-of-scope boundaries, the § Expected surface list, the fifteen Claim-labels rows, the six Verification bullets, and every `closes:` annotation.
- Located the landed commit `7566efd95` and read its full `--name-status` list (**62 files, +3377/−443**, one rename of the plan file into its directory). Read the complete diff for every deliverable-bearing hunk.
- Identified the two post-merge commits that amended this plan's own artifacts (`c39c90ddd` #1315, `068270518` #1316), so the report under test is the amended one at HEAD. Also identified that nine landed files were touched again by later work (mostly plan 520 / #1317 on the orchestrator-inbox and landing-payload surface), and re-derived every affected claim at HEAD rather than at the landing.
- **Re-derived population (a) independently.** Wrote my own frontmatter reader mirroring `extension_discovery.find_implementors`' four scan surfaces (phase-6 `workflow/` then `standards/` with `workflow/` precedence; every bundle's `skills/*/SKILL.md`; project-local `.claude/skills/finalize-step-*/SKILL.md`), with the plan's nine-key set. Result at HEAD: **26 implementors**; `reads:` declared by **5** (`finalize-step-sync-baseline` 3, `pre-push-quality-gate` 5, `finalize-step-plugin-doctor` 6 → `worktree`; `finalize-step-print-phase-breakdown` 999, `emit-landing` 1000 → `metrics`); `destroys:` by **2** (`branch-cleanup` → `worktree`, `archive-plan` → `plan-directory`); `records_facts` by **6**; `requires_prompt_fields` by **1**; `verdict_inputs` by **1**. Every cell of the report's baseline table and its eight-cell post-implementation delta table reproduces exactly.
- **Re-derived population (b)** with my own sweep over `marketplace/` and `.claude/` for a `manage-logging work` call carrying `[DISPATCH]`, applying both D1 exclusions → **0 members**. The only surviving occurrences are the three prose lines in `ref-workflow-architecture/standards/dispatch-logging.md`, which quote the shape in order to forbid it and are excluded by construction.
- **Re-derived population (c)**: 2 of 26 implementors carry a `prompt: |` block of their own (`default:pre-submission-self-review`, `default:finalize-step-simplify`); the `Required`-column tables across the 26 docs are exactly two — the self-review prompt-body table (3 rows) and `plan-retrospective`'s CLI table (6 rows) = 9 rows, of which exactly **one** (`candidates`) is a step-specific prompt-body key. `grep -rln "^| Prompt-body field" marketplace/ .claude/ doc/user doc/developer` → **10** docs. Every figure matches the report.
- **Re-derived population (d)**: `grep -rn "^from _dispatch_roster import" test/` → **9** modules, six under `phase-6-finalize/` and three outside it — exactly the set the corrected 040 report clause now names.
- **Executed the plan's test surface.** `python3 -m pytest` over `test/plan-marshall/phase-6-finalize/`, `plan-orchestrator/test_landing_completeness.py`, `workflow-integration-git/`, `manage-config/`, `build-pyproject/`, `manage-execution-manifest/` and `test_lane_refactor_cleanup_sweep.py` → **3528 passed**, zero failures. (`uv` is not on this machine's PATH; the repository's own `.venv-3.14` interpreter was used, which is the interpreter `./pw` provisions.)
- **Replayed nine of the run's own mutations** plus **three new probes of my own**, each through a harness that checks `git diff --quiet` for the target first, snapshots the target's BYTES to a scratch path outside the repository, runs pytest, and restores by copying the snapshot back in a `finally` — never `git checkout`/`restore`/`stash`. Every restore verified sha256-identical with `git status --porcelain <path>` empty afterwards. Results in § Red-first ledger, replayed below.
- **Executed the CLI** rather than reading it: `finalize-steps set-lane --lane minimal` (rejection text, `.plan/marshal.json` unchanged), `plan phase-1-init get --field deep_lane|escalation`, `plan phase-2-refine get --field revalidation`, `plan phase-6-finalize step get --step-id default:pre-submission-self-review`, `plan phase-6-finalize get --field self_review`, and `effort resolve-target --default` (to confirm the `role=default` fallback D5.1 relies on). Also drove `_cmd_quality_phases.cmd_phase` directly against an **isolated temp `PLAN_BASE_DIR`** to probe the `set --field` path without touching the repository's config.
- **Re-derived every § Build gate and § Cost figure** after fetching `refs/pull/1309/head`: `git diff --name-only 32c13fa...db2f61d -- '*.py'` → **28** (9 production / 19 test), `… | wc -l` → **61**, `git rev-list --count 32c13fa..f07f0ea` → **22**, `git diff --shortstat 32c13fa...f07f0ea` → **62 files, +3377/−443**, and `git merge-base --is-ancestor db2f61d origin/main` → **false**. All exact. The temporary ref was deleted afterwards.
- Opened all twelve source `gaps.md` files and extracted the Kind / Severity / *Done when* of each of the **36** gap ids this plan claims to close, then checked each against HEAD — including running the literal grep each *Done when* names.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D1 | Derive the four populations, or HALT | Report carries § Populations naming (a)–(d), the command, and the re-derived size; `040/report-01.md` § D2 asserts no contradicted importer set | Yes | Yes | Yes | Yes | All four re-derived independently: (a) **26**, (b) **0** at HEAD, (c) **1** step-specific Required row, (d) **9** importers. `040/report-01.md` § D2 now names all nine modules verbatim; `grep -rn "^from _dispatch_roster import" test/` returns exactly that set |
| D2 | Six guards seen RED | All six mutations performed, each producing the named failure, each file restored byte-identically; the six tests pass unmutated | Yes | Yes | Yes | Yes | **All six replayed by me**, each RED with the ledger's own message: 230/G2 → `test_era_stamp_fill_runs_between_pr_creation_and_ci_verification` (`assert 23 < 22`); 310/G4 → `test_verdict_token_drives_refire_skip_mapping`; 440/G6 (a) and (b) → `test_every_tabled_refusal_carries_its_section` (both halves); 440/G1 → `test_every_wildcard_free_declared_glob_names_a_tracked_path`; 300/G1 → `TestCanonicalDestroysDeclarationsExist::test_each_canonical_anchor_declares_its_artifact`; 302/G8 → **both** `test_payload_spec_table_names_exactly_the_required_keys` and `test_emit_landing_enumeration_names_exactly_the_required_keys`. Every restore sha-identical. See G3 for a vacuity vector inside D2.4 |
| D3 | Bind prompt-body carriage to its declaration surface | Ghost `Required: Yes` row turns a test red; a declaring generic-template step leaves the suite green; a synthetic `caller_phase` block is exercised; the two-pattern grep returns zero; the fixed link resolves | Yes | Yes | Yes | **Partial** (G1) | Ghost row → `test_input_table_required_keys_equal_the_declaration` RED (`declared=['candidates'] table=['candidates','ghost_field']`). `grep -rn "send a step-specific one\|carry a step-specific field" marketplace/ test/ .claude/` → **0**. `test_dispatcher_supplied_fields_are_not_step_specific` exists with an anti-vacuity companion. Link display text at `ext-point-finalize-step.md:128` now matches its href and resolves. **But** the input-table scope is bound to an undocumented header string — G1 |
| D4 | The finalize config surface names only keys that resolve | Every `Config key` cell resolves; every command in the following block exits 0 and round-trips; `grep -n plugin_doctor doc/user/configuration.adoc` → no hit; `set-lane --lane minimal` rejection contains `step set` | Yes | Yes | Yes | **Partial** (G2) | `grep -n plugin_doctor doc/user/configuration.adoc` → no hit. All three surviving table keys resolve (`get` executed for each → `auto`). `set-lane --lane minimal` executed → `status: error` naming `plan phase-6-finalize step set --step-id <step-id> --param lane --value <value>`, marshal.json untouched. **But** the false-success write path D4's own preamble diagnoses is live — G2 |
| D5 | Hand-written `[DISPATCH]` emissions to zero | Population (b) re-derived → 0; every `effort resolve-target` in the former dispatch-site files carries `--workflow`; no sentence in `planning.md` instructs a hand-written emission | Yes | Yes | Yes | Yes (one disclosed clause) | My own re-derivation → **0**. Five former dispatch-site files carry `--workflow`/`--plan-id`/`--caller` on their resolves; the two doc-echoes have their heading and command block deleted and the sentence rewritten. `planning.md` retains only `[ATTEMPT]` (a different marker, explicitly distinguished). The one uncarried clause (`planning.md:233`) is a zero-emission site the plan's § Out of scope excludes by name — F9, rejected with reason |
| D6 | The `baseline-reconcile` return contract | Three tokens in an authoritative skill doc; three consumer blocks branch on `status`; `grep -rn "auto_reconcilable == false" marketplace/` → 0; no doc claims the probe writes nothing | Yes | Yes | Yes | **Partial** (G4) | All three tokens present in `workflow-integration-git/SKILL.md` and `phase-2-refine/standards/refine-workflow-detail.md`. `finalize-step-sync-baseline.md:87-91` and `branch-cleanup.md:231-235` / `:524-528` each branch on `status` before `classification`. `grep -rn "auto_reconcilable == false"` → 0. The new § Return block documents **all 11** skip reasons the script can emit (re-derived from `_cmd_baseline_reconcile.py`) and both typed errors. **But** that enumeration has no binding test — G4 |
| D7 | Declare the advertised finalize-step facts | ≥1 step declares `reads:`; every token matches a `destroys` token or a documented producer; the vocabulary paragraph cites a real declaration; `emit-landing` declares `work_performed` at every `--outcome done` site; `create-pr`/`branch-cleanup` wire `pr`/`merge_state`; the conformance suite passes | Yes | Yes | Yes | Yes | 5 `reads:` declarations, matching the prose in `finalize-step-order-bands.md:101-119` exactly. `create-pr` → `records_facts: [pr_number]` with `--fact pr_number=` at both terminal branches; `branch-cleanup` → `merge_state` at all eight terminal sites with five distinct values. Mutating `emit-landing`'s false carrier → `test_work_performed_has_a_false_carrier_on_every_declaring_step` RED; mutating `pre-push-quality-gate.order` to 75 → `test_every_reader_runs_before_the_step_that_destroys_what_it_reads` RED |
| D8 | Thirteen statements made true against their substrate | Each item's own source *Done when* holds; the named absence greps re-run after the edit | Yes | Yes | Yes | **Partial** (G8, G9) | All thirteen checked individually — see § Upstream gap closure. Greps run at HEAD: `agents.md` in `pre-push-quality-gate.md` → 0; `unconditional rebase` in `branch-cleanup.md` → 0; `` `outside .plan/` `` under `phase-6-finalize/` → 0; `without a .bot_kind.` in `disposition-to-hint-routing.md` → 0. Settle-band occupancy (3–11, nine steps) matches population (a) exactly. `160/G2`'s substrate guard mutation → RED. **But** a fifth "derived" label survives (G8) and the band occupancy is an unbound restatement (G9) |

## Upstream gap closure

One row per `closes:` id. **36 claimed → 33 CLOSED, 3 PARTIALLY, 0 NOT-CLOSED.**

| Gap | Sev | Verdict | Evidence at HEAD |
|---|---|---|---|
| 040/G3 | medium | **CLOSED** | `040/report-01.md` § D2 now names nine importer modules; my `grep -rn "^from _dispatch_roster import" test/` returns exactly those nine. The paragraph's conclusion is unchanged, as the gap requires |
| 160/G2 | medium | **PARTIALLY** | The module docstring, the function docstring, `pre-push-quality-gate.md:73` and the sibling test docstring all say **RECORDED**; the `spdx-paths` cell is bound by `test_spdx_paths_note_matches_the_gate_it_describes` (mutation → RED). The literal *Done when* ("no document or docstring") is met — but `_gate_coverage.py:417`'s section-divider comment still reads *"Parity population — the derived comparison set"*, twelve lines above the docstring that denies it. See G8 |
| 160/G4 | medium | **CLOSED** | `pre-push-quality-gate.md:367-395` adds the `whole_tree_available == false` variant `"{N} bundles + whole-tree gates green, module-tests UN-GATED (PLAN-08)"` — no "green" for the module-tests dimension — and states Branch A's default inapplicable there. I re-measured its expansion: **67** chars at one digit, **68** at two, exactly as documented |
| 160/G9 | low | **CLOSED** | `pre-push-quality-gate.md:365` links to `external-step-contract.md` § "Required termination" (which states `≤80 characters` at `:52`); `grep -n "agents.md" pre-push-quality-gate.md` → no hit |
| 190/G5 | low | **CLOSED** | Executed: `finalize-steps set-lane --lane minimal` → `status: error` whose text contains `plan phase-6-finalize step set --step-id <step-id> --param lane --value <value>`. `configuration.adoc`'s admonition points the reader at that message. Argparse `choices=` removed; the help string interpolates `_RESOLVED_ASK_LANE_VALUES` |
| 190/G6 | high | **CLOSED** | The four retired rows are gone; the three survivors each resolve (`get` executed → `auto` for all three); the replacement sentence names the owning steps with their `default:` prefix and the canonical transform (`off`→never, `minimal`→always, everything else→auto). ⚠ The *Done when* is doc-scoped and is met; the false-success **write** path this gap's § Why it matters calls "this epic's archetype" is still live — filed as **G2** |
| 190/G7 | high | **CLOSED** | `grep -n plugin_doctor doc/user/configuration.adoc` → no hit. The `set` example became `plan phase-1-init set --field deep_lane --value always` (an improvement on the gap's own suggestion, per finding R10). Same live-write-path caveat as 190/G6 |
| 230/G1 | medium | **CLOSED** | `push.md:65` replaces the hardcoded pair with the discriminator (`mutates_source: true` AND `order > default:pre-push-quality-gate.order`). Its two named examples (`finalize-step-simplify` 8, `automatic-review` 30) both satisfy it against my population (a); `:67` explicitly names `lessons-capture` a non-member because it declares `mutates_source: false` |
| 230/G2 | high | **CLOSED** | Both halves. New derivation test at `test_finalize_edge_ordering.py:422`; my mutation (`order: 21`→`23`) → RED with the ledger's exact message. `230/report-01.md` § D5(a) rewritten to say `test_finalize_edge_ordering.py` derives only the two gate-relative edge families |
| 260/G1 | high | **PARTIALLY** | The third scope exists and bites: injecting `\| \`ghost_field\` \| Yes \|` into the self-review table → RED. **But** the *Done when* says "**any** finalize-step doc", and the parser only reads a table whose first header cell is the literal `Prompt-body field`. Matched control pair proves it: the identical row under `\| Field \| Required \| …\|` in `create-pr.md` → **19 passed, green**; under `\| Prompt-body field \| …\|` → **RED**. See **G1** |
| 260/G2 | high | **CLOSED** | `test_no_orphan_prompt_field_declaration` is now conditional on the step having its own `prompt:` block; the run's own mutation (b) recorded that a declaration with no block fails only the input-table direction. `grep -rn "send a step-specific one\|carry a step-specific field" marketplace/ test/ .claude/` → **0 hits**; the "extras live in its own dispatch body" clause is gone tree-wide |
| 260/G3 | medium | **CLOSED** | One named exempt set (`_EXEMPT_FIELDS` = contract ∪ `{caller_phase, iteration, producer, session_id}`), restated identically at `ext-point-finalize-step.md:48` / `:144-149` and `phase-6-finalize/SKILL.md:195-205`, with SKILL.md pointing at the ext-point as the single definition |
| 260/G6 | low | **CLOSED** | `ext-point-finalize-step.md:128` display text is now `../../../agents/execution-context.md`; joined to that file's directory it resolves to an existing file (checked with `Path.resolve().exists()` → True) |
| 260/G7 | low | **CLOSED** | `_SYNTH_CALLER_PHASE` + `test_dispatcher_supplied_fields_are_not_step_specific`, with an explicit anti-vacuity assertion that the fixture really carries `caller_phase` and `producer`. Both contracts state the same set |
| 280/G2 | high | **PARTIALLY** | Population (b) re-derived by me → **0**; all nine blocks in five files migrated; `planning.md` instructs no hand-written emission. The *Done when*'s middle clause ("every `effort resolve-target` in them carries `--workflow`") is **not** met at `planning.md:233` — a bare `--role phase-3-outline` that carried no `[DISPATCH]` block and so was never in population (b). That is a zero-emission site (280/G3, 280/G7), which the plan's § Out of scope excludes by name. The clause overreaches its own gap; the defect it names is gone |
| 280/G4 | medium | **CLOSED** | Both doc-echoes: heading and command block deleted, sentence rewritten to "passes the dispatch context to its `effort resolve-target`, so the resolve seam emits …", cross-reference kept. Confirmed the dispatcher those sentences describe really does pass the context (`phase-6-finalize/SKILL.md:626-628`, `:955-957`) |
| 300/G1 | medium | **CLOSED** | `TestCanonicalDestroysDeclarationsExist`; my mutation (delete `archive-plan`'s `destroys:` block) → RED with the contract-naming message |
| 300/G2 | medium | **CLOSED** | The Settle band is split into pre-push (1–11) and post-push (12–69); the pre-push row states **no** guaranteed insertion room and names the re-space remedy. Occupancy re-derived: integers 3–11 held by exactly nine steps, matching the prose. See G9 for the binding |
| 300/G3 | low | **CLOSED** | Five `reads:` declarations; `worktree` pairs with `branch-cleanup`'s `destroys`, `metrics` with `record-metrics` (998) as documented producer; `finalize-step-order-bands.md:101-112` cites real declarations rather than only the two `destroys` anchors |
| 300/G4 | low | **CLOSED** | `grep -n mutates_source finalize-step-order-bands.md` → four hits, all pointers at the owning contract (`:11`, `:15`, `:40`, `:41`). No obligation stated in this file |
| 300/G8 | low | **CLOSED** | `_CONFIG_DECIDED_SEED` annotations at `test_declared_step_contract_regression.py:425-430` are `70 / 1100 / 20 / 22 / 992` — every one equal to its step's declared `order:` in my population (a) |
| 300/G9 | low | **CLOSED** | `manage-execution-manifest.py:2282` now reads "carried order 61 **AT THE TIME OF THE INCIDENT**; its standards doc declares order 992 today", and 992 is what `finalize-step-preference-emitter.md:7` declares |
| 302/G3 | medium | **CLOSED** | `create-pr` declares `records_facts: [pr_number]` and wires `--fact pr_number=` at both terminal branches; `branch-cleanup`'s union gains `merge_state`, recorded at all eight terminal sites with five distinct values documented at `:1714`. `landing-payload-spec.md:92-93` Source cells now name the two facts |
| 302/G4 | medium | **CLOSED** | `emit-landing.md` frontmatter declares `records_facts: [work_performed]`; its single `--outcome done` site records `true` and its `loop_back` error branch records `false`; the conformance suite passes with the step in its derived population, and my mutation of the false carrier turns it RED |
| 302/G8 | low | **CLOSED** | `_facts_block` iterates `LANDING_REQUIRED_KEYS` and fails loudly on an unmapped key (`:96-102`); removing `merge_state` from the constant turns **both** new tests RED (`Extra items in the left set: 'merge_state'`), verified separately for each |
| 310/G3 | medium | **CLOSED** | All three tokens documented in `workflow-integration-git/SKILL.md` § Return and in `refine-workflow-detail.md`; all three finalize consumer blocks branch on `status` before parsing `classification`, each explaining why the exit-code test never fires |
| 310/G4 | high | **CLOSED** | `push_barrier_action` is production code (`git-workflow.py:1203`), published as `barrier_action` on every success payload, consumed by `phase-6-finalize/SKILL.md:706-712`; the local oracle is gone. My mutation (return `re-fire` for `remote_absent_landed`) → RED |
| 310/G5 | low | **CLOSED** | `grep -rn "auto_reconcilable == false" marketplace/ .claude/` → **0 hits**; the SKILL.md Return block states the derivation and that it never downgrades an auto-resolvable overlap |
| 310/G8 | medium | **CLOSED** | No document claims the probe "performs only fetch + diff + merge-tree" or writes nothing (the one surviving "performs no writes" is in `marshall-steward/SKILL.md` about an unrelated detection). The stale-`base_branch` write is named in `finalize-step-sync-baseline.md:78` and in the SKILL.md § Side effects; `base_branch_updated` / `original_base_branch` are documented return fields, including the sharp note that they ride the `success` payload only |
| 330/G1 | medium | **CLOSED** | `` grep -rn 'outside `.plan/`' `` under `phase-6-finalize/` → **no hit**; `emit-landing.md:225-231` matches the corrected sibling paragraph in `finalize-step-preference-emitter.md:234-240` word for word on the trackedness keying |
| 410/G1 | medium | **CLOSED** | `grep -n "without a .bot_kind." disposition-to-hint-routing.md` → exit 1. § (e)'s closing sentence is now the recognized-identity form (`:150-155`), and the two sibling surfaces already carried that wording |
| 410/G4 | low | **CLOSED** | § (d) is scoped ("**on this disposition→hint routing path**") and carries the cross-reference to `../workflow/lessons-capture.md`; `audit.py:2229-2238` and `preference-pattern-detector.md:43` carry the same scoping. `lessons-capture.md`'s cross-cutting-`default` sentence survives unchanged at `:132` |
| 440/G1 | medium | **CLOSED** | `test_every_wildcard_free_declared_glob_names_a_tracked_path` with a discriminator control; my mutation (rename one literal glob) → RED naming the step and the glob. See **G3** for the vacuity vector this leaves |
| 440/G2 | medium | **CLOSED** | `grep -n "differs from live HEAD\|re-fires the gate\|re-fires whenever" phase-6-finalize/SKILL.md` returns **one** site, and it defers to Step 3's classifier. The lead sentence defers action as well as membership; the closing summary is qualified. The `field absent` row's unconditional re-fire is deliberately retained, as the plan directs |
| 440/G3 | low | **CLOSED** | `grep -n "unconditional rebase" branch-cleanup.md` → exit 1; both operator-facing sentences route by `use_merge_queue` in the conditional form the rest of the document uses |
| 440/G6 | high | **CLOSED** | Heading-anchored ATX match replaces the substring search. **Both** halves mutated by me — renaming only the `###` heading in `.claude/skills/finalize-step-plugin-doctor/SKILL.md` (cross-reference at `:57` untouched) and only the `##` heading in `pre-push-quality-gate.md` — each → RED |

## Report accuracy

Re-derived every re-derivable figure in `report-01.md`. The substrate figures are **exact**; two counting statements are not.

**Reproduced exactly:** population (a) = 26 with every frontmatter cell; the eight-cell post-implementation delta table; population (b) = 11-at-baseline → **0** at HEAD; population (c)'s 9 rows / 3 prompt-body / 6 CLI split, the single `candidates` hit, the 2-of-26 own-block figure and the 10-doc convention-header count; population (d) = 9 modules; the Settle-band occupancy (3–11, nine steps); the `160/G4` variant expansions (67 at one digit, 68 at two); `28 / 61` files between `32c13fa...db2f61d` with a `9 production / 19 test` split; `22` commits; `62 files, +3377/−443`; `db2f61d` not an ancestor of `main`; every red-first ledger row I replayed, message for message; and the `M = 3` reviewer registry population.

**Two contradictions:**

1. **§ Cost — "52 across five rounds".** § Findings' own stated rule is "one row per **instance**", and finding S10 explicitly settled that "the row count is the figure to trust, because it is the one the table can be checked against". Counting the five tables gives **17 + 10 + 13 + 10 + 8 = 58 instances** (56 table rows). 52 is reachable only by mixing conventions — dropping round 1's three nits and round 5's three minors while keeping round 2's two out-of-round rows. The same 52 shipped in the PR body and therefore in the squash commit message. Recorded as **G6**.
2. **§ Reviewer participation — the sourcery diff-size figure.** The report states `git diff origin/main...HEAD \| wc -c` → 437,707. That command pins one endpoint and lets the other float — the exact defect § Build gate diagnoses and fixes three sections earlier ("A two-endpoint diff needs two fixed endpoints, and `origin/main` is not one"). It does not reproduce: `origin/main...f07f0ea` → **469,876**; `32c13fa...f07f0ea` → **469,876**; `32c13fa...db2f61d` → **430,549**. The substantive claim (≈2.9× a 150,000-character ceiling) holds at every candidate. Recorded as **G7**.

**Noted, not filed.** Two adjacent paragraphs in `pre-push-quality-gate.md` measure `display_detail` lengths by different conventions — the skip variant's "67 before expansion" excludes the `{N}` placeholder, while the un-gated variant's "67 characters at `{N}` = one digit" is the expanded string. Both numbers are individually correct and both strings are far inside the ceiling; a reader comparing the two would wrongly conclude they are the same length. Too small to carry as a gap.

**§ Populations' framing is sound.** The heading now states it is the D1 baseline, the post-implementation subsection derives the shipped state, and both are correct at HEAD. The R1/R2/S1 correction chain converged.

## Out-of-scope compliance

**Clean.** All seven exclusions hold against the landed diff:

- **302/G7 + 302/G5** (registry key + roster row, excluded as a bidirectionally-coupled pair) — no `.plan/` path appears in the 62-file list, and `standards/dispatch-inline-split.md` is untouched. Neither half landed alone, which is what the exclusion warns against.
- **410/G2** (`.plan/project-architecture/default/enriched.json`) — untouched. 410/G4, which the exclusion says makes the excluded work easier, did land.
- **440/G4** (before/after re-fire measurement) — not attempted.
- **230/G4** (unified-barrier fold) — `ci-verify.md` is not in the diff.
- **Unnamed gaps** — the diff introduces no change traceable to a gap id outside the 41.
- **`set-lane` widening / genuinely-derived parity population** — neither taken; both branches the plan pre-decided are the ones implemented, and each says so in the shipped text.
- **Plugin-cache sync** — no `target/` path in the diff and no sync recorded as owed.

## Expected-surface / collateral check

62 landed files, cross-checked mechanically against § Expected surface plus § Collateral check's 18 rows. **Exactly one file is explained by neither**: `test/plan-marshall/phase-6-finalize/test_step_records_facts_contract.py` (+30/−7), which appears nowhere in `plan.md` or `report-01.md` — not by path, not by name. Its change is a real behavioural one (widening the `work_performed` false-carrier guard from `--outcome done` to `{done, loop_back}`), narrated in § What have we learned without the file ever being listed. Verification §6 asks for the file list, and findings S5 and T5 each claimed to complete it. Recorded as **G5**. I confirmed the widened guard is not vacuous: mutating `emit-landing`'s `work_performed=false` to `true` turns it RED.

## Residue the report declares

| Item | Status today |
|---|---|
| Ledger row 160/G2's message cell paraphrases rather than quotes (T8) | **Accurate and still open.** I re-ran that mutation: the emission is the assertion prose plus pytest's `Extra items in the right set: 'targets' / 'build.py'`; the cell reads `missing 'build.py', 'targets'`. The mutation, the named test and the substance are all correct |
| Commit `c92dd11`'s message asserts "all six sub-steps observed" (S8) | **Accurate and unfixable as characterised.** Not re-checked at the commit level (the branch shas are absent from `main`; I fetched `refs/pull/1309/head` only for the diff figures) |
| A residual test module carries the dissolved config block's name | **Still open.** Two `manage-config` test modules named after that block remain; renaming is out of this plan's scope, and the run-report carve-out makes the problem quieter rather than gone |
| `baseline-reconcile`'s fail-closed skips prescribe no consumer control flow | **Still open**, correctly characterised. `merge_base_unresolved` and `head_unresolved` say "Fail closed: skip without a classification" while `fetch_failed` prescribes "Log warning, skip — do not block refine" |
| The `reason` token set is enumerated in one place only | **Confirmed and sharpened.** `workflow-integration-git/SKILL.md`'s § Return table is complete against the script (all 11 reasons, both errors) — but nothing binds it, so it is a fresh instance of this plan's own named mechanism. Recorded as **G4** |
| F9 stands as rejected | **Upheld.** The clause overreaches the plan's own § Out of scope; verified at `planning.md:233` |
| The emitter-admissibility gap is staged as plan 600 | **Confirmed.** `doc/plans/truthful-signals/600-preference-admissibility-is-prose-where-the-auditor-has-code.md` exists |

## What could NOT be verified

- **The five verification rounds and four cold reads as events.** Sub-agent transcripts are not committed. The *outcomes* — every fix each round names — were checked individually against the tree; the cold readers' verbatim answers cannot be re-derived.
- **The historical `./pw verify` figures** (`21103` / `21107` / `21108 passed`, the coverage-dimension line, the 5m38s–5m43s durations, the "12 whole-tree invocations"). These are point-in-time observations against a tree that has since moved eight commits. I did **not** run a full `./pw verify`; I ran the affected test surface (**3528 passed**) with the repository's own interpreter. `uv` is not installed on this machine, so the task's literal `uv run python -m pytest` form was unavailable.
- **The PR #1309 review-thread state** (four CodeRabbit passes, 8 findings, the rate-limit retries, the pr-agent guide, the sourcery refusal text, "10 of 11 threads resolved"). I made no GitHub API queries. The one behavioural finding the report attributes to CodeRabbit — the Branch F `loop_back` retry-against-unchangeable-input problem — is visible in `branch-cleanup.md`'s shipped text and its guard-widening consequence is verified.
- **The operator's merge authorisation** (§ Contract check's one deviation). A conversation event with no committed artifact. That `f07f0ea` was the merged head and is not an ancestor of `main` **is** verified.
- **The mutating half of 190/G6's *Done when*** — the `set` commands in `configuration.adoc`'s code blocks would write the repository's `.plan/marshal.json`, so I executed only the `get` half plus an isolated-temp-dir probe of the `set` code path. The values those commands write (`always` for `deep_lane`, `off` for the step lane) are in the validators' accepted sets, so they would exit 0.
- **The commit-message contents of the 22 branch commits** beyond the two strings S8 and T4 quote.

---

*Verified with nine replayed mutations and three new probes, each snapshot-and-restore outside the repository, every restore sha256-identical and `git status --porcelain` empty for the path afterwards.*
