envelope_version=1
sender_type=plan
sender_id=plan-140-run-3
epic=test-quality
kind=finding
created=2026-09-20T20:13:22Z

# PLAN-140 run 3 (slice 060) — D1 re-derivation (gating)

Command (doctor's own sweep, implementing source `marketplace/bundles/pm-plugin-development/skills/plugin-doctor/scripts/_analyze_test_conventions.py::analyze_test_module_line_budget` via `doctor-marketplace test-conventions`):
- `python3 .plan/execute-script.py pm-plugin-development:plugin-doctor:doctor-marketplace test-conventions --test-root test/plan-marshall`
- Slice filter: 14 PLAN-060 Expected Surface dirs under `test/plan-marshall/` (extension-api, lsp-client, manage-files, manage-logging, manage-providers, platform-runtime, ref-toon-format, script-shared, tools-file-ops, tools-input-validation, tools-permission-doctor, tools-permission-fix, tools-script-executor, untrusted-ingestion), counted from the TOON `issues[]` rows.

Figures at dispatch (2026-09-20, HEAD per session):
- Slice 060 over-budget findings: 66 distinct modules (all `kind: collected`, `test_*.py`; `untrusted-ingestion/` contributes 0).
- Spec table leads: 55 (Over budget at HEAD) / 53 (campaign stale). Delta: +11 vs 55, +13 vs 53. Direction matches the epic's only-rising trajectory; do NOT adopt either lead figure.
- Whole-tree context: this sweep reports 351 `test-module-line-budget` findings under `test/plan-marshall` alone; the spec's whole-tree claim trajectory ends at 362 (fb8aadc9c) with an open 279-vs-362 quantity question (modules vs findings). D1 separates newly-visible (PLAN-105 widening) from new debt: the 66 are all collected modules, so none is newly-visible helper population — the rise is live contention/growth, not instrumentation.
- Slice directory list: taken verbatim from PLAN-060 `## Expected Surface` (14 entries, re-stated above); membership re-derived, not adopted.

HYPOTHESIS verdict: the per-row table is a lead — CONFIRMED stale for run 3. No action on D2-D5 may size from 55/53.

Gating instruments: PLAN-105 landed (#1407) — `_fidelity_diff.py`, `_definition_duplication.py`, `_banner_attribution.py` all present under `test/_shared/`; PLAN-170 shipped (#1385) so per-slice attribution exists; concurrency clear (test-quality queue: only PLAN-140 launched/running, no PLAN-130/135/110/150/155 in flight).

Next: D2-D5 require PR carving (66 modules cannot land as one PR under the 100/300 file-count ceilings that refused run 1's 309-file PR; run 2 shipped as 9 PRs under PLAN-176). Carve decision is still pending per the spec's own claim — see process-compliance filing. No bulk splits were made inline in this session; implementation is staged as plan-lifecycle work, not orchestrator inline work.
