= Skill Handling
:nofooter:
:toc: left
:toclevels: 2

xref:../../README.md[Plan Marshall] » xref:README.adoc[Concepts]

In an unconstrained Claude Code session, "which skill applies here" is a runtime guess the model makes from whatever the prompt happened to surface. Two tasks with similar wording can pull in different skill sets and produce divergent outputs. The skill load isn't even logged in a way that lets you check after the fact. Plan Marshall declines to play that game. Every skill loaded by a subagent is **named by the dispatch site**, resolved against `marshal.json`, materialised into the task record at plan time, and loaded in a deterministic order at dispatch time. The LLM running the subagent has zero discretion about its own context — it loads what it is told.

Two consequences fall out of that:

* **Reproducibility.** The same `(module, profile)` pair produces the same `task.skills` list, which produces the same loaded context across runs.
* **Auditability.** Every `Skill:` load lands in `work.log` with the plan id, the dispatch name, and the skill notation. The log is the authoritative answer to "what was in this subagent's context."

== Three tiers

Every dispatch's context is built from up to three tiers, stacked in order:

[cols="1,1,3", options="header"]
|===
| Tier | Source | Role

| **Tier 1 — System** | `skill_domains.system.defaults` in `marshal.json`. Loaded unconditionally by the dispatcher. | The foundation that *every* subagent gets — `persona-plan-marshall-agent` is the canonical Tier-1 skill, carrying the hard rules.
| **Tier 2 — Domain** | `skill_domains.{domain}` in `marshal.json`, materialised into `task.skills` via per-module `skills_by_profile`. | The domain knowledge appropriate to *this* task: Java patterns, JUnit conventions, frontend ESLint rules, OCI container standards.
| **Workflow + Extensions** | Profile-routed (`execute-task` for phase-5) or finding-routed (`ext-triage-{domain}` during triage). Resolved via `manage-config` lookups and passed through `skills[]`. | The dispatch-site-specific logic: the workflow body that knows what to do, plus per-domain triage knowledge for finding-handling.
|===

Tier 1 always loads first. Tier 2 and the workflow / extension entries load in the order the dispatch site arranged them — typically domain knowledge before workflow logic so the workflow can apply the loaded standards.

== From architecture to dispatch

Skills travel through the planning pipeline as a deterministic data flow. Nothing about "which skills load" is decided by the model at runtime.

image::../resources/diagrams/skill-loading-flow.svg[Skill loading data flow — architecture's skills_by_profile becomes task.skills at plan time, travels through the dispatch verbatim, and is loaded by execution-context as Tier 2 on top of the implicit Tier-1 persona-plan-marshall-agent, align=center]

Each transition is a `manage-*` script call or a workflow-driven assignment, not an LLM inference. The architecture-enrichment step records the per-module `skills_by_profile` map; phase-4-plan materialises it into per-task `task.skills` arrays; the dispatcher reads those arrays and issues a `Skill:` load per entry. A skill that didn't enter `skills_by_profile` at enrichment time will not be in `task.skills` at plan time and will not load at dispatch time. The trail is checkable end-to-end.

== Profiles

The profile is the *category of work* a task represents — production code vs test code vs integration test vs quality / docs. It drives two things: which skills enter the dispatched context (the Tier-2 selection from `skills_by_profile`), and which workflow body executes the task (the system default is `execute-task`, but a domain can override it per profile).

Profile selection happens at architecture-discovery time, signal-based per module: a Java module with sources gets `implementation` and `quality`; with a Failsafe plugin or testcontainers dependency it adds `integration_testing`; a documentation module with `doc/*.adoc` files gets `documentation`. The detection rules per profile live in link:../../marketplace/bundles/plan-marshall/skills/extension-api/standards/profiles.md[`profiles.md`]. A sixth slot, `core`, is special: it carries foundation patterns that always merge into every other profile's load.

== Auditing what loaded

Grep the work log to answer "what did task N actually load." If a skill is missing from the log, it never loaded — regardless of what the workflow body claims to have applied. The work-log line format and the `[SKILL]` category tag live in `manage-logging`'s standards; the dispatch-site instrumentation is in the execution-context agent body. Plan Marshall does not assume the LLM remembered to load anything; the log is the record of what actually happened.

== Related

* link:../../marketplace/bundles/plan-marshall/skills/ref-workflow-architecture/standards/skill-loading.md[`skill-loading.md`] — canonical step-by-step of the two-tier loading plus extensions, including the dispatch-time six-step sequence the `execution-context` agent runs.
* link:../../marketplace/bundles/plan-marshall/skills/ref-workflow-architecture/standards/agents.md[`agents.md`] — agent inventory and the dispatch-vs-context-load distinction.
* link:../../marketplace/bundles/plan-marshall/agents/execution-context.md[`execution-context.md`] — the dispatcher agent body that issues the per-`skills[]` loads.
* link:../../marketplace/bundles/plan-marshall/skills/persona-plan-marshall-agent/SKILL.md[`persona-plan-marshall-agent/SKILL.md`] — the canonical Tier-1 skill.
* link:../../marketplace/bundles/plan-marshall/skills/persona-module-tester/SKILL.md[`persona-module-tester/SKILL.md`] — a profile-driven Tier-2 example (loaded only on `module_testing` profiles).
* link:../../marketplace/bundles/plan-marshall/skills/execute-task/SKILL.md[`execute-task/SKILL.md`] — the default profile-routed workflow body for phase-5 tasks.
* link:../../marketplace/bundles/plan-marshall/skills/manage-config/standards/skill-domains.md[`skill-domains.md`] — the `marshal.json` schema for `skill_domains` (system, per-domain, workflow-skill overrides).
* link:../../marketplace/bundles/plan-marshall/skills/extension-api/standards/profiles.md[`profiles.md`] — profile detection signals and the override mechanism for replacing the workflow body per profile.
* xref:execution-context.adoc[Concepts › Execution Context] — why every dispatch routes through one canonical agent with level variants.
* xref:planning-workflow.adoc[Concepts › Planning Workflow] — where each tier loads across the six phases.
