# 2026-09-08-13-002 — Reconcile pre-submission-self-review mutates_source with its actual edit behaviour

component: plan-marshall:phase-6-finalize
category: bug
status: moved-to-epic (OPEN — declaration-sync future work; corpus original retained)
source: corpus lesson 2026-09-08-13-002, copied into finalize-machinery per operator direction
(note: condensed 2026-09-17)

## Context

Step declares `mutates_source: false`; dispatch edited 10 files and skipped
mark-step-done — two violations, one dispatch. Completion guard caught the handshake;
orchestrator committed the verified edits (right call, wrong reason to need it). The
declaration is load-bearing: it tells freshness verdicts whether a return can invalidate
them — an editing step declaring otherwise is invisible to source-movement checks.
Frontmatter describes an earlier read-only shape (fix-application path grown since).

## Proposed action

Set `mutates_source: true` (or split fix-application into its own declared step);
land mark-step-done on the edit branch's actual path.

## Epic status

Open future work. Corpus original RETAINED.
