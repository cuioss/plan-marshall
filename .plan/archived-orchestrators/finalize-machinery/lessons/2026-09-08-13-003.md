# 2026-09-08-13-003 — Scope review-producer refusals by invocation so a spent rate-limit stops re-asserting

component: plan-marshall:automatic-review
category: bug
status: moved-to-epic (OPEN — review machinery future work; corpus original retained)
source: corpus lesson 2026-09-08-13-003, copied into finalize-machinery per operator direction

## Context

`refusals[]` carries no time/invocation scope: a served CodeRabbit rate-limit notice
re-appears on every subsequent poll. 5 firings each had to infer liveness from an
incidentally-differing field. Mis-reading a spent refusal as live starts a second
90-minute quota wait for nothing (operator policy: 90min × up to 10 waits).

## Proposed action

Stamp each row with invocation id + observation timestamp; drop non-current rows (or
publish `refusals_current[]` alongside the accumulating audit array).

## Epic status

Open future work. Corpus original RETAINED.
