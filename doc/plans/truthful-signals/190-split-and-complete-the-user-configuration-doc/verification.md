# Verification — 190-split-and-complete-the-user-configuration-doc

**Verified against:** commit `ac06e4fc94782328f87d1395472a72b7e7a8559d`   **Landed as:** PR #1179, commit `a8b9b42d223e4eca06f1242bc6cd47effbc44157`   **Verdict:** implemented-with-gaps

## Method

Read `plan.md` and `report-01.md` in full. Located the landed commit with `git log --oneline --all --grep '#1179'`; confirmed it is an ancestor of `HEAD` (`git merge-base --is-ancestor`). Read `git show --stat a8b9b42d` and the per-file diffs for `doc/user/configuration.adoc`, `doc/user/parallelism-and-locking.adoc`, `doc/developer/marketplace-build.adoc`. Checked for post-landing drift with `git log --oneline a8b9b42d..HEAD -- <the three files>` (two later commits: `c0b4f3e8`, `ee78fd91`).

Files opened at HEAD: `doc/user/configuration.adoc` (whole), `doc/user/parallelism-and-locking.adoc` (whole), `doc/user/recipes.adoc`, `doc/user/enforcement-hook.adoc`, `doc/user/installation.adoc`, `doc/developer/distribution.adoc`, `doc/developer/marketplace-build.adoc`, `marketplace/bundles/plan-marshall/skills/manage-config/scripts/_config_defaults.py`, `.../_cmd_quality_phases.py` (`_cmd_step`), `.../_cmd_finalize_steps.py`, `.../_cmd_coverage.py`, `.../coverage_presets.py`, `.../manage-execution-manifest/scripts/_manifest_lanes.py`, `.../automatic-review/SKILL.md` (frontmatter, at the landed commit), `.../manage-config/standards/data-model.md`.

Commands executed (not merely read):

- **Executed the population source.** `PYTHONPATH=<all bundle scripts dirs> python3 -c "import _config_defaults; get_default_config()"` and flattened every leaf key + value. This is the code-authoritative seed the report claims to have derived from; running it (rather than reading the literals) picks up the lazily-seeded `steps` map, which is assembled from step `configurable:` frontmatter and is therefore invisible in the module literals.
- **Two-way population diff.** Diffed that flattened key set against `configuration.adoc` + its three sibling pages (missing-from-docs direction), *and* extracted all 76 `| \`…\`` table key cells from `configuration.adoc` + `parallelism-and-locking.adoc` and checked each leaf name against `marketplace/bundles/` (documented-but-nonexistent direction — the direction the run's D1 did not run). Exactly one orphan.
  ⚠ **Corrected by adversarial review — this sweep's criterion was too weak and its "exactly one orphan" conclusion does not hold.** Testing a leaf *name* for a substring match anywhere under `marketplace/bundles/` passes on any prose mention of the word, so a documented key can "resolve" against a comment. Re-run against the *executed seed* plus `data-model.md` instead of a text search, and widened to `efforts.adoc` / `terminal-title.adoc` as well (90 cells rather than 76), the sweep returns **three** documented keys that resolve to nothing: `drop_review_on_scope_gate` (G1), `plan.phase-6-finalize.plugin_doctor` (G7 — the leaf name survived the original sweep only because `doctor-marketplace.py:706` mentions a test file called `test_plugin_doctor_extension.py` in a comment), and — found by executing the verbs rather than by the sweep — the four retired ceremony-gate rows `self_review` / `qgate` / `simplify` / `security_audit` (G6), whose leaf names appear all over the bundles as *migration* prose. The corrected count of non-resolving `Config key` cells across the two pages is **six**, not one.
- **Content-move fidelity.** Extracted every `-` line of the `configuration.adoc` diff and every `+` line of the `parallelism-and-locking.adoc` diff, then tested each removed line for an exact match at the destination. Ten non-matches; each classified by hand (all are the intended D4/D5 rewrites or the two documented cross-file xref rewrites).
- **Anchor/reference integrity, re-derived twice** — once at `a8b9b42d`, once at `HEAD`: anchor uniqueness (`[#…]`), every internal `<<…>>` against the anchor set, and every repo-wide inbound `configuration.adoc#…` / `parallelism-and-locking.adoc#…` reference against the two anchor sets.
- **Cited-rationale checks.** Opened `installation.adoc:111` ("no tarballs, no GitHub Pages") and `distribution.adoc:72` ("contains only the marketplace files the generator produced") to confirm the D1 reference-link verdict rests on what the report says it rests on. Opened `_cmd_coverage.py:127-155` to confirm the doc's `thoroughness >= T4 AND scope < component` coupling claim. Opened `_cmd_step` (`_cmd_quality_phases.py:296-349`) to establish the consequence of G1.
- **Re-derived every count** the report states: 28 anchors at the landed commit (23 before, +5 new); 11 pre-existing inbound refs across 8 files; `_config_defaults.py` 1451 lines with `get_default_config` at line 1272; `auto_route_recipe` at 605 / `auto_route_recipe_threshold` at 613 at that commit.

**No mutation check was applicable.** The plan added no code and no test; the landed diff is three `.adoc` files plus the plan directory. There is no guard to break. No test file was run for the same reason — `grep -rl configuration.adoc test/` returns three files, all of which use the path only as a footprint fixture string, none of which asserts anything about the document's content.

`git status --porcelain` shows only this file and `gaps.md` from my work (three `verification.md` files under other plan directories are other agents' outputs, untouched by me).

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D1 | GATE: derive population, settle split | all four settled + derivation method stated | yes | yes | **no** | **no** | Report § D1 gives a LIST not a count. STOP CONDITION correctly not fired. But re-running `get_default_config()` shows two seeded knobs absent from the UNCOVERED list (`review_rate_window_await`, `review_rate_window_timeout_seconds`), and the diff was run one-way only, missing the retired `drop_review_on_scope_gate` (→ G1, G2). **Correct? downgraded from `partly` to `no` by adversarial review:** the Covered bucket asserts "finalize run-at-all gates (§run-at-all-gates)" as documented knobs, but four of those five keys were retired by the run_at_all→lane migration and the fifth never existed (→ G6, G7). The gate's own output certifies five dead keys as covered |
| D2 | Document the `lane` configuration | authoritative set settled and documented, or divergence reported | yes | yes | yes | yes | `configuration.adoc:180` states the `set-lane` subset with reason; `:277-279` NOTE disambiguates planning lane vs execution-profile lane; `:293` states the override space. Verified against `VALID_LANE_OVERRIDE` (`_config_defaults.py:481`), `LANE_TIERS`/`LANE_OVERRIDES` (`_manifest_lanes.py:19-20`), `_RESOLVED_ASK_LANE_VALUES` (`_cmd_finalize_steps.py:68`), `manage-config.py:749` argparse `choices` |
| D3 | Split, with pointer stubs | stub at every origin, no content deleted | yes | yes | yes | yes | Stubs `configuration.adoc:395-398` / `:402-405` preserve both anchors and name destination file + anchor + human label. Line-by-line move check: every removed merge line present at `parallelism-and-locking.adoc:35-61`, only the two declared xref rewrites differ |
| D4 | Close the coverage gap | uncovered set documented, narrative enumeration gone | yes | yes | yes | **no** | `configuration.adoc:49` is now a pointer naming **zero** knobs. All 16 named-uncovered knobs present, every default matches the executed seed. But the two rate-window knobs D1 missed remain undocumented (§automated-review table `:412-425` carries 9 of the step's 11 seeded params) → G2 |
| D5 | Evict meta-project content | sweep done, every hit moved or re-pointed | yes | yes | yes | partly | Sweep of `configuration.adoc` for `meta-project`/`sync-plugin-cache`/`deploy-target`/`target/claude` now returns exactly one hit (`:529`, the recorded deliberate keep). Caveat relocated to `marketplace-build.adoc:166-170`. The identical prose-embedded phrase survives on a different consumer page → G3 |
| D6 | Verification (cross-reference integrity) | all inbound references resolve | yes | yes | yes | yes | Re-derived at both commits: 28 unique anchors, zero unresolved `<<…>>`, all 13 distinct inbound `configuration.adoc#…` targets and all 3 inbound `parallelism-and-locking.adoc#…` targets exist. No inbound reference ever pointed at `#merge-strategy`/`#merge-queue` from outside the file |

**D1 (not a clean pass).** `_config_defaults.py`'s `DEFAULT_PLAN_FINALIZE['steps']` is the literal `None` — the step map is seeded lazily by `_seed_finalize_steps()` from each step's `configurable:` frontmatter. Executing `get_default_config()` yields eleven params under `plan-marshall:automatic-review`; the report's covered/uncovered lists account for nine of them. `review_rate_window_await` (default `false`) and `review_rate_window_timeout_seconds` (default `3600`) were both present in `automatic-review/SKILL.md`'s frontmatter at the landed commit (verified with `git show a8b9b42d:…/automatic-review/SKILL.md`), so they were in the population D1 claimed to derive. Separately, D1 ran the diff in one direction only — "which knobs are undocumented" — and never asked "which documented knobs no longer exist". That second direction is what surfaces G1.

**D1, further (added by adversarial review).** Running that second direction against the *executed seed* rather than against a text search over `marketplace/bundles/` surfaces four more instances, all in the `[#run-at-all-gates]` table of `configuration.adoc`, all pre-existing rows the landed diff never touched: `plan.phase-6-finalize.self_review` (`:190`), `.qgate` (`:191`), `.plugin_doctor` (`:192`), `.simplify` (`:193`), and `steps['default:finalize-step-security-audit'].security_audit` (`:194`). The first, second, fourth and fifth were retired by the run_at_all→lane migration (`manage-config/SKILL.md:377,723`; migration shim at `_cmd_sync_defaults.py:111-126`); the third has never existed in the repository's history at all. `get_default_config()['plan']['phase-6-finalize']` holds exactly `max_iterations`, `finalize_without_asking`, `loop_back_without_asking`, `checks_wait_timeout_seconds`, `steps`, `effort`. Executing the documented verbs: `get --field self_review` → `error: Unknown field`; `set --field self_review --value never` → `success`, persisted, never read; `set --field self_review --value banana` → `success` (while the live `set --field escalation --value banana` is correctly rejected with the allowed-values list). Report-01.md § D1 places these in its **Covered** bucket, so the population gate the plan says to grade on its own certified five keys that do not exist. → G6, G7.

**D4 (not a clean pass).** The parenthetical replacement is exactly what the plan demanded: `configuration.adoc:49` now ends "…the canonical per-key reference in `data-model.md` — which is the single, exhaustive enumeration of every field and its validator. This page never restates that enumeration, so it cannot drift out of step with it." No knob is named. The added sections are accurate: I re-checked `confidence_threshold=95`, `compatibility='breaking'` (enum `breaking|deprecation|smart_and_ask` per `data-model.md:603`), `simplicity='lean'` (`VALID_SIMPLICITY_LEVELS`, `_config_defaults.py:638`), `coverage.thoroughness/scope='inherit'`, `phase-5 max_iterations=5`, `phase-6 max_iterations=3`, `touched_file_cleanup='new_code_only'`, `do_transition=False`, `merge_queue_managed_externally=False`, and all four new `system.retention.*` values (`7`, `5`, `5`, `3`) against the executed seed. The gap is only the two rate-window knobs inherited from D1.

**D5 (partly complete).** Within its declared surface the sweep is complete and correct — re-run by adversarial review, `grep -n 'meta-project\|sync-plugin-cache\|deploy-target\|target/claude' doc/user/configuration.adoc` still returns exactly the one hit at `:529`. The plan's *Goal* is broader than D5's *Done-when* — "no consumer-facing page carries meta-project-only content or a reference a consumer cannot follow" — and against that wording two `doc/user/` pages outside the plan's Expected surface still fail (G3, G8, split from one another during adversarial review). A third instance sits *inside* `configuration.adoc` and the sweep's four-term pattern cannot see it: the `plugin_doctor` row at `:192` names a lint whose finalize step is project-local under `.claude/skills/`, i.e. meta-project-only per `marketplace-build.adoc:164`, without carrying any of the four sweep terms (→ G7). A literal-term sweep finds only the residue that happens to spell one of the four terms.

## Report accuracy

Re-derived every figure the report states. Findings:

- **Confirmed exactly right:** "All 28 `[#anchor]` ids … unique; the 5 new ones … collide with nothing" — 23 anchors before, 28 after, no duplicates, and `refine-behaviour`/`coverage-cell`/`iteration-limits`/`sonar-roundtrip`/`build-queue` are all new. "Inbound external refs (11 occurrences across 8 files under `doc/concepts/` + `doc/user/`)" — `git grep` at the landed commit gives 14 total occurrences minus the 3 this plan itself created (2 in `parallelism-and-locking.adoc`, 1 in `marketplace-build.adoc`) = 11, across `branches-and-worktrees`, `build-management`, `planning-workflow`, `recipes` (concepts), `token-management`, `commands`, `getting-started`, `recipes` (user) = 8 files. "None pointed at the extracted `#merge-strategy`/`#merge-queue`" — confirmed. `data-model.md` omits `auto_route_recipe`/`auto_route_recipe_threshold` while `_config_defaults.py:605,613` carries them — confirmed at the landed commit. `get_default_config()` at `_config_defaults.py:1272` — confirmed. `_RESOLVED_ASK_LANE_VALUES = ('off','standard','full')` at `_cmd_finalize_steps.py:68` and `VALID_LANE_OVERRIDE` at `:481` — confirmed. `distribution.adoc:72` and the no-GitHub-Pages statement — confirmed (the latter is at `installation.adoc:111` today, the report says `:110`; the file has shifted by one line since).
- **The report's D1 completeness claim is overstated.** It presents the covered/uncovered/mentioned split as a derivation from `get_default_config()` and describes the population as "knowable and bounded". Two seeded params of `plan-marshall:automatic-review` appear in none of its four lists — not covered, not uncovered, not mentioned-but-undocumented, not out-of-population. The list is therefore not the complete diff it presents itself as.
- **The report cites the stale knob as evidence of a clean move.** § D6 records the sub-agent's finding as "every moved line (the 10-row table incl. `drop_review_on_scope_gate` …) exists verbatim at the destination". The move *is* verbatim; the row itself documents a knob that no longer exists anywhere in `marketplace/bundles/` (see G1). Both the run and its independent verification sub-agent named the string and neither checked whether it resolved.
- **Trivia, non-contradicting:** the report says `_config_defaults.py` is "1452 lines"; `wc -l` at that commit gives 1451. The report writes the target anchor as `configuration.adoc#automatic-review` in one D6 bullet; the actual xref in the file is `#automated-review` and resolves.
- **Not contradicted, having checked:** the "no Python changes → docs-only build skip" claim (the landed diff is three `.adoc` + two plan `.md`, zero `.py`); the plan-directory lifecycle (`git show --name-status` shows `R100` of `190-….md` → `190-…/plan.md` plus `A` of `report-01.md`); the `enabled_bots` beyond-diff sweep (all surviving hits are migration/provenance references in `marshall-steward`, `upgrade.py`, `data-model.md`, `bot-participation-contract.md`, and tests); the sequencing note (`8f23d7d2`, plan 090, is an ancestor of `a8b9b42d`, so this plan did run after the knob-surfacing plan as preferred).

## Out-of-scope compliance

Clean. The landed diff touches exactly `doc/user/configuration.adoc`, `doc/user/parallelism-and-locking.adoc`, `doc/developer/marketplace-build.adoc` (+6 lines, one additive subsection — the declared destination), and the plan's own directory. `data-model.md` is unmodified, as its read-only status required. No `.py` file changed, so no knob behaviour or default moved. Both reported code-level defects were left unfixed as Out-of-scope demanded. No undeclared collateral change.

One observation that is **not** a scope breach but sits oddly against the Problem statement: `configuration.adoc` went from 613 lines / 26 subsections to 638 lines / **31** subsections (re-derived by adversarial review — `grep -c '^=== '` at `a8b9b42d^` and `a8b9b42d`; the "34" originally stated here is the count at today's `HEAD`, after two later commits). The plan opens by framing the document's size as the defect and D3 as the remedy; the single extraction removed ~58 lines while D4 added ~83. The report discloses the choice ("more aggressive atomisation was considered and deferred … the plan's own framing puts the value in D1/D4/D5, not in shrinking line-count"), which is a defensible disposition — but it never states the net effect, so a reader of the report alone would not know the document grew. (The plan's claim of "~587 lines across 26 subsections" was itself a lead: 26 subsections and 5 top-level sections were exact, the line count was 613.)

## Residue carried forward

| Report residue | Still open at HEAD? | Evidence |
|---|---|---|
| Finding #1 — `data-model.md` omits the seeded `auto_route_recipe` / `auto_route_recipe_threshold` | **open** | `grep -c auto_route_recipe …/manage-config/standards/data-model.md` → `0`, while the executed seed yields both under `plan.phase-1-init` |
| Finding #2 — `finalize-steps set-lane` rejects `minimal`/`ask`, both valid per-element override values | **open** | `_RESOLVED_ASK_LANE_VALUES = ('off','standard','full')` at `_cmd_finalize_steps.py:68`; `VALID_LANE_OVERRIDE` still admits five values at `_config_defaults.py:481` |
| Landing delegated to the merge queue | **closed** | `a8b9b42d` is an ancestor of `HEAD` and is on `main` |
| Step-9 observation: make `plugin-script-architecture` a conditional load in `cloud-plan-lane` | not tracked here — an operator-judgement item the run deliberately did not open as a PR | — |

## What could NOT be verified

- **Whether the two reported code-level findings were correctly *classified* as intentional.** I confirmed the divergence exists and that `_cmd_finalize_steps.py:298-305` documents the subset as deliberate. Whether that intent is *right* is a design question, not a tree fact.
- **The CI / merge-queue `merge_group` verification the report defers to.** The report's build-gate reasoning is checkable from git evidence (and checks out); the actual CI run is not in this clone.
- **The cold-read stub test as a fresh experiment.** I read the stubs and they name destination file, anchor, and human label — but I had already read the plan and report, so this is a judgement on the stub text, not an uncontaminated cold read.
- **Whether `skill_domains.system.defaults` / `.optionals` should count as documentable knobs.** They appear in the executed seed but are the system domain's bundle lists rather than operator dials, and the report's out-of-population reasoning does not name them either way. I flag the ambiguity rather than scoring it.

## Adversarial review

**Reviewed by:** an independent agent that did not write this document.

**Checked.** Every gap (G1–G5) and every clean-pass row (D2, D3, D6), plus both "swept, clean" claims.

*Functions executed* (not read — a fixture `.plan` dir seeded from `get_default_config()` via `PLAN_TRACKED_CONFIG_DIR`, with every `marketplace/bundles/*/skills/*/scripts` dir on `sys.path`):

- `get_default_config()` — flattened; 124 distinct leaf names. `steps['plan-marshall:automatic-review']` returns exactly eleven keys (ten params + `lane`); `steps['default:pre-submission-self-review']` returns `{}`; `plan['phase-6-finalize']` returns six top-level keys, none of them a ceremony gate.
- `cmd_plan(... step_verb='set', step_id='default:pre-submission-self-review', param='drop_review_on_scope_gate', value='true')` → `success`, persisted (G1's mechanism). Repeated with `param='totally_made_up_knob_xyz'` → also `success`: the acceptance is unconditional.
- `cmd_plan(... verb='get', field=X)` and `verb='set', field=X, value=Y` for `self_review` / `qgate` / `plugin_doctor` / `simplify`, and the control `escalation`. `get` errors on all four; `set` succeeds on all four including with the nonsense value `banana`, while the control gate rejects `banana` with its allowed-values list. This is what surfaced G6 and G7.
- `manage-config.py finalize-steps set-lane --lane {minimal,ask,standard}` as a real subprocess → `rc=2`, `rc=2`, `rc=0` (G5's mechanism), and `cmd_plan(... param='lane', value='minimal')` → `success` (the alternative channel G5's Fix depends on; it also accepts `utter_garbage`).

*Sweeps re-run with a broader pattern than the original:* the table-key-cell sweep over four pages (90 cells vs. 76) and against the executed seed + `data-model.md` rather than a substring match under `marketplace/bundles/` — see the corrected bullet in § Method; the D5 four-term sweep over `configuration.adoc`; a repo-wide `meta-project|sync-plugin-cache|target/claude|deploy-target|marshall-steward` sweep over all of `doc/user/`.

*Figures re-derived:* anchors at `a8b9b42d^`/`a8b9b42d`/`HEAD` (23 / 28 / 29, all unique, zero unresolved `<<…>>`, zero unresolved inbound refs at HEAD across 14 files); the "11 across 8 files" inbound count at the landed commit (14 raw occurrences − 3 created by the plan = 11 — exact); `_config_defaults.py` 1451 lines and `get_default_config` at `:1272` at the landed commit (1456 / `:1277` today); `auto_route_recipe` at `:605,613` landed and `:610,618` today; subsection counts 26 → 31 (not 34); the D3 removed-line move check (40 non-blank removed lines, 7 without a verbatim destination match, each classified — the `:49` rewrite, the two D5 evictions, the `project:` example, and three declared xref/`enabled_bots` rewrites); `git merge-base --is-ancestor` for `ac06e4fc`, `a8b9b42d`, `8cd280a3`.

*Not re-checked:* the CI / merge-queue run (still not in this clone); the cold-read stub experiment (I read the plan first, same contamination as the original); whether `skill_domains.system.defaults` counts as a knob; the report's `enabled_bots` beyond-diff sweep and its plan-directory-lifecycle claims (spot-confirmed only); the `doc/concepts/` and `doc/developer/` pages beyond the specific lines cited here; and the *non-table* prose of `configuration.adoc` — the new gaps come from table cells and code blocks, and a claim buried in a paragraph would not have been caught.

| Item | Original claim | Verdict | Evidence |
|---|---|---|---|
| G1 | `drop_review_on_scope_gate` row is stale; `step set` accepts it silently; severity `high` | **upheld** (evidence strengthened) | Executed `step set` → `success` + persisted; step's seeded params `{}`; still the only orphan under a 90-cell four-page sweep; `8cd280a3` confirmed an ancestor of `a8b9b42d` |
| G2 | two rate-window knobs undocumented; severity `medium` | **upheld, arithmetic corrected** | Seed yields ten params + `lane`, not "eleven plus `lane`"; table has nine rows of which eight are seeded; `bot_lists_provenance` checked and cleared as a second orphan; `report-01.md` mentions `review_rate_window` zero times |
| G3 | meta-project residue on two pages; severity `medium` | **split and re-severitied** → `low` | Both texts confirmed verbatim. Bundled two defects on two files with two fixes; the `enforcement-hook.adoc` instance is now G8 at `medium`. The `recipes.adoc` half is informational, not actionable → `low` |
| G4 | `data-model.md` omits the two recipe-routing knobs; severity `low` | **upheld, re-severitied** → `medium` | `grep -c` → `0` confirmed; line numbers wrong (`:608,616` → `:610,618`); same magnitude as G2, and `configuration.adoc:49` now states the exhaustiveness this falsifies |
| G5 | `set-lane` rejects `minimal`/`ask`; severity `low` | **upheld** | Both paths executed: `rc=2` vs `success`. `low` is right — loud rejection, accurate docs at `:180`, working alternative. Docstring cite corrected to `:297-306` and the quotation made verbatim |
| G6 | *(new)* four retired ceremony-gate rows at `configuration.adoc:190,191,193,194` + the `get --field self_review` example | **added**, `high` | `set --field self_review --value banana` → `success` while the live `escalation` gate rejects it; `manage-config/SKILL.md:377,723`; migration shim `_cmd_sync_defaults.py:111-126`; tests at `test_cmd_quality_phases.py:1196-1204` |
| G7 | *(new)* `plan.phase-6-finalize.plugin_doctor` at `:192` + the `set --field plugin_doctor` example at `:205` | **added**, `high` | `git log -S"'plugin_doctor'"` → no commit ever; absent from the seed and `data-model.md`; the step is project-local (`ext-point-finalize-step.md:234`, `_manifest_rules.py:416-417`), i.e. meta-project-only per `marketplace-build.adoc:164` |
| G8 | *(new, split from G3)* `enforcement-hook.adoc:21` sends consumers to `/sync-plugin-cache` | **added**, `medium` | No `sync-plugin-cache` skill or command directory under `marketplace/bundles/`; `marketplace-build.adoc:164` states consumers do not get the surface |
| "Exactly one orphan" (§ Method) | the 76-cell sweep found one non-resolving key | **refuted** | Its criterion was a substring test over `marketplace/bundles/`, which a prose mention satisfies. Corrected count: six non-resolving `Config key` cells across the two pages |
| "28 anchors / 23 before / +5" | § Report accuracy | **upheld** | Re-derived at both commits: 23 → 28, no duplicates |
| "11 inbound refs across 8 files" | § Report accuracy | **upheld** | 14 raw − 3 plan-created = 11, across the 8 named files — exact |
| D2 / D3 / D6 clean passes | authoritative lane set documented; stubs preserve anchors, nothing deleted; all inbound refs resolve | **upheld** | `:180`/`:277`/`:293` read at HEAD and cross-checked against the executed constants; move check re-derived (7 explained non-matches); zero unresolved anchors at HEAD, and zero inbound refs ever pointed at `#merge-strategy`/`#merge-queue` (`git grep` at `a8b9b42d^` → empty) |
| Verdict `implemented-with-gaps` | headline | **upheld** | All six deliverables are implemented and every *Done-when* is met as written; the defects are completeness and accuracy of D1's output, not an absent deliverable. `partially-implemented` would require an unimplemented deliverable and there is none |

**Documents corrected.** *verification.md:* the "exactly one orphan" claim in § Method is marked refuted and replaced with the corrected six; D1's **Correct?** downgraded `partly` → `no` with a new narrative paragraph for the retired gates; the D5 narrative gained the third, pattern-invisible instance; the subsection count corrected 34 → 31. *gaps.md:* open items 5 → 8; G1 and G5 gained executed evidence; G2's arithmetic corrected; G3 narrowed and re-severitied to `low`; G4 re-severitied to `medium` with corrected line numbers; G5's docstring citation corrected and quoted verbatim; G6, G7, G8 added; a `## Refuted during adversarial review` section records that no gap was refuted and what each was tested against.

**Residual doubt — what a third reviewer should look at first.** The `[#run-at-all-gates]` table was the *only* section whose keys I exercised by running the verbs. Every other table on the page was checked by name-resolution against the executed seed, which is exactly the class of check that let G6 through here: a key can resolve by name and still be read by nothing, or be validated on one path and not another. Start by executing `get` **and** `set` for every `Config key` cell in `configuration.adoc` and asserting the round trip, rather than asserting the name exists. Second: `report-01.md` § D1's *Covered* bucket is written as prose groups ("the four review gates", "finalize run-at-all gates"), not as an enumerated key list — the plan demanded a LIST and got a list of *groups*, which is what allowed five dead keys to hide inside one bullet. Third: the `Covered` list is explicitly labelled "representative — not exhaustive", so nothing in it was ever a completeness claim; whether that concession is compatible with D1's Done-when is a judgement I did not score.
