# 2026-09-09-01-002 — Re-close the 5-execute metrics row when a finalize loop-back re-enters it

component: plan-marshall:phase-6-finalize
category: bug
status: moved-to-epic (OPEN — boundary-pairing future work; corpus original retained)
source: corpus lesson 2026-09-09-01-002, copied into finalize-machinery per operator direction
(note: condensed 2026-09-17 — exact 429,730-token attribution gap, checkable)

## Context

Two 6→5 loop-backs ran further execute work; the 5-execute row still reads first-close
values (close_count 1, first end_time) — 429,730 tokens of post-end_time dispatches
unabsorbed; wall/idle spans missing. Machinery exists (`end-phase` accumulates
correctly) but is never invoked on the loop-back path. `generate` picks the larger
measure so the rendered total was right — safety net compensating for a row left wrong.
`re_entered_phases: []` on a 3-loop-back plan: false-clean on the trusted field.

## Proposed action

Pair every `set-phase 6→target` with its metrics boundary call (and mirror on return) —
one indivisible action; backstop: `reconcile-ledgers` arm reporting boundary rows
outside their phase window.

## Epic status

Open future work (metrics theme). Corpus original RETAINED.
