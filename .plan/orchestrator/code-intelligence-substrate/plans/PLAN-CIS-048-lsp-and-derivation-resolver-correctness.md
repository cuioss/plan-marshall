# PLAN-CIS-048: LSP and derivation-resolver correctness

epic: code-intelligence-substrate
workstream: WS-07

> ⚠ **POINTER SPEC — this plan has already SHIPPED (PR #1321).**
> It is not a hand-off brief and must never be emitted. It exists so the queue row and the spec
> corpus reconcile in both directions; `corpus enumerate` would otherwise report this row as
> `rows_without_spec`.

## Provenance

The first of the eight 5xx remediation plans, derived by the 2026-08 audit of this epic's landed corpus rather than from this queue. It ran before the ingest, so this file is a POINTER seated during the 2026-08-22 ingest.

## The authoritative spec

The plan body this run actually executed is archived verbatim at:

```text
cloud-runs/500-lsp-and-derivation-resolver-correctness/plan.md
```

Read that file, not this one, for what the plan required.

## Outcome

See `landings/PLAN-CIS-048.md` for the landing analysis — deliverable verdicts, the gaps carried out of the
landing, the inconsistencies verified against the tree, and where the follow-ups were routed. The
run's own account, the independent post-run verification, and the recorded gap entries are archived
beside the plan body under `cloud-runs/500-lsp-and-derivation-resolver-correctness/`.

## Hand-Off Command

None. **This plan has shipped.** A pointer spec carries no hand-off command, and emitting one would
re-run landed work.
