id=2026-09-03-05-001
component=plan-marshall:phase-6-finalize
category=bug
status=active
created=2026-09-03

# Sweep every re-fireable finalize step's terminal --outcome done branch for a missing --force

## Observation

The rule itself is now codified as a standing contract clause — see
`phase-6-finalize/standards/external-step-contract.md` § "Recording the step outcome",
the `--force` bullet and the block under the command template. Both terminal branches of
`workflow/pre-submission-self-review.md` have been corrected against it.

What remains open is the SWEEP. Every OTHER re-fireable finalize step's terminal branch is
still unexamined against that clause.

Observed on plan `misconfigured-reviewer-name-reads-missing-review` (PLAN-PR-044), finalize
round 2, after round 1 filed finding `81f0d9`.

## Directive

Enumerate every re-fireable finalize step and check its terminal `--outcome done` branch for
the same omission, stating on each why `--force` preserves the superseded firing in
`prior_firings` rather than discarding it.
