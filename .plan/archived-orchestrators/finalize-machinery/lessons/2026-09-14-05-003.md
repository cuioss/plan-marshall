# 2026-09-14-05-003 — Name the rejected flag in capture-footprint argparse failures

component: plan-marshall:manage-references
category: anti-pattern
status: moved-to-epic (OPEN — failure-record future work; corpus original retained)
source: corpus lesson 2026-09-14-05-003, copied into finalize-machinery per operator direction
(note: full body retrieved 2026-09-17; condensed here)

## Context

`capture-footprint` exit 2 ×3 with empty excerpts — blind to which flag was wrong; same
shape on manage-files list, manage-logging read. Classifier falls back to
`argparse_other`; whether emptiness comes from argparse/capture/truncation is itself
unknown — the record cannot say why it is uninformative.

## Proposed action

Capture argparse usage on exit 2; record genuinely-empty as distinct subtype
(uncapturable rejection), never folded into `argparse_other`.

## Epic status

Open future work. Corpus original RETAINED.
