id=2026-09-05-08-001
component=pm-dev-java:java-core
category=anti-pattern
status=active
created=2026-09-05

# Narrowing a catch clause silently drops the cleanup that followed the try block

## Observation

A plan narrowed two broad `catch` clauses in the same class. At `refresh` the narrowing was done
correctly: the cleanup the old broad catch had been performing was first hoisted into a `finally`
block, so it still ran on the newly-escaping exception types. At `revokeAndClearFailClosed` the same
narrowing was applied **without carrying the pattern across**.

The result was a **retained-credential exposure on a fail-closed path** — exactly the path whose entire
purpose is to guarantee the credential is gone.

A review bot caught it. The run's own pre-submission self-review did not.

## The rule

When you narrow a `catch`, the question is **not** *"are the new exception types handled?"* — it is:

> **What ran AFTER the try/catch, and must it now run unconditionally?**

A broad catch swallows everything, so any statement after it executes on every path. Narrowing the catch
turns those statements into **conditionally-executed code without editing a single line of them**.

⛔ **The diff shows a smaller catch; it does not show the cleanup that stopped running.** That is what
makes this class survive review — there is no removed line to notice.

## How to apply

1. **Move the cleanup into `finally` FIRST, then narrow.** Two commits, in that order, are reviewable;
   one combined edit is not.
2. Narrowing a catch requires an explicit statement of what followed the try block and whether it is now
   conditional. If nothing followed it, say so — that is the cheap case and recording it is what
   separates it from the case that simply forgot.
3. ⚠ **Apply the check to EVERY sibling site in the same change.** This instance is an N−1-of-N miss:
   the correct pattern existed in the same class, one method away, and was not carried across. See
   [[the n-1-of-n sibling-site archetype]] — the most-recurrent defect shape in this corpus.
4. Weight the review by the path's purpose. A fail-closed or credential-clearing path is where a dropped
   cleanup costs most, and it is exactly where a narrowed catch looks most defensible.

## Provenance

Relayed from the Token-Sheriff epic `lessons-handling-26-09-04-01`, PLAN-01
(`pre-commit-gate-truthfulness`, merged as PR #713 / `29d9f6c5`), via the `truthful-signals` epic inbox
as message `lessons-handling-26-09-04-01-011.md`.

⭐ **The sender routed it here deliberately and correctly.** Its component `pm-dev-java:java-core` is a
bundle this repository owns; the Token-Sheriff lessons store refused it with `wrong_store`, which is how
the routing was **determined rather than guessed**, and the `--allow-foreign-store` override was
deliberately NOT used. The allocation here succeeded with no wrong-store refusal, confirming that
analysis.

The companion half of the candidate — the self-review surfacer improvement that would let a candidate
list see this class — was relayed separately and is folded into the epic spec `PLAN-TRUTH-108`.
