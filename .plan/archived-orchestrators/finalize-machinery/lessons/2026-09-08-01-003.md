# 2026-09-08-01-003 — branch-cleanup removes the worktree but leaves worktree metadata, deadlocking the archive gate

component: plan-marshall:phase-6-finalize:branch-cleanup
category: bug
status: moved-to-epic (OPEN — metadata-reconciliation future work; corpus original retained)
source: corpus lesson 2026-09-08-01-003, copied into finalize-machinery per operator direction
(note: full body retrieved 2026-09-17; condensed here)

## Problem

`branch-cleanup` deletes the worktree but leaves `metadata.use_worktree: true` +
`worktree_path` pointing at the deleted dir — every later phase-entry assertion fails
both arms (resolvable-neither-missing-nor-present); only hand repair (flip both flags)
passes. The assertion's own docs name this scenario; nothing produces the repair. Ergonomics
compound: `--value ""` rejected, only `--value=` clears.

## Directive

Reconcile metadata in the removal step (empty path + use_worktree false); consider
splitting historical vs live meaning (`worktree_removed` marker); accept `--value ""`.

## Epic status

Open future work (branch-cleanup theme). Corpus original RETAINED.
