status: success
id: 2026-09-03-16-001
component: "plan-marshall:phase-6-finalize"
category: anti-pattern
lifecycle_status: active
created: 2026-09-03
title: A finding's diagnosis and its proposed resolution are separately falsifiable - settle both against the primary source walked to its root
content: "## The rule

A finding arrives as two claims welded together: *this is wrong* (diagnosis) and *here is what it should say instead* (resolution). They fail independently. A correct diagnosis routinely ships with an inferred resolution that is itself false, and a confident diagnosis routinely rests on a source read only partway.

So the disposition of a finding is never \"the finding is right, apply it\" or \"the finding is wrong, dismiss it\". It is **two verdicts**, each settled against the PRIMARY SOURCE — and for anything inherited, the primary source is the full ancestor chain, not the nearest ancestor that happens to mention the subject.

## Instance 1 — correct diagnosis, wrong inferred fix

`pre-submission-self-review` flagged genuinely ambiguous inherited-coverage wording. The diagnosis was sound. Its inferred resolution (\"the bundle gate is BRANCH-only\") was false: the primary source (`cui-java-parent-1.5.10.pom` lines 409-421) gates BOTH `INSTRUCTION >= 0.80` and `BRANCH >= 0.80`. Applying the finding as written would have replaced ambiguous prose with a NEW false claim, and would have read as a fix because the diagnosis it rode in on was correct.

⛔ The project's own existing prose said the same wrong thing at three separate sites. **Corroboration across sites in the same repo is not corroboration — it is one claim counted three times.**

## Instance 2 — confident diagnosis from an incomplete inheritance walk

CodeRabbit filed a MAJOR finding that jacoco `prepare-agent` runs only inside an inactive coverage profile, so the whole coverage gate is decorative. It had read the PARENT and stopped. The binding is one level further up, in the GRANDPARENT `cui-parent-pom-1.5.10.pom`, in the DEFAULT `pluginManagement` block with no enclosing profile. A false positive produced entirely by where the walk stopped.

It was also refutable without reading any POM: the plan's own negative control turned a plain `verify` RED and reported real per-class coverage ratios. **Instrumentation that does not run cannot produce ratios.** When a static reading and a behavioural observation disagree about whether something executes, the behavioural observation wins.

## Practice

1. Split every finding into diagnosis and resolution and rule on them separately. Record both verdicts. \"Valid finding\" is not a disposition.
2. Settle each verdict against the primary source — the POM, the schema, the argparse declaration, the plugin's own docs. Never against the finding's own reasoning, and never against the project's existing prose, which is the artifact under suspicion.
3. For any inherited or layered configuration, walk to the ROOT before concluding a binding is absent. \"Not in the parent\" is not \"not bound\".
4. Prefer an empirical disposition where one exists. A negative control that already ran outranks a re-reading of the config.
5. A correct diagnosis is not a licence to apply the attached fix.

## Recurrences — the same rule, pointed the other way: the triager's OWN claim

Three further sightings, from two consumer repos, relayed to `review-apparatus` as `truthful-signals-054.md` items 6 and 7. They matter because they run in the opposite direction from Instances 1 and 2: there the unverified claim was the REVIEWER's; here it is the claim the triager brings to dismiss or confirm a finding.

- **A dismissal resting on a recalled mechanism** (Token-Sheriff, `lessons-handling-…-030`). A CodeRabbit finding was dismissed on a JLS claim recalled from memory — that enum switch statements are exhaustiveness-checked. They are not: an enum is a legacy selector type under JLS SE21 §14.11.1. The claim was promoted into a fix-task hard constraint and shipped as production Javadoc citing the JLS, until a second rebuttal unwound it. The same run later settled a harder claim with a controlled two-form experiment and the reviewer withdrew. **The contrast is the lesson: the recalled claim shipped, the tested one held.**
- **A confirmation reported before the source was resolved** (Token-Sheriff, `lessons-handling-…-029`). A review-bot claim about inherited Maven configuration was reported to the operator as a confirmed defect BEFORE the effective POM was resolved; triage refuted it. Practice 3 applied to confirmation, not only to dismissal.
- **Practice 1 as a first-class disposition** (API-Sheriff, `-003`). *\"Accepted: correct fix, incorrect premise\"* recorded as two separate fields (`fix_correct`, `premise_verified`), so a false premise never enters a commit message or a reply as accepted fact. A third-repo arrival at the same two-verdict split.

⛔ A claim the triager supplies is under the same obligation as a claim the finding supplies. Recalled knowledge is a lead, never a primary source.

## Provenance

Relayed from Token-Sheriff epic `deployment-and-refresh-gaps`, plan `coverage-gate-visibility` (PR #681), via `truthful-signals` inbox message `deployment-and-refresh-gaps-002.md`. Refused at the Token-Sheriff store with `wrong_store` because it names a plan-marshall bundle."
