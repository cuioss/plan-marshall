id=2026-09-18-20-003
component=plan-marshall:plan-marshall
category=bug
status=active
created=2026-09-18

# Finalize dispatch template requires session_id on the transcript-less path

# Finalize dispatch template requires session_id on the transcript-less path

The transcript-less branch requires finalize without `session_id`, but the only
finalize dispatch template always included `session_id: {resolved session id from
resolver above}`. The transcript-less branch has no resolved value, so it cannot
implement the optional-session contract with that template.

## Proposed rule

When an input contract demotes a field from required to optional, ship both dispatch
forms (with-field and without-field) and keep the with-field form for the resolved
path; a single always-present template cannot implement the optional contract.

## Evidence

- PR #1530 inline finding d1bd5d (coderabbit, execution.md:591), fixed via TASK-5
- Follow-up commit added a separate dispatch form omitting `session_id`
- Plan: plan-07-session-identity. Source: automated-review remediated finding.
