id=2026-08-25-09-007
component=plan-marshall:phase-6-finalize
category=improvement
status=active
created=2026-08-25

# Bound self-review re-firing: measure billing cost before bounding the loop

# Bound self-review re-firing: 4 rounds cost 679K of the 3.57M finalize spend

## Context

Finalize is this plan's cost centre, not implementation:

| Phase | Worked | Tokens | Share |
|---|---|---|---|
| 5-execute | 1h8m | 1,437,812 | 24% |
| 6-finalize | 2h19m | 3,572,386 | 60% |

Finalize consumed 2.5x what execute did, and its spend is dominated by re-firing rather than first-pass work. Firing counts from `status.json`: `pre-push-quality-gate` 6, `pre-submission-self-review` 4 (3 failed), `project:finalize-step-plugin-doctor` 3, `ci-verify` 2, `push` 2, `automatic-review` 2, `finalize-step-simplify` 2.

The plan-efficiency anchors for `single_module + feature` are 1.0M warning / 1.6M error. Observed: 5,991,048 tokens — 3.7x the error anchor — and 243 worked minutes against a 120-minute error anchor. Every one of the four fallback ratio thresholds also trips. And because `6-finalize` never closed an `end_time`, all of these are floors.

This is not an argument that the re-firing was wrong. Round 4 of self-review examined 301 candidates and caught a genuine correctness bug. `plugin-doctor` was clean on all 3 firings; whole-tree CI was the real check and passed.

## Root cause

Two distinct contributors, worth separating because they need different remedies:

1. **Productive re-firing** — self-review rounds 1-3 each found real findings; the loop converged in 4. Decision `fae1f5` notes round 2 had \"a self-seeding character\" (the SKILL.md half of the flagged claim was authored by round 1's own fix), so at least part of the iteration was the loop reacting to itself.
2. **Unproductive re-firing** — `plugin-doctor` fired 3 times and was clean each time in scoped mode, with cross-skill rules structurally out of reach. `pre-push-quality-gate` fired 6 times.

## Proposed action

Measure before bounding. The cheapest concrete step is the one already proposed separately: populate the four context-load columns and run `enrich`, so the finalize phase has a `Billing (cost)` figure to reason about instead of only a dispatched-token count.

Then consider: a self-seeding guard for the self-review loop (a round whose findings target text the previous round authored is a distinct class from a round finding pre-existing drift — decision `fae1f5` already draws this line by hand); and whether a scoped `plugin-doctor` that is structurally clean should re-fire at all when the whole-tree CI check is the real gate.

## Evidence

- aspect: plan_efficiency — `max_phase_token_share 0.60`, error anchor crossed 3.7x, all 4 fallback ratios tripped
- aspect: log_analysis — 13 finalize dispatch rows; `pyproject_build` 84 calls / 4,122,990ms = 48% of all script time
- aspect: logging_gap_analysis — 3 of 13 finalize rows carry 679,275 tokens
- decision `fae1f5` — the self-seeding character of round 2, diagnosed at the time

## Recurrence — 2026-09-02, plan `prompt-standard-and-doctor-rule` (PR #1378)

**Second independent instance.** Filed afresh by that run as
`prompt-standard-and-doctor-rule-004.md` (epic `operator-ux`); folded here rather than
duplicated. Observed figures: finalize carried ~52% of a 3,899,218-token plan, with the
self-review loop a **plurality** of that (677,934 tokens, ~43% of the phase-6 dispatch total)
rather than the whole — a refinement of this lesson's original framing.

⛔ **Caveat that strengthens the case for the lesson's own remedy:** that run's ledger
integrity check found only **8 of 20** finalize rows pair with a boundary row, so both the
52% and the 43% are FLOORS over a partially-paired ledger, not measurements. This lesson asks
for cost to be measured before the loop is bounded; the recurrence shows the measurement
substrate is not yet trustworthy enough to bound against. See the sibling lesson on
finalize ledger pairing.

As with the paired lesson, `lessons-housekeeping` reviewed and RETAINED this entry on
2026-09-01, one day before the run that recurred it.

## Recurrence — 2026-09-03 (plan-170-make-the-attribution-usable)

**Third independent instance, and the sharpest figures yet.** Same shape, larger magnitude,
on a change of only 15 files.

| Phase | Worked | Tokens | Share |
|---|---|---|---|
| 5-execute | 2h56m | 1,414,503 | 23% |
| 6-finalize | 3h13m | 3,667,334 | **59%** |

Finalize again exceeded every other phase **combined** (3.67M vs 2.57M). The anchor row here is
`multi_module + enhancement`, which is `fallback`-graded, so the four Section-1 ratio thresholds
govern — and all four trip: `tokens_per_file_modified` 479,845 (9.6x the 50K threshold),
`total_tokens_per_deliverable` 2,079,329 (4.2x), `max_phase_token_share` 0.59, and
`worked_seconds_per_task` 2,498.71 (2.8x, against a threshold the reference itself documents as
unanchored for worked time). As before, `6-finalize` never closed an `end_time`, so all figures
are floors on the largest row.

### The measurement this lesson asks for

This run supplies it for the error-terminated half of the loop, which the earlier instances could
only estimate:

- `pre-submission-self-review` **dispatched 5 times** — 4 terminating `error`, 1 `step_complete`.
- The **4 error-terminated finalize dispatches cost 945,180 tokens**: **15.2%** of the plan's
  entire 6,237,987-token spend, on one step, re-examining a 15-file change.
- `retryable_total_tokens` is **0** in both dispatching phases, so none of that is infrastructure
  a re-run would have recovered — it is the loop's own cost.

The rounds converged 4/3/2/1/0 findings and each of the first four produced real fixes, so this
is again *productive* re-firing rather than waste. But the price of the convergence is now
measured rather than inferred.

⛔ **The ledger caveat from the second instance has not improved, and is quantified here.**
`reconcile-ledgers` on this plan reports `union_rows: 36` against `execution_log_rows: 23` and
`boundary_rows: 23` — neither ledger alone sees more than **64%** of dispatches, and 2,361,789
tokens (37.9% of the plan) appear in the boundary ledger with no `record-step` row naming them.
The 945,180 figure above is drawn from the boundary ledger, which is the more complete of the two
for finalize, but it remains a floor. Three consecutive instances have now asked for cost
measurement before bounding, and three have had to qualify their own numbers.

A further disagreement surfaced this run: three surfaces report different firing counts for the
same step — the boundary ledger says 5 dispatches, `status.metadata.phase_steps` says
`firing_count: 3`, and only 2 `record-step` rows name it. Any bound placed on "number of rounds"
would depend on which surface the bounding code happened to read.

### Evidence

- aspect: plan_efficiency — all four fallback ratios tripped; `dominant_phase 6-finalize=3667334`
- aspect: logging_gap_analysis — `error_total_tokens` 945,180 (6-finalize) + 139,309 (5-execute)
- `manage-metrics reconcile-ledgers --plan-id plan-170-make-the-attribution-usable`
- `project:finalize-step-lessons-housekeeping` again reviewed and RETAINED this entry on both of
  its firings during the very run that recurred it, recording it as "2nd independent observation,
  still unapplied" — the retain was correct (this plan did not touch the finalize lane), but the
  lesson has now been retained-unapplied across three runs.
