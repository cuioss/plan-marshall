# Verification — 520-orchestrator-inbox-lifecycle-cleanup-and-landing-payload

**Verified against:** commit `e0764ab14`   **Landed as:** PR #1317, commit `9038b8c6e`   **Verdict:** implemented-with-gaps

## Method

What was actually done:

- Read `plan.md` (501 lines) and `report-01.md` (632 lines) in full; extracted all eight deliverables with their *Done when* conditions, the six Out-of-scope boundaries, the seventeen-entry Expected surface, the ten Claim-labels rows, the six Verification bullets, and every `closes:` annotation.
- Read the landed commit `9038b8c6e` (`Orchestrator inbox lifecycle, landing payload, and compaction signals (truthful-signals/520) (#1317)`) — full `--name-status -M` list (**25** paths, one of them the `R100` rename of the plan file into its directory) and every substantive hunk.
- **Tree currency established first.** `git diff --name-only 9038b8c6e HEAD` touches 147 paths, but **none** of this plan's code or documentation surface: the only overlap is `.plan/marshal.json` (a later `chore(steward)` adding `plan.phase-6-finalize.steps.default:emit-landing` — 302/G7, which this plan deliberately left next door — and re-stamping `system.provisioned_version`), plus five unrelated `doc/plans/**` reports. Every citation below therefore re-derives at HEAD.
- Read all seven source `gaps.md` files (`090`, `120`, `180`, `250`, `280`, `300`, `302`) and re-derived the id population: **51** gap ids across the seven files (3 + 4 + 7 + 12 + 7 + 9 + 9); this plan claims **30 closed + 1 excluded = 31**, and names four further ids (302/G3, G4, G5, G7) as next-door work. **The source set is seven directories, not six** — independently confirmed by enumerating the `{plan}/gaps.md` paths every `closes:` annotation resolves to.
- **Executed rather than read.** Every behavioural claim was driven through the real code with `test/conftest.py`'s marketplace `sys.path` bootstrap and a `PLAN_BASE_DIR`-isolated store under `$TMPDIR`:
  - `check_landing_completeness` over **thirteen** payload bodies: all-real → `(True, [])`; `total_tokens`/`steps`/`deliverables_*` all `n/a` → `(False, ['deliverables_total','deliverables_done','total_tokens','steps'])`; `pr=n/a` + `merge_state=n/a` with everything else real → `(True, [])`; all-`n/a` → `(False, ['schema'])` (the schema branch fail-closing first, exactly as the plan's ⛔ warns); `'  N/A  '` → `(False, ['total_tokens'])`; a genuine `deliverables_done=0` → `(True, [])`; a prose-only body → all eight keys missing; and `merge_state` over each of its five spec values.
  - `cmd_compact` over **five** fixture ledgers: markers absent → `abstained_count 4`, `unreachable_count 1`, and the `Ordered Queue` row's `treatment` is `markers_absent_not_regenerated` while the four narrative rows stay `preserved_verbatim`; a hand-written line between present markers → `replaced_body` carries that line's text; a second pass → both blocks `unchanged` with `replaced_body: ''`; a **partial** marker pair (`BEGIN` with no `END`) → `unreachable_count 1` (CR-1's fix, confirmed live); a `phase: closed` epic → `status: error`, `error: refused_closed`.
  - The whole stream-closure lifecycle through the real handlers: `write` → success; `close-stream` → success `already_closed: False`; a second `write` for the same sender → `status: error`, `error: stream_closed` with a full explanatory message; a second `close-stream` → success `already_closed: True` naming the **existing** marker (nothing allocated); another sender's `write` → unaffected.
  - The three zeros: EMPTY `{count 0, live 0, closed [], invalid 0}`; BLOCKED `{count 1, live 0, closed [], invalid 1}` (after corrupting `kind=` in a queued envelope); FINISHED `{count 1, live 0, closed ['p1'], invalid 0}`. Also reproduced survivor **S3**: a superseded-only queue yields the EMPTY triple with `count: 1`.
  - The `inbox` sub-verb set, by building the real argparse group and reading `_SubParsersAction.choices`: **ten**, in registration order — `write, amend, supersede, close-stream, validate, list, archive, migrate-archive, detect, landing-check`.
  - `_marker_indices` over five marker layouts, and `_read_queue_spec_reconciliation`'s call graph via an AST walk.
- **Ran the plan's tests.** `uv run python -m pytest` (via the repo venv) over the five changed test modules → **399 passed**. The whole `test/plan-marshall/plan-orchestrator/` directory at HEAD → **586 passed**.
- **Two mutations, each restored from a byte snapshot** (never with a git command; `git diff --quiet` checked clean on each path first, md5 verified after, `git status --porcelain` empty at the end):
  1. Deleted row 13 (`revision_not_monotonic`) from `SKILL.md` § `inbox validate` → `test_inbox_validate_still_lists_every_retained_rejection_code` **RED**, `1 failed, 53 passed` — reproducing the report's figure exactly.
  2. Dropped `'steps'` from `LANDING_SENTINEL_REJECTING_KEYS` → `TestDegradedSentinelFacts::test_degraded_counts_tokens_and_steps_are_reported_missing` **RED**, `1 failed, 21 passed`.
- **Re-derived every count the plan or report states**, from the tree rather than from the report: the ten verbs; the fourteen rejection codes (7 base + 4 state + 3 verb-own, plus the unreachable `invalid_envelope` fallback); the three `ORCHESTRATOR_KNOWN_KEYS`; `git ls-files .plan/marshal.json`; `plugin.json`'s adjacent-inversion count (**4**, all four pre-existing); the 120 token census at `68a21cac` (`git grep -c` → **264 lines / 73 files**, **13** tracked `.plan/` files, **0** `.plan/` hits); the 250 test counts by extracting `git archive 51d1c9bc` into `$TMPDIR` and re-running (**42** / **224** / **550**); the 300 disposition table's multiplicities (8 rows summing to **13** across 5 files); the D4 report-contract statement sites (**4**); the corpus-count producer sites (**5** across three functions, none of them a branch); and the landed diff's footprint (**8** `*.py`, **14** `marketplace/**`, **5** `doc/plans/**`, **0** `.claude/skills/**`).
- **Ran both D2 closure sweeps at HEAD**: `PLAN-48` over `marketplace/` → **0 hits**; the empty-block phrasings → **0 hits**. Ran the D3 overclaim sweep (`nothing material`, `whole mechanisable delta`) over `marketplace/`, `test/` and `.claude/` → **0 hits** outside `doc/plans/`. Ran the `quiescence` sweep over both orchestrator skills → 5 hits, all inside `cleanup.md`.
- Read `analyze.md` Step 3, Step 5b, Step 6 and § Output end to end (it is LLM-executed markdown with no entry point, so this check is a **read**, as the plan requires it be stated), plus `cleanup.md` Steps 8–9 and its `## Output` block, `inbox-envelope.md`, `landing-payload-spec.md`, `orchestration-model.md` § Dispatch Decision Rule / § Carve-outs / § Ledger-Compaction Stage, and `emit-landing.md`.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D1 | Derivation gate | Four populations derived and recorded verbatim with file+symbol; no premise failed | Yes | Yes | Yes | Yes | All four independently re-derived and identical to the report: 10 verbs from the live `_SubParsersAction.choices`; 14 codes from `validate_envelope` + `_validate_state_fields` + `cmd_inbox_validate`; 3 keys in `_config_defaults.ORCHESTRATOR_KNOWN_KEYS`; `git ls-files .plan/marshal.json` → tracked. The `invalid_envelope` fallback is genuinely unreachable — every `validate_envelope` rejection branch returns a literal |
| D2 | Orchestrator block discoverable | Guard passes and was seen RED; both sweeps return no hit | Yes | Yes | Yes | Yes | `.plan/marshal.json` → `{"auto_emit": false, "effort": {}, "parallelization_scope": 1}`; `test_config_defaults.py:2016` derives its expectation from the constant, asserts the block is an **object**, and pins values against `get_default_config()['orchestrator']` (CR-3/CR-15 folded); `marshal-json-reference.md:119-160` rewritten; both sweeps → 0 |
| D3 | A degraded landing fact is missing | Degraded case `False` naming exactly those keys, `pr`/`merge_state` case still `True`, both pinned; every MECHANISABLE row required-or-recorded-optional | Yes | Yes | Yes | Yes | `_orchestrator_inbox.py:843/857/866` (`LANDING_DEGRADED_SENTINELS`, `LANDING_SENTINEL_REJECTING_KEYS`, `_is_unsupplied`); executed → exactly the two required outcomes. `landing-payload-spec.md:40-56` marks each row **required**/**optional** with an explicit "MECHANISABLE does not mean required" note; `analyze.md:115` states the narrower claim. Mutation → RED |
| D4 | could-not distinguishable from chose-not | An `abstained[]` row whose treatment is not `preserved_verbatim`, RED-first; repaired narrative test non-tautological; three keys declared; exactly one branching function | Yes | Yes | Yes | Yes | Executed: `markers_absent_not_regenerated` + `unreachable_count 1` with `abstained_count` excluding it. `test_orchestrator_compact.py:368` now performs a real second `_run()` and asserts `after_second == after_first`. `cleanup.md:216-222` declares four `compaction_*` keys. AST walk → only `_read_queue_spec_reconciliation` branches; the other five sites are dict-literal producers in `cmd_corpus_enumerate`/`_verdicts`/`_cross_check` |
| D5 | Drain acts on lifecycle; `stream-end` enforced | Superseded/stream-end rows dispositioned without running their `kind` branch, closure equation intact; post-close `write` refused; double `close-stream` idempotent; blocked zero pinned; Step 9 no longer asserts no signal exists | Yes | Yes | Yes | Yes | `analyze.md:70-77` reads `lifecycle` **before** `kind` with `retired_by_successor` / `stream_end_noted`; `:89` and `:244` fold both into `messages_archived` and restate the equation. Executed: `stream_closed` refusal and `already_closed: True` idempotence. Three zeros reproduced. `cleanup.md:153` now names `closed_senders`/`live_count` and gives the sender-population shortfall |
| D6 | Epic tree, migration, dispatch label | `settled.md` in both places; Step 8 marker-insertion conditioned on absence; a hand-written line named in the report; canonical form carries `--workflow` and `--plan-id none` | Yes | Yes (one disclosed `[A]`→`[B]` deviation) | Yes | Yes — and wider than scoped | `orchestration-model.md:29` (layout) and `:87` (carve-out list); `cleanup.md:105-141` carries Rule 1 / Rule 2 with the phase gate ahead of both judgement writes; `replaced_body` executed and carries the overwritten text; `orchestration-model.md:132-145` canonical form. CR-4 additionally landed `--workflow` at `analyze.md:38`, `decompose.md:34` and `effort-roles.md:86` — the sites that actually resolve |
| D7 | Every `inbox` enumeration names the registered set | Contract test RED when a code is deleted; § Related and `_add_inbox_group` name exactly D1(a); help names the new verbs; Invariants names exactly two in-place verbs with a matching lead; module docstring names the foldered target | Yes | Yes (one disclosed refuted premise) | Yes | **Partial** (G6) | 14-row table at `SKILL.md:245-259`; mutation → `1 failed, 53 passed`. `inbox-envelope.md:169` names all ten verbs; `orchestrator.py:2649-2651` docstring and `:2666-2669` help literal both correct; `:33` module docstring names `inbox/archive/{sender}/` with the flat error-path carve-out. **But** `inbox-envelope.md:129` still calls `amend` "the one sanctioned in-place edit" twelve lines above `:135`'s "two sanctioned in-place edits" |
| D8 | Records and registration cosmetics | Two entries in order and nothing else moved; each corrected figure stated with its base; no figure contradicted by its own evidence table | Yes | Yes | Yes | Yes | `plugin.json`: 76 entries, **4** adjacent inversions, all four the pre-existing ones § Out of scope names; both moved entries in alphabetical position; `marshall-steward` still directly after `manage-terminal-title`. 120 report re-derived at `68a21cac` (264/73 confirmed by me); 250 report 148 lines, `_pending_` count **0**, every `##` heading once, 42/224/550 confirmed by re-running the extracted `51d1c9bc` tree; 300 report states **13** at all four sites and 8 rows sum to 13 |

**D7 — the one non-clean cell.** 250/G7's *Done when* has four clauses. Three are met exactly: the § Invariants bullet at `inbox-envelope.md:135` now leads with "two sanctioned in-place edits", names exactly `amend` and `supersede`, classifies `close-stream` as an **append** ("composes a fresh envelope and allocates a new sequenced path, opening no existing file" — confirmed against `cmd_inbox_close_stream`, which does exactly that), and cites rather than contradicts `orchestration-model.md`. What survives is the *sibling* clause the gap itself cited as evidence: `inbox-envelope.md:129`, in § Message-state vocabulary, still reads "**`amend`** replaces a message's body IN PLACE — **the one sanctioned in-place edit** —". One document now states one count at `:129` and a different one at `:135`. The gap's fix instruction was "Rewrite the bullet … so the lead and the list agree"; that bullet was rewritten and its sibling was not. Recorded as G6, `low`: no behaviour is wrong and `close-stream`'s classification — the load-bearing half — is correct everywhere.

Everything else in D7 checks out on execution: the fourteen rejection codes are the fourteen the code can return, in the order the checks run (verified by reading `cmd_inbox_validate` → `validate_envelope` → `_validate_state_fields`); the cross-reference to `inbox-envelope.md`'s own table is correctly scoped to codes 4–14 and explicitly warns the row numbers do not line up (R2-13's fix); every one of the ten names on the § Related line resolves to a `### inbox {verb}` section in `SKILL.md`; and the report's disclosed premise refutation is real — the module docstring at `:57-69` now names all ten verbs, `landing-check` included.

## Upstream gap closure

Every `closes:` id, checked against its own `gaps.md` *Done when* at HEAD.

| Deliverable | Gap id | Verdict | Evidence |
|---|---|---|---|
| D2 | 090/G1 | **CLOSED** | `.plan/marshal.json` → three-key block; `test_committed_marshal_json_surfaces_every_orchestrator_knob` derives from `ORCHESTRATOR_KNOWN_KEYS`, asserts the value is an object, and pins values against the seed |
| D2 | 090/G2 | **CLOSED** | `marshal-json-reference.md:123` names all three seeded keys and their effective defaults; `:147` makes the seeded `1` the stated default with the unset case a legacy note; `:151` table row rewritten. Sweep → 0 |
| D2 | 090/G3 | **CLOSED** | `grep -rn "PLAN-48" marketplace/` → **0 hits**; `auto_emit` has its own § at `marshal-json-reference.md:158` and a table row at `:156` |
| D3 | 302/G1 | **CLOSED** | **Executed at HEAD.** `total_tokens`/`steps`/`deliverables_*` = `n/a` → `(False, ['deliverables_total','deliverables_done','total_tokens','steps'])`; `pr=n/a`+`merge_state=n/a` with all else real → `(True, [])`; `' N/A '` → rejected; a genuine `0` → accepted. Both directions pinned by `TestDegradedSentinelFacts`, and the rejection case turns RED when one key is dropped from `LANDING_SENTINEL_REJECTING_KEYS` |
| D3 | 302/G2 | **CLOSED** | `landing-payload-spec.md:44-46` marks `steps` as carrying outcomes only and routes the typed facts, wall-clock and end-state to explicitly **optional** keys; `:49-56` adds the "MECHANISABLE does not mean required" note; `analyze.md:115` claims only the required subset. Overclaim sweep → 0 in `marketplace/`, `test/`, `.claude/` |
| D3 | 302/G9 | **CLOSED** | `_orchestrator_inbox.py:809-826` names `landing-payload-spec.md` § "Required machine-readable fact keys" and `emit-landing.md` Step 2 as restating sites, names the two binding tests (both present at `test_landing_completeness.py:334` and `:344`), and explicitly defers the tie-break to the spec's own sentence rather than restating it |
| D4 | 180/G6 | **CLOSED** | Executed on a marker-less fixture: `{'section': 'Ordered Queue', 'treatment': 'markers_absent_not_regenerated'}` with `unreachable_count: 1` and `abstained_count: 4` excluding it. `TREATMENT_UNREACHABLE` at `orchestrator.py:311`; `GENERATED_BLOCK_OWNING_SECTION` parity asserted at import (`:318`, CR-2) |
| D4 | 180/G4 | **CLOSED** | `cleanup.md:216-222` declares `compaction_regenerated[R]`, `compaction_invariants[I]`, `compaction_abstained[B]` (the `[A]` collision with `applied[A]` is a disclosed deviation) plus `compaction_migrated[M]`; `:143` names them in the Step 8 instruction; `:234` states each as required-never-omitted |
| D4 | 180/G1 | **CLOSED** | `test_orchestrator_compact.py:368-388` performs the second `_run()` and asserts `after_second == after_first`; both misleading comments corrected. No assertion is true independently of the code under test |
| D4 | 180/G8 | **CLOSED** | AST walk: only `_read_queue_spec_reconciliation` (`orchestrator.py:1881-1888`) branches on the three counts; `_corpus_signal` (`:1897`) and `_invariant_queue_spec` (`:2132`) map its neutral triple into their own vocabularies; both public shapes unchanged (586 tests green unedited) |
| D5 | 250/G3 | **CLOSED** | `analyze.md:70` reads `lifecycle` **before** `kind`, with a two-row table dispositioning `superseded` → `retired_by_successor` and `stream-end` → `stream_end_noted`, each stopping before the `kind` branch; `:89` and `:244` fold both into `messages_archived` so `messages_archived + messages_invalid + messages_archive_failed == messages_scanned` still closes; `:189-197` keys the zero on all three fields |
| D5 | 250/G2 | **CLOSED** | `cleanup.md:153` — fact 2 now reads "No EPIC-WIDE emission-quiescence signal exists today", names `closed_senders` and `live_count` explicitly, and gives the true shortfall (the sender population is open, so a closure over the observed set is consistent with a sender about to appear). The deferred-mechanism block is kept as the successor surface |
| D5 | 250/G12 | **CLOSED** | **Executed.** `write` after `close-stream` → `status: error`, `error: stream_closed`; second `close-stream` → `status: success`, `already_closed: True`, naming the existing marker and allocating nothing; another sender unaffected. One shared predicate `find_stream_end_marker` at both entry points; documented in `inbox-envelope.md` § Write-side deliverability and `SKILL.md` § `inbox write` / § `inbox close-stream` |
| D5 | 250/G9 | **CLOSED** | Three zeros reproduced by execution. `inbox-envelope.md:22-28` and `SKILL.md:270` both tabulate EMPTY / FINISHED / BLOCKED with the `invalid_count` condition named; a test pins the blocked zero against the empty one on the same two fields |
| D6 | 180/G3 | **CLOSED** | `orchestration-model.md:29` — `settled.md` in the tree-layout code block between `history.md` and `references.json` with the mid-life comment; `:87` — added to the § Carve-outs ledger-document parenthetical |
| D6 | 180/G7 | **CLOSED** | `cleanup.md:105-141` carries the one-time migration **before** the script call, per **block** (a table gives each block's owning section and annotation zone — CR4-3 extended it past the queue to `resume-summary`), with Rule 1 gated on the pair being absent file-wide and Rule 2 on it being present. § Ledger-Compaction Stage in `orchestration-model.md` names the same obligation next to the never-fabricate rule |
| D6 | 180/G2 | **CLOSED** | Executed: with a hand-written line between present markers, the `ordered-queue` `regenerated[]` row's `replaced_body` carries that line's text; `unchanged` and `markers_absent` both report `''`. `TestReplacedBody` pins both. `cleanup.md:238` additionally declares `compaction_migrated[]` so the inline migration itself is reportable (CR4-6) |
| D6 | 280/G7 | **CLOSED** — wider than the gap's Done-when | `orchestration-model.md:132-139` carries `--role orchestrator.{surface}`, `--plan-id none`, `--caller` and `--workflow`, with `:145` stating the resolve seam emits the `[DISPATCH]` line and its paired decision-log record per firing. Beyond the Done-when, CR-4 landed the same four facts at the two sites that actually resolve (`analyze.md:38`, `decompose.md:34`) and at `effort-roles.md:86`. `_cmd_effort.py:529-546` confirms `--workflow` is the gate |
| D7 | 250/G1 | **CLOSED** | `SKILL.md:245-259` tables all fourteen codes in check order with the raising seam, and the "checked in that order" clause is made exact. **Mutation-verified**: deleting row 13 turns `test_inbox_validate_still_lists_every_retained_rejection_code` RED (`1 failed, 53 passed`) |
| D7 | 250/G5 | **CLOSED** | `inbox-envelope.md:169` names all ten verbs; each resolves to its own `### inbox {verb}` section in `SKILL.md` (checked by name) |
| D7 | 250/G6 | **CLOSED** | `orchestrator.py:2666-2669` — the help literal now names correction (`amend`/`supersede`), stream termination, archive foldering, detection and landing-check |
| D7 | 250/G11 | **CLOSED** | `orchestrator.py:2649-2651` — the "Sub-verbs, in registration order:" line is exactly the ten the parser registers, `landing-check` included |
| D7 | 250/G7 | **PARTIALLY** | `inbox-envelope.md:135` (§ Invariants) is fixed on all four of its own clauses — lead and list both say **two**, `close-stream` is classified an append, and it cites `orchestration-model.md`. But `:129` (§ Message-state vocabulary) — the sibling clause the gap itself quoted as the disagreeing half — still reads "the one sanctioned in-place edit". The document contradicts itself on the count twelve lines apart. See G6 |
| D7 | 250/G8 | **CLOSED** | `_orchestrator_inbox.py:30-40` names `inbox/archive/{sender}/` as the write target, states the off-shape flat fallback preserving `invalid_message_name`, and keeps the never-a-caller-supplied-path claim (true in both branches) |
| D8 | 120/G1 | **CLOSED** | Re-derived: 76 entries, **4** adjacent inversions, all four the pre-existing set § Out of scope leaves alone; `persona-plan-orchestrator` sits between `persona-plan-marshall-agent` and `persona-security-expert`; `plan-orchestrator` between `plan-marshall-plugin` and `plan-retrospective`; `marshall-steward` directly after `manage-terminal-title` |
| D8 | 120/G2 | **CLOSED** | 120 `report-01.md:20` re-derived at `68a21cac` — I independently reproduce **264 lines / 73 files**. The unreproducible 265/74 is stated as unrecoverable, the delta carries no assigned cause, and the figure is **not** re-attributed to `68a21cac` (the plan's ⛔ honoured). The union-count bullet — a site the gap did not name — was corrected with it |
| D8 | 120/G3 | **CLOSED** | 120 `report-01.md:40` now scopes git-ignorance to `.plan/local/**` and the generated executor, names the two tracked paths, and states they were searched and returned nothing. Independently confirmed: `git ls-tree -r 68a21cac -- .plan/` → **13** files; `git grep -c 'marshall-orchestrator' 68a21cac -- .plan/` → **0** |
| D8 | 120/G4 | **CLOSED** | 120 `report-01.md:19` now states that ripgrep honours the `.gitignore` **negations**, so the tracked `.plan/` paths were *inside* the swept population and contributed no hits |
| D8 | 250/G10 | **CLOSED** | 250 `report-01.md` is **148** lines, `_pending_` count **0**, every `##` heading exactly once. Counts corrected to 42 / 224 / 550, each stated at `51d1c9bc` — all three reproduced by me from the extracted tree (`42 passed`, `224 passed`, 550 collected). The `16287` figure is left standing with its command, its base, and an explicit no-cause-assigned note — the honest disposition, since the report line it corrects states no collected total |
| D8 | 300/G5 | **CLOSED** | `grep -c '11 stale'` → 0 and `grep -c 'all 11 fixed'` → 0; the figure is **13** at all four sites (`:226`, `:230`, `:303`, `:315`) and the table's lead-in states the multiplicity. Re-summed independently: 8 rows × (2+2+1+1+1+4+1+1) = **13** across **5** files |
| — | 250/G4 | **correctly EXCLUDED** | Its population is `.plan/local/orchestrator/{epic}/inbox/archive/`, git-ignored and absent from this clone (confirmed: `.gitignore` re-includes only `marshal.json` and `project-architecture/`). Recorded as an operator obligation in § Proposals, with `inbox migrate-archive` shipping and idempotent |

**Totals: 30 claimed closed → 29 genuinely CLOSED, 1 PARTIALLY (250/G7), 0 NOT-CLOSED.** 302/G1 — *"`n/a` is truthy, so a landing that transmitted nothing reads complete"* — is **CLOSED**, established by executing `check_landing_completeness` over thirteen bodies at HEAD and by a mutation that turns the pinning test RED.

## Report accuracy

Re-derived every re-derivable figure in `report-01.md`. Four contradictions, none of which changes a deliverable verdict:

1. **§ Survivors S2 is stale — and the report itself says so elsewhere.** S2 reads "the dispatch record is not emitted from the two sites that actually resolve … **Recorded, not fixed**". Twelve rows earlier, CR-4 reads "**FIXED at all three sites** (`effort-roles.md` too) … 280/G7's symptom is now closed where it occurs." The tree agrees with CR-4: `analyze.md:38`, `decompose.md:34` and `effort-roles.md:86` all carry `--workflow`. § Contract check step 6 inherits the error ("four survivors each with its bound"). Recorded as G1.
2. **§ Expected surface names one real off-list deliverable surface; there are two.** The confirm/refute table lists three off-list paths (the plan move, this report, `emit-landing.md`) and calls exactly one a "real deliverable surface beyond the list". The landed diff carries **four** off-list paths: `plan-orchestrator/workflow/decompose.md` is also absent from the plan's seventeen-entry Expected surface and is also a marketplace bundle file, reached by the same CR-4 fix. Recorded as G2.
3. **§ Contract check's footprint count is one low.** It states "13 `marketplace/**`"; `git show 9038b8c6e --name-only -M | grep -c '^marketplace/'` → **14**. The missing path is `decompose.md` again — the same unswept fix, at a second site. Recorded as G3.
4. **§ Residue item 3 is false at HEAD.** It reads "Neither Rule 1 nor Rule 2 firing has any declared output field, so a migration is unreportable." `cleanup.md:222` declares `compaction_migrated[M]{block,rule,zone_created,moved_lines}` and `:238` states it as required-never-omitted. § Residue was filled at round 5 (`83a0861f`); `compaction_migrated[]` was added by cold read 4's fix (`13d32a18`) and the residue table was not re-swept. Recorded as G5. Residue items 1, 2, 4 and 5 all still stand — verified: no deferred-relocation field exists in the `## Output` block, `applied[A]{spec,finding_class,source,destination}` still has no shape for a relocated section, `real_marshal_json` appears only in `build.py:376` and `:517` (zero group markers anywhere), and `emit-landing.md:169` still names three `merge_state` values against `landing-payload-spec.md:93`'s five.

Claims re-derived and found **accurate**: all four D1 populations; the "four report-contract statements updated, the plan named three" claim (sweep → `_abstained_sections` docstring, `cmd_compact` docstring, `SKILL.md`, `orchestration-model.md`); "five sites across three functions … a dict-literal producer, not a branch" (AST-confirmed: `cmd_corpus_enumerate` ×3, `cmd_corpus_verdicts`, `cmd_corpus_cross_check`); the D7 premise refutation (the module docstring was one verb short and now names ten); the plugin.json "6 → 4" inversion claim and "only the two named entries moved" (2 insertions / 2 deletions); the 51-id / 35-cited / 31-accountable gap arithmetic; "seven source plan directories" (the earlier "six" is refuted by the id set itself); the PLAN-48 and empty-block sweeps ("0 after" both); the `quiescence` HYPOTHESIS (all five hits inside `cleanup.md`); the CR-6 refutation figures (250 report = 148 lines, 552 backticks — even, `_pending_` = 0); "8 `*.py` — three production and five test"; "zero `.claude/skills/**`"; "five test modules changed"; M = 3 review bots (three `{bot_kind}.md` registry docs); every red-first figure I re-ran (`1 failed, 53 passed`; the D3 rejection case RED); and survivor S3's characterisation, which I reproduced exactly (a superseded-only queue yields the EMPTY triple with `count > 0`).

## Out-of-scope compliance

Clean on every declared boundary.

- **250/G4** — not attempted; recorded as an operator obligation for a machine holding `.plan/local/`.
- **`plan.phase-6-finalize.steps.default:emit-landing`** — untouched by this landing. `git diff 9038b8c6e^ 9038b8c6e -- .plan/marshal.json` changes only the `orchestrator` block; the finalize-step registration arrives later, in an unrelated `chore(steward)` commit between the landing and HEAD. `sync-defaults` was correctly **not** run: `system.provisioned_version` and `system.config_seed_fingerprint` are unchanged in this commit.
- **302/G3, G4 (typed producers, `records_facts`)** — no `phase-6-finalize` step contract changed. The one `phase-6-finalize` file touched, `emit-landing.md`, gains a single advisory sentence and a cross-reference; it changes nothing a producer must emit (`n/a` on a failed read remains correct and still never blocks emission). The plan's own § Out of scope excludes changing the producer *contract*, and this is not one — but it **is** outside the declared Expected surface, which the report correctly records as REFUTED.
- **Promoting the optional keys to required** — not taken; recorded as operator proposal 1.
- **The four pre-existing `plugin.json` inversions** — all four still present, untouched.
- **Plugin cache and `.plan/execute-script.py`** — neither in the diff.

The one genuine surface overrun is `decompose.md`, which is inside the plan's *substance* (280/G7's real dispatch site) but outside its declared file list — and the report's § Expected surface does not name it. That is a disclosure gap, not a scope violation.

## Residue carried forward

`report-01.md` § Residue declares five pre-existing items plus three process items.

| Item | Status today |
|---|---|
| 1 — no `## Output` field for a **deferred relocation** | **Still open.** The `## Output` block declares `applied[]`, `declined[]` and four `compaction_*` keys; nothing carries a deferred judgement |
| 2 — `applied[A]{spec,finding_class,source,destination}` cannot carry a relocated `epic.md` section | **Still open.** The row shape is unchanged |
| 3 — "Neither Rule 1 nor Rule 2 firing has any declared output field" | **FALSE at HEAD.** `compaction_migrated[M]{block,rule,zone_created,moved_lines}` was declared by this same landing. Recorded as G5 |
| 4 — `build.py:376`/`:517` cite a non-existent `real_marshal_json` xdist group | **Still open, confirmed.** A whole-tree sweep finds the name only in those two docstrings and in this report |
| 5 — `emit-landing.md:169` names three `merge_state` values against the spec's five | **Still open, confirmed.** `emit-landing.md:167-168` → `merged`/`open`/`n/a`; `landing-payload-spec.md:93` → `merged`/`open`/`closed`/`unknown`/`n/a`. This is load-bearing beyond cosmetics: a producer following the producer doc can never emit `closed` or `unknown`, so the consumer's careful "neither collapses into `open`" distinction is inert |
| Process — `cleanup.md` § Step 8 has not been cold-read in its shipped form | **Still open**, and honestly stated |
| Process — the loop did not converge for five rounds; CR-1 was found by CodeRabbit, not by any round | **Recorded**, and independently corroborated: CR-1's partial-marker-pair case is real and its fix works (executed) |
| Process — a document corruption introduced while filling the report | **Repaired**; the 250 report's headings each appear exactly once |

## What could NOT be verified

- **The three cold reads and the eight verification rounds.** Every round, every cold-read pass, and their per-round token figures are conversation events with no committed artifact. Their *outcomes* are verified — the shipped text, the tests, and the mutations — but "round 5 found 15" and "cold read 2's first pass found two defects" are not re-derivable from the tree. The report itself concedes the shipped § Step 8 text has had no cold read in its current form.
- **The historical CI figures.** `./pw verify` (`21353`/`21354 passed, 14 skipped`), `test-compile` (`783 → 934` source files), `mypy 416 files`, the two wall-clock durations, and round 6's "1747 tests green" are point-in-time observations. I ran the plan's five test modules (**399 passed**) and the whole `plan-orchestrator` suite (**586 passed**) at HEAD, but did not attempt a full `./pw verify`.
- **The D6(iv) `[DISPATCH]` log line.** The report quotes it from `.plan/local/logs/work-2026-08-20.log`, which is git-ignored and outside what this verification may read or write. I verified the *mechanism* instead: `_cmd_effort.py:529-546` gates the two audit records on `--workflow`, and all three call sites now pass it. I deliberately did not run a resolve, because it would write into `.plan/`.
- **The `--default` counterfactual and the six `orchestrator.effort` configurations** round 6 executed. Same reason: exercising the resolver writes to the global log.
- **The rebase range-diff** (`25` commits, all `=`), the `31`/`35` citation rewrites, and the two commit messages carrying stale SHAs. `9038b8c6e` is a squash, so the pre-squash history is not in this clone's mainline; I did not fetch the PR branch.
- **The PR #1317 review-thread state.** I made no GitHub API calls. The 15 CodeRabbit findings' *substance* is verified where it reaches the tree — CR-1 (partial marker pair) reproduced and its fix confirmed by execution; CR-2 (the import-time parity assertion) present at `orchestrator.py:318`; CR-3/CR-15 (object-shape and value pinning) present in the guard test; CR-4 (three dispatch sites) present; CR-6's refutation figures re-derived exactly.
- **250/G4's physical migration** — its population is absent from every clone by construction.
