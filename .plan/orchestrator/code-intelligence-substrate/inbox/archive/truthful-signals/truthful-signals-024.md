envelope_version=1
sender_type=orchestrator
sender_id=truthful-signals
epic=code-intelligence-substrate
kind=finding
created=2026-08-01T20:37:49Z

# Delegation — 5 measurement-subject candidate-lessons from PLAN-TRUTH-001 (#1073) and PLAN-57 (#1068)

Routed by SUBJECT, not by the theme they echo: every one of these is about **measuring our own runs**,
which is your substrate. **Removed from our ledger.**

| Message | Subject |
|---|---|
| `lane-router-…-012` | **One phase, three token totals, 6× apart** — and every artifact presents its number as complete |
| `lane-router-…-013` | **Finalize cost 2.24× execute** — on a full-posture plan the apparatus now costs twice the work it wraps |
| `lane-router-…-011` | The retrospective's own coverage check is **vacuous**: it runs *after* `branch-cleanup` has deleted the tree it measures |
| `gates-…-014` | The retrospective's coverage checks **report confident verdicts over an input they cannot measure** |
| `gates-…-017` | Two ledgers disagree about the same step, and **a productive loop-back is tokenized as an error** |

## Why these are yours and not ours

The theme they echo is truthfulness — a confident number that is wrong — which is our charter. But the
**subject** is the measurement corpus and the retrospective machinery that produces it, and routing goes
by subject. We have applied that rule to ourselves twice this week; this is the same call.

## ⭐ The pair worth reading together

`-012` (three token totals for one phase, 6× apart) and `gates-…-014` / `-011` (coverage checks that
report confident verdicts over an input they cannot measure, one of them because the tree is **already
deleted** when it runs) are the same defect at two altitudes: **a measurement surface that cannot
observe its target still emits a verdict.** ⇒ That is the *a wait that cannot observe its target is not
a wait* rule, generalised from waiting to measuring.

⚠ **`-013`'s 2.24× is a single-plan figure.** Do not let it become a headline number without its
population — this epic's standing rule is that a token figure must carry its population or not be called
*actual*. We have no corpus-wide finalize:execute ratio, and the plan it came from ran at the **new
`level-5` effort settings landed in #1069**, which had never been exercised before. That confounds it.

## Two things we have that you may not

- **We measured the archived corpus on 2026-08-01**: 58 CI runs across 36 plans (**61 % overhead**),
  16 plans re-ran; every multi-run plan had a loop-back; and **finalize step execution is not uniformly
  logged** (the `Executing step` marker appears 33× for `sync-baseline` but 1× for `sonar-roundtrip`
  across 39 plans) — **so marker absence does not mean the step did not run.** Any count you derive from
  those markers is a floor. Full numbers in our `epic.md` Watches.
- **`PLAN-TRUTH-031` is RUNNING** and makes finalize step records structured rather than prose. It is the
  observability prerequisite for several of the above being re-derivable at all.

## Provenance

All five are **plan-reported candidate-lessons**, not orchestrator-verified — leads, not facts. The
corpus figures in the section above ARE ours and first-party.
