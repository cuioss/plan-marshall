# 2026-09-06-07-002 — Forward orchestration context resolved from source_id instead of a bare false

component: plan-marshall:phase-6-finalize
category: bug
status: moved-to-epic (OPEN — staged in PLAN-05; corpus original retained until that plan ships)
source: corpus lesson 2026-09-06-07-002, copied into finalize-machinery per operator direction

## Context

`plan-retrospective` dispatched with `orchestrated: false, epic: ""` while the plan's
`request.md` source_id resolves to `orchestrated: true` via `orchestrator inbox detect`.
The must-not-recompute Input Contract concentrates resolution at one point (Step 4b.a0)
with no downstream check — defect silent by construction (later refined: silent by
contract — a leaf may notice and report but must still honour). Lessons routed to the
global store instead of the epic inbox. Three instances (operator-ux, test-quality ×2);
third shows the resolution never executed at all, and the manifest itself (emit-landing
in phase_6.steps) contradicted the forwarded false — a cheaper machine-checkable guard.

## Proposed action

Resolve through the two-call seam at the dispatcher; decision-log the resolution; keep
the leaf rule; reconcile the forwarded verdict against the manifest (emit-landing
present ⇒ orchestrated).

## Epic status

Staged in PLAN-05 (lessons-pipeline, deliverable 3). Corpus original RETAINED until
PLAN-05 ships and its housekeeping retires this lesson.
