# Verification — inert-thinking-directives-in-dispatched-docs

**Verified against:** commit `4ea24cbd` (work performed against `ac06e4fc`; the intervening commit is doc-only and touches no file in this plan's surface)   **Landed as:** PR #1138, commit `94eb7521`   **Verdict:** implemented-with-gaps

## Method

What was actually done:

- Read `plan.md` and `report-01.md` in full; extracted the three deliverables with their *Done when* conditions, the Out-of-scope boundaries, the Expected surface, the seven Claim-labels rows, and the four Verification bullets.
- Located the landed commit `94eb7521` (`fix(plugin-doctor): flag inert thinking directives in dispatched workflow docs (#1138)`) via `git log --oneline -- doc/plans/truthful-signals/040-…/`. Read its full `--raw`/`-M` file list (12 files, one of them a rename of the plan file into the plan directory) and the complete D1 diff.
- Identified the three follow-up commits that amended `report-01.md` after the merge (`4eadea43` #1144, `f5903015` #1146, `a3eb36bb` #1147), so the report under test is the amended one at HEAD.
- Opened at HEAD: the analyzer `_analyze_thinking_directive_in_workflow_docs.py` (full read, 480 lines), the test file `test_analyze_thinking_directive_in_workflow_docs.py` (full read), the two D1 docs, `_runner.py` (quality-gate + analyze wiring and the `_scoped`/`_suppressed` closures in `doctor-marketplace.py`), `_rule_registry.py`, `test_runner.py` golden labels, `_fixtures.py` firing-fixture entry, `rule-catalog.md`, `rule-provenance.md`, `test/_shared/_dispatch_roster.py`, and `marketplace/bundles/plan-marshall/agents/execution-context.md`.
- **Executed** the detector rather than reading it:
  - `enumerate_execution_context_workflow_docs(marketplace/bundles)` at HEAD → **33** docs; `analyze_…(…)` → **0** findings.
  - Reconstructed the pre-fix tree with `git archive 94eb7521^ marketplace/bundles` into a scratch dir and ran the *current* detector against it → population **33**, findings **5** (`research-best-practices.md:9,46,112` family `ultrathink`; `content-review.md:7,177` family `careful-reasoning`). This reproduces D2's two numbers exactly and independently confirms the `:9/:46/:112` and `:7/:177` leads from the Claim-labels table.
- **Independently re-derived the population** with my own frontmatter reader over `marketplace/bundles/**/*.md` (all paths, not the analyzer's shape restriction): 33 declaring docs, of which 33 fall inside the analyzer's `SKILL.md` + `workflow/*.md` shape and **0** fall outside it — so the analyzer's path restriction loses nothing.
- **Independent false-negative sweep**: scanned all 33 population docs with a regex superset of the plan's originating sample grep (`ultrathink`, `extended thinking`, `think step by step`, `step[- ]by[- ]step`, `take your time`, `before answering`, careful/deep + reason|think|consider|analyz, `chain of thought`, `reflect on`, `deliberate`) — ~30 hit lines (the exact figure is regex-construction-dependent and did not reproduce at 30 under adversarial re-derivation; a deliberately broader superset yields 230 hit lines and **0** directive-shaped candidates), every one procedural prose or the deliberately-excluded "careful analysis" review label. No live directive missed.
- **Whole-tree stale-restatement sweep**: `grep -rni 'ultrathink|ultra think|ultra-think'` and `grep -rni 'careful step-by-step reasoning|step-by-step reasoning|take your time|extended thinking'` over `*.md`/`*.py`/`*.json` — the only surviving occurrences are inside the rule's own module docstring, `rule-catalog.md`, `rule-provenance.md`, `_fixtures.py`, and the rule's test file. No prose doc, docstring, worked example, or string literal restates a removed directive.
- **Degenerate-fence check**: measured `_build_fence_map` over the population (14 524 lines, 2 941 inside-fence, ratio 0.20) and scanned for single-line and unclosed fences — 0 issues, so no systematic blind spot from the fence tracker.
- **Ran the plan's tests**: `uv run python -m pytest test/pm-plugin-development/plugin-doctor/test_analyze_thinking_directive_in_workflow_docs.py -o addopts="" -q` → **46 passed** (collected count re-derived with `--collect-only`: 46 tests).
- **Mutation check** (the highest-risk guard): saved the bytes of `research-best-practices.md` to a scratch snapshot, appended the exact removed directive `Use **ultrathink mode** for deep analysis and synthesis of research findings.`, re-ran the real-tree anchor test → **RED** (`AssertionError: Found 1 in-prose reasoning-level directive(s) …`, line 299, family `ultrathink`). Restored by copying the saved snapshot back; md5 matches the pre-mutation value (`270256fe414996885389a7464bbf0f92`) and `git status --porcelain` shows no modification to any file in this plan's surface.
- Verified the STOP-CONDITION rationale by opening `test/_shared/_dispatch_roster.py` (a heading-bounded Markdown-section/roster-row parser, last touched by #1026, i.e. before this plan) and enumerating its importers at commit `94eb7521` with `git grep -ln 'from _dispatch_roster import' 94eb7521`.
- Checked whether the two residue items are closed by later work: `.claude/skills/cloud-plan-lane/SKILL.md` and `.claude/skills/author-cloud-plan/SKILL.md`.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D1 | Fix the confirmed instances | Directives gone from both files; no criteria prose altered | Yes | Yes (one disclosed non-verbatim rewrite) | Yes | Yes | `git show 94eb7521 -- …/research-best-practices.md …/content-review.md`: three standalone `ultrathink` paragraphs deleted; `content-review.md:7` second sentence deleted, `:177` directive replaced. Detector over pre-fix tree → 5 hits; over HEAD → 0 |
| D2 | GATE: derive the dispatch roster, then sweep it | Roster size and hit count reported as two separate numbers; every hit fixed or recorded | Yes (re-scoped, disclosed) | Yes | Yes | Yes | Re-derived: population **33**, pre-fix hits **5**, post-fix hits **0**. Two separate figures present in `report-01.md` § D2 and in the commit message. `test/_shared/_dispatch_roster.py` unmodified (`git log` → last change #1026) |
| D3 | plugin-doctor rule preventing reintroduction | Rule ships with tests in both directions; detector publishes the population size it examined | Yes | Mostly — see below | Yes | **Partial** (G1, G6) | `_analyze_thinking_directive_in_workflow_docs.py::analyze_thinking_directive_in_workflow_docs`; wired at `_runner.py:203-205` (quality-gate, `scoped`) and `_runner.py:357` (analyze), `_rule_registry.py:132`, `test_runner.py:81` golden label, `_fixtures.py:453` firing fixture, `rule-catalog.md` § Content Rules, `rule-provenance.md:52`. 46 tests pass; mutation → RED |

**D3 — the one non-clean cell.** The plan's Verification section states: *"D3's detector must publish the population size it examined **in its own output**. Verify by running it and reading that number."* The implementation publishes `details.population_size` **inside each finding** (`_analyze_thinking_directive_in_workflow_docs.py`, the `Finding(... details={'family': family, 'population_size': population_size} ...)` construction in `_scan_doc`), and emits a dedicated `empty_population` finding when the derived population is zero *and* the ext-point standard doc exists. On the real tree — which is clean — there are **no** findings, so the runner's per-rule summary is `{'rule': 'analyze_thinking_directive_in_workflow_docs', 'findings': 0}` (`_runner.py`, the `emit()` closure: `rule_summaries.append({'rule': label, 'findings': len(findings)})`) and the population size appears nowhere in the output. The literal Verification instruction — run it and read the number — cannot be carried out on a clean tree. The vacuity hole itself is closed twice over (the zero-population finding, plus `test_real_marketplace_population_is_non_empty` asserting `>= 20`), so this is a partial miss on the stated requirement, not a false green. Recorded as G1.

Everything else in D3 checks out on inspection and on execution: the population is derived from each doc's own `implements:` frontmatter via `_declared_implements` (scalar and block-sequence forms both handled — `test_block_sequence_implements_is_recognized`, and the real tree exercises both: 26 docs use the inline scalar form, 7 the block-sequence form — re-derived during adversarial review; the document previously said 28/5); the six families are ordered with `break` after the first match so a line yields exactly one finding; the two bare-term families require a directive cue in a clause-bounded window, so `Extended thinking is configured by the dispatcher.` and `Use the search tool; ultrathink is documented below.` do not fire (both are committed negative cases and both pass); `finditer` + `_offset_in_inline_code` means a live directive after an in-code mention is still caught (`test_live_directive_after_inline_code_mention_fires`); fences are CommonMark-tracked for backtick and tilde, three-or-more markers. The negative cases are not vacuous — I confirmed the positive counterparts of each discriminator fire (`careful reasoning` fires while `careful analysis` does not; `step-by-step reasoning` fires while `step-by-step workflow/analysis` does not).

## Report accuracy

Re-derived every figure in `report-01.md`. Three contradictions, none of which changes a deliverable verdict:

1. **"39 both-direction tests"** (§ D3). The landed test file collects and passes **46** tests (`pytest --collect-only` → `46 tests collected`; full run → `46 passed`). The file has not been touched since `94eb7521` (`git log --oneline -- test/pm-plugin-development/plugin-doctor/test_analyze_thinking_directive_in_workflow_docs.py` → one commit only), so 46 is what landed. The 39 figure describes the pre-review-fix state of the branch and was never re-derived when the review fixes (commit `de61276`, folded into the squash) added the mixed-content, tilde-fence, four-backtick, and descriptive-negative cases. The PR body carries the same stale 39. Recorded as G2.
2. **"whose sole consumers are the phase-6-finalize tests"** (§ D2, describing `test/_shared/_dispatch_roster.py`). False at the moment it was written. At the landed commit `94eb7521`, `git grep -ln 'from _dispatch_roster import' 94eb7521 -- test/` lists three importers outside `phase-6-finalize`: `test/plan-marshall/manage-lessons/test_lesson_store_resolution_population.py`, `test/plan-marshall/phase-5-execute/test_execute_phase_markers.py`, `test/plan-marshall/ref-workflow-architecture/test_citations_only_conformance.py`. The importer set is **unchanged** at HEAD (the same nine files under `test/`); only prose/docstring mentions of the module have grown. (This document previously said the set had grown further — corrected during adversarial review.) The substantive conclusion the sentence supports — that `_dispatch_roster.py` is a heading-bounded Markdown-section/roster-row parser and **not** the execution-context workflow roster — is independently correct: I read the module (`section_lines`, `parse_roster_rows`, `parse_roster`, `ROSTER_ROW = ^-\s+\`([^\`]+)\``) and its docstring scopes it to `dispatch-inline-split.md`'s `## Dispatched steps` / `## Inline steps` sections. Recorded as G3.
3. **"Verified by a boundary harness (10 positive / 10 negative)"** (§ D3). The committed negative parametrization in `TestNegativeBoundary::test_procedural_prose_does_not_fire` has **13** cases, not 10; the positive family parametrization has 10. The separate "boundary harness" is not in the tree, so the 10/10 figure cannot be re-derived from committed artifacts. Recorded as G4.

Claims re-derived and found **accurate**: roster size 33; hit count 5 pre-fix and 0 post-fix; "the two D1 files were the **only** offenders across all 33" (confirmed by running the detector over the reconstructed pre-fix tree); "all five claim-label strings were confirmed by string" (the `content-review.md` HYPOTHESIS at `:7`/`:177` is real — both lines carried `careful step-by-step reasoning`); the pin-contract OBSERVED claim (`marketplace/bundles/plan-marshall/agents/execution-context.md:30` — *"Model and effort are NOT prompt-body fields. They are pinned by the variant filename (`execution-context-{level}.md`) that the caller dispatched against"*); "12 changed files, all within the expected surface"; "`test/_shared/_dispatch_roster.py` was correctly left unmodified"; the wiring list (runner quality-gate + analyze, `_rule_registry`, `_fixtures`, `rule-provenance.md`, `rule-catalog.md`) — all five verified present at HEAD; the population-derivation and empty-population-guard descriptions; the false-positive-boundary description (procedural sequencing, "careful analysis", "step-by-step analysis" all confirmed non-firing).

## Out-of-scope compliance

Clean. The landed diff is exactly 12 files:

- the plan file rename (`040-….md` → `040-…/plan.md`, R100, zero content change) and the new `report-01.md`;
- the two D1 docs;
- `rule-catalog.md`, `rule-provenance.md`, the new analyzer, `_rule_registry.py`, `_runner.py` under `marketplace/bundles/pm-plugin-development/skills/plugin-doctor/`;
- `_fixtures.py`, the new test file, and `test_runner.py` under `test/pm-plugin-development/plugin-doctor/`.

No `CRITICAL`/`NEVER`/`MUST NOT`/`MANDATORY`/`ALWAYS`/`FORBIDDEN` emphasis marker was swept (the diff touches one `**CRITICAL:**` line — the `content-review.md:177` directive itself, which is D1's target, not an emphasis-marker sweep). `test/_shared/_dispatch_roster.py` is untouched (last modified by #1026). No second detector framework was built; the sibling-plan overlap with `050-migration-shims-have-no-expiry` was noted and not waited on, as the plan permits. The test files live under `test/…` rather than literally under `marketplace/bundles/pm-plugin-development/**` — that is where this repository puts bundle tests, so it is the plan's declared "the plugin-doctor rule's home and its tests", not collateral.

## Residue carried forward

`report-01.md` § Residue declares two items, plus two Step-9 proposals:

| Item | Status today |
|---|---|
| No `/sync-plugin-cache` owed by this run | **Closed / correctly framed.** `CLAUDE.md` § Standalone Plan Lane now states the same rule verbatim ("A lane plan that edits `marketplace/bundles/` therefore neither performs a sync nor records one as owed"). |
| Proposed contract clarification — a reachable operator MAY escalate via `AskUserQuestion`; a headless run takes the plan's autonomous fallback | **Closed.** `.claude/skills/cloud-plan-lane/SKILL.md:1574` and `:1590` now carry exactly this rule, including the reachability definition. Landed via plan 450 D4 (`a3eb36bb` #1147). |
| Build-gate misframing — the gate should trigger on `*.py` only | **Closed.** `.claude/skills/cloud-plan-lane/SKILL.md:59` ("The build gate triggers on `*.py` only; the merge queue's `merge_group` run verifies docs-only changes before they land") and § Step 5 line 432 ("Why the gate is `*.py`-only"). |
| Plan-authoring note: population-derived detectors should point at ext-point `implements:` frontmatter, not `_dispatch_roster.py` | **Still open.** `.claude/skills/author-cloud-plan/SKILL.md` contains no occurrence of `population-derived`, `_dispatch_roster`, or `implements:`. `doc/plans/truthful-signals/050-migration-shims-have-no-expiry/plan.md:84` still instructs "copy the pattern from `test/_shared/_dispatch_roster.py`" — 050 has since run and its own report caught the mis-pointer independently (`050/report-01.md:169,261`), so the concrete damage is contained, but no durable authoring guidance landed. Recorded as G5. |

## What could NOT be verified

- **The D2 operator escalation.** The report states the re-scope was approved by the operator via an interactive `AskUserQuestion`. That is a conversation event with no committed artifact; it is unverifiable from the tree by construction, exactly as the report itself concedes. The *outcome* (a frontmatter-derived population that is genuinely derived and not hand-listed) is verified.
- **The historical CI figures.** `./pw verify` → "18711 passed, 14 skipped" then "18718 passed", `./pw quality-gate` → `total_issues: 0` across "32 rules", and the two wall-clock durations (6m05s, 4m48s) are point-in-time CI/local observations that cannot be re-derived from today's tree. I did not attempt a full `./pw verify` run.
- **The pre-PR verification sub-agent's cold read ("8/8 agreement")** and the separate 10-positive/10-negative boundary harness. Neither the sub-agent transcript nor the harness script is committed. The property they were checking — that the boundary does not fire on procedural prose — *is* verified independently, by the 13 committed negative cases (all passing) and by my own sweep of the 33 population docs, which produced 30 procedural-prose hit lines and 0 detector findings.
- **The PR review-thread dispositions** (coderabbit/cuioss-review-bot findings 1–4 and their resolutions). I did not query the GitHub API. The three *code* fixes those findings describe — `finditer` instead of `search`, the directive-cue requirement, and CommonMark tilde/4-backtick fences — are all present in the landed analyzer and each has a dedicated passing test, so the substance is confirmed even though the thread state is not.

## Adversarial review

**Reviewed by:** an independent agent that did not write this document.

**Checked.** Every `high`/`medium` gap, every clean-pass deliverable row, and every "swept, clean" claim, by re-execution rather than re-reading:

- **Tree currency.** `git diff --name-only 4ea24cbd HEAD` → doc-only (`doc/plans/truthful-signals/**`); no file in this plan's surface moved, so every citation below re-derives at HEAD.
- **Population, two ways.** Ran `enumerate_execution_context_workflow_docs(marketplace/bundles)` → **33**; `analyze_thinking_directive_in_workflow_docs(…)` → **0** findings. Independently re-derived with my own frontmatter reader over `marketplace/bundles/**/*.md` (all path shapes) → **33** declaring docs, **0** outside the analyzer's `SKILL.md` + `workflow/*.md` restriction. A broader `grep -rl` for the ext-point string returns 40 files; the 7 extras (`agents/execution-context.md`, the three `ext-point-*.md` standards, `manifest-schema.md`, `phase-6-finalize/SKILL.md`, `rule-catalog.md`) were each opened and carry the string as a **body reference**, not an `implements:` declaration.
- **Pre-fix reconstruction.** `git archive 94eb7521^ marketplace/bundles` into scratch, ran the current detector → population **33**, findings **5** at exactly `research-best-practices.md:9,46,112` (`ultrathink`) and `content-review.md:7,177` (`careful-reasoning`).
- **Tests.** `pytest --collect-only` → **46 tests collected**; full run → **46 passed**. Parametrization counts re-derived from the collected node ids: `test_family_fires` **10**, `test_removed_d1_directives_fire` **5**, `test_procedural_prose_does_not_fire` **13**.
- **Mutation (mine, not the original author's).** `git diff --quiet` on `content-review.md` → clean; saved bytes to scratch; appended `**CRITICAL:** Use careful step-by-step reasoning for comprehensive tone assessment.`; `test_real_marketplace_tree_produces_zero_findings` → **RED** (1 finding, line 482, `careful step-by-step reasoning`). Restored from my own snapshot (md5 `4fa0e6a040187e29967ddd49fc789e39`, matches pre-mutation). No `git checkout`/`restore`/`stash` used.
- **Quality gate, executed.** Built `RuleRunner(CorpusContext.build(MARKETPLACE_ROOT))` and ran `run_quality_gate(scope_dirs=[], …)` over the real tree → `total issues: 0`, 33 summary rows, and the row for this rule is literally `{"rule": "analyze_thinking_directive_in_workflow_docs", "findings": 0}`. This is what G1 asserts, confirmed by running it.
- **Scoping probe (new).** Built a synthetic tree defining the ext-point with zero implementors → 1 `empty_population` finding whose `file` is the bundles root; `_finding_in_scope` against two different scope dirs → `False` both times. See G6.
- **Broader false-negative sweep.** Re-ran the population sweep with a deliberately wider superset than the one in § Method (adding `cogniti|deliberat|ponder|contemplat|introspect|scratchpad|budget|effort level|reasoning level|verbos|out loud|show your work|explain your reasoning|before (you )?(answer|respond|conclud)`) → **230** hit lines, of which **0** are directive-shaped (imperative cue within 60 chars of a cognition noun). No missed directive.
- **Broader stale-restatement sweep.** `grep -rniE` over `*.md`/`*.py`/`*.json`/`*.adoc` for the union of all six families plus `chain of thought` → hits only in the rule's own module, its test file, `_fixtures.py`, `rule-catalog.md`, and `rule-provenance.md`. Upheld.
- **Fence statistics re-derived exactly:** 14 524 lines, 2 941 inside-fence, ratio 0.202, **0** unclosed fences.
- **Line/symbol citations re-derived:** `_runner.py:203-205`, `_runner.py:357`, `_rule_registry.py:132`, `test_runner.py:81`, `_fixtures.py:453`, `rule-provenance.md:52`, `execution-context.md:30`, `cloud-plan-lane/SKILL.md:59,432,1574,1590`, `050/plan.md:84`, `CLAUDE.md:91`. All correct. `emit()` was cited at `_runner.py:143-145`; it is at **144-146**.
- **Importer set re-derived** at `94eb7521` and at HEAD: identical nine files.

**Not re-checked:** the D2 operator escalation (no committed artifact, unverifiable by construction); the historical CI figures (`./pw verify` 18711/18718, `./pw quality-gate` 32 rules, wall-clock durations) — I did not run a full `./pw verify`; the pre-PR sub-agent's 8/8 cold read and the uncommitted boundary harness; the PR #1138 review-thread dispositions (no GitHub API queries); the three post-merge report-amending commits' contents beyond the strings G2/G3/G4 quote; the D1 diff's effect on every criteria sentence beyond reading the full diff (3 deletions + 2 rewrites, all inspected).

| Item | Original claim | Verdict | Evidence |
|---|---|---|---|
| Verdict `implemented-with-gaps` | All three deliverables landed | **upheld** | No deliverable is unimplemented, so `partially-implemented` would be wrong. D3's Done-when clause "so a zero can never read as coverage" is only partly met (G1 + G6), which is why its **Complete?** cell is now **Partial** rather than Yes |
| D1 row (clean pass) | Directives gone, criteria preserved | **upheld** | Full `git show 94eb7521 -M` diff of both files read: 3 standalone paragraphs deleted, 2 lines rewritten, no criteria sentence touched. Detector over pre-fix tree → 5, over HEAD → 0 |
| D2 row (clean pass) | Roster 33 / hits 5 → 0, two separate figures | **upheld** | Both numbers reproduced by execution; `report-01.md` § D2 and the commit message each carry them as two figures. `test/_shared/_dispatch_roster.py` unmodified since #1026 |
| D3 row (clean pass on Complete?) | Rule ships, both-direction tests, publishes population size | **re-severitied** | Complete? Yes → **Partial**. 46/46 tests pass and the mutation bites, but the "publishes the size" clause fails on a clean run (G1) and the empty-population guard is discarded under `--paths` (G6) |
| "swept the tree, clean" (stale restatements) | Only the rule's own artifacts carry the strings | **upheld** | Re-run with a union-of-all-six-families pattern over four file types; same five files |
| "no live directive missed" (population sweep) | 30 hit lines, all procedural | **upheld, figure softened** | A wider superset gives 230 hit lines and 0 directive-shaped candidates. The stated 30 did not reproduce exactly (my construction of the same term list gives 26) — the pattern is not quoted verbatim, so the count is a self-report, not a re-derivable figure. Softened in § Method |
| "33 declaring docs, 0 outside the shape" | Path restriction loses nothing | **upheld** | Reproduced independently; the 7 extra grep hits are body references, each opened |
| "28 scalar / 5 block-sequence" | Both frontmatter forms exercised by the real tree | **rewritten** | Re-derived: **26 scalar / 7 block-sequence**. The conclusion (both forms live in the real tree) holds; the split was wrong |
| G1 | Population size invisible on a clean run | **upheld** (line ref fixed) | `run_quality_gate` executed → `{"rule": …, "findings": 0}`. `emit()` is at `_runner.py:144-146`, not `143-145` |
| G2 | 39 → 46 tests | **upheld** | `--collect-only` → 46; full run → 46 passed; one commit in the file's history |
| G3 | "sole consumers are the phase-6-finalize tests" is false | **upheld** (clause corrected) | Three non-finalize importers at `94eb7521`. But "the set has grown since" is false — the importer set is identical at HEAD (nine files both) |
| G4 | 13 negative cases, not 10 | **upheld** | Collected node ids counted: 13 negative, 10 positive family, 5 D1-regression |
| G5 | Authoring note never landed | **upheld** | `.claude/skills/author-cloud-plan/SKILL.md` matches none of `population-derived` / `_dispatch_roster` / `implements:`; `050/plan.md:84` still says "copy the pattern from `test/_shared/_dispatch_roster.py`" |
| G6 (new) | — | **added, `high`** | `empty_population` finding anchors at the marketplace root; `_finding_in_scope` drops it under every `--paths` run. Confirmed by executing both functions on a synthetic zero-implementor tree |

**Documents corrected.** In `verification.md`: the scalar/block-sequence split (28/5 → 26/7); the "the set has grown further" clause about `_dispatch_roster.py` importers; the 30-hit-line sweep figure, now marked pattern-dependent with the broader re-derivation alongside; D3's **Complete?** cell (Yes → Partial). In `gaps.md`: `**Open items:** 5` → **6**; G1's `emit()` line reference (143-145 → 144-146); G3's unsupported "the set has grown since"; the opening summary; **new G6** (`high`); and a `## Refuted during adversarial review` section recording that nothing was refuted and naming the two clauses corrected in place.

**Residual doubt — what a third reviewer should look at first.** (1) Whether G6's class is wider than this rule: `shim-marker-missing` (plan 050's detector) also publishes a `population_size`, and any other rule that emits a root-anchored or otherwise unanchored finding is dropped by the same `_finding_in_scope` predicate — `.claude/skills/finalize-step-plugin-doctor/SKILL.md:88` already enumerates a *cross-skill* divergence class but does not name the *root-anchored* one, so the enumeration may itself be incomplete. (2) The D1 rewrite at `content-review.md:177` replaced a `**CRITICAL:**` directive with plain prose — worth a judgement call on whether the emphasis it removed was guarding something the plan's Out-of-scope section meant to protect. (3) The historical CI figures in `report-01.md` remain un-re-derived by anyone.
