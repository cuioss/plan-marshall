# WS-05: Outline & Sweep Declarations

epic: quality-aspect

> Charter document for one workstream — a coherent slice of the epic with its own goal
> and surface. Lives at `workstreams/WS-05-quality-aspect.md` and is tracked in the epic
> `status.json` `workstreams[]` field.

## Charter

Owns outline-time declarations a sweep execution can actually satisfy: assessment
coverage over every declared path, declaration forms for tree-wide work, success
criteria operationalizable from the outline alone, and re-assessment on scope change.
Closed when no sweep declares an enumerated snapshot it cannot hold.

## Scope

- In scope: phase-3-outline assessment passes, sweep declaration forms, success
  criteria, Affected-files/Change-per-file sync, decline-on-merits upper bounds.
- Out of scope: phase-4 task planning mechanics (WS-06), self-review of sweeps (WS-07).

## Plans

| Plan | Status | Notes |
|------|--------|-------|
| PLAN-09-outline-sweep | shipped | All 12 outline/sweep declaration lessons (#1551) |

## Sequencing and Surface Notes

- Single-plan workstream; no internal sequencing. Adjacent to WS-06 (its task
  mechanics consume outline declarations) without touching it.
