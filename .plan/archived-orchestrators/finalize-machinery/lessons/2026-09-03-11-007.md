# 2026-09-03-11-007 — Finalize cost 3.7x execute on a three-file documentation-only change

component: plan-marshall:phase-6-finalize
category: improvement
status: moved-to-epic (corpus original retired 2026-09-17: completely_covered — symptom of 19-005's mechanism, addressed by PR #1505's anchor)
source: corpus lesson 2026-09-03-11-007, moved into finalize-machinery per operator direction

## Context

Three markdown files changed; `6-finalize` cost 1.12M tokens (47%, 3.7x execute).
Gating keys on scope estimate + execution profile, never on the realized footprint's
kind — docs-only, test-only, and production diffs get the same step set. Recurrences:
test-only diff at 5.1x (PR #1406, dominated by re-firing per 19-005) and mixed diff at
3.74x (PR #1422, productive rounds — argues against cutting round counts, for
narrowing per-round scope via delta anchors).

## Proposed action

Make footprint kind a gating input (mid-finalize re-gate on the realized footprint);
narrow iterative review per-round scope. Recorded as observation with numbers as input
to epic design, not a settled fix.

## Evidence

- plan_efficiency (fallback thresholds tripped on all three instances), manifest_decisions
  (filtered_by_category classifications exist and nothing consumes them), log_analysis.

## Coverage (finalize-machinery)

Cost mechanism addressed by PLAN-01 / PR #1505 (single-pass anchor, ≤1 firing bound);
footprint-kind gating remains future work tracked in this epic. Corpus original retired
as completely_covered (mechanism half).
