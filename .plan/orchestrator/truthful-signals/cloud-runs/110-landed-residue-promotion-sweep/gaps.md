# Gaps — 110-landed-residue-promotion-sweep

**Source:** verification.md (same directory)   **Open items:** 7

## G1 — State the ledger stamp predicate as the three-way conjunction source implements

- **Kind:** incomplete-sweep
- **Severity:** medium
- **Where:** `marketplace/bundles/plan-marshall/skills/manage-change-ledger/SKILL.md:74-83` — § Entry Shapes, the `kind=build` bullet
- **What is wrong:** The bullet says the row is "written by the executor dispatch boundary after every build-**executing** invocation that runs to completion. Build-class-ness is a **conjunction**: the notation must sit under a `build-*` skill AND the dispatched subcommand must be the build-executing verb (`run`)", and names only "a bare `--help` dispatch that carries no subcommand at all" as suppressed. Source has had a **third** conjunct since `415dcf13` (#1127, 2026-08-09): `execute-script.py.template:1609` reads `if _is_build_class_notation(notation, subcommand) and not _mentions_help(script_args):`, so `run --help` — which satisfies both stated conditions — stamps **no** row. (The two-condition sentence is a correct description of `_is_build_class_notation` itself, which really is two-way; the defect is that the doc equates *build-class-ness* with *the stamp predicate*, and the stamp predicate is not that function alone.) Removing the third conjunct turns 6 tests in `test_build_class_stamp_discriminator.py` red, so the behaviour is pinned; only the doc is behind. The promoted section this plan landed states the conjunction as three-way (`extension-api/standards/build-systems-common.md:110-124`) and cross-links this exact section for the ledger contract, so the two documents now disagree.
- **Why it matters:** A reader following the promotion's cross-link lands on a weaker rule and can conclude that a `run --help` probe stamps a freshness-satisfying row — the precise false-fresh hole the third conjunct closed. It also falsifies the run's recorded "beyond-diff staleness sweep found no false statement in any untouched build/ledger doc".
- **Fix:** In the `kind=build` bullet, state the stamp predicate as three conditions — a `build-*` notation, the build-executing `run` verb, and no help spelling anywhere in argv (`--help`, `--help=…`, `-h`, `-h` inside a short-flag cluster) — and extend the suppression list from "a bare `--help` dispatch" to include `run --help` and `run -h`. Cite `_mentions_help` beside `_is_build_class_notation` as the boundary that applies it, and say plainly that `_is_build_class_notation` is only two of the three conjuncts.
- **Done when:** `manage-change-ledger/SKILL.md` § Entry Shapes names all three conjuncts and lists `run --help` among the dispatches that write no row, and no wording there contradicts `build-systems-common.md:110-124`.
- **Module/topic:** `plan-marshall:manage-change-ledger` (ledger contract docs) / build-ledger stamp discriminator.

## G2 — Give the promoted mechanism a path from the surface that actually runs builds

- **Kind:** omission
- **Severity:** low *(re-severitied from medium during adversarial review — see the corrected "Why it matters")*
- **Where:** `marketplace/bundles/plan-marshall/skills/extension-api/standards/build-systems-common.md:69-90` and `:186-196` — the (a) and (c) subsections; the missing pointer belongs in `marketplace/bundles/plan-marshall/skills/persona-plan-marshall-agent/SKILL.md` § "Bash: Timeout from architecture-resolved canonical command" (heading at :71, floor sentence at :78) and in `marketplace/bundles/plan-marshall/skills/plan-marshall/workflow/await-long-running.md:84-88`
- **What is wrong:** Residues (a) ("don't poll a backgrounded build's output") and (c) ("don't self-background a long build; run it in the foreground at 600000 ms") are addressed to the agent invoking a build, but they were filed in `extension-api`, whose SKILL.md declares it `mode: knowledge`, a "Reference library; load on-demand when creating or modifying extensions" (`extension-api/SKILL.md:5,14`). A whole-repo sweep for `build-systems-common` (not scoped to `marketplace/` and `.claude/`, as the original sweep was) returns readers only in `extension-api/**`, the four `build-*/standards/*-impl.md` files, and `doc/concepts/build-management.adoc:46` — the last being project documentation, not a skill an agent loads. No agent-facing skill surface (`persona-plan-marshall-agent`, `phase-5-execute`, `execute-task`, `await-long-running`, `.claude/skills/cloud-plan-lane`, `CLAUDE.md`) references it.
- **Why it matters:** *(corrected)* The bare prohibition is **not** in fact absent from the agent-facing surfaces: `plan-marshall/workflow/await-long-running.md:84` already states "`run_in_background` is a **known-lossy primitive**: the harness kills backgrounded jobs with zero output", :88 already says "Do NOT poll for completion", and `ref-workflow-architecture/standards/agents.md:164-178` already forbids a leaf from backgrounding a build at all. What has no path from those surfaces is the promoted *mechanism* — why the captured output carries no information in either state, and that the `kind=build` ledger row is the substitute oracle to read instead. So the defect is narrower than "the audience never meets the rule": the audience meets the prohibition and not the diagnosis, which is what they need when the prohibition has already been violated.
- **Fix:** Add one cross-reference from `persona-plan-marshall-agent/SKILL.md` § "Bash: Timeout from architecture-resolved canonical command" (immediately after the 600000 ms floor sentence at :78) and one from `await-long-running.md` (immediately after :84's known-lossy sentence) to `extension-api/standards/build-systems-common.md` § "Background build execution — reading a long build's completion signal", each naming what the reader gets there (the captured output carries no liveness information in either direction; read the `kind=build` ledger row's `status` instead). Keep the rule text in one place — a pointer, not a copy.
- **Done when:** `grep -rn "build-systems-common" marketplace/bundles/plan-marshall/skills/persona-plan-marshall-agent/ marketplace/bundles/plan-marshall/skills/plan-marshall/workflow/` returns a hit in each that reaches the background-execution section, and an agent following either the build-invocation guidance or the `await-long-running` seam can arrive at residues (a) and (b) without loading the extension-author skill.
- **Module/topic:** `plan-marshall:persona-plan-marshall-agent` + `plan-marshall:plan-marshall` + `plan-marshall:extension-api` (build guidance reachability).

## G3 — Reconcile the two opposed framings of harness auto-backgrounding

- **Kind:** doc-drift
- **Severity:** low
- **Where:** `marketplace/bundles/plan-marshall/skills/extension-api/standards/build-systems-common.md:186-196` vs `marketplace/bundles/plan-marshall/skills/persona-plan-marshall-agent/SKILL.md:75`
- **What is wrong:** The promoted (c) presents harness auto-backgrounding as the thing that "preserved the job every time" and instructs the reader to "let the harness auto-background the job at its own ceiling". `persona-plan-marshall-agent/SKILL.md:75` presents the same event as a loss: "the host platform will silently auto-move the call to background and the dispatch will lose the synchronous-return path". Both are true of different aspects (the job survives; the caller's synchronous return does not), and both documents in fact prescribe the same *action* (set an explicit bound rather than accept the default) — but neither says so, so only the connotation differs and nothing reconciles it.
- **Why it matters:** A reader who meets both takes away contradictory postures on whether auto-backgrounding is an acceptable outcome, which is exactly the ambiguity the (a) subsection was already amended once (commit `74122313`, the F2 disambiguation) to remove.
- **Fix:** In `build-systems-common.md:186-196`, add one clause stating that auto-backgrounding preserves the *job* but forfeits the caller's synchronous return, so the explicit 600000 ms bound is set to make auto-backgrounding the rare case rather than the plan — and cross-link `persona-plan-marshall-agent` § "Bash: Timeout from architecture-resolved canonical command" for the caller-side consequence.
- **Done when:** The two documents state the same thing about auto-backgrounding, with the job-survival and synchronous-return halves named apart in at least one of them.
- **Module/topic:** `plan-marshall:extension-api` build standards / agent build-invocation guidance.

## G4 — Build-gate section contradicts the run continuation in the same file

- **Kind:** stale-statement
- **Severity:** low
- **Where:** `doc/plans/truthful-signals/110-landed-residue-promotion-sweep/report-01.md:116` (§ Build gate) vs `:270` (§ Run continuation)
- **What is wrong:** § Build gate states "**no local build was run** (\"no buildable footprint, build skipped\")". The continuation section appended below it records, for the `merge_group` historical-prose fix, "Verified locally with `./pw quality-gate` — `total_issues: 0`". A local build *was* run; the Build-gate section was never reconciled with the events appended after it.
- **Why it matters:** The report is the durable record a retrospective reads for build-invocation counts; this one would be counted as a zero-build run when it was not.
- **Fix:** In § Build gate, append one sentence: the pre-PR gate correctly skipped the build on the docs-only footprint, and a local `./pw quality-gate` was subsequently run during the run continuation for the `merge_group` plugin-doctor fix (see § Run continuation). The dated-record exemption in `CLAUDE.md` § Standalone Plan Lane permits the correction to stay inside the report.
- **Done when:** § Build gate no longer asserts that no local build was run, and names the continuation build.
- **Module/topic:** `doc/plans/truthful-signals/110-landed-residue-promotion-sweep` run record.

## G5 — Contract-check table records a branch and PR the run superseded

- **Kind:** stale-statement
- **Severity:** low
- **Where:** `doc/plans/truthful-signals/110-landed-residue-promotion-sweep/report-01.md:199` (Contract check row 2) and `:206` (row 7)
- **What is wrong:** Row 2 records "DONE — harness-assigned `claude/landed-residue-promotion-sweep-11npl8`, kept as-is" and row 7 records "DONE — PR #1161". The work landed from `chore/landed-residue-promotion-sweep` as PR #1169 (GitHub API on #1169: `head.ref = chore/landed-residue-promotion-sweep`, `merged: true`), and the report's own header at `:3` says so. The contract-check table is a *conformance* record, not a moment record: row 2 asserts compliance with the lane's keep-the-assigned-branch rule, and that assertion is now false.
- **Why it matters:** A retrospective auditing branch-form conformance across the epic would read this run as having kept its harness-assigned branch when it did not — the one figure in the table whose whole purpose is to be read as a compliance fact.
- **Fix:** Update row 2 to name `chore/landed-residue-promotion-sweep` as the branch the work landed from, marked as an operator-directed re-issue away from the harness-assigned `claude/landed-residue-promotion-sweep-11npl8`, and update row 7 to PR #1169 (superseding #1161). Keep the re-issue reason already given in § Run continuation.
- **Done when:** Contract-check rows 2 and 7 name the branch and PR that landed, and no row in the table contradicts the report header at `:3` or the merged PR.
- **Module/topic:** `doc/plans/truthful-signals/110-landed-residue-promotion-sweep` run record.

## G6 — The promoted third conjunct names only `--help`, but the boundary also fires on `-h`

- **Kind:** incomplete-statement
- **Severity:** low
- **Where:** `marketplace/bundles/plan-marshall/skills/extension-api/standards/build-systems-common.md:112-113` — the promoted stamp-predicate bullet
- **What is wrong:** The promoted text states the third conjunct as "**AND** no `--help` anywhere in argv — so a query subcommand (`parse`, `discover`, …), a bare `--help`, and a `run --help` probe each write **no** row." The predicate it names, `_mentions_help` (`execute-script.py.template:812-854`), fires on four spellings, not one. Executed against real argv: `['run','--help'] → True`, `['run','--help=x'] → True`, `['run','-h'] → True`, `['run','-vh'] → True`, `['run','--helpful'] → False`. So `run -h` also writes no row, and the promoted sentence does not say so.
- **Why it matters:** This is a defect in the text this plan itself shipped, not in an untouched doc. A reader who has an executor log showing `... run -h` and no ledger row consults the promoted rule, finds the argv does not match "`--help` anywhere in argv", and concludes the row is missing for some other reason — sending them to look for a kill that did not happen. It is the same class of error G1 records, one file further along the cross-link.
- **Fix:** In `build-systems-common.md:112`, replace "no `--help` anywhere in argv" with "no help spelling anywhere in argv — `--help`, `--help=…`, `-h`, or `-h` inside a short-flag cluster (`-vh`)", and extend the suppression examples from "a `run --help` probe" to "a `run --help` or `run -h` probe". The Enforcement note at :119-124 already cites `_mentions_help`; leave it as the citation.
- **Done when:** `build-systems-common.md`'s stamp-predicate bullet enumerates all four help spellings `_mentions_help` matches, and a reader can decide from the text alone whether `run -h` stamps a row.
- **Module/topic:** `plan-marshall:extension-api` build standards / build-ledger stamp discriminator.

## G7 — Residue (c)'s blanket "never background a long build" contradicts the build-tier SSOT

- **Kind:** doc-conflict
- **Severity:** medium
- **Where:** `marketplace/bundles/plan-marshall/skills/extension-api/standards/build-systems-common.md:186-196` (residue (c)) vs `marketplace/bundles/plan-marshall/skills/ref-workflow-architecture/standards/agents.md:164-178` and `marketplace/bundles/plan-marshall/skills/plan-marshall/workflow/await-long-running.md:76-88`
- **What is wrong:** (c) closes "Do **not** background a long build yourself — run it in the foreground at the 600000 ms bound and let the harness manage it", and the section opens by asserting the rules "hold for every build engine". `agents.md:164-178` is declared the **single source of truth** for build-tier ownership on the leaf boundary and says the opposite in two directions: an `execution_tier: orchestrator` build "is not runnable in-leaf" and the leaf "MUST NOT run it (inline OR backgrounded)" — so (c)'s "run it in the foreground at the 600000 ms bound" is exactly what that invariant forbids for such a build — while the `await-long-running` seam "is the ONLY component permitted to background a build" and does so with `run_in_background: true` (`await-long-running.md:78-84`), which (c) forbids without qualification. Neither document cross-links the other; the promoted section links only `manage-change-ledger`, `pyproject-impl.md`, and its own anchors.
- **Why it matters:** (c) is stated as a universal rule but is only correct for the `per_task` tier. A reader who applies it as written either runs an orchestrator-tier build inside a leaf (violating the invariant that exists because a leaf cannot reap it) or treats the sanctioned `await-long-running` detach as a mistake. It is rated `medium` rather than `low` — unlike G3, which is a connotation difference between two documents that prescribe the same action — because this one contradicts an explicit MUST-NOT in a declared SSOT. Its practical reach is limited by G2 (the section has no agent-facing reader today), which is why it is not `high`.
- **Fix:** In `build-systems-common.md:194-195`, scope the closing instruction: state that it applies to a build the caller runs itself — i.e. an `execution_tier: per_task` build — and that an `execution_tier: orchestrator` build is neither foregrounded nor backgrounded by the caller but handed to the `await-long-running` seam, cross-linking `../../ref-workflow-architecture/standards/agents.md` § "Leaf cannot reap a backgrounded build" as the owning contract.
- **Done when:** `build-systems-common.md` § (c) names the tier its instruction applies to and cross-links `agents.md` § "Leaf cannot reap a backgrounded build", and no sentence there forbids the backgrounding that `await-long-running.md:78` mandates.
- **Module/topic:** `plan-marshall:extension-api` build standards / `plan-marshall:ref-workflow-architecture` leaf-dispatch topology.

## Refuted during adversarial review

### G4(iii) (as originally filed) — "all six commits" is not a stale figure

The original G4 bundled a third statement: `report-01.md:249`, "The complete work (all six commits) plus this updated report is re-published on `chore/landed-residue-promotion-sweep` as **PR #1169**", filed as contradicted by #1169 carrying 10 commits.

**Refuted.** The commit count re-derives (`pull_request_read get_commits` on #1169 returns exactly the 10 SHAs `b65401de`, `0b73707d`, `21e4b6f2`, `74122313`, `2238eea4`, `ebccb170`, `ac06cd08`, `ac079c94`, `66d9b886`, `5e55f2bf`), but the sentence is not a claim about #1169's final commit count. It describes what was re-published *at the moment of the re-issue*: the six commits then on the `claude/…-11npl8` branch (`b65401de` … `ebccb170`), "plus this updated report" — which is `ac06cd08`, the very commit that added the sentence. The remaining three (`ac079c94`, `66d9b886`, `5e55f2bf`) were appended by the run's own later continuation and are described in the sections below it. `CLAUDE.md` § Standalone Plan Lane exempts a run report as "a dated record of one execution rather than documentation of the current state", which is exactly what this sentence is. Nothing to fix.

The other two statements originally bundled into G4 are real and remain open, split per instance as **G4** (the § Build gate contradiction) and **G5** (the contract-check table). The split is itself a correction: they sit in different sections, have different fixes, and had a compound done-when that could be half-satisfied.
