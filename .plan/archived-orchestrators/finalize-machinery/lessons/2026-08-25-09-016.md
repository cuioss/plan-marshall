# 2026-08-25-09-016 — Directory and glob claims are dropped by both the queue renderer and the disjointness check, so absence reads as disjointness

component: plan-marshall:plan-orchestrator
category: bug
status: moved-to-epic (OPEN — renderer residue; corpus original retained)
source: corpus lesson 2026-08-25-09-016, copied into finalize-machinery per operator direction
(note: full body retrieved 2026-09-17; condensed here)

## Context

Queue `Surface` column drops directory-shaped entries (PLAN-155's 14 dirs → "(no
expected surface)"; PLAN-110's 15 entries → 3 files) — collides-with-most reading as
disjoint-from-almost-everything. `corpus cross-check` shared the defect (PLAN-130
claiming `test/` in zero overlap rows) — fail-open toward pairing colliding plans.
Partial coverage: cross-check half fixed (containment + indeterminate reporting);
renderer half open — retained for that residue. Hand-maintained disjointness notes hid
the cause.

## Proposed action

Render directories as recursive claims (stated-count truncation, never silent drop);
distinguish no-section from resolved-to-nothing; derive the column from the partition
tool's resolver.

## Epic status

Open (renderer residue). Corpus original RETAINED. This epic's gate reads the parser,
not the rendered column (the fixed half).
