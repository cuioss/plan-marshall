= Role Variants and Per-Role Models
:toc:
:toclevels: 3

Overview and entry-point map for the per-role model variant-emission system. The canonical specifications live next to the code they govern; this document is a navigational summary that points there.

Authors configure `models.roles.<key>` (hierarchical kebab-case keys like `cross.research`, `phase-6.create-pr`) in `.plan/marshal.json`. The Claude target emits six `execution-context[-{level}].md` agent variants for the sole role-eligible agent. Dispatch sites resolve the target via `manage-config models resolve-target --role <key>` and call the matching variant by name. End users get the right model per role at dispatch time without any user-side build step or Claude Code restart.

== Where things live

[cols="1,3,4", options="header"]
|===
| Layer | Document | Purpose

| User
| link:../marketplace/bundles/plan-marshall/skills/plan-marshall/standards/role-variants.md[`plan-marshall/standards/role-variants.md`]
| Stand-alone end-user / project-author guide. Configuration walkthrough, troubleshooting (invalid keys, `CLAUDE_CODE_SUBAGENT_MODEL` env-var override, level resolution rules). Read this first.

| User (wizard)
| link:../marketplace/bundles/plan-marshall/skills/marshall-steward/standards/models-menu.md[`marshall-steward/standards/models-menu.md`]
| `/marshall-steward` → Models submenu UX contract: preset-picker workflow with `economic` / `balanced` / `high-end` payloads sourced from `model_presets.py`.

| Architecture (extension point — agent emission)
| link:../marketplace/bundles/plan-marshall/skills/extension-api/standards/ext-point-dynamic-level-executor.md[`extension-api/standards/ext-point-dynamic-level-executor.md`]
| Contract the sole variant-eligible canonical agent (`plan-marshall:execution-context`) satisfies: required `implements:` value, optional `levels:` whitelist, forbidden `model:`/`effort:` on canonical, validation rules.

| Architecture (extension point — workflow body)
| link:../marketplace/bundles/plan-marshall/skills/extension-api/standards/ext-point-execution-context-workflow.md[`extension-api/standards/ext-point-execution-context-workflow.md`]
| Contract every workflow doc dispatched through `execution-context` satisfies: `implements:` declaration in YAML frontmatter, input contract (`plan_id`, `WORKTREE`, `skills[]`, plus workflow-specific runtime inputs), output contract (`status` + `display_detail` + workflow-specific return fields).

| Architecture (level binding)
| link:../marketplace/bundles/plan-marshall/skills/plan-marshall/standards/model-levels.md[`plan-marshall/standards/model-levels.md`]
| Single source of truth for the six ordinal levels (`low / medium / high / xhigh / xxhigh / max`) plus the `inherit` sentinel. Maps each to a `(model, effort)` pair. Aliases-not-IDs rule. `max` Opus-4.7-only build-time guard.

| Architecture (role registry)
| link:../marketplace/bundles/plan-marshall/skills/plan-marshall/standards/model-roles.md[`plan-marshall/standards/model-roles.md`]
| Hierarchical 15-key role registry (7 groups: `phase-1` through `phase-6`, `cross`). Three accepted lookup forms (`--role phase-1`, `--role phase-6.create-pr`, `--phase phase-6 --role create-pr`). Polymorphic-value resolver rules. Lists every workflow doc each role key drives.

| Build system
| link:refactor/02-build-system/plan.md[`doc/refactor/02-build-system/plan.md`] § Variant Emission
| Cluster-02 contributor view: variant-generation algorithm, `levels:` whitelist mechanics, `max` model-support guard, drift-check semantics, deploy-step + sync-skill integration.
|===

== How the pieces fit

[source]
----
role-variants.md  (user)                          ── single config target
        │
        ├─ model-levels.md     (level → primitive)
        ├─ model-roles.md      (role-key → workflow doc)
        └─ marshall-steward/standards/models-menu.md   (wizard preset picker)

ext-point-dynamic-level-executor.md  (agent contract — execution-context)
        │
        └─ model-levels.md

ext-point-execution-context-workflow.md  (workflow doc contract — all workflow/*.md)
        │
        └─ model-roles.md      (which role key drives this workflow)

doc/refactor/02-build-system/plan.md § Variant Emission   (build view)
        │
        ├─ ext-point-dynamic-level-executor.md
        ├─ role-variants.md
        ├─ model-levels.md
        └─ model-roles.md
----

== Quick reference

* **Configure a role** — set `plan.models.roles.<group>.<key>` in `.plan/marshal.json` to one of `low | medium | high | xhigh | xxhigh | max | inherit`. See `role-variants.md` for the polymorphic resolution rules.
* **Resolve a role at dispatch** — `python3 .plan/execute-script.py plan-marshall:manage-config:manage-config models resolve-target --role <key>` returns the canonical or variant name directly (e.g. `execution-context` for `inherit`, `execution-context-high` otherwise). See `model-roles.md`.
* **Add a new workflow doc** — place it under `{bundle}:{skill}/workflow/{file}.md` (single-phase consumer) or `plan-marshall/skills/plan-marshall/workflow/{file}.md` (multi-phase / cross-cutting), declare `implements: plan-marshall:extension-api/standards/ext-point-execution-context-workflow` in frontmatter, and document the Output contract (`status` + `display_detail`).
* **Inspect emitted variants** — generate the Claude target output (`python3 marketplace/targets/generate.py --target claude --output target/claude`) and look under `target/claude/plan-marshall/agents/execution-context[-{level}].md`. The canonical no-suffix file is the `inherit` target.

== Rationale for keeping the canonical specs in `marketplace/bundles/`

Each standard is co-located with the skill or extension that owns it so it stays under the same review lifecycle as the code it governs. This `doc/` page is intentionally a thin summary — duplicating the substance here would create two places to update on every change and the `marketplace/bundles/` copy is the one that downstream consumers (plugin-doctor, the build target, dispatch sites) read.
