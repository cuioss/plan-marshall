# Gaps — 190-split-and-complete-the-user-configuration-doc

**Source:** verification.md (same directory)   **Open items:** 8

## G1 — Remove the retired `drop_review_on_scope_gate` row from the merge-knob table

- **Kind:** stale-statement
- **Severity:** high
- **Where:** `doc/user/parallelism-and-locking.adoc:55` — table row `steps['default:pre-submission-self-review'].drop_review_on_scope_gate`
- **What is wrong:** The row documents `drop_review_on_scope_gate` as a live, settable step-owned param with default `false`. The knob does not exist: `grep -rn drop_review_on_scope_gate marketplace/bundles/` returns **nothing** — it is absent from `get_default_config()`, from `data-model.md`, and from every step's `configurable:` frontmatter. It was removed by `8cd280a3` (an ancestor of this plan's commit), and two tests assert its absence outright — `test/plan-marshall/manage-config/test_config_defaults.py:345` and `test/plan-marshall/manage-config/test_sync_defaults.py:416`, both `assert 'drop_review_on_scope_gate' not in …`. This plan moved the row verbatim out of `configuration.adoc` into `parallelism-and-locking.adoc` and cited it in `report-01.md` § D6 as evidence the move was faithful. A sweep of all 76 table key cells across both pages found this as the only orphan.
- **Why it matters:** `_cmd_step` (`marketplace/bundles/plan-marshall/skills/manage-config/scripts/_cmd_quality_phases.py:324-347`) validates the *step id* but accepts any `--param` name without checking it against a known-param list — the only set-time param check in that branch is the `use_merge_queue` probe at `:333-336`. An operator following this table and running `manage-config plan phase-6-finalize step set --step-id default:pre-submission-self-review --param drop_review_on_scope_gate --value true` gets a success return and a persisted `marshal.json` key that nothing reads. The documentation makes a dead write look like a configuration change — a false signal shipped to the operator, which is exactly this epic's archetype.
  **Executed, not inferred** (adversarial review): calling `cmd_plan(Namespace(sub_noun='phase-6-finalize', verb='step', step_verb='set', step_id='default:pre-submission-self-review', param='drop_review_on_scope_gate', value='true'))` against a fixture seeded from `get_default_config()` returns `status: success` with `params: {'drop_review_on_scope_gate': True}` and persists that key to `marshal.json`. The same call with `--param totally_made_up_knob_xyz --value 42` also returns `success` — the acceptance is unconditional, not knob-specific. In the same run the step's seeded param object is `{}`, confirming the knob is absent from the population the table claims to describe.
- **Fix:** Delete the `drop_review_on_scope_gate` table row at `doc/user/parallelism-and-locking.adoc:55`. Nothing else in either page references the knob, so no surrounding prose needs adjusting. While in the file, note that the row is also mis-homed — it is a `default:pre-submission-self-review` param sitting in a table whose own following NOTE (`:58`) enumerates "All `default:branch-cleanup` step params" and does not list it.
- **Done when:** `grep -rn drop_review_on_scope_gate doc/` returns no hit outside `doc/plans/`, and the merge-knob table contains only params that resolve to a real key in `get_default_config()` or `data-model.md`.
- **Module/topic:** `doc/user/` — configuration documentation (`parallelism-and-locking.adoc`)

## G2 — Document the two `automatic-review` rate-window knobs the coverage sweep missed

- **Kind:** incomplete-sweep
- **Severity:** medium
- **Where:** `doc/user/configuration.adoc:412-425` — the `[#automated-review]` table
- **What is wrong:** Executing `get_default_config()` yields **eleven** keys under `plan.phase-6-finalize.steps['plan-marshall:automatic-review']` — ten step-owned params plus the per-element `lane` override, in this order: `required_bots`, `optional_bots`, `review_bot_buffer_seconds`, `review_completion_poll_timeout_seconds`, `re_review_on_loopback`, `re_review_on_branch_cleanup`, `re_review_await_timeout_seconds`, `re_review_on_timeout`, `review_rate_window_await`, `review_rate_window_timeout_seconds`, `lane`. The `[#automated-review]` table carries nine rows, of which eight are seeded params; the ninth (`bot_lists_provenance`) is a real key that the `marshall-steward` migration writes rather than one `get_default_config()` seeds, so it is correctly documented and is not an orphan. Missing: `review_rate_window_await` (default `false`) and `review_rate_window_timeout_seconds` (default `3600`). Both were already declared in `marketplace/bundles/plan-marshall/skills/automatic-review/SKILL.md`'s `configurable:` frontmatter at the landed commit (verified via `git show a8b9b42d:…`), so they were inside the population D1 claimed to derive — yet they appear in none of `report-01.md` § D1's four lists (covered / covered-by-sibling / uncovered / mentioned-but-undocumented). `review_rate_window_timeout_seconds` appears nowhere in `doc/user/`; `review_rate_window_await` appears once, in passing, inside a NOTE at `doc/user/parallelism-and-locking.adoc:60`, with no default and no row of its own.
- **Why it matters:** The rewritten `configuration.adoc:49` now promises that `data-model.md` is "the single, exhaustive enumeration" and that the page "surveys the fields project authors tune regularly". An operator hitting a CodeRabbit rate-limit refusal has an opt-in recovery knob available and no user-facing page that names it. More broadly, D1 was the plan's graded gate and its output is presented as a complete derivation; leaving two seeded knobs out of all four buckets undercuts the claim the deliverable exists to make.
- **Fix:** Add two rows to the `[#automated-review]` table in `doc/user/configuration.adoc`, after `re_review_on_timeout` (`:424`): `steps['plan-marshall:automatic-review'].review_rate_window_await` | `false` | opt-in gate arming the rate-limit refusal recovery sequence instead of settling on a detected refusal; when `true` an `awaitable_window` bot's rate window is claimed and awaited, while `hard_quota`/`unknown`/`size` causes escalate immediately. And `steps['plan-marshall:automatic-review'].review_rate_window_timeout_seconds` | `3600` | await budget (seconds) capping the rate-window expiry poll, only consulted when `review_rate_window_await` is `true`; on exhaustion the step escalates with reason `rate_window_timeout`. Take the wording from the `configurable:` frontmatter in `automatic-review/SKILL.md:46-51` and point onward to `automatic-review/SKILL.md` § "Rate-limit refusal recovery (opt-in)" for the escalation contract.
- **Done when:** every leaf key that `get_default_config()` emits under `steps['plan-marshall:automatic-review']` has either a row in the `[#automated-review]` table or an explicit recorded reason for being out of population.
- **Module/topic:** `doc/user/` — configuration documentation (`configuration.adoc` § automated review)

## G3 — Drop the surviving meta-project sentence from `recipes.adoc`

- **Kind:** incomplete-sweep
- **Severity:** low
- **Where:** `doc/user/recipes.adoc:30` — the micro-lane "Keeps" bullet
- **What is wrong:** `recipes.adoc:30` ends its micro-lane "Keeps" bullet with "Lessons capture and the meta-project derived-state steps also stay in." That is the *same* prose-embedded sentence pattern D5 removed from `configuration.adoc` (the deleted line read "…lessons capture, and the meta-project derived-state steps"), surviving on a different consumer-facing page. (`efforts.adoc:173` also mentions `target/claude/`, but explicitly labels the paragraph "For meta-project developers only", so it is fine as-is and is the model to follow.)
- **Why it matters:** The plan's stated Goal is "no consumer-facing page carries meta-project-only content or a reference a consumer cannot follow". A consumer reading `recipes.adoc` is told their minimal posture retains steps their project does not have. D5's *Done-when* was scoped to `configuration.adoc` alone, so this is not a scope breach — but the Goal is not met, and the residue is the identical defect class the plan was written to remove. Severity is `low` rather than `medium` because the sentence is informational: nothing in it invites the reader to run a command or set a key, so no operator action miscarries on it. (Adversarial-review re-severity; the actionable half of the original G3 is split out as G8.)
- **Fix:** In `doc/user/recipes.adoc:30`, drop "and the meta-project derived-state steps" from the sentence, leaving "Lessons capture also stays in."
- **Done when:** `grep -n 'meta-project' doc/user/recipes.adoc` returns no hit.
- **Module/topic:** `doc/user/` — consumer-facing documentation hygiene

## G4 — Add the seeded recipe-routing knobs to `data-model.md` (carried-forward residue)

- **Kind:** doc-drift
- **Severity:** medium
- **Where:** `marketplace/bundles/plan-marshall/skills/manage-config/standards/data-model.md` — the `plan.phase-1-init` field table
- **What is wrong:** `data-model.md` presents itself as the complete `marshal.json` schema ("## Complete Structure", `data-model.md:9`), and `doc/user/configuration.adoc:49` now points at it as "the single, exhaustive enumeration of every field and its validator". It omits two knobs the code seed carries: `plan.phase-1-init.auto_route_recipe` (default `true`) and `plan.phase-1-init.auto_route_recipe_threshold` (default `0.6`), at `_config_defaults.py:610,618` today (`:605,613` at the landed commit `a8b9b42d`). `grep -c auto_route_recipe` over `data-model.md` returns `0`. Reported as Finding #1 by the run and deliberately left unfixed — `data-model.md` was read-only for that plan.
  Severity raised from `low` to `medium` during adversarial review: this is the same magnitude of defect as G2 — two real knobs absent from the authority a reader is explicitly pointed at — and the pointer this plan installed at `configuration.adoc:49` states exhaustiveness as fact, so the page now ships a completeness claim that one `grep` falsifies.
- **Why it matters:** This plan *increased* the weight resting on `data-model.md`: it deleted the hand-written escape-hatch list and replaced it with a pointer that asserts exhaustiveness. Every knob missing from `data-model.md` is now invisible from the user page as well.
- **Fix:** Add the two keys to the `plan.phase-1-init` section table and to the "Complete Structure" JSON sample in `data-model.md`, with defaults `true` / `0.6` and the validator each carries, sourced from the block comments at `_config_defaults.py:604-618`.
- **Done when:** every key `get_default_config()` emits under `plan.phase-1-init` appears in `data-model.md`'s `plan.phase-1-init` table.
- **Module/topic:** `plan-marshall:manage-config` — `standards/data-model.md`

## G5 — Reconcile `set-lane`'s accepted set with the per-element override validator (carried-forward residue)

- **Kind:** bug
- **Severity:** low
- **Where:** `marketplace/bundles/plan-marshall/skills/manage-config/scripts/_cmd_finalize_steps.py:68` — `_RESOLVED_ASK_LANE_VALUES`; the argparse `choices` at `manage-config.py:749`
- **What is wrong:** `_RESOLVED_ASK_LANE_VALUES = ('off', 'standard', 'full')` gates the `finalize-steps set-lane --lane` verb, while `VALID_LANE_OVERRIDE = ('off', 'minimal', 'standard', 'full', 'ask')` (`_config_defaults.py:481`, twinned at `_manifest_lanes.py:20`) is what the reader/validator accepts for the same `steps.<step>.lane` field. An operator who reads `configuration.adoc:293` — which correctly documents the five-value override space — and then reaches for `set-lane --lane minimal` is rejected by argparse. Reported as Finding #2 and explicitly assigned to its own change by the plan's Out of scope.
  **Executed, not inferred** (adversarial review): `manage-config.py finalize-steps set-lane --step-id plan-marshall:automatic-review --lane minimal` exits `2` with `argument --lane: invalid choice: 'minimal' (choose from 'off', 'standard', 'full')`; `--lane ask` fails identically; `--lane standard` exits `0`. The alternative route the Fix depends on does work: `cmd_plan(... step_verb='set', param='lane', value='minimal')` returns `success` and persists `lane: minimal`. It performs no validation at all, though — the same call with `value='utter_garbage'` also returns `success` and persists that, so the wider channel is not a validating substitute, only a permissive one.
- **Why it matters:** The write path and the read path for one field admit different value sets, so the only way to set a documented value (`minimal`, `ask`) is to hand-edit `marshal.json` or go through the generic `step set --param lane` channel. `configuration.adoc:180` now papers over this in prose, which is the right docs answer but leaves the CLI friction in place.
- **Fix:** Decide the intended contract and make one side match. Either widen `_RESOLVED_ASK_LANE_VALUES` (and the argparse `choices` at `manage-config.py:749`) to the full `VALID_LANE_OVERRIDE` set — the docstring at `_cmd_finalize_steps.py:297-306` defends the current narrowing as "``off`` / ``standard`` / ``full`` are the resolved answers an operator dialogue produces, while ``minimal`` and ``ask`` are seed values only shipped frontmatter and marshal seeding emit", which is a reason for the *dialogue's* options, not for the *verb's* accepted set — or keep the narrow write set and replace the bare argparse `choices` rejection with a custom check whose message names the wider channel (`step set --step-id <id> --param lane --value minimal`) so the operator has a working route.
- **Done when:** either `manage-config finalize-steps set-lane --step-id <id> --lane minimal` exits `0`, or its rejection text contains the string `step set` and `configuration.adoc:180` points the reader at that message.
- **Module/topic:** `plan-marshall:manage-config` — `finalize-steps set-lane` verb

## G6 — Re-home the four retired finalize ceremony gates in the run-at-all table

- **Kind:** stale-statement
- **Severity:** high
- **Where:** `doc/user/configuration.adoc` — the `[#run-at-all-gates]` table rows at `:190` (`plan.phase-6-finalize.self_review`), `:191` (`plan.phase-6-finalize.qgate`), `:193` (`plan.phase-6-finalize.simplify`), `:194` (`plan.phase-6-finalize.steps['default:finalize-step-security-audit'].security_audit`), and the worked example at `:202` (`plan phase-6-finalize get --field self_review`)
- **What is wrong:** All four keys were retired by the run-at-all → lane migration and no longer exist. The canonical statement is `marketplace/bundles/plan-marshall/skills/manage-config/SKILL.md:377`: "The four finalize ceremony gates (`qgate` / `self_review` / `simplify` / `security_audit`) no longer ride a run-at-all knob — each is governed by its owning finalize step's per-element `steps.<step>.lane` override … set via `step set --step-id <owning-step> --param lane --value <value>`", restated at `SKILL.md:723` (with the owning-step map) and at `extension-api/standards/marshal-json-reference.md:101`. `_cmd_sync_defaults.py:111-126` carries the migration shim that *removes* the legacy keys and folds them onto the owning step's `lane`, keyed by `_CEREMONY_GATE_OWNER_STEP`. Executing the seed confirms the absence: `get_default_config()['plan']['phase-6-finalize']` has keys `max_iterations`, `finalize_without_asking`, `loop_back_without_asking`, `checks_wait_timeout_seconds`, `steps`, `effort` — none of the four — and `steps['default:finalize-step-security-audit']` is `{}`. Two tests assert the absence directly: `test/plan-marshall/manage-config/test_cmd_quality_phases.py:1196-1201` (`'simplify' / 'self_review' / 'security_audit' / 'qgate' not in result`) and `:1204` (`get --field simplify` must error).
  This is not the drift the plan introduced — the rows predate `a8b9b42d` and the landed diff does not touch them (`git show a8b9b42d -- doc/user/configuration.adoc` contains no hunk for them). It is what D1 was supposed to catch and instead endorsed: report-01.md § D1 lists "`deep_lane`/`escalation`/`revalidation` + finalize run-at-all gates (§run-at-all-gates)" in its **Covered** bucket, so the gate's own output certifies four dead keys as documented knobs.
- **Why it matters:** The failure mode is asymmetric and both halves are bad. `plan phase-6-finalize get --field self_review` — the exact command the page prints at `:202` — returns `status: error`, `Unknown field 'self_review' in phase-6-finalize`. But `plan phase-6-finalize set --field self_review --value never` returns `status: success` and persists `"self_review": "never"` into `marshal.json`, where nothing reads it and the sync-defaults shim (which migrates `self_review` only from its *step-owned* location, `_cmd_sync_defaults.py:181-183`) never removes it. Executed proof that the field is not merely undocumented but unrecognised: `set --field escalation --value banana` is rejected with `Invalid plan.phase-1-init.escalation 'banana'. Allowed: ['auto', 'always', 'never']`, while `set --field self_review --value banana` returns `success` — the live gate validates, the retired one falls through an unknown-field path. An operator who follows this table to switch off the finalize self-review gets a success return and no behaviour change. That is a shipped false signal on the highest-risk gate the page describes ("`never` can push a red tree"), which is this epic's archetype.
- **Fix:** In `doc/user/configuration.adoc`, delete the four table rows at `:190`, `:191`, `:193`, `:194` and replace them with one sentence beneath the table stating that the four finalize ceremony gates are set through their owning step's `lane` override, naming the owning steps from `manage-config/SKILL.md:723` (`default:pre-push-quality-gate` → qgate, `default:pre-submission-self-review` → self_review, `default:finalize-step-simplify` → simplify, `default:finalize-step-security-audit` → security_audit) and the transform (`off` → never, `minimal` → always, every other value → auto), with an xref to `<<execution-profile-lane-selection>>` where the `lane` override row already lives at `:293`. Replace the `get --field self_review` line in the code block at `:202` with `plan phase-6-finalize step get --step-id default:pre-submission-self-review`. Leave the three surviving rows (`deep_lane` `:187`, `escalation` `:188`, `revalidation` `:189`) untouched — all three resolve and validate.
- **Done when:** every `Config key` cell in the `[#run-at-all-gates]` table resolves to a key that `get_default_config()` emits, and every command in the code block that follows the table exits `0` and round-trips its value through `get`.
- **Module/topic:** `doc/user/` — configuration documentation (`configuration.adoc` § run-at-all gates)

## G7 — Remove the `plan.phase-6-finalize.plugin_doctor` row and its worked example

- **Kind:** stale-statement
- **Severity:** high
- **Where:** `doc/user/configuration.adoc:192` — table row `plan.phase-6-finalize.plugin_doctor`; and the worked example at `:205` (`plan phase-6-finalize set --field plugin_doctor --value always`)
- **What is wrong:** `plan.phase-6-finalize.plugin_doctor` has never existed as a configuration key. `git log -S"'plugin_doctor'"` over the whole repository returns **no commit** that ever added it as a quoted key, `get_default_config()` does not emit it, `data-model.md` does not list it, and the only occurrence of the token anywhere under `marketplace/bundles/` is a prose mention of a test file name in a comment (`pm-plugin-development/skills/plugin-doctor/scripts/doctor-marketplace.py:706`). The lint it names is a **project-local finalize step**, `project:finalize-step-plugin-doctor` (`.claude/skills/finalize-step-plugin-doctor/`, registered in `extension-api/standards/ext-point-finalize-step.md:234` and `_manifest_rules.py:416-417`), whose activation rides the lane/scope machinery, not a phase-level field. It is a separate root cause from G6: the other four rows document keys that *were* real and got migrated; this one documents a key that never was.
- **Why it matters:** Two compounding defects on one row. First, the same false-success mechanism as G6 — executing `plan phase-6-finalize set --field plugin_doctor --value always`, verbatim from the page at `:205`, returns `status: success` and persists `"plugin_doctor": "always"` into `marshal.json`, while `get --field plugin_doctor` answers `Unknown field 'plugin_doctor' in phase-6-finalize`. The page's only worked `set` example is a no-op write that reports success. Second, the step is meta-project-only: it lives under `.claude/skills/`, which `doc/developer/marketplace-build.adoc:164` states "consumer projects do not get … only the meta-project". So the row is also D5-class residue that D5's sweep could not see, because its pattern (`meta-project` / `sync-plugin-cache` / `deploy-target` / `target/claude`) does not match the step's name.
- **Fix:** Delete the `plan.phase-6-finalize.plugin_doctor` row at `doc/user/configuration.adoc:192`, and change the `set` example in the code block at `:205` to a knob that exists — `plan phase-6-finalize set --field finalize_without_asking --value false` reads and writes cleanly and is already documented in the same page's review-gates section. If the plugin-doctor step is to stay on a consumer-facing page at all, it must be re-homed as `steps['project:finalize-step-plugin-doctor'].lane` and carry an explicit "meta-project developers only" label in the style of `doc/user/efforts.adoc:173`.
- **Done when:** `grep -n plugin_doctor doc/user/configuration.adoc` returns no hit, and every command in the code block at `:199-206` exits `0` and round-trips through `get`.
- **Module/topic:** `doc/user/` — configuration documentation (`configuration.adoc` § run-at-all gates)

## G8 — Scope or drop the `/sync-plugin-cache` remedy in `enforcement-hook.adoc`

- **Kind:** incomplete-sweep
- **Severity:** medium
- **Where:** `doc/user/enforcement-hook.adoc:21` — the "Generated-executor edits" bullet
- **What is wrong:** The bullet tells the reader to "Regenerate it via `/sync-plugin-cache` + `/marshall-steward` rather than editing it". `/sync-plugin-cache` is meta-project-only: `doc/developer/marketplace-build.adoc:164` states "The slash command and its finalize-step counterpart are project-local under `.claude/skills/` — consumer projects do not get this surface, only the meta-project." Confirmed against the tree: the skill exists only at `.claude/skills/sync-plugin-cache/` — there is no `marketplace/bundles/*/skills/sync-plugin-cache/` directory and no `marketplace/bundles/*/commands/sync-plugin-cache*` entry, so nothing a consumer installs supplies the command (the bundle files that mention the name do so only in prose about the meta-project). Split out of the original G3 during adversarial review — it is a different page, a different root cause, and a different remedy from the `recipes.adoc` sentence, so it is recorded as its own instance.
- **Why it matters:** Unlike G3's informational residue, this instruction is *actionable and wrong*: a consumer whose executor is stale is directed to a slash command their installation does not carry, and the redirect fires from a PreToolUse hook denial — a moment when the operator is blocked and following the message literally. The correct consumer path (`/marshall-steward`) is already in the same sentence, so the wrong half is what a reader tries first.
- **Fix:** In `doc/user/enforcement-hook.adoc:21`, replace "Regenerate it via `/sync-plugin-cache` + `/marshall-steward`" with "Regenerate it via `/marshall-steward`", and — if the meta-project variant is worth keeping — add a parenthetical in the `efforts.adoc:173` style: "(in the plan-marshall meta-project, run `/sync-plugin-cache` first)".
- **Done when:** `grep -n 'sync-plugin-cache' doc/user/enforcement-hook.adoc` returns either no hit or only a hit inside a clause explicitly scoped to the meta-project.
- **Module/topic:** `doc/user/` — consumer-facing documentation hygiene

## Refuted during adversarial review

No gap was refuted. Every one of G1–G5 was re-tested against the tree rather than re-read, and each survived:

- **G1** — re-swept 90 table key cells across `configuration.adoc`, `parallelism-and-locking.adoc`, `efforts.adoc` and `terminal-title.adoc` (broader than the two-page, 76-cell sweep the verification ran): `drop_review_on_scope_gate` remains the only key whose leaf name appears nowhere under `marketplace/bundles/`. The escalation mechanism was **executed**, not read (see the gap body). `8cd280a3` confirmed an ancestor of `a8b9b42d`. Severity `high` upheld.
- **G2** — the seeded param set was re-derived by executing `get_default_config()`; both knobs confirmed absent from the `[#automated-review]` table and from all four of report-01.md § D1's buckets (`grep -c review_rate_window report-01.md` → `0`). The arithmetic in the gap body was wrong (eleven keys total, not eleven plus `lane`) and has been corrected; the finding itself stands. `bot_lists_provenance` was checked as a possible second orphan and is **not** one — it is a real key written by the steward migration.
- **G3** — text confirmed verbatim at `recipes.adoc:30`. Re-severitied `medium` → `low` and narrowed to one instance; the `enforcement-hook.adoc` half is now G8.
- **G4** — `grep -c auto_route_recipe data-model.md` → `0` confirmed; line numbers corrected (`:610,618` today, not `:608,616`); re-severitied `low` → `medium`.
- **G5** — both write paths **executed**: `set-lane --lane minimal` exits `2`, the generic `step set --param lane --value minimal` succeeds. Severity `low` upheld — the rejection is loud, the docs at `configuration.adoc:180` describe it accurately, and a working alternative exists.
