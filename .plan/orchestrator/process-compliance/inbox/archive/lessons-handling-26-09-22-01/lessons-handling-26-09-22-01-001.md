envelope_version=1
sender_type=orchestrator
sender_id=lessons-handling-26-09-22-01
epic=process-compliance
kind=candidate-lesson
created=2026-09-22T07:15:08Z

# Candidate lessons routed from lessons-handling-26-09-22-01

12 lessons matched to this epic's scope (structural rule-following, transition gates, worktree
machinery, generator/wrapper contracts, persona behavior rules). All `active` in the source corpus.

## Argparse / script-invocation recurrence (cross-ref pair)
- **2026-09-20-08-014**: `merge_lock` budget-reclaim declares `--hold-start` as a float while every
  caller holds an ISO-8601 instant.
- **2026-09-20-08-013**: a documented argparse recurrence signature recurred anyway — the router-scoped
  `--plan-id` placed AFTER the `ci` verb.

## Persona-plan-marshall-agent foundational conduct (shared-component cluster of 5)
- **2026-09-21-10-001** (primary): argparse rejections recur across scripts despite the executor already
  naming the fix — one plan recorded 20 script failures, 12 unique, dominant class argparse rejection
  not script-internal error.
- **2026-09-19-21-003**: a fix's unit is the derived co-reference population, never the site the finding
  named.
- **2026-09-19-21-004**: every only/all/none/the-set-is-N claim carries the enumeration and the scope it
  was derived over.
- **2026-09-20-08-012**: a closed-vocabulary rule's escape hatch must place the out-of-scope case OUTSIDE
  the set, not assign it a set member.
- **2026-09-21-10-004**: coverage figures re-derived in transit produce structurally impossible zeros.

## Finalize-mechanism structural defects (phase-6-finalize, 4 of the 5-member shared-component cluster —
the 5th, "owed architecture hints," routed to post-run-quality instead as an archival-obligation subject)
- **2026-09-20-08-003** (primary): `emit-landing`'s fail-closed epic guard contradicts `archive-plan`'s
  irreversibility when the dispatcher omits orchestration inputs.
- **2026-09-20-08-004**: `archive-plan`'s SKILL.md documents the wrong archive path, missing the
  `local/` segment.
- **2026-09-20-08-010**: a forked finalize subagent inherits a worktree-pinned cwd and cannot finish
  `branch-cleanup`.
- **2026-09-21-13-004**: `pr_title` settled at refine is never re-derived after a gate deliverable
  removes scope.

## Session/worktree machinery (unconfirmed hypothesis — needs investigation before acting)
- **2026-09-20-08-005**: `archive-plan`'s session-binding orphan sweep reported the non-UUID literal
  `by-cwd` as an orphan and removed it — may be deleting its own cwd-lookup index directory every
  finalize, across the whole install. Explicitly flagged by its author as unconfirmed; read
  `platform-runtime`'s `session_binding` orphan classifier before treating this as settled.

## Disposition
All 12 are `standalone`/`clustered-into`, none `already-covered`. Source files removed from
`.plan/local/lessons-learned/` after this message is confirmed queued.
