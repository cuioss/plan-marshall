# Run finding: PR #1059

epic: review-apparatus · analysed 2026-07-30 · evidence: `ci pr comments --pr-number 1059`
(4 comments, 4 unresolved)

## Participation

| Bot | Result | Detail |
|---|---|---|
| CodeRabbit | rate-limited | "Review limit reached" — **known, NOT an issue** |
| Sourcery | rate-limited | "weekly rate limit of 500000 diff characters" — **known, NOT an issue** |
| PR-Agent | ✅ result present, **0 findings** | informational Guide: tests present / no security concerns / no major issues detected |

## ✅ Verdict — CLEAN on the per-bot rules. No structural-deficit assessment possible.

PR-Agent provided a result; "no findings" is a result. Both other reviewers were rate-limited, so
**no baseline exists** and the comparative rule is not assessable.

## ⛔ But this run carries a SEPARATE, still-live defect — the stale review

PR-Agent's result was published against HEAD `acbdcecf3` at **15:36:44Z**. The **merged** HEAD was
`cf634762` at **16:51:40Z** — **75 minutes later**, after the rebase that produced the merge candidate.
Its registry sets `participation_requires_update: true` and `updated_at` never moved, so participation
was **correctly not credited** — and then reported as **`absent`**.

⭐ **The per-bot rules above and this defect are orthogonal, and it matters not to conflate them.**
PR-Agent satisfied "always provide a result" — it ran, reviewed and published. What failed is that the
result described a diff that is not the one that merged, and the taxonomy then named a healthy
reviewer as one that never engaged. **This is the evidence base for `PLAN-PR-007`** (Defect F), and it
is not a participation-rule violation.

⚠ Note the org fail-closed step *"Verify the reviewer actually produced a review"* **passed** on
workflow run `30466587421` (conclusion success). It was right to pass — a review was produced. It
simply cannot see staleness.

## Posted answers

✅ Complete. PR-Agent's Guide was answered ("Informational PR Reviewer Guide summary comment from
pr-agent … no actionable suggestion, nothing …"). No `untransmitted`, no unjustified `skipped`.

## Could we have found it ourselves?

**Nothing to answer.** No accepted findings from any reviewer, so the corpus for this run is empty.

⚠ **The stale-review defect is NOT a back-feed candidate.** It is not a diff-local shape a `_detect_*`
function could recognise — it is a step-ordering and taxonomy defect, already owned by `PLAN-PR-007`.
Attempting a detector for it would be the complexity mistake the standing practice prohibits.

## Feeds

- `PLAN-PR-007` — primary evidence base (Defect F, the `stale`-vs-`absent` conflation).
- **Recovery question open** — does an explicit re-review trigger still recover a review on a merged
  PR? Common to all five owed revisits, unanswered.
- `PLAN-PR-004` — ⛔ **NOT usable as charter evidence.** No baseline.
