# Verification — 200-respread-the-effort-preset-ladder

**Verified against:** commit `ac06e4fc` (code state at verification time; later commits on this branch touch only `doc/plans/**` verification artifacts)   **Landed as:** PR #1181, commit `55639a50`   **Verdict:** implemented-with-gaps

## Method

What was actually done:

- Read `plan.md` and `report-01.md` in full.
- Located the landed commit with `git log --oneline --all --grep '#1181'` → `55639a50`; read `git show --stat 55639a50` (12 non-plan files) and the full diff for `effort_presets.py`, `_cmd_effort.py`, `manage-config.py`, `_config_defaults.py`, `api-reference.md`, `manage-config/SKILL.md`, `effort-menu.md`, `wizard-flow.md`, `doc/user/efforts.adoc`, and the three test files.
- Read the pre-change payloads via `git show 55639a50^:.../effort_presets.py` to re-derive the old 23/30/34 ladder independently, and `git show 55639a50^:.../effort-menu.md` to confirm the pre-change wizard really did an LLM deep-equality eyeball.
- Walked the whole `effort_presets.py`, `_cmd_effort.py` (`_expand_phase_effort`, `cmd_effort_apply_preset`, `reconstruct_effort_payload`, `cmd_effort_identify`, `_identify_message`), `manage-config.py` argparse wiring, and `_config_defaults.py` seed blocks at HEAD.
- **Executed** the module: `uv run python -c` importing `EffortPresets`, computing the summed-level spread of all three presets and both legacy shapes, and calling `identify()` on current and legacy payloads. Results: economic 30, balanced 36, high-end 41; legacy economic 23, legacy high-end 34; `identify(ECONOMIC) = {'name':'economic','status':'current'}`; `identify(legacy economic) = {'name':'economic','status':'previous-ladder'}`; no `balanced` key in `_LEGACY_PRESETS`.
- **Ran tests**: `uv run python -m pytest test/plan-marshall/plan-marshall/test_effort_presets.py test/plan-marshall/manage-config/test_cmd_effort.py -o addopts="" -q` → **71 passed** (re-run during adversarial review: still 71 passed). `test/plan-marshall/manage-config/test_config_defaults.py` → **234 passed, 0 failed**. An earlier run of this file reported 232 passed / 2 failed (`test_get_default_config_seeds_orchestrator_block_with_every_knob`, `test_seeded_orchestrator_leaves_effort_and_scope_resolution_unchanged`); on re-run with a clean working tree both pass, so those failures were a transient artifact of a concurrent agent's uncommitted edit and not a property of this tree — see § What could NOT be verified. This plan's own guard `test_seeded_effort_shape_deep_equals_economic_preset` passes.
- **Mutation-checked twice** (bytes saved to scratchpad first, restored by byte copy; `git status --porcelain` afterwards shows no modification to any file outside this directory):
  1. Reverted `HIGH_END` to the pre-respread payload → 9 tests went RED, including `test_preset_spread_matches_target[high-end]`, `test_preset_spread_ladder_is_evenly_distributed`, `test_high_end_reaches_level_5_but_never_level_6_or_7`, `test_preset_ladder_is_monotonic`. The spread assertion is real, not decorative.
  2. Deleted the `_LEGACY_PRESETS` loop from `EffortPresets.identify` → 4 tests went RED (`test_identify_recognises_pre_respread_economic_as_previous_ladder`, `…_high_end_…`, and both `cmd_effort` genuinely-old-config tests). The migration guard is non-vacuous.
- Swept the tree for stale restatements: `grep -rniE "(economic|balanced|high-end)"` over `marketplace/`, `doc/`, `.claude/` (excluding `doc/plans/`); `grep -rn "EffortPresets|effort_presets|effort apply-preset|effort identify"` over `marketplace/ doc/ test/ .claude/`; `grep -rn "deep-equality"`; `grep -rn "Current: "`; `grep -rn "level-3|level-4|level-5"` over the effort-owning skills and `doc/`.
- Opened the two REFUTED-claim artifacts directly: `check-routing-decisions.py` and `routing-decision-verification.md`.
- Opened `effort-levels.md` and `marketplace/targets/claude/variant_emitter.py` to confirm `level-5` is **not** alias-capability-gated and that `execution-context` (no `levels:` frontmatter) emits all seven variants — i.e. the rationale for admitting level-5 while excluding level-6/7 holds.
- Opened `_cmd_sync_defaults.py` to confirm the asserted `RETIRED_STEP_KEY_RENAMES` migration precedent exists.
- Walked `git log --follow` on `effort_presets.py` to check for ladders older than the one `_LEGACY_PRESETS` records.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D1 | GATE: settle what can be settled, record proposals for what cannot | each question settled with reasoning, or recorded as a proposal with options + consequences | Yes | Yes | Yes | Yes | `report-01.md` § "D1 GATE — claim verification" + § "the four questions" + § "Escalation to operator". All ten HYPOTHESIS claims re-derived; I independently re-derived nine of them from the tree and confirm each verdict (see § Report accuracy). The level-5 fork was escalated, not self-decided; the ~30 % economic floor increase is disclosed in the report and in the PR body |
| D2 | MIGRATION: legacy shapes recognised, re-apply offered | legacy shapes recognised or old→new mapped explicitly; ships even if D1 halts D3 | Yes | Yes | Yes | Yes | `effort_presets.py:237` `_LEGACY_PRESETS` (SHIM(A)-marked with owner/floor/remove-when), `:375` `EffortPresets.identify`; `_cmd_effort.py:932` `reconstruct_effort_payload`, `:959` `_identify_message`, `:973` `cmd_effort_identify`; `manage-config.py:581-587` subparser + `:936-937` dispatch; `effort-menu.md` Step 1/2. Executed `identify()` on the real legacy payloads → `previous-ladder`. Mutation 2 confirms the tests bind the behaviour. *(Line numbers in this row were re-derived during adversarial review; the originals were wrong by a constant offset — see § Adversarial review.)* |
| D3 | Apply the new ladder | the payloads and their descriptions agree | Yes | **Partly** | Yes | **No** | Executed spread computation: 30 / 36 / 41, per-slot monotonic at all nine slots — the payloads are right. `ECONOMIC` and `HIGH_END` are restated exactly, slot by slot, in `_DESCRIPTIONS` (`effort_presets.py:274`), the module docstring (`:8-42`) and their attribute docstrings. **`BALANCED` is not:** it is the only preset with slots below its own `default`, and none of its three prose surfaces accounts for all of them. `_DESCRIPTIONS['balanced']` (`:282-289`) names no level-3 slot at all — reconstructing from it gives spread **39** → **G6**. The module-docstring bullet (`:20-28`) and the attribute docstring (`:161-172`) name the two `verification-feedback` slots but omit `phase-6-finalize.default` — reconstructing gives **37**, contradicting the "Summed-level spread 36" sentence three lines later → **G7**. D3's done-when is exactly this condition, so it is not a clean pass |
| D4 | Update the FULL documentation population | every site updated; population derived by sweeping all three names plus the apply verb | Yes | Partly | Yes | **No** | Value-restating sites updated: `effort_presets.py`, `_config_defaults.py`, `api-reference.md`, `effort-menu.md`, `manage-config/SKILL.md` (Canonical invocations), `doc/user/efforts.adoc`, `wizard-flow.md`. **Missed:** the `RESERVED_LEVELS` comment (`effort_presets.py:86-88`) still says presets "may reference" level-7 while the file's own docstring, `_DESCRIPTIONS['high-end']`, `efforts.adoc:67` and a passing test all say the opposite → **G1**. `manage-config/SKILL.md:594` API-Reference verb table omits `identify` → **G2**. `_cmd_effort.py:5-13` "Handles:" list omits `identify` → **G3**. `effort-menu.md:89` cross-ref row omits `identify` → **G4**. `wizard-flow.md:372` / `menu-configuration.md:282` still describe effort-menu Step 1 as an LLM deep-equality walk → **G5**. `doc/user/efforts.adoc:66` carries the same `balanced` omission as G7, inside a three-row table whose `high-end` row (`:67`) states the convention correctly → **G8**. The consumer-script verdict IS in the report explicitly, with evidence, and I independently confirm it |
| D5 | Tests | all pass, each seen red first | Yes | Yes | Yes | Yes | 71 tests green. Round-trip for all three presets: `test_cmd_effort.py::test_identify_recognises_each_applied_preset_round_trip`. Legacy migration: `…genuinely_old_economic_config` / `…genuinely_old_high_end_config`, written through the real on-disk storage shape (`_write_marshal_with_models` writes `plan.effort` + `plan.<phase>.effort`, the same route `apply-preset` takes — not a different-route fixture). Spread assertion: `test_effort_presets.py::test_preset_spread_matches_target` + `…_ladder_is_evenly_distributed`; mutation 1 proves it fails against the old ladder. The old `test_high_end_contains_no_level_5_anywhere` was **inverted, not deleted** (`test_high_end_reaches_level_5_but_never_level_6_or_7`) |

### D3 and D4 — the two deliverables that are not a clean pass

`marketplace/bundles/plan-marshall/skills/plan-marshall/scripts/effort_presets.py:86-88` carries the comment above `RESERVED_LEVELS`:

```
# No effort levels are currently reserved. ``level-7`` is the current top
# tier (resolves to fable, max — sits above Opus) so presets may reference
# it. Future palette expansion may repopulate this tuple.
```

report-01.md's Findings table names this exact comment as the artifact contradicting the docstrings and dispositions it as *"Contradiction resolved."* It was not touched by the landed diff. After the re-spread the same module's docstring (`:37-42`) states level-6/level-7 "are NOT used as preset defaults … stay reserved for explicit per-phase opt-in", `_DESCRIPTIONS['high-end']` (`:295`) states "level-6/level-7 stay opt-in only (alias-gated)", `doc/user/efforts.adoc:67` states they "are **not** used by any preset", and `test_high_end_reaches_level_5_but_never_level_6_or_7` **executably forbids** a preset from carrying either. The comment is now the single site in the tree stating the opposite — the doc-contract-divergence archetype the plan explicitly warned against, in the very module the plan owns. Recorded as **G1**.

The four secondary misses (G2–G5) are all `identify`-verb sweep residue: the verb was documented in `api-reference.md` (verb table + full § Verb: identify) and in `manage-config/SKILL.md` § Canonical invocations, but not in the SKILL.md § API Reference verb table, not in its own module's docstring verb list, and not in the `effort-menu.md` cross-reference row that enumerates `EffortPresets`' methods. G5 is a second-order effect: two docs tell an LLM to eyeball deep-equality "mirroring effort-menu Step 1", and effort-menu Step 1 no longer does that.

D3's miss (G6–G8) is a different family and was found only under adversarial re-check. `balanced` is the sole preset whose payload holds slots **below** its own `default`, and every prose surface describing it was written in the "name the lifts" style that works for `economic` and `high-end` — where nothing sits below the default — and silently loses information for `balanced`. Three of nine slots (`phase-5-execute.verification-feedback`, `phase-6-finalize.verification-feedback`, `phase-6-finalize.default`) are `level-3` against a stated default of `level-4`. `_DESCRIPTIONS['balanced']` names none of them (G6, and it is the string the wizard prints verbatim to the operator choosing a cost tier — `effort-menu.md:44`, `:56`); the two docstrings and the user-guide row name two of the three, omitting `phase-6-finalize.default` (G7, G8). The self-contradiction is checkable without leaving the file: the attribute docstring's narrative reconstructs to spread 37 and its next sentence states 36. Independently demonstrated by mutation — setting that one slot to the `level-4` the prose implies moves the spread to 37 and turns three tests red.

## Report accuracy

Re-derived every figure in report-01.md at the moment of stating it.

**Confirmed (evidence in hand):**

- *"Re-summed from effort_presets.py: ECONOMIC …=23; BALANCED …=30; HIGH_END …=34"* — re-derived from `git show 55639a50^`. Exact.
- *"high-end matches balanced in 5/9 slots"* — re-derived slot by slot from the pre-change payloads: `default`, `phase-3-outline`, `phase-5-execute.default`, `phase-6-finalize.default`, `phase-6-finalize.post-run-review`. Exactly the five the report names. Max level across all old high-end slots = level-4. Exact.
- *"Only `default` (3) and `phase-6-finalize.default` (3) are below 4; all-4 total = 36 = +2"* — re-derived. Exact.
- *"`RESERVED_LEVELS = ()` is empty"* — still `()` at HEAD (`effort_presets.py:89`).
- *"Wizard recognises a preset by deep-equality match"* — `git show 55639a50^:.../effort-menu.md` Step 1 reads *"identify which preset (if any) it matches by deep-equality against `EffortPresets.ECONOMIC`, `.BALANCED`, and `.HIGH_END`"* with a `Current: custom (manually edited)` non-match branch. Exact.
- *"Retired-key migration precedent exists in defaults-sync"* — `_cmd_sync_defaults.py:34` `RETIRED_STEP_KEY_RENAMES`, `:78` `_migrate_retired_step_keys`, `:115` `_RUN_AT_ALL_TO_LANE`, SHIM-marked at `:32`. Exact.
- *"`check-routing-decisions.py` … no reference to any preset name or level value (the only `economic` grep hit is the substring 'token-economics')"* — independently confirmed: `grep -niE "preset|economic|balanced|high.end|effort"` over that script returns exactly one line, `:8`, the `token-economics` substring. `routing-decision-verification.md` likewise returns one `token-economics` line. **The re-spread changes no retrospective verdicts on past runs.** The plan's "highest-consequence unknown" is genuinely refuted.
- *"`_config_defaults.py` hardcodes … not imported"* — confirmed: the seed values are literals; the guard test `test_seeded_effort_shape_deep_equals_economic_preset` is what binds them to the preset, and it passes.
- *"Seeded defaults kept at their current values"* — the landed diff to `_config_defaults.py` changes comments only; every `'effort':` literal is byte-identical. A fresh project's cost is unchanged, as claimed.
- *"level-5 is the practical ceiling; level-6/7 are alias-capability-gated"* — confirmed against `effort-levels.md` § The Alias-Capability Guard (only level-6/level-7 are gated) and `marketplace/targets/claude/variant_emitter.py:59` (`level-5 → opus/high`, no gate). The `execution-context` and `execution-context-reader` agents declare no `levels:` frontmatter, so `selected_levels` emits all seven — `execution-context-level-5` exists. The rationale is sound, not asserted.
- *"identify … `_LEGACY_PRESETS` hold the actual old economic-23/high-end-34 shapes"* — byte-compared against `git show 55639a50^`. Exact, including the string-vs-dict phase shapes `_expand_phase_effort` would have produced.
- *"old balanced (30) is byte-identical to the current economic"* — confirmed field by field; `test_identify_pre_respread_balanced_resolves_to_current_economic` asserts it directly and passes.

**Contradicted by the tree:**

1. **Findings row 1 — *"Contradiction resolved."*** It is not. The `RESERVED_LEVELS` comment the same row names as the contradicting artifact is unchanged at `effort_presets.py:86-88` and now conflicts with four other sites plus a passing test. See G1. This is the single most consequential report inaccuracy: the report asserts a fix that did not land.
2. **D4 site count — *"9 files changed (6 docs/code + 3 tests)."*** Re-derived: `git show --format="" --name-only 55639a50 | grep -vc "^doc/plans/"` → **12**. The landed diff changed 9 non-test files (`effort_presets.py`, `_cmd_effort.py`, `manage-config.py`, `_config_defaults.py`, `api-reference.md`, `manage-config/SKILL.md`, `effort-menu.md`, `wizard-flow.md`, `doc/user/efforts.adoc`) plus 3 test files. The report's "6" counts only the value-restating subset and then labels the sum "9 files changed", which under-reports the changed-file volume by three. The *coverage* claim it pairs with the count is the more important one, and that claim is where the real miss lives (G1–G5), not in the volume.
3. **D4 row — *"grep sweep for stale reservation prose clean."*** The sweep missed `effort_presets.py:86-88`, the reservation-adjacent prose in the plan's own primary file. The Step-6 sub-agent's re-dispatch is reported as confirming "no sibling stale claim survives"; G1–G5 survive.
4. **Step 6 sub-agent clean verdict — *"D5 tests … level-5 guard inverted not deleted, identify coverage complete."*** The first two clauses are true (verified by mutation). "Complete" is true for `identify()`'s behaviour but not for its documentation surface (G2–G4).

Minor, non-contradicting: the report and PR body call the registry `LEGACY_PRESETS`; the symbol is the private `_LEGACY_PRESETS`. The report itself uses the correct spelling elsewhere.

## Out-of-scope compliance

The run stayed inside its declared boundaries.

- **"Deciding the level-5 policy"** — not self-decided. The report records an `AskUserQuestion` escalation and the operator's answer. The escalation itself is not verifiable from the tree (see below), but the tree is consistent with it: the PR (#1181) was authored and merged under a human account.
- **"Changing which preset a new project gets by default"** — respected in the sense that matters. Every seeded `'effort':` literal in `_config_defaults.py` is byte-identical across the diff; only comments changed. A fresh project's cost is unchanged. What changed is the *label* the wizard prints for that seed (`balanced` → `economic`), which is a naming consequence of the value re-spread, not an unprompted cost increase — and the report discloses it explicitly.
- **"Redesigning the effort-role slot set"** — `KNOWN_ROLES` (`_cmd_effort.py:79-85`) is untouched by the diff; the nine slots are the same nine.

**Undeclared collateral:** none that is out of scope. The diff touches four files the plan's "Expected surface" did not name (`_cmd_effort.py`, `manage-config.py`, `manage-config/SKILL.md`, `wizard-flow.md`). All four are D2's migration implementation and its documentation — D2's *Done when* required a recognition path, and the Expected-surface list is explicitly a sample the plan told the run to re-derive. Two files the Expected surface named were correctly left unchanged after inspection (`check-routing-decisions.py`, `routing-decision-verification.md` — refuted with evidence; `execution-context.adoc` — generic cross-refs only, confirmed: `grep -niE "preset|economic|balanced|high-end|level-[0-9]"` returns three lines, all bare cross-references).

Additionally checked: no ladder older than the one `_LEGACY_PRESETS` records is stranded. `git log --follow` on `effort_presets.py` shows only one prior payload edit (`32d283bb`, #862), which removed the `phase-1-init` role and changed no other level. A pre-#862 config reconstructs identically because `reconstruct_effort_payload` iterates `KNOWN_ROLES` and simply ignores the orphaned `plan.phase-1-init.effort` key. `_LEGACY_PRESETS` therefore covers the whole pre-respread history, not just the immediately preceding release.

## Residue carried forward

| report-01.md residue | Status in today's tree |
|---|---|
| `coderabbitai` review deferred by its own rate limit; optional `@coderabbitai review` re-request | **Closed / moot.** PR #1181 landed as `55639a50`, which is an ancestor of `main` today. The disclosure was explicitly not a blocker |
| Local developers owe `/sync-plugin-cache` after this lands | **Not a repo-tracked debt**, as the report itself states. `CLAUDE.md` § Standalone Plan Lane confirms a cloud run neither performs nor owes the sync. Nothing open in the tree |
| "Nothing else open" | **Refuted.** G1–G8 were open at merge and remain open at HEAD (G6–G8 added during adversarial review) |

## What could NOT be verified

- **The operator escalation itself.** report-01.md records two `AskUserQuestion` exchanges (the level-5 fork and the tier naming) and their answers. Nothing in the tree records an interactive exchange, so the answers cannot be confirmed or refuted from here. The tree is *consistent* with them (level-5 shipped; the three names are unchanged), and PR #1181 was merged under a human account — but that is corroboration, not proof. Stated explicitly rather than passed.
- **"each seen red first" (D5).** Whether the author observed each new assertion red before writing the code is unobservable from the tree. What I could substitute — and did — is a mutation check proving the assertions are non-vacuous today: reverting `HIGH_END` to the pre-respread payload turns 9 tests red, and deleting the legacy branch of `identify()` turns 4 red.
- **`./pw quality-gate` / `./pw verify` results and the plugin-doctor verdict.** Not re-run here (heavy build, and out of proportion to a read-only verification pass). The report's `19168 passed, 14 skipped` figure is therefore unconfirmed.
- ~~**The two failing tests in `test_config_defaults.py`.**~~ **Resolved during adversarial review — this bullet no longer holds.** Re-run against a working tree with no uncommitted source modifications (`git status --porcelain` clean outside `doc/plans/`), `test/plan-marshall/manage-config/test_config_defaults.py` reports **234 passed, 0 failed**, and both named tests pass when selected individually (`-k "orchestrator_block_with_every_knob or leaves_effort_and_scope_resolution_unchanged"` → 2 passed). The original failures were caused by the concurrent agent's uncommitted `_cmd_planning_lane.py` edit that the bullet itself suspected; that edit is gone. Retained rather than deleted because the earlier reading was real and a later reader should know why the numbers differ.
- **Three pre-existing statements adjacent to this plan's surface**, verified as pre-existing (present at `55639a50^`) and therefore not this plan's regressions, but noted so a later sweep can pick them up: (a) `EffortPresets.all_names()`'s docstring (`effort_presets.py:344`) says it is *"Used as the `argparse choices=...` list"*, while `manage-config.py:526-541` deliberately uses `type=` and `api-reference.md:471` states `choices=` is intentionally not used; (b) `doc/user/efforts.adoc`'s worked example configures a `phase-3-outline.research` sub-key that is not in `KNOWN_ROLES`; (c) `plan-marshall/standards/effort-roles.md:171` says *"`BALANCED` is stored in literal-expanded form"* — singular — although all three presets are literal-expanded and the deep-equality recogniser depends on all three being so; the same singular phrasing is present at `55639a50^`. None of the three is a tier-value restatement, so none fell inside D4's declared population.

## Adversarial review

**Reviewed by:** an independent agent that did not write this document.

**Checked.** Every `high` gap (there are none — the original set is one `medium` and four `low`/`medium`), every deliverable row marked a clean pass, and every "swept, clean" claim.

*Files opened in full or in the relevant range:* `effort_presets.py` (whole module), `_cmd_effort.py` (docstring, `ALLOWED_LEVELS`/`RESERVED_LEVELS`/`KNOWN_ROLES` block, `validate_effort_level`, the three `identify`-path functions), `manage-config.py` (argparse `identify` wiring + the whole `effort` dispatch branch), `_config_defaults.py` (all five seeded `effort` blocks and their comment headers), `manage-config/SKILL.md` § API Reference verb table, `standards/api-reference.md` (`identify` row + § Verb: identify), `effort-menu.md` (Step 1, Step 2, Cross-References), `wizard-flow.md:372`, `menu-configuration.md:282`, `doc/user/efforts.adoc` § presets, `doc/concepts/execution-context.adoc`, `effort-roles.md`, `check-routing-decisions.py`, `routing-decision-verification.md`, `_cmd_sync_defaults.py`, `test_effort_presets.py`, `test_cmd_effort.py`, `report-01.md` Findings table.

*Functions executed* (`UV_HTTP_TIMEOUT=600 uv run python -c …`, on the actual arguments the claims are about, not read-and-judged): `EffortPresets.get()` for all three names and `_LEGACY_PRESETS` for both, with the summed-level metric recomputed per slot → **30 / 36 / 41** and **legacy 23 / 34**; `EffortPresets.identify()` on the current economic payload, on both legacy payloads, **and on the payloads loaded out of the pre-change module at `55639a50^`** — `identify(old ECONOMIC) = previous-ladder/economic`, `identify(old HIGH_END) = previous-ladder/high-end`, `identify(old BALANCED) = current/economic`; `EffortPresets.describe('balanced')`; equality of `_LEGACY_PRESETS['economic'|'high-end']` against the pre-change `ECONOMIC`/`HIGH_END` objects (both `True`) and of the current `ECONOMIC` against the pre-change `BALANCED` (`True`).

*Commands run:* `git show --format="" --name-only 55639a50` (14 paths, 2 under `doc/plans/` → **12** non-plan, re-deriving the corrected count); `git show 55639a50 -- effort_presets.py | grep "RESERVED_LEVELS\|may reference"` (**empty** — the comment is provably outside the landed diff); `git show 55639a50 -- _config_defaults.py` filtered to `effort` lines (comments only, no `'effort':` literal changed); `git merge-base --is-ancestor 55639a50 HEAD` (true); `git cat-file -t ac06e4fc` (real commit); the two pytest files (**71 passed**) and `test_config_defaults.py` (**234 passed, 0 failed** — correcting the 232/2 figure); and broader re-sweeps than the originals: `grep -rn "summed-level|spread 2x|3x|4x|front-loaded|evenly-spaced|monotonic ladder"`, `grep -rniE "\breserved\b"` scoped to the three effort-owning skills plus both `.adoc` pages, `grep -rn "deep-equality"` over `marketplace/ doc/ .claude/ test/`, and `grep -rn -l "EffortPresets|apply-preset|apply_preset"` to re-derive the consumer population from scratch.

*Mutations applied* (bytes saved to scratchpad first, `git diff --quiet` checked clean before mutating, restored by byte copy with md5 re-verified — `git status --porcelain` afterwards shows no modification outside this directory): a **new, different** mutation from the two the original document ran — `BALANCED.roles['phase-6-finalize']['default']` `level-3` → `level-4`, the single value the `balanced` prose implies. Result: spread 37, **3 tests red** (`test_get_balanced_returns_balanced_preset`, `test_preset_spread_matches_target[balanced]`, `test_preset_spread_ladder_is_evenly_distributed`, the last failing with `balanced->high-end gap 4 < 5`). This independently establishes that the spread assertion binds **all three** presets and every slot, not just the `HIGH_END` payload the original mutation reverted.

**NOT re-checked.** The two original mutations were not re-run (a third, stronger one was run instead). `./pw verify` / `quality-gate` / plugin-doctor were not run, so report-01.md's `19168 passed, 14 skipped` remains unconfirmed. The `AskUserQuestion` operator escalation remains unverifiable from the tree — unchanged. Test files were read for fixture provenance but not audited line by line for other vacuous assertions. `git log --follow` on `effort_presets.py` was not re-walked; the original's `32d283bb`/#862 finding is carried forward unchecked.

| Item | Original claim | Verdict | Evidence |
|---|---|---|---|
| G1 | `RESERVED_LEVELS` comment still says presets "may reference" level-7; report-01.md claims the contradiction resolved | **upheld** (severity `medium` confirmed) | Comment read verbatim at `:86-88`; `git show 55639a50 -- effort_presets.py` grepped for it returns **empty**, so the landed diff provably never touched it; `report-01.md:111` says *"Contradiction resolved."*; the four contradicting sites read individually and `test_high_end_reaches_level_5_but_never_level_6_or_7` executed green. Fix's `:429-432` pointer to `_validate_level_keyword` verified exact |
| G1 (scope) | — | **rewritten** | Fix now warns that `_cmd_effort.py:60-63` and `:118-119` are **not** second instances — the mirror comment omits the "so presets may reference it" clause, and that module validates a user-supplied per-scope level where `level-7` opt-in is sanctioned. Without the warning a fixer would introduce the error G1 removes |
| G2 | `manage-config/SKILL.md:594` `effort` row omits `identify` | **upheld**, exact | Row read at `:594` — lists `read`, `resolve-target`, `apply-preset`, `set`, no `identify`. Its supporting clause re-checked at the source: `manage-config.py` dispatches **5** effort verbs and exactly **3** `finalize-steps` verbs, so the "table reads as exhaustive" rationale holds. `api-reference.md:388` and `SKILL.md:1217` confirmed already updated |
| G3 | `_cmd_effort.py:5-13` "Handles:" omits `identify` | **upheld**, citation corrected | `Handles:` block confirmed at `:5-13`; `head -45 … | grep -c identify` → **0**. The gap cited `cmd_effort_identify` at `:868`; the real definition is at **`:973`**. Corrected in gaps.md |
| G4 | `effort-menu.md:89` cross-ref row omits `identify` | **upheld**, exact | Row read at `:89`; Step 1 at `:27-40` confirmed to depend on the recogniser it omits |
| G5 | `wizard-flow.md:372` / `menu-configuration.md:282` cite a mechanism effort-menu Step 1 no longer uses | **upheld**, exact | Both lines located by a tree-wide `grep -rn "mirroring effort-menu Step 1"` (exactly two hits, both as cited, byte-identical); `effort-menu.md:27` confirmed to read *"do **not** eyeball the deep-equality yourself"* |
| D1 | clean pass | **upheld** | Nine of ten HYPOTHESIS claims re-derived here independently: old ladder 23/30/34 from `55639a50^`; old `high-end` matches old `balanced` in exactly the five slots named (`default`, `phase-3-outline`, `phase-5-execute.default`, `phase-6-finalize.default`, `phase-6-finalize.post-run-review`) and never exceeds level-4; only 2 slots below 4, so +2 → 36; `RESERVED_LEVELS = ()`; `RETIRED_STEP_KEY_RENAMES` precedent present at `_cmd_sync_defaults.py:34`. The escalation itself remains unverifiable |
| D2 | clean pass | **upheld**, line numbers corrected | Re-established by **execution against the real pre-change module**, not by reading: both `_LEGACY_PRESETS` entries compare equal to the pre-change `ECONOMIC`/`HIGH_END` objects, and `identify()` returns `previous-ladder` on each. Fixture provenance checked — `_PRE_RESPREAD_ECONOMIC`/`_PRE_RESPREAD_HIGH_END` (`test_cmd_effort.py:665-692`) are **independent literals**, not aliases of `_LEGACY_PRESETS`, so the test is not tautological, which is what the plan's "genuinely old-shaped configuration" clause demanded. **Five of the six line citations in the D2 row were wrong** (`_cmd_effort.py:826/852/868` → `932/959/973`; `manage-config.py:554/909` → `581-587/936-937`; `effort_presets.py:374` is `identify`'s `@classmethod`, the `def` is `:375`); corrected in the row |
| D3 | clean pass — *"`_DESCRIPTIONS`, the module docstring and all three attribute docstrings restate exactly those values — checked slot by slot"* | **re-severitied → not a clean pass**; **3 gaps added** | The claim does not hold for `BALANCED`, the one preset with slots below its own `default`. `_DESCRIPTIONS['balanced']` names no level-3 slot (reconstructs to 39 vs. shipped 36) → **G6**; the module-docstring bullet and attribute docstring omit `phase-6-finalize.default` (reconstruct to 37, contradicting their own "spread 36" sentence) → **G7**; `doc/user/efforts.adoc:66` repeats it inside a table whose `high-end` row states the convention correctly → **G8**. Verified by executing `get('balanced')` and `describe('balanced')`, and demonstrated by the new mutation |
| D5 | clean pass; spread assertion non-decorative | **upheld**, strengthened | Re-proved with a **different** mutation than the original used (one slot of `BALANCED`, not a wholesale `HIGH_END` revert) → 3 red including the ladder-gap assertion. Confirms the guard covers every preset and every slot, which the original mutation alone did not show |
| "swept the tree, clean" (consumer set) | 2 REFUTED artifacts + no other consumer depends on payload values | **upheld** | Population re-derived from scratch with `grep -rn -l "EffortPresets\|apply-preset\|apply_preset"`: the resulting file set is exactly the set the document accounts for. `check-routing-decisions.py` re-grepped with the broader `preset\|economic\|balanced\|high.end\|effort\|level-[0-9]` → **one** hit, `:8`, the `token-economics` substring. `execution-context.adoc` → 3 hits, all bare cross-references |
| "9 files changed" / re-derived 12 | report figure wrong; 12 non-plan files | **upheld**, exact | `git show --format="" --name-only 55639a50` → 14 paths, 2 under `doc/plans/` → 12 (9 non-test + 3 test) |
| `test_config_defaults.py` 232 passed / 2 failed | recorded as unattributable, suspected concurrent agent | **refuted as a current fact** | **234 passed, 0 failed** on re-run with a clean working tree; both named tests pass individually. The suspicion in the original bullet was correct — the concurrent edit is gone. Bullet rewritten rather than deleted |
| Verdict `implemented-with-gaps` | — | **upheld** | All five deliverables are implemented and behaviourally correct; D3 and D4 are incomplete on their documentation half only. No deliverable is unimplemented, so `partially-implemented` would overstate it |

**Documents corrected.**
*gaps.md* — open-item count `5` → **8**; intro rewritten into three gap families; **G6, G7, G8 added** (the `balanced`-prose omission, recorded per instance across `_DESCRIPTIONS`, the two docstrings, and the user guide); G1's **Fix** given an explicit do-not-touch scope for `_cmd_effort.py`, and its **Done when** extended to assert that file is unchanged; G3's `cmd_effort_identify` citation corrected `:868` → `:973`; a `## Refuted during adversarial review` section added recording that **no gap was refuted** and naming the two candidates considered and deliberately not filed.
*verification.md* — D2 row's five wrong line citations corrected; D3 row moved from clean pass to `Partly` / `Complete? No` with its evidence rewritten; D4 row extended with G8; § heading `D4 — the one deliverable…` → `D3 and D4 — the two deliverables…` with a new paragraph explaining the G6–G8 family; the test-run bullet corrected to 234/0; the "two failing tests" bullet struck through and replaced with the resolved reading; the pre-existing-statements bullet extended from two items to three (adding `effort-roles.md:171`); residue row updated to G1–G8.

**Residual doubt — what a third reviewer should look at first.**
1. **`FinalizeStepPresets`.** G5 notes in passing that the finalize-step picker still carries the exact deep-equality brittleness this plan removed for effort, with no `identify` equivalent and no legacy-shape registry. That is out of this plan's scope but is the same latent defect, one preset family over, and nobody owns it.
2. **The remaining prose surfaces of `economic` and `high-end`.** G6–G8 were found by reconstructing each preset from its own description and comparing to the executed payload. That method was applied to all three names here, but only for the summed-level metric — a reviewer should redo it per-slot for the `resolve-target` output an operator actually sees.
3. **`report-01.md` Findings row 1.** It asserts a fix that never landed (G1). Whether the same row pattern — "Contradiction resolved" with no diff behind it — appears in other reports in this epic is worth a cross-plan sweep; it is a report-level false signal, not a code defect, and this epic exists for exactly that.
4. **The two original mutations** (`HIGH_END` revert → 9 red; `identify` legacy-loop deletion → 4 red) were **not** re-run. They are the only claims in the Method section still resting solely on the original author's execution.
