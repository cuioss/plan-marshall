# Verification — 140-detect-artifacts-offers-a-live-audit-trail-as-safe-to-delete

**Verified against:** commit `0314cc4db5fa28fa8b478334c35583a0262060b7`   **Landed as:** PR #1171, commit `fb41f0148babfb57cff5324b8c756102371f482a`   **Verdict:** implemented-with-gaps

## Method

What was actually done, so an empty finding list is distinguishable from an empty check:

**Read.** `plan.md` and `report-01.md` in full. The landed squash commit `fb41f014` (`git show --stat`, then the full diff for each of the three source files). At HEAD: `git-workflow.py` lines 531-742 (`get_gitignored_files`, `_is_nested_git_boundary`, `_split_ignored`, `_is_ignored`, `scan_artifacts`, `cmd_detect_artifacts`), its argparse declaration (lines 2017-2028), `SKILL.md` lines 97-100 and 273-296, `standards/artifact-patterns.json`, `test_git_workflow.py` lines 71-95 and 902-1050, `.gitignore`, `tools-file-ops/scripts/file_ops.py` (`get_base_dir`, `_resolve_plan_root`), `script-shared/scripts/marketplace_paths.py` (`resolve_main_anchored_path`), `ref-workflow-architecture/standards/artifacts.md`, `git_provider.run_git`.

**Executed (git, git 2.43.0).** Built controlled temp repositories and ran `git ls-files --others --ignored --exclude-standard` to re-derive the collapse mechanism the plan's premise rests on — independently confirming (a) an individually-ignored file is enumerated (`.plan/local/plans/EXAMPLE-PLAN/logs/work.log`), (b) a linked worktree collapses to one trailing-slash entry (`.plan/local/worktrees/EXAMPLE-PLAN/`), and (c) in a repo with `build/` and `ignored-dir/` ignored, plain ignored directories are still enumerated file-by-file — **only a nested git repository ever produces a trailing-slash entry**.

**Executed (the real function).** Loaded `git-workflow.py` through `test/conftest.py`'s `load_script_module` and ran `scan_artifacts` against purpose-built trees:
- scan root = main repo, live worktree nested at `.plan/local/worktrees/EXAMPLE-PLAN/` → `{'safe': ['scratch.temp'], ...}`; no worktree path offered.
- scan root = the worktree itself, plan state at the production location `<worktree>/.plan/local/plans/{id}/logs/work.log` → `{'safe': ['.mypy_cache/…', 'scratch2.temp']}`; `work.log` excluded.
- same tree, `respect_gitignore=False` → `{'safe': ['.mypy_cache/…', '.plan/local/plans/EXAMPLE-PLAN/logs/work.log', 'scratch2.temp']}` — **the running plan's own work log offered as safe-to-delete.**
- scan root outside any repo → `get_gitignored_files` returns `set()`, everything matching a pattern offered (residue item confirmed still open).

**Tests.** `uv run python -m pytest test/plan-marshall/workflow-integration-git/test_git_workflow.py -o addopts="" -q` → `96 passed` at HEAD.

**Mutations (both restored from saved bytes; `git status --porcelain` re-checked, md5 of the restored file equal to the pre-mutation md5).**
- **Mutation A** — reverted the exclusion test in `scan_artifacts` to the pre-fix exact-string form (`rel.replace(os.sep,'/') in ignored_files`), keeping boundary pruning: **96 passed, 0 failed.** No test detects the removal of the prefix-aware exclusion.
- **Mutation B** — removed the nested-boundary pruning, keeping prefix-aware exclusion: **3 failed, 93 passed** (`test_live_plan_worklog_never_offered_as_safe`, `test_exposure_derivation_nonempty_and_excludes_live_member`, `test_worklog_excluded_independent_of_gitignore`).

**Re-derived counts.** The D4 caller population (`grep -rnE 'detect-artifacts|detect_artifacts|scan_artifacts'` over the tracked tree, `.plan/` excluded) → 8 files at HEAD, of which one (`doc/plans/.../210-…/report-01.md`) post-dates this run and two are this plan's own files; that is the report's 6 files plus the later report and this plan's own `report-01.md`. Test count at the landed commit: 79 `def test_` + 2 `parametrize` decorators, consistent with the reported 95 collected; 80 defs / 96 collected today after two later commits (`60e5fd81`, `d3ba81fd`).

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D1 | GATE: re-establish the defect at HEAD, derive exposure | contract text and behaviour quoted from source; exposure derived with method stated | yes | yes | yes | yes | Contract at `SKILL.md:100`, `SKILL.md:276`, `git-workflow.py:652` docstring — the report's three quotations are **pre-fix** text and were re-checked at `fb41f014^`, not at today's HEAD, where this plan's own doc alignment rewrote the first two (see § Report accuracy). Collapse mechanism independently reproduced: `git ls-files --others --ignored --exclude-standard` in a temp repo returns `.plan/local/worktrees/EXAMPLE-PLAN/` for a linked worktree and enumerates plain ignored files individually. STOP CONDITION correctly not triggered. |
| D2 | Decide direction, record why | one direction chosen, rationale recorded | yes | yes | yes | yes | Direction (a) recorded in `report-01.md` § "Direction chosen (D2)"; implemented as `_split_ignored`/`_is_ignored` (`git-workflow.py:626,641`) plus the doc alignment in `SKILL.md:100,276`. |
| D3 | An active plan's own artifacts are never offered | invariant holds and is pinned by a test | partly | **no** | **no** | **no** | Holds for the nested case (`_is_nested_git_boundary`, `git-workflow.py:613`; mutation B → 3 red). Fails when the plan's own worktree is the scan root, under **three** executed triggers: `--no-gitignore`; default flags with a `.gitignore` lacking `.plan/*`; default flags with the git call failing. Each returns `safe` containing `.plan/local/plans/EXAMPLE-PLAN/logs/work.log`. `SKILL.md:100` states the invariant without any of those conditions. See G1. |
| D4 | Find the callers; latent or active | caller set derived, not sampled, with query stated | yes | yes | yes | yes | Query re-run at HEAD; only behavioural consumer is `SKILL.md:100` ("For safe artifacts, delete them"). `test/conftest.py` and `persona-plan-marshall-agent/standards/argument-naming.md` are documentation mentions. **Latent** confirmed. |
| D5(a) | Gitignored path excluded per the contract | test holds, seen red first | yes (test exists) | **no** | **no** | no | `test_gitignored_worktree_contents_excluded_per_contract` (`test_git_workflow.py:946`) survives mutation A — the exact pre-fix defect its own docstring names — because boundary pruning covers the same fixture. Nothing in the suite exercises the prefix-aware exclusion inside `scan_artifacts`. |
| D5(b) | A live plan's own `logs/work.log` is never in `safe` | test holds, seen red first | yes | yes | yes | **no** | `test_live_plan_worklog_never_offered_as_safe` (`test_git_workflow.py:917`); mutation B turns it and two siblings red with the message `live plan's work.log offered as safe without gitignore: ['nested-wt/logs/work.log', 'scratch.temp']`. Genuinely non-vacuous — but it fixes the scan root at the *outer* repo, so it does not cover the configuration a phase-5+ finalize actually runs in (scan root = the plan's own worktree), where the invariant fails under all three G1 triggers. The test is correct; the deliverable's coverage is incomplete. |
| D5(c) | Exposure derivation asserted non-empty with a known member | test holds, seen red first | yes | yes | yes | yes | `test_exposure_derivation_nonempty_and_excludes_live_member` (`test_git_workflow.py:976`) asserts `result['safe']` truthy **and** `'scratch.temp' in result['safe']` before the negative. Red under mutation B. |

**D3.** `git-workflow.py:697-699` prunes `dirnames` at a nested git boundary, which never applies to the scan root itself. When a phase-5+ task runs `detect-artifacts` from its pinned worktree cwd, `cmd_detect_artifacts` (`git-workflow.py:725`) defaults `root` to `Path.cwd()` — the worktree. In that configuration the only thing keeping the plan's own `.plan/local/plans/{id}/logs/work.log` out of `safe` is `.gitignore`; `--no-gitignore` removes it. Verified by execution, not by reading. The report's D3 row asserts the invariant "holds with `respect_gitignore=False` (pinned by `test_worklog_excluded_independent_of_gitignore`)"; that test (`test_git_workflow.py:1004`) covers only the *nested* worktree, so it does not support the unconditional claim.

**D5(a).** The test's own docstring names the defect it is supposed to catch: *"Red pre-fix: the collapsed `.plan/local/worktrees/EXAMPLE-PLAN/` entry does not exclude its descendants."* Restoring exactly that defect (mutation A) leaves the whole file green. Removing the *other* mechanism instead (mutation B) also leaves this test green — the prefix branch then covers its fixture. Only mutation C, both mechanisms removed, turns it red. So the test is pinned by the disjunction of the two mechanisms: genuinely red against the real pre-fix code, and blind to the loss of either mechanism alone, which is why it cannot attribute its redness to the gitignore mechanism the report credits. Compounding this: my git probes show a trailing-slash entry is emitted *only* for a nested git repository, and every such directory is pruned before the ignore test is reached, so the `ignored_dirs` prefix branch of `_is_ignored` (`git-workflow.py:649`) is currently unreachable from `scan_artifacts`. It is defensible as defence-in-depth, but it is untested and unobservable, and the report presents it as the D2(a) fix that D5(a) verifies.

## Report accuracy

Re-derived at the moment of statement:

- **CONFIRMED, with the anchor corrected** — the three contract-text locations quoted in § "The defect" read as quoted **at `fb41f014^`**, the pre-fix parent, which is the state D1 examined: `SKILL.md:100` there reads exactly *"The script respects `.gitignore` by default — gitignored files are excluded since they cannot be accidentally committed."* At **HEAD** that sentence no longer exists (`grep -c` → 0): this plan's own doc alignment rewrote it, and rewrote `SKILL.md:276` so the quoted sentence survives only as the leading clause of a longer one. Only the `scan_artifacts` docstring quote is verbatim at HEAD. The report's quotations are sound; a reader must not expect to find the first two in today's tree.
- **CONFIRMED** — "plain gitignored files and plain gitignored directories are enumerated individually … a nested git worktree collapses to a single trailing-slash entry." Independently reproduced on git 2.43.0.
- **CONFIRMED** — D4's caller set and the **latent** verdict. Only `SKILL.md` Step 3 acts on the list, and it is agent-followed.
- **CONFIRMED** — "10 new tests", "95 tests pass" at the landed commit (79 test defs + 2 parametrize decorators; 96 collected today after two later unrelated commits).
- **CONTRADICTED** — D3 row: *"this holds with `respect_gitignore=False` (pinned by `test_worklog_excluded_independent_of_gitignore`)."* The cited test covers only a worktree nested below the scan root. Executed `scan_artifacts(<plan worktree>, respect_gitignore=False)` returns the plan's own `.plan/local/plans/{id}/logs/work.log` in `safe`. The invariant is conditional, and the report states it unconditionally.
- **CONTRADICTED (in force, not in wording)** — the D5 table's claim that `test_gitignored_worktree_contents_excluded_per_contract` verifies D5(a). The test does not distinguish the gitignore mechanism from boundary pruning; it survives a revert of the former (mutation A). The literal claim "seen red first" is not contradicted.
- **NOT CONTRADICTED, NOT VERIFIABLE** — the branch commits `104b984`, `ee780ed`, `f09eb37` (squash-merged; absent from `main`), the `15982 passed, 1 skipped` figure, and the reviewer-participation table (a GitHub surface, not a tree fact).
- **Unrelated pre-existing drift found while checking D1** — `get_gitignored_files` (`git-workflow.py:534`) documents itself as *"Uses `git check-ignore`"* while it runs `git ls-files --others --ignored --exclude-standard` (line 539). It predates this plan, whose own re-documentation pass did not correct it. The earlier attribution *"introduced by `87c677bb` (#823)"* is **withdrawn**: `.git/shallow` contains exactly `87c677bb410882e230d83fad44ad7efb9a346caf`, so that commit is this clone's graft root and `git log -S` / `--diff-filter=A` naming it is an artifact of truncated history. See gaps G3.

## Out-of-scope compliance

Clean. The landed diff touches five paths: the plan file (a `git mv`), the run report, `workflow-integration-git/SKILL.md`, `workflow-integration-git/scripts/git-workflow.py`, and `test/plan-marshall/workflow-integration-git/test_git_workflow.py` — exactly the "Expected surface". No retention policy was introduced. The `worktree-remove` timeout was not touched: `cmd_worktree_remove` (`git-workflow.py:1392`) still calls `run_git`, whose default `timeout=60` lives in `git_provider.py:29-33` — the out-of-scope item is intact and untouched, as the report states.

## Residue carried forward

| Report residue item | Status in today's tree |
|---|---|
| Non-git-root secondary exposure: a `--root` outside a git repo excludes nothing | **Still open, and wider than the report's rationale allows — now raised as G5.** Executed `get_gitignored_files(<non-repo tmpdir>)` → `set()`; `scan_artifacts` then offers `a.log` and `sub/b.pyc`. The report deferred this as *"a running plan is always inside a git repo"*, but the same `set()` is returned on **any** git failure — non-zero exit, the 30s timeout, git absent from `PATH` — and that happens inside a repo too: forcing the call to raise in a repo whose `.gitignore` carries `.plan/*` reproduces `safe = ['.plan/local/plans/EXAMPLE-PLAN/logs/work.log', 'scratch2.temp']` with `status: success`. |
| Worktree teardown timeout left out of scope | **Still open, correctly out of scope.** Hardcoded 60s default in `git_provider.run_git`. |
| `/sync-plugin-cache` not owed by a cloud run | Not applicable to the tree; nothing to verify. |
| F2 (stray `.git` file treated as a boundary) — rejected | Still true of the code (`git-workflow.py:623`, the `os.path.exists` test); the rejection rationale is consistent with git's own handling. Not raised as a gap. |
| F3 (`artifact-patterns.json` `_note` silent on the boundary skip) — rejected | Still true (`standards/artifact-patterns.json:2`). The `_note` does describe only the config fields, so the rejection rationale holds on inspection. Not raised as a gap. |

## What could NOT be verified

- The intra-branch commits `104b984`, `ee780ed`, `f09eb37` and the per-commit gate results attributed to them — the PR was squash-merged and the branch is not present in this clone.
- The `./pw verify plan-marshall` figures (`15982 passed, 1 skipped`; quality-gate output) — not re-run; only the single test file was executed.
- The reviewer-participation table, the 1-of-3 coverage disclosure, and the merge-gate condition checks — GitHub state, not tree state.
- The "111,433 entries / 19.9 MB" motivating figure — the plan itself labels it unverifiable from this clone, and the report correctly declines to restate it as its own finding.
- ~~Whether the pre-fix runs were red *for the reasons stated*.~~ **Superseded by the adversarial review.** Mutation C (both mechanisms removed — the actual pre-fix classification) turns all four behavioural tests red, so the report's "seen red first" claim is now re-derived, not merely uncontradicted. What remains false is the *attribution*: mutation A shows D5(a)'s redness cannot be credited to the gitignore mechanism.

## Adversarial review

**Reviewed by:** an independent agent that did not write this document.

**Checked.** Everything below was re-derived from the tree, not read back out of this document.

- **Files opened:** `plan.md`, `report-01.md`; `git-workflow.py` lines 520-760 and 2010-2035; `SKILL.md` lines 90-110 and 265-300 at HEAD **and** at `fb41f014^`; `test_git_workflow.py` lines 72-92 and 900-1060; `standards/artifact-patterns.json`; `.gitignore`; `tools-file-ops/scripts/file_ops.py` (`get_base_dir`, `_resolve_plan_root`, the ADR-002 header comment); `marshall-steward/scripts/gitignore_setup.py`; `git_provider.py:25-40`; `test/conftest.py` (`load_script_module`, `collect_ignore`); `test/plan-marshall/workflow-shared/test_config_validation.py`.
- **Functions executed** (`uv run python`, module loaded through `conftest.load_script_module`): `scan_artifacts` and `get_gitignored_files` against six purpose-built trees — main repo as scan root (ignore on/off); plan worktree as scan root (ignore on/off); plan worktree as scan root in a repo whose `.gitignore` omits `.plan/*`; plan worktree as scan root with the git call forced to raise; and a non-repo temp dir.
- **git probes:** `git ls-files --others --ignored --exclude-standard` (git 2.43.0) over a tree containing an ignored directory with files, an ignored directory containing only directories, a three-level ignored tree, an empty ignored directory, an ignored nested git repository, a non-ignored nested git repository, and a symlink to an ignored directory — a deliberately **broader** probe than the one this document originally ran. Exactly one trailing-slash entry, the nested repository.
- **Mutations** (`git diff --quiet` checked clean before each; restored from bytes saved by this reviewer, md5 equal to the pre-mutation md5, `git status --porcelain` empty after): **A** exact-string exclusion, pruning kept → `96 passed`. **B** pruning removed, prefix exclusion kept → `3 failed, 93 passed`. **C** (new) both removed, i.e. the pre-fix classification → `4 failed, 92 passed`.
- **Counts re-derived:** 80 `def test_` at HEAD, 2 `parametrize` decorators expanding to 9 cases each (`VALID_TYPES` printed from the loaded module: 9 entries; the word list: 9 entries) → 80 − 2 + 18 = 96 collected, matching the observed run. 79 defs at `fb41f014` → 95, matching the report. `git show fb41f014 -- <test file> | grep -c '^+    def test_'` → 10, matching "10 new tests". `git show fb41f014 --stat` → 5 paths, matching the out-of-scope row. D4 population re-swept twice: the original pattern (12 files today; 8 at the verification commit, reconciling exactly to the report's 6 + the 210 report + this plan's `report-01.md`) and a **broader** pattern (`Clean Artifacts|clean-artifacts|artifact-patterns|SAFE_ARTIFACT_PATTERNS|safe_patterns`), which added only `test/plan-marshall/workflow-shared/test_config_validation.py`, a schema test with no behavioural consumption. **Latent** stands.
- **Not re-checked:** the `./pw verify plan-marshall` figures (`15982 passed, 1 skipped`) and the quality-gate output — only `test_git_workflow.py` was run; the intra-branch commits `104b984`, `ee780ed`, `f09eb37` (absent from this shallow clone); every GitHub-surface claim (reviewer-participation table, coverage disclosure, merge-gate conditions); the `111,433 / 19.9 MB` motivating figure; the run's cloud-lane contract compliance (skills loaded, report structure); and `_compile_patterns`/`get_tracked_files`, which this plan did not touch.

| Item | Original claim | Verdict | Evidence |
|---|---|---|---|
| **Verdict** | `implemented-with-gaps` | **upheld** | No deliverable is wholly unimplemented — D3 is implemented for the nested case and D5(a)'s test exists and is red against the true pre-fix code. `partially-implemented` would overstate. |
| **G1** | `--no-gitignore` from a plan's worktree offers the run's own `work.log` | **upheld and widened (title + body rewritten, severity `high` kept)** | Executed: `scan_artifacts(<worktree>, False)` → `safe` contains `.plan/local/plans/EXAMPLE-PLAN/logs/work.log`. Two further triggers found and executed — a `.gitignore` without `.plan/*` (the rule `marshall-steward/scripts/gitignore_setup.py` writes), and a failing git call — both reproduce the same `safe` list **with default flags**. The gap is not a flag hole; the scan-root protection is wholly delegated to gitignore. `SKILL.md:100`'s "never offers" is a shipped false signal → `high` confirmed. |
| **G2** | `test_gitignored_worktree_contents_excluded_per_contract` survives the defect its docstring names | **upheld, rationale corrected, severity `high` kept** | Mutation A reproduced exactly (`96 passed, 0 failed`). But the claim "every live-plan test in the class is pinned by pruning alone" is refuted by mutation B, in which this test does **not** fail. Mutation C shows it goes red only when both mechanisms go. Prefix-branch unreachability re-verified with a broader git probe. `high` holds on the rubric's "a guard that passes against the defect it names". |
| **G3** | `get_gitignored_files` docstring names `git check-ignore`; introduced by `87c677bb` | **upheld as a defect; attribution refuted** | Docstring at line 534 vs command at line 539 confirmed by reading. The attribution is an artifact of a shallow clone: `.git/shallow` = `87c677bb…`, the graft root, so nothing can be shown to predate it. Clause rewritten; severity `medium` kept (wrong command name in the pivotal function, no behavioural effect). |
| **G4** | `--no-gitignore` help over-promises; the boundary skip is unconditional | **upheld, `low` kept** | Executed `scan_artifacts(<main repo>, False)` → `{'safe': ['scratch.temp']}`; no worktree path appears. Both strings read exactly "Include gitignored files in results" (`git-workflow.py:2025`, `SKILL.md:285`). Doc-drift with no wrong behaviour → `low` correct. |
| **G5** | *(new)* `get_gitignored_files` fails open on any git error | **added, `medium`** | Executed with the `git ls-files --ignored` call forced to raise inside a repo ignoring `.plan/*`: `get_gitignored_files` → `set()`, `scan_artifacts(<worktree>, True)` → `safe` contains the live `work.log`, `status: success`. The report's residue rationale ("a running plan is always inside a git repo") does not cover a git failure inside a repo. |
| **D5(b) row** | clean pass on all four columns | **re-scored: Complete? → no** | The test fixes the scan root at the outer repo, so it never exercises the configuration a phase-5+ finalize runs in. Same defect as G1; no new gap raised. |
| **D1 row / "read as quoted"** | three contract quotations verified | **re-anchored** | The first quotation returns `grep -c` → 0 at HEAD; it reads verbatim only at `fb41f014^`. This plan's own doc alignment rewrote it. |
| **"pre-fix redness not verifiable"** | re-derivable only by mutation, attribution unproven | **partly refuted** | Mutation C re-derives it: all four behavioural tests red against the pre-fix classification. Only the attribution remains unproven. |
| Line refs `689-696`, `622` | pruning / boundary test locations | **corrected to `697-699`, `623`** | Read from the file; 689-696 is the walk header and its comment, 622 is the closing docstring quote. |

**Documents corrected.** In `verification.md`: the D1 evidence cell now names `fb41f014^` as the quotation anchor; the D3 cell names all three G1 triggers; the D5(b) cell is re-scored `Complete? no`; the D5(a) paragraph replaces "pins the boundary-pruning mechanism" with the mutation-C disjunction result; two line references corrected; the "read as quoted" and `87c677bb` bullets rewritten; the non-git-root residue row widened and pointed at G5; the "pre-fix redness" non-verifiable bullet superseded. In `gaps.md`: G1 retitled and rewritten around three executed triggers with a three-case Done-when; G2's false inference replaced and its sweep restated from the broader probe; G3's commit attribution withdrawn; G5 added; a `## Refuted during adversarial review` section records the two refuted clauses; open items 4 → 5.

**Residual doubt — for a third reviewer, in order.** (1) Whether `detect-artifacts` is in practice invoked with `--root` pinned to the *main* checkout during finalize; if it always is, G1's blast radius shrinks to `--no-gitignore` alone — this could not be settled from the tree, because `SKILL.md:100`'s Step 3 leaves `--root` optional and no script supplies it. (2) The `./pw verify plan-marshall` figures, still unre-run here. (3) Whether the `ignored_dirs` prefix branch is reachable on git versions other than 2.43.0 — the whole G2 unreachability finding rests on one version's `ls-files` behaviour, and a git that emits `--directory`-style collapsed entries would make the branch live and G2's fix option (ii) wrong.
