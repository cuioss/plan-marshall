# WS-01: Core Token-Optimization (CLOSED)

epic: token-optimization

> Charter document for one workstream — a coherent slice of the epic with its own goal
> and surface. Lives at `workstreams/WS-NN-{slug}.md` and is tracked in the epic
> `status.json` `workstreams[]` field. See
> `persona-marshall-orchestrator/standards/orchestration-model.md` for the tier contract.

## Charter

The original data-driven cost-driver roadmap: drive per-plan token cost to the armed targets by fixing dispatch multiplication and finalize-wait drivers. CLOSED with the plan-8 finale (#899, 2026-07-15) — every cost-driver plan shipped; outcome frozen in `history.md`.

## Scope

- In scope: dispatch topology, lane/posture routing, finalize consolidation, context trim — all SHIPPED.
- Out of scope: the post-roadmap tail (owned by WS-02..WS-06).

## Plans

All shipped — see `history.md` and `.plan/plan-optimization/HANDOVER.md` §3 for the frozen per-plan ledger (design #811 through BK #912).

| Plan | Status | Notes |
|------|--------|-------|
| (shipped ledger) | shipped | Frozen in history.md; no active plans remain in this workstream |

## Sequencing and Surface Notes

- Closed workstream — no sequencing constraints remain. Residual open defects and watches carried into `epic.md`.
