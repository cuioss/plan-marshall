id=2026-07-28-23-002
component=plan-marshall:automatic-review
category=improvement
status=active
created=2026-07-28
bundle=plan-marshall

# A size-keyed bot refusal is unrecoverable by any wait-and-retry strategy

# A size-keyed bot refusal is unrecoverable by any wait-and-retry strategy — a big PR silently loses that reviewer

## Observation

On PR #1041 Sourcery hard-refused because the diff exceeded its **150 000-character** limit. Unlike
a rate limit, this refusal is keyed on **diff size, not on time**: waiting changes nothing, a
new-commits event changes nothing, a trigger comment changes nothing. **Every wait-and-retry recovery
strategy is structurally inapplicable.**

The per-bot registry already models this correctly — each bot declares `awaitable-window` vs
`hard-quota`, and a size refusal classifies as `refused-hard`, so the contract does not pretend a
retry is possible. **The gap is not in the classification; it is that no plan-side practice exists
for the consequence.**

## The consequence

Any plan whose PR exceeds the vendor limit loses that reviewer entirely, and **there is no signal at
planning time that this will happen** — it surfaces only at finalize, after the diff already exists.
On #1041 the loss was tolerable because Sourcery was classified `optional`. Had it been `required`,
the plan would have hit an **unrecoverable gate at the end of a long run**.

## Solution

- Treat a `refused-hard` (size-keyed) outcome as a **terminal coverage gap on that PR**, recorded
  explicitly — never as a transient to retry. Never let a recovery loop spend attempts on it.
- Where a size-limited bot is classified `required`, **the size ceiling becomes a de-facto planning
  constraint**: either decompose the deliverable so the PR stays under the vendor limit, or
  reclassify the bot to `optional` deliberately — rather than discovering the wall at finalize.
- ⛔ Do **not** attempt to work around the limit by splitting the PR after the fact at merge time.
  **The decision belongs at decomposition.**

## Impact

Adds a planning-time constraint that was previously invisible until finalize. The open policy
question it raises — whether a large-diff plan owes an explicit accepted-coverage-gap record when a
size-limited bot refuses, in the same way partial required-bot coverage owes an explicit operator
decision — sits above any single plan and is recorded as an epic decision rather than folded into
one.
