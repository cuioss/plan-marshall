id=2026-08-25-09-004
component=plan-marshall:phase-6-finalize
category=improvement
status=active
created=2026-08-25

# Populate the four context-load columns and run enrich at the metrics close

# Populate the four context-load columns and run enrich at the metrics close

## Context

This plan has no four-field cost attribution from either of the two independent sources that exist to provide it — on a plan whose own subject is budget attribution.

- Per-dispatch: all 19 dispatch-boundary rows across 4-plan, 5-execute and 6-finalize carry `input_tokens`, `output_tokens`, `cache_read_input_tokens` and `cache_creation_input_tokens` as the literal `unmeasured`. `context_position_cost.measured_rows` is `0/19`; `position_multiple` is `unmeasured` for the whole plan.
- Per-phase: `manage-metrics enrich` was never invoked, so `totals_billing_weighted_total` is `0` with `totals_billing_weighted_total_population_count: 0`, and every phase row carries `inline_main_context_tokens: unmeasured`.

The `Billing (cost)` column of `metrics.md` is therefore empty for all six phases, and the plan's 5.99M dispatched tokens have no cost figure beside them.

Both channels already exist and are declared. `record-dispatch-boundary` declares all four flags. `enrich` is a documented verb. Neither is a missing capability; both are uncalled.

## Root cause

The four context-load flags are optional on `record-dispatch-boundary` and no dispatcher call site passes them — correctly writing `unmeasured` rather than a false `0`, which is why the gap is visible at all rather than silently reading as zero cost. `enrich` has no scheduled call site in the finalize step sequence; it is left to a caller who never runs.

## Proposed action

1. Pass the four `message.usage` values at every `record-dispatch-boundary` call site — the dispatcher already holds the returning agent's usage envelope when it stamps the row.
2. Invoke `manage-metrics enrich --plan-id P --session-id S` as part of the finalize metrics close, before `record-metrics` performs the authoritative phase close.

Both are call-site changes over existing verbs; neither needs new script surface.

## Evidence

- aspect: log_analysis — `context_position_cost: total_rows 19, measured_rows 0, unmeasured_rows 19`; every row lists all four columns under `unmeasured_columns`
- aspect: plan_efficiency — `totals_billing_weighted_total: 0`, `population_count: 0`; `Billing (cost)` column empty for all 6 phases
- aspect: logging_gap_analysis — both channels reported as one combined gap

## Recurrence — 2026-09-03 (plan-150-close-the-namespace-conversion, PR #1383)

Both halves reproduced, and the plan filed them as two separate observations that fold here
because this lesson already names both.

Per-dispatch: 13 dispatch-boundary rows across `4-plan` (1), `5-execute` (4) and `6-finalize`
(8). All 13 carry all four columns as `unmeasured` — `measured_rows: 0`,
`unmeasured_rows: 13`, `position_multiple: unmeasured`, `position_multiple_basis: unmeasured`.

Per-phase: `enrich` again never ran. `totals_billing_weighted_total: 0` with
`population_count: 0`; the `Billing (cost)` column renders `-` on every phase row and on the
Total. The sharper consequence this time is that `1-init` carries **no token and no worked
figure at all** — 32m1s of wall recorded as 100% idle — because it dispatched nothing and
nothing folded its inline spend in. Every column Total in `metrics.md` is marked `(n=5/6)`
and is a floor rather than a total.

That floor propagated into a budget verdict: the plan-efficiency aspect fired its `[BUDGET]`
error findings on 3,952,297 tokens, a figure excluding a whole phase, while the landing's own
`total_tokens` is 4,376,049. The verdicts hold a fortiori — the real number is higher — but
they were reached on an incomplete measurement, which is luck rather than design.

The plan's own root-cause note adds a detail this lesson did not carry: the retrospective's
Step 2.5 reconcile calls `generate`, which backfills accumulators but explicitly does **not**
perform the transcript walks `enrich` owns — so a caller who assumes reconcile covers it is
wrong. Separately, "`enrich` never ran" as reported by the retrospective is partly an
ORDERING ARTIFACT: `enrich` is executed by `record-metrics` at order 996, after the
retrospective measures at 995, so it structurally cannot have run at measurement time. It
subsequently ran clean (641 messages, 6 four-field phases attributed). The per-dispatch half
has no such excuse and is unqualified.

Folded here by the orchestrator (`test-quality` epic) from inbox messages
`plan-150-close-the-namespace-conversion-002.md` and `-003.md` rather than raised as two new
lessons.

## Recurrence — 2026-09-04 (plan-145-publish-the-missing-parser-seams, PR #1395)

Both halves reproduced again, unchanged in shape and larger in population.

Per-dispatch: **24 of 24** dispatch-boundary rows across `4-plan` (1),
`5-execute` (3) and `6-finalize` (20) carry all four columns as `unmeasured`.
`measured_rows: 0`, `unmeasured_rows: 24`, `position_multiple: unmeasured`,
`position_multiple_basis: unmeasured`. Still no dispatcher call site passes the
flags.

Per-phase: `totals_billing_weighted_total: 0` with `population_count: 0`; the
`Billing (cost)` column renders `-` on all six phase rows and on the Total. So a
plan that spent 5,361,229 tokens has no cost figure beside it at all.

### A third cause this lesson did not yet carry

The previous recurrence correctly distinguished the *ordering artifact* (enrich
runs at order 996, after the retrospective measures at 995) from the
unqualified per-dispatch half. This run adds a **third**, independent reason
`enrich` could not have produced a useful result even at order 996:

`status.metadata.session_ids` held only the phase-1..5 session for the whole of
finalize. The finalize session was appended by `platform_runtime session
capture` at **plan-retrospective Step 1** — after every other finalize step had
run. So an `enrich --session-id S` fired anywhere in finalize would have walked
a *different session's* transcript, or none.

That means fix (2) above ("invoke enrich as part of the finalize metrics
close") is **necessary but not sufficient** on a plan that changes sessions
mid-finalize: the session id must be captured at finalize entry first. Filed
separately as lesson `2026-09-04-14-006`, and named here because it is a
precondition of this lesson's own remedy.

## Evidence (recurrence)

- aspect: log_analysis — `context_position_cost: total_rows 24, measured_rows 0, unmeasured_rows 24`; every row lists all four columns under `unmeasured_columns`
- aspect: plan_efficiency — `totals_billing_weighted_total: 0`, `totals_billing_weighted_total_population_count: 0`
- aspect: logging_gap_analysis — reported as two gaps plus the session-capture precondition
- Precondition: lesson `2026-09-04-14-006`

## Recurrence — 2026-09-06 (every-test-runs-and-the-suite-does-not-slow-down, PR #1426)

Sixth observation. Both halves reproduced, and this run **removes the excuse the previous
recurrence offered for one of them**.

Per-dispatch: **24 of 24** dispatch-boundary rows across `4-plan` (1), `5-execute` (10) and
`6-finalize` (13) carry all four context-load columns as `unmeasured`. `measured_rows: 0`,
`unmeasured_rows: 24`, `position_multiple: unmeasured`, `position_multiple_basis:
unmeasured`. Still no dispatcher call site passes the flags. Unqualified, as before.

Per-phase: `totals_billing_weighted_total: 0` with `population_count: 0`; the `Billing
(cost)` column renders `-` on all six phase rows and on the Total. A plan that spent
6,473,575 dispatched tokens — the largest in this lesson's five-plan series — carries no cost
figure at all. `1-init` again has neither a token nor a worked figure, so every column Total
in `metrics.md` reads `(n=5/6)` and is a floor.

**The session-id precondition did not apply here, and enrich still had not run.** The
2026-09-04 recurrence added a third cause: `status.metadata.session_ids` held only the
phase-1..5 session, so an `enrich` fired during finalize would have walked the wrong
transcript. This plan ran end to end in a **single** session
(`session_ids[1]: fa002ce0-…`, and the retrospective's own `platform_runtime session capture`
returned that same id), so `enrich --session-id S` would have resolved correctly from any
point in finalize. It was still never called before the retrospective measured. Lesson
`2026-09-04-14-006` therefore remains worth fixing, but it is **not** what is blocking this
one — fix (2) is now unblocked on a single-session plan and simply has no call site.

One correction to the 2026-09-03 recurrence's ordering note, from this plan's live manifest:
`record-metrics` is at order **998**, not 996, and `plan-marshall:plan-retrospective` at
**995**. The retrospective's Step 2.5 reconcile calls `generate`, which backfills the
accumulators but performs none of the transcript walks `enrich` owns — so the ordering
artifact is three steps wide, not one, and the plan-efficiency aspect reads a metrics.md that
structurally cannot yet carry a Billing column.

### Evidence (2026-09-06 recurrence)

- aspect: log_analysis — `context_position_cost: total_rows 24, measured_rows 0, unmeasured_rows 24`; every row lists all four columns under `unmeasured_columns`
- aspect: plan_efficiency — `totals_billing_weighted_total: 0`, `totals_billing_weighted_total_population_count: 0`, `totals_tokens_population_count: 5` of 6
- `manage-execution-manifest read` — `phase_6.steps` orders `plan-marshall:plan-retrospective` before `record-metrics`
- `status.metadata.session_ids` holds exactly one id, so the `2026-09-04-14-006` precondition is satisfied on this plan
