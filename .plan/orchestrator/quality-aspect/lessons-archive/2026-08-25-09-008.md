id=2026-08-25-09-008
component=project:finalize-step-deploy-target
category=bug
status=active
created=2026-08-25

# Resolve the generator interpreter instead of hard-coding uv run python

# Resolve the generator interpreter instead of hard-coding uv run python

## Context

`project:finalize-step-deploy-target` documents its invocation as `uv run python marketplace/targets/generate.py`. On this machine `uv` is not on `PATH` and is not present under `/home/oliver` at all, so the documented invocation failed.

The step completed only because the executing agent substituted an interpreter by hand and recorded the substitution (decision `f79302`):

> Documented invocation 'uv run python marketplace/targets/generate.py' failed — uv is not on PATH in this environment (not found under /home/oliver either). Ran the generator with the project's own venv interpreter (.venv/bin/python), the same runtime ./pw provisions, rather than skipping the step. Output: 1174 entries, v0.1.1545 stamped into 11 bundle plugin.json, dist-manifest.json emitted. The skill doc's hard-coded 'uv run' prefix is an environment assumption worth revisiting.

The recovery was correct and the outcome was right. What is fragile is that the step's success depended on an agent choosing to reason about the failure rather than reporting the step blocked — and the two available wrong answers (skip the step; report it failed) would both have left the marketplace target un-regenerated after a bundle-editing plan.

## Root cause

The step hard-codes a launcher (`uv`) that the project does not require and this machine does not have, rather than resolving the interpreter the project actually provisions. `./pw` provisions `.venv`, which is the runtime the generator ran under successfully.

This is the same class as the repository's own \"Build commands: resolve via architecture\" hard rule, applied to a project-local finalize step: a hard-coded tool prefix is an environment assumption.

## Proposed action

Resolve the interpreter rather than naming a launcher — prefer the project venv interpreter that `./pw` provisions, falling back to `uv run python` where `uv` is present. Update the skill body so the documented invocation is the one that works on a machine with no `uv`.

## Evidence

- decision `f79302` — the failure, the substitution, the successful output, and the recommendation, all recorded at the moment it happened
- aspect: script_failure_analysis — the step's own record-step row shows `outcome=executed`, so the failure is invisible to the script-failure sweep; only the decision log carries it
- `project:finalize-step-deploy-target` completed `1174 entries emitted to target/claude/, v0.1.1545 stamped into 11 plugin.json`

## Recurrence — 2026-09-03 (plan-170-make-the-attribution-usable)

### Context

Recurred verbatim, and this run located `uv` precisely: it is **not absent from the machine**, it is simply not on `PATH` — it exists only under `.pyprojectx/uv-0.9.16/uv`. The step was resolved by invoking that pyprojectx-vendored binary directly, and completed with `1181 files emitted to target/claude/`.

That the same failure produced the same manual recovery on a second, unrelated plan confirms the step is unrunnable as written on any machine without a global `uv`, and that each run pays a fresh diagnosis for it.

### Two further doc defects in the same SKILL.md

Both found while working around the primary failure, and neither is covered by the original lesson:

1. **The `mark-step-done` notation is written with an underscore** — `plan-marshall:manage-status:manage_status` — which the executor rejects. The registered notation is `manage-status`. The documented command cannot be copied and run.
2. **The Parse-the-result table describes an output the generator does not produce.** It tells the caller to read a TOON document with a `status` field; the generator emits human-readable lines. A caller following the doc has nothing to parse.

### Root cause (unchanged, plus)

The primary defect is the hard-coded launcher, as originally recorded. The two doc defects share a cause with it: the step's documented surface has drifted from its shipped behaviour on three independent points, which suggests the block was never executed end-to-end from the doc.

### Proposed action (extends the original)

Alongside resolving the interpreter, **prefer the pyprojectx-vendored `uv`** (`.pyprojectx/uv-*/uv`) or route through `./pw`, since that path demonstrably exists on a machine where the global one does not. Fix the underscore notation to `plan-marshall:manage-status:manage-status`, and correct the Parse-the-result table to describe the generator's actual line-oriented output — or give the generator a TOON envelope so the table becomes true.

### Evidence

- decision.log `f2f515` (2026-09-03T01:53:28Z) — \"finalize-step-deploy-target's documented command 'uv run python marketplace/targets/generate.py' FAILED with 'uv: Kommando nicht gefunden' - uv is not on PATH in this environment. It exists only under .pyprojectx/uv-0.9.16/uv, so the step is unrunnable as written unless the caller happens to have uv installed globally ... Two smaller doc defects in the same file: the mark-step-done notation is written as 'plan-marshall:manage-status:manage_status' with an underscore, which the executor rejects ... and the generator emits human-readable lines rather than the TOON document with a status field that the Parse-the-result table tells the caller to read.\"
- `status.metadata.phase_steps[\"6-finalize\"][\"project:finalize-step-deploy-target\"]` — `outcome: done`, `display_detail: \"1181 files emitted to target/claude/\"`. As in the first observation, the step's own record shows success and the failure is visible only in the decision log.
- project:finalize-step-lessons-housekeeping retained this lesson on both firings of this plan as \"component surface untouched by this plan\" — correctly, since plan-170 changed neither the step nor the generator.

## Recurrence — 2026-09-03 (user-language-and-vocabulary, PR #1382)

Reproduced at deploy-target. decision.log `2026-09-03T05:39:11Z`: "`uv` is not on PATH in this shell (exit 127), so the documented `uv run python marketplace/targets/generate.py` was run as `.venv/bin/python3 marketplace/targets/generate.py` with identical arguments." The step still depends on an agent noticing the failure and substituting the launcher.

Filed by the plan as a batched recurrence observation and folded here by the orchestrator (`operator-ux` epic) rather than raised as a new lesson. Retained by `finalize-step-lessons-housekeeping` on 2026-09-02 as "untouched by this plan. No coverage." — correct at the time, and it reproduced during the same run.
