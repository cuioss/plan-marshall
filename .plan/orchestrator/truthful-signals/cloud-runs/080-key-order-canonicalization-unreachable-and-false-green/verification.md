# Verification — 080-key-order-canonicalization-unreachable-and-false-green

**Verified against:** commit `a884110e` (the plan-touched files are byte-identical to `ac06e4fc`, the HEAD at the start of this check; the intervening commits are sibling verification-doc commits only)   **Landed as:** PR #1156, commit `10de4d12`   **Verdict:** implemented-with-gaps

## Method

- Read `plan.md` and `report-01.md` in full; extracted all ten deliverables, their *Done when* conditions, the Out-of-scope list, the Expected surface, the twelve-row claim table, and the Verification section.
- Located the landing: `git log --oneline --all --grep '#1156'` → `10de4d12` (`fix(truthful-signals/080): honest config-write signals, reachable canonicalizer, truthful freshness scope`). Read the full diffstat and the per-file diffs for `manage-config.py`, `marshall-steward/SKILL.md`, `test_manage_config_cli.py`.
- Opened at HEAD: `_config_core.py` (`order_config_keys`, `save_config`, `load_config`, `_CONFIG_FINGERPRINTS`, `ConcurrentConfigModificationError`, `unrecognized_top_level_keys`, `normalize_keys`, `ext_defaults_set`, `ext_defaults_set_default`), `manage-config.py` (dispatch + `main()` tail), `_cmd_sync_defaults.py` (`added_count`), `_cmd_init.py`, `_providers_core.py` (`_save_marshal`, `write_provider_config`), `opencode_runtime.py`, `_claude_runtime_impl.py` (`project_initial_setup`), `claude_runtime.py` (`_write_json`), `platform_runtime.py` (`_resolve_target`), `cache_freshness.py` (whole file), `upgrade.py` (`_STAGE_SPECS`), `upgrade-flow.md` (freshness sub-step, Stage 2), `marshall-steward/SKILL.md` (upgrade-verb bypass, Re-Run Remediation Pass a–f), `manage-config/SKILL.md` (verb table row + Canonical invocations), `manage-contract.md` (error-response contract), `doc/user/installation.adoc`, `README.md`.
- Re-derived the contested write-site count from scratch by enumerating every writer of `.plan/marshal.json` across `marketplace/bundles/**` (grep for marshal-path holders, then for `write_text` / `json.dump` / `os.replace` in each).
- Verified the parent-commit citations in the report by reading `10de4d12^` for `_config_core.py` (`normalize_keys`, `order_config_keys`, `save_config`), `cache_freshness.py` (`REMEDIATION`), and `test_sync_defaults.py` (`added_count` line numbers).
- Ran tests: `uv run python -m pytest test/plan-marshall/manage-config/test_config_write_guard.py test/plan-marshall/marshall-steward/test_upgrade_flow_stage2.py test/plan-marshall/marshall-steward/test_cache_freshness.py -o addopts="" -q` → **23 passed**.
- **Mutation 1 (D2 + D4).** Saved `_config_core.py` bytes to the scratchpad, then (a) forced `recorded = None` in `save_config`, and (b) replaced `normalize_keys`' return with the pre-fix constant `{'status': 'success', 'action': 'normalized', 'unrecognized_keys': []}`. Result: `test_save_config_refuses_a_concurrent_overwrite` **FAILED** ("DID NOT RAISE ConcurrentConfigModificationError") and `test_normalize_keys_names_unrecognized_top_level_key` **FAILED**; 6 others still passed. Restored by writing back the saved bytes (md5 re-checked identical).
- **Mutation 2 (D9 red-first re-demonstration).** Saved `cache_freshness.py` bytes, reverted `REMEDIATION` to the exact pre-fix string from `10de4d12^`, ran `-k remediation` → `test_remediation_names_the_commands_literally` **FAILED** on `assert '/plugin update plan-marshall' in cache_freshness.REMEDIATION`, reproducing the report's red-first evidence verbatim. Restored from the saved bytes.
- **Execution probe (not a mutation).** Ran a scratchpad script that points `_config_core.MARSHAL_PATH` at a tmp file, wraps `load_config` so a concurrent writer commits between load and save, and calls the real handler `_cmd_orchestrator.cmd_orchestrator_set`. Result: `UNCAUGHT: ConcurrentConfigModificationError` — the exception escapes the `cmd_*` handler (see G1).
- Working tree: my two mutated files were restored byte-for-byte and `git diff` for them is empty. Other files show as modified in `git status` throughout — those are concurrent edits by other sessions in this shared clone (the set changed between two consecutive `git status` calls, and none of them is a file I opened for writing). I modified no repository file except the two documents below.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D1 | GATE — honest-signal shape | three verdicts recorded with reasons | yes | yes | yes | yes | `report-01.md` § Gate verdicts (a) warning+`unrecognized_keys`, (b) warning not error, (c) LAST — each with a reason |
| D2 | `normalize-keys` names what it could not order | caller cannot read `normalized` and conclude canonical | yes | yes | yes | yes | `_config_core.py:821` `unrecognized_top_level_keys`, `:834-864` `normalize_keys`; `manage-config.py:915-921` surfaces the verb's own status; mutation 1(b) turns the D10a test RED |
| D3 | `sync-defaults` reports effect not intent | no-op reports zero + test pins the inversion | pre-existing | yes (recorded as refuted) | yes | yes | `_cmd_sync_defaults.py:486` `added_count: len(added)`; `test_sync_defaults.py:310` `== 0` on no-op, `:321` `== len(added)` (report cited `:296`/`:307`, correct at `10de4d12`) |
| D4 | Guard the whole-document RMW | two concurrent writes cannot silently drop one side, demonstrated by a test | yes | yes | partially — see below | **no** — see G6 (and G1 at reduced severity) | `_config_core.py:64-105,189-225`; `test_config_write_guard.py::test_save_config_refuses_a_concurrent_overwrite` (RED under mutation 1(a)) |
| D5 | `upgrade` Stage 2 runs the canonicalizer | flow can produce canonical order; doc **and** any emitted `sub_steps` in lock-step | yes | yes | yes | **no** — see G3 | `upgrade-flow.md:302-332` (three verbs, `normalize-keys` last); `upgrade.py:105`, Stage-2 spec at `:117-124`, `_STAGE_SPECS` emits only the coarse `reconcile-marshal-json` (plus `migrate-bot-lists`) — the code/doc-divergence check is correct |
| D6 | GATE — what `fresh` may mean | verdict recorded | yes | yes | yes | yes | `report-01.md` § D6 — declare-it-cannot, `fresh` is clone-scoped |
| D7 | Implement D6 | a behind clone can no longer produce a bare `fresh` | yes (declare-path) | yes | yes, within D6's verdict | mostly — see G7 | `cache_freshness.py:96-103` `COMPARED_AGAINST`, stamped in `_unknown()` (`:147`) and the terminal verdict (`:202`) |
| D8 | Retire the vacuous ownership claim | the doc describes only behaviour that exists | yes | yes | yes | yes | `upgrade-flow.md:158-168` — "owns THAT question — is the cache current with the local marketplace clone?"; "Neither sub-step has an upstream leg" |
| D9 | Correct the remediation text + the test | text right, test right, report records red-first | yes | yes | text unverified — see G5 | yes for the code/test pair | `cache_freshness.py:86-90` `REMEDIATION`; `test_cache_freshness.py:255-268`; mutation 2 reproduced the RED |
| D10 | Tests (a)–(e) | all five hold | (a)(b)(c)(e) yes; (d) substituted | (d) not as literally specified | yes | yes for what was shipped | `test_config_write_guard.py` (a,b,e), `test_upgrade_flow_stage2.py` (c), `test_cache_freshness.py::test_every_verdict_declares_its_local_clone_comparison_scope` (d-substitute); 23 passed |

**D4 (`_config_core.py:189` `save_config`).** The guard is genuine: `load_config` records a sha256 of the raw file keyed by path, `save_config` re-reads the file at save time and raises `ConcurrentConfigModificationError` on mismatch, and the write is atomic via a uniquely-named temp + `os.replace`. Mutation 1(a) proves the test is not vacuous. Two things are nevertheless incomplete. First, no caller anywhere handles the new exception type by name (`grep 'except ConcurrentConfigModificationError'` across `marketplace/` and `test/` → zero hits) while **36** `save_config` call sites exist across **14 files** (35 outside `_config_core` itself, in 13 modules); only the `normalize-keys` dispatch arm is wrapped, and my execution probe showed the exception escaping `cmd_orchestrator_set`. ⚠ The adversarial review below refuted the conclusion originally drawn from that — `main()` carries `@safe_main`, so the CLI still emits parseable TOON; see the rewritten G1 and the genuine no-TOON instance at G8. Second, `manage-providers`' `write_provider_config` → `_save_marshal` (`_providers_core.py:212-239`, `:91-111`) is itself a whole-document read-modify-write that is unguarded *and* non-atomic; it is a **routed** writer, so the report's residue — which enumerates only the three *bypass* writers as remaining unguarded — silently omits it (G6).

**D5 (`marshall-steward/SKILL.md:490` Re-Run Remediation Pass).** Stage 2 in `upgrade-flow.md` now runs `normalize-keys` last and explains at `:322-329` that the position is load-bearing. The Re-Run Remediation Pass, which the plan's Expected surface names as a file that "must stay consistent with the new Stage 2 wiring", still runs `normalize-keys` as step **(a)** — before `sync-defaults` (e) and `steps-sort` (f). The run edited step (a)'s prose (to surface `unrecognized_keys`) but not its position. The effect is benign — `sync-defaults`/`steps-sort` persist through `save_config`, which canonicalizes — but the two documents now give opposite accounts of whether the position matters, and the D10(c) test pins the order only inside Stage 2. See G3.

**D7 (`cache_freshness.py:155` `check_freshness`).** The plan's Goal allowed "compares against upstream **or** declares that it cannot", and D6 chose the declare path, so shipping a constant `compared_against: local_clone_manifest` on every verdict is inside the plan's own allowance. Two caveats, neither a defect in itself: the field is a module constant, identical on `fresh`, `stale` and `unknown`, so it carries no per-run information — it is a scope label, not a computed signal; and the upgrade-flow's parse instruction (`upgrade-flow.md:139`, "Parse `freshness`, `refuses_upgrade`, and `remediation`") does not list it, so the one documented consumer is not told to read the new disclosure (G7).

**D10(b) byte-stability (`test_config_write_guard.py:104`).** The test canonicalizes once, snapshots the bytes, then re-runs and compares. That pins idempotence of `normalize_keys` against its own output — it does not pin that a hand-authored already-canonical file with different whitespace survives untouched (it would not: `normalize_keys` always re-serializes through `json.dumps(indent=2)`). Given the verb is documented as "reuses the `save_config` writer", this is consistent with what was promised; noted rather than raised as a gap.

**D10(d) "the upstream-skew path".** No upstream leg exists after D6's verdict, so the shipped test asserts scope disclosure on all three verdicts instead. That is the honest substitute, but the plan's literal (d) is not satisfied and cannot be while D7 takes the declare path. Recorded here rather than as a gap.

## Report accuracy

Checked every claim-table row, every deliverable row, the build-gate figures, the Findings, and the Residue. Contradictions found:

1. **The contested write-site count is under-derived.** The report states "5 marshal.json write sites … Two routed, **three bypass**", and the corrected `order_config_keys` docstring enumerates the bypass writers as `ext_defaults_set`, `ext_defaults_set_default`, and "the OpenCode runtime seed". Re-derived at HEAD there are **six** sites, **two routed / four bypass**: the fourth bypass is `_claude_runtime_impl.py:44` `ClaudeRuntime.project_initial_setup`, which writes `.plan/marshal.json` through `claude_runtime._write_json` (`claude_runtime.py:1205`) with top-level `runtime` and `project_dir` keys and no ordering. That site predates the plan (`55cf61f1`, 2026-07-05, an ancestor of `10de4d12`), so it was present and missable at re-derivation time. Because the docstring presents its list as the exhaustive set of paths `order_config_keys` does not reach, the omission is a stale statement in production code, not merely an arithmetic slip. See G2.
2. **The residue's enumeration of unguarded writers is incomplete.** It names the three bypass writers as what "remain unguarded whole-document writes". `manage-providers`' `write_provider_config` → `_save_marshal` is equally an unguarded whole-document read-modify-write (and non-atomic), and is not named anywhere in the report. See G6.
3. **The `/plugin update plan-marshall` row overstates what was confirmed.** The plan's claim table required "confirm before shipping it as guidance". The report's evidence column offers "validated by the Step-6 cold-read sub-agent" — but a cold read tests whether a reader follows the wording, not whether the command exists or is the right recovery in this repository. The repository's own user documentation prescribes a different procedure (`doc/user/installation.adoc:57-64`, `README.md:46`: `/plugin marketplace update plan-marshall`, reinstall, then restart or `/reload-plugins`). See G5.

Immaterial: the report cites `_config_core.py:717-731` for the pre-fix `normalize_keys`; at `10de4d12^` the function spans `715-729` (a two-line offset). All other line citations I re-checked at the parent commit were exact: `order_config_keys` at `:96`, `save_config` at `:117`, `REMEDIATION` at `cache_freshness.py:76-79`, `test_sync_defaults.py:296`/`:307`, `upgrade.py` `_STAGE_SPECS` emitting only `reconcile-marshal-json`.

Not contradicted: the D3 refutation (verified in code and in two existing assertions), the `upgrade` bypasses-the-menu claim (`marshall-steward/SKILL.md:148-150`), the code/doc-grain finding for Stage 2, the test-pins-the-defect finding (mutation 2 reproduced the exact red), and the D9 test correction. The build-gate figures (`verify: SUCCESS`, 15887 passed / 1 skipped) are historical and were not re-run.

## Out-of-scope compliance

Compliant. The landed diff touches 11 files plus the plan move, all inside the Expected surface except `manage-config/scripts/manage-config.py` (the dispatch arm for the verb whose return shape changed) — a necessary lock-step edit, not scope creep. `CANONICAL_TOP_LEVEL_KEY_ORDER` itself was not redesigned. No general locking primitive was introduced — the guard is a per-path content fingerprint confined to `save_config`. Nothing in the executor-preflight / version-resolution surface was touched; `upgrade.py` was correctly left unchanged. No undeclared collateral change appears in `git show --stat 10de4d12`.

## Residue carried forward

| Report residue item | Still open at HEAD? |
|---|---|
| Three `order_config_keys` bypass writers remain unguarded (`ext_defaults_set`, `ext_defaults_set_default`, `opencode_runtime._save`) | **Open** — all three unchanged; and there is a fourth (the Claude runtime seed) the residue never named (G2) |
| In-process concurrent-writer serialization not provided | **Open**, and documented at `_config_core.py:80-98` as stated |
| Upstream-currency question structurally out of scope | **Open** — no upstream leg; `compared_against` is the only disclosure |

New residue this verification adds: G1 (concurrent-modification reported in the crash tier), G3 (Stage 2's position rationale is false and the Re-Run Pass contradicts it), G4 (first-party keys reported as unrecognized), G5 (unverified remediation command), G6 (unguarded routed provider writer), G7 (disclosure field absent from the documented parse list), G8 (`upgrade.py` emits no TOON on an exception), G9 (the Claude project seed destroys an existing marshal.json).

## What could NOT be verified

- Whether `/plugin update plan-marshall` exists and is non-destructive in Claude Code. Nothing in this repository substantiates it, and the repository's own installation documentation prescribes a different update sequence. This is the plan's own explicitly-unverified row.
- The build-gate figures (`./pw verify plan-marshall` → 15887 passed / 1 skipped) and the per-commit `quality-gate` cleanliness — historical run artifacts, not re-run here.
- The Step-6 cold-read sub-agent result and the pre-PR verification sub-agent's verdict — one-shot run artifacts.
- Reviewer participation (`cuioss-review-bot` reviewed; the two rate-limited bots) — no GitHub API calls were made in this check.
- Whether the generated executor converts an uncaught script exception into a structured error for the LLM caller. `.plan/execute-script.py` is git-ignored and absent from this clone; the template (`tools-script-executor/templates/execute-script.py.template:1525-1545`) streams the child's stderr and classifies a non-zero exit as `script_internal_failure`, but the generated file itself was not inspected. ⚠ For `manage-config` the question turned out not to arise — `@safe_main` puts a TOON payload on stdout before the exit-1; it does arise for `upgrade.py`, which has no such wrapper (G8).
- Whether `/plugin update <name>` exists in the Claude Code plugin CLI. Neither the original check nor this adversarial review could settle it from inside the repository; G5 keeps it as the open action.

## Adversarial review

**Reviewed by:** an independent agent that did not write this document.

**Checked.** All seven original gaps. Every mechanism clause was re-derived at the named file and symbol, and every clause asserting a return value or an observable outcome was **executed** rather than read:

- **Files opened at HEAD** (`9afba956`; `git diff a884110e HEAD` over the plan-touched skill trees and `test/plan-marshall` is empty, so the original document's provenance statement still holds): `_config_core.py` (`ConcurrentConfigModificationError`, `_CONFIG_FINGERPRINTS`, `load_config`, `CANONICAL_TOP_LEVEL_KEY_ORDER`, `order_config_keys`, `save_config`, `unrecognized_top_level_keys`, `normalize_keys`), `manage-config.py` (imports + the whole `main()` dispatch, `:169`–`:998`), `_cmd_init.py`, `_cmd_orchestrator.py`, `_cmd_sync_defaults.py`, `_cmd_steps_sort.py`, `_providers_core.py` (`_marshal_path`, `_save_marshal`, `write_provider_config`, `create_credential_file`), `file_ops.py` (`safe_main`, `output_toon_error`), `_claude_runtime_impl.py`, `claude_runtime.py` (`_write_json`), `opencode_runtime.py`, `platform_runtime.py` (`_resolve_target`), `cache_freshness.py`, `upgrade.py` (`_STAGE_SPECS`, `cmd_migrate_bot_lists`, `main`), `upgrade-flow.md`, `marshall-steward/SKILL.md`, `manage-config/SKILL.md`, `manage-contract.md`, `output-contract.md`, `platform-runtime/standards/contract.md`, `doc/user/installation.adoc`, `README.md`, `test_config_write_guard.py`, `test_upgrade_flow_stage2.py`, `test_cache_freshness.py`, `test_sync_defaults.py`, `test_claude_runtime.py`.
- **Sweeps re-run with broader patterns.** The write-site enumeration was re-derived from scratch with a script that scans every `*.py` under `marketplace/bundles/` naming a marshal path and reports every `write_text` / `json.dump` / `_write_json` / `os.replace` / `.write(` line — six sites, two routed, four bypass. `save_config(` was counted directly: 36 matches across 14 files. `except ConcurrentConfigModificationError` was re-run and confirmed at zero — **and then widened** to the decorator layer, which is where the handling actually is. `/plugin update|uninstall|install|reload-plugins` was swept across `marketplace/`, `doc/` and `.claude/`.
- **Functions executed** (four scratchpad probes, `python3` with the full cross-skill `sys.path` the executor builds): `_cmd_orchestrator.cmd_orchestrator_set` under a forced load→concurrent-write→save race; `manage-config.py:main()` under the same race with stdout/stderr/exit captured; `upgrade.main(['migrate-bot-lists'])` under the same race; `unrecognized_top_level_keys` / `normalize_keys` on a config carrying `runtime` + `project_dir`; `load_config` → add a key → `save_config` to settle whether position affects ordering; and `ClaudeRuntime.project_initial_setup` vs `OpenCodeRuntime.project_initial_setup` against an identical pre-populated `marshal.json`.
- **Tests re-run:** `test_config_write_guard.py test_upgrade_flow_stage2.py test_cache_freshness.py -o addopts="" -q` → **23 passed**, re-deriving the original figure.
- **Git re-derivations:** `git show --stat 10de4d12` (11 files + the plan move — matches); `10de4d12^:cache_freshness.py` (pre-fix `REMEDIATION` at `:76-79`, confirming the D9 red-first citation and revealing what the replacement displaced).
- **No repository file was mutated.** Every check above is read-only or runs against a `tempfile` tree; the two mutation experiments the original document performed were not repeated, and their conclusions are accepted on the strength of the code read (mutation 1 turns tests red on a constant return / a disabled fingerprint — both are direct; mutation 2's red-first is corroborated by the parent-commit string).

**Not re-checked.** The build-gate figures (`./pw verify plan-marshall` → 15887 passed / 1 skipped) and the per-commit `quality-gate` runs — historical artifacts, not re-run. The Step-6 cold-read and pre-PR verification sub-agent results — one-shot artifacts. Reviewer participation and any GitHub API state — no API calls were made. The two original mutation experiments — not repeated (see above). Whether `/plugin update <name>` exists in the Claude Code CLI — unresolvable from inside this repository, and it remains G5's open action. The D10(b) byte-stability nuance and the D10(d) substitution were re-read and left as recorded, not re-litigated.

| Item | Original claim | Verdict | Evidence |
|---|---|---|---|
| G1 | The new exception is unhandled: "the process dies with a traceback and prints no TOON", unparseable by LLM callers; severity `medium` | **refuted in its mechanism, rewritten and re-severitied to `low`** | `manage-config.py:61,169` — `main()` is `@safe_main`-decorated; `file_ops.py:1664-1700` catches every `Exception` and prints `output_toon_error('internal_error', …)` on **stdout**. Executed: stdout `status: error / error: internal_error / message: … refusing to overwrite a concurrent write …`, stderr empty, exit 1. The `grep 'except ConcurrentConfigModificationError'` sweep was too narrow to see a bare `except Exception` in a decorator. What survives — a recoverable condition reported in the crash tier with no discriminator, against `manage-contract.md:34-35` — is re-filed at `low`; the refuted clause is preserved in gaps.md § Refuted |
| G2 | Six write sites, 2 routed / 4 bypass; the docstring omits the Claude runtime seed | **upheld** | Independently re-derived by script: `save_config` `:223-224` and `_providers_core._save_marshal` `:108` routed; `ext_defaults_set` `:442`, `ext_defaults_set_default` `:465`, `opencode_runtime` `:79`, `_claude_runtime_impl` `:75` bypass. Docstring at `_config_core.py:168-177` names only three bypasses. `_claude_runtime_impl` range corrected `:44-79` → `:45-94` |
| G3 | Stage 2 and the Re-Run Pass give opposite ordering accounts; fix offered as an either/or | **upheld and made determinate** | Both texts confirmed (`upgrade-flow.md:321-327`; `marshall-steward/SKILL.md:490, 580, 632`). Executed: `save_config` canonicalizes on every write, so `load_config` → add `build` → `save_config` yields `['plan','build','system']`; `_cmd_sync_defaults.py:481` and `_cmd_steps_sort.py:84` both persist through `save_config`. Position therefore does **not** affect key order — Stage 2's "load-bearing" rationale is the wrong half, so the Fix now names one direction instead of two |
| G4 | `runtime` / `project_dir` trip the new warning | **upheld; Done-when rewritten** | Executed: `normalize_keys()` → `{'status': 'warning', …, 'unrecognized_keys': ['runtime','project_dir']}`. The original Done-when named "`project initial-setup` followed by `manage-config init`" — impossible as a check: `_cmd_init.py:22-23` refuses on an existing file and `--force` rebuilds from `get_default_config()`, discarding `runtime`, so the assertion would pass vacuously. Replaced with the OpenCode order, verified to preserve both. Constant range corrected `:130-152` → `:126-154` |
| G5 | Remediation shipped on an unverified operator claim | **upheld, evidence strengthened; severity left at `medium`** | Three in-tree facts added: `/plugin update <name>` occurs nowhere else in the repository; the string it replaced (`10de4d12^:cache_freshness.py:76-79`) was the sequence `installation.adoc:55-62` prescribes; the new text omits the reload step the tree elsewhere calls mandatory; and `test_cache_freshness.py:267-268` now forbids `/plugin install`, blocking restoration of the documented sequence. `high` was considered and declined — the decisive fact, whether the command exists, is still unverified. Doc range corrected `:57-64` → `:55-62` |
| G6 | `write_provider_config` → `_save_marshal` is an unguarded, non-atomic routed writer omitted from the residue | **upheld** | `_providers_core.py:212-239`, `:91-111` read exactly as described; plain `write_text`, no fingerprint, no `os.replace`. Fix confirmed viable: `_marshal_path()` is `get_marshal_path()` (`:66-67`), the same path `_config_core.MARSHAL_PATH` resolves |
| G7 | `compared_against` absent from the documented parse set | **upheld** | `upgrade-flow.md:139-140` parses three fields; the fourth is stamped at `cache_freshness.py:147` and `:202` and explained only at `:164-168` |
| G8 | *(new)* `upgrade.py` has no `@safe_main`, so an exception emits no TOON | **added, `medium`** | Executed: `upgrade.main(['migrate-bot-lists'])` under a race → `UNCAUGHT: ConcurrentConfigModificationError`, stdout empty. `upgrade.py:372` carries no decorator, unlike `bootstrap_plugin.py:339`; `output-contract.md:103` makes `@safe_main` the required crash path. This is the genuine instance of the failure G1 misattributed to `manage-config` |
| G9 | *(new)* `ClaudeRuntime.project_initial_setup` destroys an existing `marshal.json` | **added, `high`** | Executed against an identical fixture carrying `plan` + `system` + `credentials_config`: Claude → `['runtime','project_dir']` (everything else gone); OpenCode → `['plan','system','credentials_config','runtime']`. `_claude_runtime_impl.py:71-80` writes unconditionally with no read and no existence check; `contract.md:63-64` calls it a seed, and `test_claude_runtime.py:181-194` invokes it twice with nothing pinning config survival. The maximal lost update on the very file D4 guards |
| Verdict | `implemented-with-gaps` | **upheld** | Every deliverable is implemented or (D3) correctly recorded as already-satisfied; the two incompletes (D4, D5) are completeness gaps, not unimplemented rows, and D10(d)'s substitution is forced by D6's in-plan verdict. The two added gaps are pre-existing defects outside the plan's Expected surface and do not lower the verdict |
| Report accuracy #1–#3 | write-site under-derivation, incomplete residue, overstated `/plugin update` confirmation | **upheld** | Each re-derived above; the write-site figure is corrected from "35 call sites across 12 modules" to 36 across 14 files |
| Test totals / diffstat | 23 passed; 11 files + plan move | **upheld** | Both re-run |

**Documents corrected.** In `gaps.md`: G1 rewritten (mechanism refuted, severity `medium` → `low`, Fix and Done-when rewritten around the real tier defect) with the refuted clause preserved under a new `## Refuted during adversarial review` section; G3's *What is wrong* and *Fix* rewritten from an either/or into a determinate correction backed by an execution probe; G4's Done-when replaced because the original sequence could not be run; G5's evidence, Fix and Done-when expanded with three in-tree findings; file:line ranges corrected in G2, G4 and G5; G8 and G9 added; `**Open items:**` 7 → 9. In `verification.md`: the D4 paragraph's `35 call sites across 12 modules` corrected to 36 across 14 files and its no-TOON conclusion retracted; the D4 row's completeness pointer re-aimed; line refs corrected for `COMPARED_AGAINST` (`:100-110` → `:96-103`), `check_freshness` (`:154` → `:155`), the D8 upgrade-flow block (`:158-167` → `:158-168`), `_STAGE_SPECS` and the `manage-config.py` dispatch arm; the new-residue list extended with G8 and G9; the executor-behaviour item under *What could NOT be verified* qualified, and the `/plugin update` question added there.

**Residual doubt — what a third reviewer should look at first.**

1. **G9's blast radius.** I proved the Claude seed destroys an existing config but did not trace who invokes `project initial-setup` in a live install, nor whether any wizard path can reach it after `manage-config init`. If a reachable path exists, G9 is worse than `high` on paper; if the operation is genuinely once-only and gated upstream, it may be `medium`. That call needs the invocation graph, which lives outside this repository's markdown.
2. **G5's decisive fact.** Nothing in this repository can confirm or deny `/plugin update <name>`. Until someone checks the Claude Code CLI, G5's severity is a guess between `medium` and `high`, and a test currently pins whichever answer is wrong.
3. **The `@safe_main` gap as a class.** `cache_freshness.py`, `cache_retention.py`, `determine_mode.py` and `gitignore_setup.py` also lack the decorator. I scoped G8 to `upgrade.py` because it is the only one that writes config, but a sweep of every entry point in `marketplace/bundles/**` against `output-contract.md:103` would establish whether this is four stragglers or a systemic hole — and that sweep is exactly the kind neither the original check nor this one performed.
4. **D4's in-process limit.** Both reviews accepted the documented cross-process-only guarantee at face value. Nobody has tested two writers inside one process, which is the mode `_CONFIG_FINGERPRINTS` explicitly does not cover.
