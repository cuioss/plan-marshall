# Run finding: PR #1055

epic: review-apparatus · analysed 2026-07-30 · evidence: `ci pr comments --pr-number 1055`
(35 comments, 17 unresolved)

## Participation

| Bot | Result | Detail |
|---|---|---|
| CodeRabbit | ✅ reviewed, **4 findings** | two review bodies: "Actionable comments posted: **3**" then "**1**". All four inline, thread-bearing, with severity/effort tags |
| Sourcery | rate-limited | "weekly rate limit of 500000 diff characters" — **known, NOT an issue** |
| PR-Agent | ✅ result present, **0 findings** | informational Guide: tests present / no security concerns / no major issues detected |

## ⛔ Verdict — STRUCTURAL DEFICIT, ratio 4 : 0

PR-Agent **provided a result**, so the must-always-provide-a-result rule is satisfied. But CodeRabbit
found **four** substantiated findings on the **same diff** where PR-Agent found none. A baseline
existed, so this run IS assessable — and it is a **bug** under the comparative rule.

The four CodeRabbit findings were all real and all actioned:

1. `_resolve_footprint(plan_id)` invoked twice in `cmd_compose` — fixed via TASK-005, and CodeRabbit
   verified the follow-up.
2. `security_class_omitted[]` mandated at Step 7b but absent from the Step 10 Output TOON — fixed via
   TASK-006.
3. Opening activation summary contradicting the new gate (still said feature/bug-fix/tech-debt-only
   and touched-files-only) — fixed via TASK-007.
4. Two output contracts for one return shape; the earlier `## Output` omitted `security_class_omitted`
   — fixed inline.

⭐ **None of these are exotic.** They are duplicate-call, declared-contract-vs-returned-shape, and
stale-summary-prose — the classes PR-Agent's charter explicitly asks for.

## Posted answers

✅ Complete. Every finding carries a posted reply, and PR-Agent's informational Guide was itself
answered ("PR-Agent's PR Reviewer Guide is informational … "). No `untransmitted`, no unjustified
`skipped`.

⚠ The triage disposition for PR-Agent's Guide was posted **twice** (`IC_…MRM2eQ`, `IC_…MSLCSw`),
both replying to the same `comment_id`. Not a missing answer — the opposite — but worth noting as a
possible duplicate-transmit.

## Could we have found it ourselves?

| CodeRabbit finding | Answer |
|---|---|
| `_resolve_footprint` called twice in one function | **YES** — a repeated identical call to a pure resolver within one function body is diff-local and mechanically recognisable |
| Step 10 Output TOON omits a field Step 7b mandates | **YES** — this is the `_detect_producer_consumer` / declared-contract family already in the registry; check whether an existing detector should have fired |
| Opening summary contradicts the detailed contract below it | **YES** — same-document consistency; `_detect_same_document_consistency` exists, so check why it did not fire |
| Two `## Output` blocks, one stale | **YES** — `_detect_source_of_truth` family |

⭐ **All four answer YES**, and three of them plausibly map onto detectors that **already exist**. That
makes this run's most valuable question not "what detector is missing" but **"why did the existing
detectors not fire?"** — a materially different and cheaper investigation. Feed to `PLAN-PR-012` D1.

## Feeds

- `PLAN-PR-012` — four YES answers, three pointing at possibly-inert existing detectors.
- `PLAN-PR-004` — **strong corroboration** that the `#13` charter did not change behaviour: 4 : 0 on a
  real diff, with the raised `num_max_findings: 12` live.
- `PLAN-PR-006` — ⚠ **re-aim required**, see `epic.md` decision of 2026-07-30.
