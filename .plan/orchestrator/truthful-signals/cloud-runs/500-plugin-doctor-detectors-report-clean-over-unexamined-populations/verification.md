# Verification — plugin-doctor-detectors-report-clean-over-unexamined-populations

**Verified against:** commit `e0764ab14` (HEAD; the four commits between the landing and HEAD touch no file in this plan's surface)   **Landed as:** PR #1320, commit `8dd6f1a5e`   **Verdict:** implemented-with-gaps

## Method

What was actually done — execution first, reading second:

- Read `plan.md` in full (640 lines): the eight deliverables with their *Done when* clauses, the six Out-of-scope boundaries, the Expected surface, the 24 Claim-label rows, and the five Verification bullets. Read `report-01.md` in full (865 lines).
- Read the landed commit: `git show 8dd6f1a5e --stat` → **44 files, +5768/−296**, and the full `--name-status -M` list. Read the hunks for `doctor-marketplace.py`, `_runner.py`, `resolve_project_dir.py`, `test_ci_base.py`, the three sibling reports, `permission-architecture.md`, and the plan-file rename.
- **Ran the plan's tests** with `python3 -m pytest <file> -o addopts="" -q` from the repo root (this machine has no `uv`; `.venv-3.14/bin/python3` with pytest 9.1.1 is on PATH):
  - `test_quality_gate_root_anchored_findings.py` + `test_plugin_pin_trap.py` + `test_analyze_canonical_enum_drift.py` → **104 passed**
  - `test_analyze.py` + `test_analyze_incident_reference_in_docs.py` + `test_analyze_shim_marker.py` + `test_runner.py` + `test_analyze_thinking_directive_in_workflow_docs.py` → **248 passed**
  - `test_router_flag_placement.py` + `test_ci_base.py` + both `plan-retrospective` files + `test_execute_script.py` + `test_dispatch_boundary_error.py` → **339 passed**
  - Whole directory `test/pm-plugin-development/plugin-doctor/` → **1885 passed, 2 failed**. Both failures are `test_analyze_target_scope.py` raising `ModuleNotFoundError: No module named 'yaml'` from `marketplace/targets/component_targets.py:126` — an ad-hoc-venv gap (`pyyaml` is absent), reproduced on the unmutated tree, unrelated to this plan.
- **Executed the real quality gate** (`doctor-marketplace.cmd_quality_gate` over `marketplace/`): `status pass`, `total_issues 0`, 37 rule summaries, and exactly three rows carrying a population figure —
  `analyze_thinking_directive_in_workflow_docs` **33**, `analyze_shim_marker` **414**, `canonical-enum-choices-drift` **152**. This is D5's *Done when* confirmed by running the gate, not by reading it.
- **Re-derived every census figure** the report and the shipped docstrings state, with `derive_population` / `derive_coverage` over the real tree: population **152**, resolved **81**, unresolved **71**, fraction **0.467**, `blind_spots` **69**, causes `{notation_unresolved 0, script_unparseable 0, parser_surface_not_derived 43, authority_incomplete 18, no_choices_declared 2, choices_unresolvable 7, single_member_ambiguous 1}`, **53** distinct notations, `authority_incomplete` spread across **seven** notations with the exact per-notation split the module docstring states (`manage-execution-manifest` 9, `phase_handshake` 3, `manage-tasks` 2, and one each on `manage-findings` / `manage-references` / `manage-status` / `collect-fragments`). The two `no_choices_declared` members re-derive as exactly the pair the docstring and `rule-provenance.md` name. **Every figure matches.**
- **Re-derived the brace-less enum site count** (100/G7) by re-reading each population site's own source line: **6**, at `manage-change-ledger query --kind`, `ci pr safe-merge --strategy`, `ci checks status --error-style`, `validate_struct validate --schema`, and two `git-workflow prune-local-and-remote-ref --mode` sites. Matches the report's "all six brace-less sites".
- **Executed every D2/D7 *Done when* condition** directly against `_plugin_pin_trap`: zero-file comparison → `indeterminate` (`empty_comparison`); non-empty union with nothing readable → `indeterminate` (`nothing_scanned`, the CodeRabbit C1 fix); pin superset → `fail` with `extra_in_pin` rendered distinctly; two samples differing only in `content` → `indeterminate` (`read_during_write`); version-split executor → `fail` naming both versions, unreadable executor → `indeterminate`; a real chmod-000 source file drives `compare_pin_content` to `partial=True` with `PARTIAL scan: 1 of 2 scanned` in `render()`; `loader_selected_version(dirs, frozenset({'0.1.99'}))` → the **older** dir and an empty eligibility set → `None`; the literal shape-3 tree (older unmarked beside newest correct pin) → **pass**; the fail remedy names three invocable surfaces including `cache_retention sweep --apply` with the dry-run caveat; `observe_twice` is exported with an injectable `sleep`; `NON_BLOCKING_SHAPES == {shape1:empty-unmarked-set}` and an all-marked tree returns `indeterminate`, not `fail`.
- **Executed the D6 router-flag note against the real CI parser**: `['checks','status','--plan-id','X']` → exit 2 with *"--plan-id is a top-level flag and belongs BEFORE the subcommand (verb) … e.g. `python3 .plan/execute-script.py plan-marshall:tools-integration-ci:ci --plan-id X checks status`"* — the caller's real verb, the executor notation, no bare `*.py`. Separately confirmed the recommended form is runnable end-to-end: `ci.py::main` calls `extract_routing_args` before the provider parser is built, and `ci_base.extract_routing_args(['--plan-id','X','pr','view','--pr-number','7'])` does consume the flag.
- **Executed the C3 unreadable-file fix**: a chmod-000 script/workflow doc yields explicit `shim_marker_target_unreadable` and `thinking_directive_target_unreadable` findings rather than a silent `[]`.
- **Four mutations, each snapshotted and restored by byte-copy** (never `git checkout`/`restore`/`stash`; `git diff --quiet` checked clean on each path first, md5 re-checked after, `git status --porcelain` empty at the end):
  1. `_scoped`: dropped `or _finding_is_tree_wide(f, marketplace_root)` → **3 failed / 4 passed**, all three empty-population guards red. Reproduces the mutation register's D1 row exactly.
  2. `_scoped`: replaced the anchor test with a hand-maintained finding-type registry (`{thinking_directive_population_empty, shim_marker_population_empty}` plus the `incident_reference`+`pattern_family=='empty_population'` pair) → **7 passed**, and the whole 1885-test plugin-doctor suite stayed green. See G2.
  3. `_analyze_incident_reference_in_docs`: disabled the `_offset_in_inline_code(_exemption_offset(m), spans)` skip → **5 failed**, including `test_backticked_inline_code_ref_is_exempt`. D4's 130/G5 red reproduces.
  4. (`_analyze_argument_naming` needed none — the vacuity is reachable from a fixture; see G1.)
- **Executed the argument-naming no-op**: `analyze_argument_naming(bundles)` over a tree with no `.plan/execute-script.py` returns `[]`, and `cmd_quality_gate` records `{'rule': 'analyze_argument_naming', 'findings': 0}` — no population figure, no anti-vacuity finding.
- **Derived § Collateral independently** from `git show 8dd6f1a5e --name-status -M` against the plan's Expected surface, rather than from the report's own table. Two files are in the diff and in neither. See G3.
- Ran the 460/G5 and 360/G3 *literal* Done-when greps (`grep -rn --include="*.py" "three-way\|three ways\|three_ways" test/plan-marshall/plan-retrospective/` → one hit, `test_chat_provenance.py:270`; `grep -n -i 'retention.pin\|degraded fallback' _plugin_pin_trap.py` → exit 1). Both met.
- Re-collected the 040 test file: **46 tests collected**, `grep -c 'def test_'` → **21** — matching the corrected sentence D8 wrote into `040/report-01.md`.
- Checked SHA reachability for the report's citations. The fetched remote branch `origin/claude/plugin-doctor-detectors-report-ar5emr` is stale at `b55ad1d7b` (which independently corroborates retry-log row 2's head `b55ad1d`); `323bb12`, `fcfbc8c`, `8f1a67c`, `740d635`, `c494c8a`, `5283f50`, `a34819d`, `0533633` all resolve, while the final `2ac93468` / `edec1856` do not exist in this clone.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D1 | Root-anchored anti-vacuity findings survive a scoped run *(GATE)* | Scoped run over a zero-population tree reports non-zero for every rule in the derived set; a committed test builds that tree | Yes | Yes | Yes | Yes | `doctor-marketplace.py:607` `_finding_is_tree_wide` + `:848-856` `_scoped`. Derived set = the three the plan named, and the bypass keys on the **anchor**, not a type list. `test_quality_gate_root_anchored_findings.py` (7 tests) drives the real `cmd_quality_gate`. Mutation 1 → 3 failed / 4 passed. Catalogue corrected for all three rules (`rule-catalog.md:226,236,774`) |
| D2 | The pin-trap oracle stops issuing verdicts over unread axes | Six executable conditions (see § Method) | Yes | Yes | Yes | Yes | All six executed and confirmed, including the `nothing_scanned` arm CodeRabbit added (`_plugin_pin_trap.py:260-295` `unusable_because`). `compare_pin_content` walks the **union** (`:913`); `_volatile_signature` (`:466`) carries content counts, executor anchor and `eligible_versions`. 320/G2's red was observed *before* the adapter change, per the register |
| D3 | Documented-invocation analyzers attribute flags and enums to the scope that owns them | (a) shared-fence enums scope to their own invocation, real-tree half re-derived; (b) router-flag-misplaced fires after-verb and not before; (c) flag-first spelling produces no unknown-flag finding | Yes | Yes | Yes | Yes | `_enum_sites_in_skill` latch replaced (`_analyze_canonical_enum_drift.py:482`); `test_real_tree_shared_fence_sites_resolve_to_their_own_invocation` asserts by property over the live `manage-run-config` `architecture-refresh set-tier-1` subject. `_entry_from_surface`'s docstring now states the union is over-approximation and that **argparse does not honour a root flag after the verb** (`_analyze_argument_naming.py:344`). `ARGUMENT_NAMING_ROUTER_FLAG_MISPLACED` wired into the module, `rule-catalog.md:188`, `rule-provenance.md:106`, `_plugin_doctor_fixtures.py:1340`, and `test_analyze.py:2406+` |
| D4 | Two vacuous tests replaced by tests seen RED | Skip disabled → exemption test fails; docstrings retracted; recall MEASURED and published | Yes | Yes | Yes | Yes | Mutation 3 → `test_backticked_inline_code_ref_is_exempt` red. `test_analyze_shim_marker.py:348-357` docstring retracted verbatim; `_measure_marker_recall` strips each marker block and re-scans → **25 anchors / 4 detectable**, asserted and published at `rule-catalog.md:235` |
| D5 | The runner publishes the examined population; the shim rule is reachable where its catalogue says | Clean gate reports a non-zero population for both population-derived rules; a test asserts it for a clean non-empty population; `analyze_shim_marker` reachable from the analyze pass or no surface claims it | Yes | **Exceeded** (a third rule wired) | Yes | Yes (see G7 for a residual one level in) | Real gate executed → three `population_size` rows. `_runner.py:160-180` `emit`. `test_runner.py:318` asserts the figure with `findings == 0` alongside; `:336` asserts an absent figure is never a zero. `analyze_shim_marker` now in `run_analyze_marketplace_rules` (`_runner.py:411`) |
| D6 | Router-flag guidance reaches its surface and names a runnable invocation | Real CI parser produces the note; the example names the caller's verb and no bare `*.py`; the standards doc carries a mirror signature and its stated count equals its numbered entries | Yes | Yes | Yes | Yes | Executed against `ci_base.build_parser` — note verbatim above. `agent-behavior-rules.md:339` says "these **five**" over **5** numbered entries, #4 being the mirror with bidirectional cross-references; `persona-plan-marshall-agent/SKILL.md:104` and `recipe-fix-argparse-rejection/SKILL.md:13` agree, and the recipe now forbids restating the count at all |
| D7 | The pin-trap loader model, shape table, remedy and sampling API say what they do | Seven executable conditions | Yes | Yes | Yes | Yes | `loader_selected_version` is one expression (`:458-463`), no marker branch; 360/G3's literal grep exits 1; `SHAPE_3_LOADER_FOLLOWS_NON_PIN_DIR` names the condition evaluated and `test_shape3_literal_older_stale_beside_newest_pin_is_a_pass` pins the literal tree as a PASS; saturation is in `NON_BLOCKING_SHAPES`; the remedy's three steps each name an invocable surface; `observe_twice` exported and tested with a fake sleep; the two loader tests renamed `..._is_marker_insensitive_...` |
| D8 | Coverage widened where cheap, declared where not, stale documentation corrected | Brace-less sites collected, dict-spec authority resolves, unresolved fraction + causes published; incident rule fires on both new forms and not on a version constraint, real-tree anchor green with no suppression; catalogue count equals its subsections; two retrospective docstrings rewritten; one report count corrected | Yes | Yes | Yes | Yes | 6 brace-less sites collected (re-derived by name); 7-cause census published on findings and via `derive_coverage`; incident analyzer → **0** findings over the real tree; `rule-catalog.md:1058` says "Six rules" over **6** `###` subsections; the `As of 2025-10-27:` line is deleted from `permission-architecture.md`; `040/report-01.md` § D3 now reads "46 collected cases across 21 test functions" and re-derives exactly |

**No deliverable is unimplemented, and no *Done when* clause I could execute failed.** Every gap below is either a residual of the plan's own defect class that the plan did not assign to a deliverable (G1, G7), a guard that is correct but unpinned (G2), or a report/bookkeeping defect (G3–G6, G8).

## Report accuracy

Re-derived every figure in `report-01.md` that describes the tree. **Every census, population and count figure reproduces exactly** — 152 / 81 / 71 / 69, the 43/18/2/7/1 cause split, 18 across seven notations with the exact per-notation breakdown, the two `no_choices_declared` members by name, six brace-less sites, 25 marker anchors / 4 detectable, 33 and 414 populations, 46 collected / 21 functions in the 040 file, 29 coverage rows over 29 gap ids, 6 mirror-drift subsections, 5 signature entries. That is an unusually clean record for a report of this size, and it is the direct product of the eight rounds' re-derivation discipline.

The contradictions are all in the report's **framing and bookkeeping**, not its measurements:

1. **The header is frozen mid-run.** `**PR:** not yet opened   **Outcome:** in progress`, and the block quote `> **Verification loop exit:** not yet reached — the loop is open at the time of writing.` Both are contradicted by the report's own § Review participation (Step 7) — *"PR **#1320**, created open and unlabelled"* — and by § "Step 6's exit", which records the loop closed on `operator-stop` after eight rounds. Recorded as G4.
2. **§ Reviewer participation reads `*Pending — no PR yet.*`** while § Review participation (Step 7) 100 lines below carries the full three-reviewer verdict table plus a five-attempt CodeRabbit retry log ending in `obtained`. Two sections of one document disagree about whether a PR exists. Part of G4.
3. **§ Cost's wall-clock is `*pending*`** and **§ Residue is `*Pending — completed at finalization.*`** — the report's final section, never written. Recorded as G5.
4. **§ Collateral is stale again.** Two files in the landed diff appear in neither the Expected surface nor the table: `marketplace/bundles/plan-marshall/skills/script-shared/scripts/resolve_project_dir.py` (a **production** change adding `ROUTER_FLAGS` / `ROUTER_PLAN_ID_FLAG` / `ROUTER_PROJECT_DIR_FLAG` and three `__all__` entries) and `test/plan-marshall/tools-integration-ci/test_ci_base.py` (the new lock-step test). Both came from the CodeRabbit C9 fix, i.e. from the commit *after* round 8 re-derived the table. Recorded as G3.
5. **One mutation-register collateral count is short.** The D4/130/G5 row reads *"`test_backticked_inline_code_ref_is_exempt` (+3 others)"*. Re-run with the skip disabled: **5** tests fail — the named one plus `test_bare_ref_after_backticked_ref_on_same_line_fires`, `test_reversed_term_of_art_with_a_backticked_ref_stays_exempt`, `test_real_marketplace_has_zero_findings` and `test_observed_on_with_a_backticked_ref_is_exempt`. The row was written before D8's later tests landed. Recorded as G6.
6. **The plan itself was edited during the run.** `git diff 8dd6f1a5e~1:…500-….md 8dd6f1a5e:…500-…/plan.md` shows exactly one content change: D7's *Done when* "no branch whose outcome **is independent of** the marker" → "no branch whose outcome **depends on** the marker". The report discloses this as C19 and gives a sound reason (the criterion as authored forbade the design the same item specified). Recorded as G8 — not as a defect in the change, but because the plan text at HEAD is not the text the run was handed.

Claims re-derived and found **accurate** beyond the figures above: the D1 derived set is exactly three and the bypass is anchor-keyed; `analyze_shim_marker` is reachable from the analyze pass, so 050/G2 was closed by wiring rather than by retracting the catalogue; the coverage table's "28 met, 1 recorded-as-proposal over 29 rows" is arithmetically correct and every gap id the plan names has a row; P1's re-derivation discipline (the figure is a function of the tree at the moment it is taken, including the run's own prose) is sound and the five sites are named with their verdicts; the `--paths` help string names both unfiltered classes; RÄ-1's "two live members" example matches the census; RÅ-7's "seven notations" correction is right and my derivation agrees; the retry log's attempt-2 head `b55ad1d` matches the remote branch tip still fetched here.

## Out-of-scope compliance

Clean, with the one bookkeeping miss at G3. Checked each of the six declared exclusions:

- **Narrowing the back-tick exemption (130/G2)** — not done. `_offset_in_inline_code(_exemption_offset(m), spans)` is unconditional at `_analyze_incident_reference_in_docs.py:401`, and P1 records the proposal with its five live sites and the argument *against* the narrowing that this run's own measurement produced (four of five would be false positives, three of them a sibling analyzer's specification prose). The gap's own *Done when* is deliberately unmet and says so.
- **Broadening the pin-trap shape-3 condition (320/G4)** — not done. The literal tree is pinned as a **pass** by execution and by `test_shape3_literal_older_stale_beside_newest_pin_is_a_pass`; P2 records the alternative.
- **The import hop in the enum authority resolver (100/G7)** — the resolver *does* follow `_import_sources` into the defining module (RÄ-2 corrected the opposite claim), but no cross-module *parser-building* walk was added; the unresolved fraction is published instead, which is what D8 promised.
- **Unnamed gaps in the source `gaps.md` files** — none picked up. The 29 rows are exactly the plan's assigned set.
- **Plugin cache sync** — none performed, none recorded as owed.
- **Wiring the pin-trap into a live gate** — not done; it remains a library with adapters.

Every other file in the 44-file diff is either inside the Expected surface or justified in § Collateral. The two exceptions are G3.

## Residue the report itself declares

| Item | Status today |
|---|---|
| **C4** — root-flag arity: a zero-arity root switch breaks verb resolution and yields an invalid `--verbose VALUE list` correction. *Raised, not taken.* | **Open.** Lives only on the PR thread; § Residue was never written, so nothing in the tree records it. Part of G5. |
| **C7** — the D6 note is inert for `--project-dir` at the provider entrypoints. *False claim fixed; behaviour change raised.* | **Half-closed.** `parse_ci_args`'s docstring (`ci_base.py:887-901`) now states the reach exactly, verified by reading. The behaviour change is open on the thread only. Part of G5. |
| **Four condition-B survivors** (argparse `parents=[base]`; a parser rebound to a local; `for`/`with`/`lambda`/comprehension bindings; three collection-time drops with no census figure) — each "0 live sites, measured" | **Recorded in § Step 6's exit table, re-measured in round 8.** `_paths_with_incomplete_authority`'s docstring names the `parents=` gap explicitly rather than implying coverage. Adequately declared. |
| **P1 / P2** — two operator proposals | **Recorded, not decided**, exactly as the plan directs. Neither shipped. |
| **Proposal 1** (a `behaviourally-clear` loop exit) and **Proposal 2** (re-run derivations at a checkpoint) | **Open, for the operator.** Proposal 2 is independently vindicated by G3 — the very defect it describes recurred once more after it was written. |

## What could NOT be verified

- **The build gate.** `./pw verify` → "21425 passed, 14 skipped" in 528.38s with all six dimensions clean, and the five earlier runs (21402 / 21420 / 21422 / 21424 ×2). I did not run a full `./pw verify`; these are point-in-time observations. I ran 691 tests across the plan's 15 named test files and the whole 1885-test plugin-doctor directory instead.
- **PR #1320's thread state.** No GitHub API queries. The 19 CodeRabbit findings' resolutions, the two raised-not-taken threads, `sourcery-ai`'s size-ceiling refusal, `cuioss-review-bot`'s Reviewer Guide, and the five-attempt retry log are all unverifiable from the tree. Their *substance* is confirmed where it produced code: C1 (`nothing_scanned`), C2 (`eligible_versions` in the signature), C3 (`*_target_unreadable`), C5/C6 (quoted-run blanking and continuation folding), C9 (`ROUTER_FLAGS`), C10 (`(?:19|20)\d{2}`), C15 (the D1 control rewritten to drive a real gate) each execute as described.
- **The four cold reads.** No sub-agent transcript is committed. CR-1's substance is confirmed (the remedy now carries `--apply` and states what its absence means); CR-2's is confirmed (the census is published per cause).
- **The eight verification rounds.** I verified the artifacts at HEAD, not the round-by-round history. The report's own account of which round found what is internally consistent and its arithmetic (§ Item accounting) checks out, but is not independently re-derivable.
- **Commit-level history claims.** "23 commits, every one carrying the trailer" and the `git range-diff 3083553...eb2123a a34819d...323bb12` pairing. The fetched remote branch is stale at `b55ad1d7b`, so the final five commits (`c3fa560`, `680c83e`, `5f56288`, `2ac93468`, `edec1856`) are absent from this clone; `2ac93468` and `edec1856` resolve to nothing. Over the range I *can* see, 23 of 24 commits carry the trailer and the one that does not is `c294e8650`, a main commit pulled in by the base merge — consistent with the claim.
- **Wall-clock and token cost.** Never published by the run.
- **`test_analyze_target_scope.py`'s two tests** — blocked by a missing `pyyaml` in the ad-hoc venv, not by anything this plan did.
