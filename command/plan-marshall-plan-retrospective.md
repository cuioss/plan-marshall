---
description: Opt-in plan quality audit — analyzes artifacts, logs, metrics, chat, and invariants; compiles report and seeds lessons
---

Load and run the `plan-marshall-plan-retrospective` skill via the `skill` tool, then carry out its instructions using the user input below.

User input:

$ARGUMENTS
