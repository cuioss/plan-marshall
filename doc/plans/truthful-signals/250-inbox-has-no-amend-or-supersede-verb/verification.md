# Verification — 250-inbox-has-no-amend-or-supersede-verb

**Verified against:** commit `0314cc4db5fa28fa8b478334c35583a0262060b7`   **Landed as:** PR #1198, commit `51d1c9bc2a4450d9296d84d359052582be117bde`   **Verdict:** partially-implemented

> **Verdict corrected during adversarial review** (was `implemented-with-gaps`). The rows do not support the stronger word: the D4 row below reads **Implemented? Partly**, and two of that deliverable's three *Done when* clauses — "the existing files are migrated" and "the count moved per sender is reported" — are unmet and were never attempted. A deliverable that is partly implemented makes the plan partially implemented, whatever the reason. See § Adversarial review.

## Method

- Read `plan.md` and `report-01.md` in full.
- Located the landed commit with `git log --oneline --all --grep '#1198'` → `51d1c9bc` (squash merge, one commit, 10 files, +1744/-113). Read `git show --stat`, the full diff of `orchestrator.py`, `inbox-envelope.md`, `orchestration-model.md`, and `SKILL.md`, and the pre-commit form of `inbox-envelope.md` (`git show 51d1c9bc^:…`).
- Read the whole of the current `marketplace/bundles/plan-marshall/skills/plan-orchestrator/scripts/_orchestrator_inbox.py` (1804 lines), the `_add_inbox_group` registration in `orchestrator.py` (lines 2520–2733), the whole of `standards/inbox-envelope.md`, the `### inbox *` sections of `SKILL.md`, and the whole of `test/plan-marshall/plan-orchestrator/test_inbox_message_state.py` (642 lines).
- Read the supporting primitives the plan's claim-labels rest on: `file_ops.parse_markdown_metadata` / `generate_markdown_metadata` (header parsing stops at the first blank line, so no body content can leak into an amended envelope) and `input_validation.validate_plan_id` (`PLAN_ID_RE = ^[a-z][a-z0-9-]*$`, plus a `NO_PLAN` sentinel carve-out).
- Read the consumer surfaces the change touches but the plan did not name: `plan-orchestrator/workflow/analyze.md` (the drain), `workflow/cleanup.md` Step 9, `workflow/resume.md`, `templates/plan-spec.md`, `persona-plan-orchestrator/standards/orchestration-model.md`.
- Ran tests: `uv run python -m pytest test/plan-marshall/plan-orchestrator/test_inbox_message_state.py -o addopts="" -q` → **42 passed**; the four inbox files → **224 passed**; the whole `plan-orchestrator` suite → **563 passed**.
- Executed the code rather than reading it, on a scratchpad harness with `PLAN_BASE_DIR` redirected to a temp dir (`_orchestrator_inbox` imported directly): `cmd_inbox_write` → corrupt the `kind` header → `cmd_inbox_list` returned `count 1, invalid_count 1, live_count 0, closed_senders []`; a hand-built message with `lifecycle=retired` fed to `cmd_inbox_validate` returned `error: invalid_lifecycle`.
- **Mutation check 1 (the plan's own control, D5(e)).** Snapshotted `_orchestrator_inbox.py` bytes to the scratchpad after confirming `git diff --quiet -- <path>` exited 0, then deleted the foldered-archive scan from `next_sequence` (the naive flat-only implementation). Result: **3 tests RED**, including `test_next_sequence_advances_past_a_foldered_archived_message` (`assert 1 == 2`) and the end-to-end `test_write_then_drain_then_write_folds_and_never_reuses_a_sequence` (`demo-plan-001.md` vs `demo-plan-002.md` — the silent sequence reuse). Restored from the saved bytes; `git diff --quiet` exited 0 again.
- **Mutation check 2 (D1/D5(a)/D5(b)).** Same snapshot/restore discipline: made `cmd_inbox_amend` restamp `created` and drop the `amended`/`revision` stamps. Result: **7 tests RED**, including `test_amended_message_is_distinguishable_from_a_virgin_one_from_the_envelope` and `test_should_preserve_created_across_an_amend`. Restored; `git diff --quiet` exited 0.
- Checked for later supersession: `git log 51d1c9bc..HEAD -- <inbox surface>` shows only `5a5446d3` (#1215, `inbox landing-check`), which is purely additive (0 occurrences of `lifecycle` in its diff of `_orchestrator_inbox.py`). Nothing from this plan has been superseded.
- Swept for stale restatements with `grep -rn` across `marketplace/`, `doc/`, `test/` for `inbox/archive`, `append-only`, and verb enumerations.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D0 | Design the message-state vocabulary once, record the rejection | Model chosen, rejected alternative recorded, stream termination shown to fit | Yes | Yes | Yes | Yes | `_orchestrator_inbox.py:88-116` (`LIFECYCLES`, `STREAM_END_KIND` — one enum, `kind` deliberately not extended); rejection recorded in `report-01.md:37` and the PR body; `inbox-envelope.md:101-113` § Message-state vocabulary |
| D1 | Envelope makes post-filing mutation visible | Amended/superseded message distinguishable from its envelope alone | Yes | Yes | Yes | Yes | `cmd_inbox_amend` `_orchestrator_inbox.py:1444-1513`; `_apply_state_fields:336-359`; `test_amended_message_is_distinguishable_from_a_virgin_one_from_the_envelope` (header-only assertions) — goes RED under mutation check 2 |
| D2 | `validate` enforces, `list` surfaces | Both verbs reflect the state; row schema carries it | Yes | Yes | Yes | **No** — doc sweep | `_validate_state_fields:490-515` (codes 8–11); `cmd_inbox_list:1126-1164` (`lifecycle`/`revision`/`superseded_by` per row, `live_count`, `closed_senders`). Executed: `cmd_inbox_validate` returns `invalid_lifecycle`. But `SKILL.md:242` still enumerates only the seven pre-existing rejection codes → G1 |
| D3 | Stream termination and the drain | Sender can mark stream ended; drain can tell empty from finished | Yes (script surface) | Yes | Yes | **No** — consumer not wired | `cmd_inbox_close_stream:1607-1664`; `closed_senders` computed at `:1143-1151`; `test_drain_tells_an_empty_queue_from_a_finished_one`. The actual drain (`workflow/analyze.md:57-93`) never reads `lifecycle`/`live_count`/`closed_senders` → G3; `workflow/cleanup.md:108` still asserts no quiescence signal exists → G2 |
| D4 | Fold the archive into per-sender subdirectories | Archive foldered, **existing files migrated**, per-sender count reported | Partly | Yes for the capability | Yes | **No** — migration never run | Code atomic in one commit: `next_sequence:549-587`, `resolve_message_path:686-718`, `_count_archived:661-683`, `cmd_inbox_archive:1276-1298`, `cmd_inbox_migrate_archive:1667-1728`. Directory-safety guard `_foldered_archive_dir:518-531` (re-validates with `validate_plan_id`), pinned by `test_archive_refuses_a_sender_unsafe_as_a_directory_name`. No `.plan/local/orchestrator/` exists in this checkout, so the physical migration is unperformed and unverifiable here → G4 |
| D5 | Five tests, each verified to fail pre-fix | All five pass, each seen red first | Yes | Yes | Yes (non-vacuous, proven for all five) | Yes | 42 tests pass. Mutation-proven **individually** in adversarial review: (a)+(b) — restamping `created` alone turns `test_should_preserve_created_across_an_amend` RED and nothing else; (c) — widening `live_count` to every valid row turns 3 RED including `test_superseded_message_stops_appearing_as_live_but_stays_resolvable`; (d) — disabling the co-presence check turns both monotonicity tests RED; (e) — dropping the foldered scan from `next_sequence` turns **6** RED across all four inbox files. The D4 directory-safety guard was mutation-proven too (2 RED) |

**D2 (paragraph).** The enforcement itself is correct and reachable: `_validate_state_fields` runs after the seven base checks so no pre-existing code moved, and I executed `cmd_inbox_validate` against a `lifecycle=retired` message and got `invalid_lifecycle`. What is incomplete is the documented surface: `SKILL.md:242` § inbox validate lists the rejection codes a caller should expect and stops at `filename_sender_mismatch`. The doc-contract test that guards that list (`test_inbox_channel_contract.py:926 test_inbox_validate_still_lists_every_retained_rejection_code`) pins an **eight**-entry tuple at `:933-943` — the seven base envelope codes plus the resolution-side `invalid_message_name` — and none of the four new ones, so nothing in CI notices they are absent. (The "seven" originally written here was a sample, not a re-derivation; corrected in adversarial review.)

**D3 (paragraph).** `cmd_inbox_close_stream` and `closed_senders` are correct and tested, and the `not_supersedable` guard (`cmd_inbox_supersede:1575-1582`) genuinely prevents a stream-end marker being flipped to `superseded` and silently re-opening the stream — `test_should_refuse_to_supersede_a_stream_end_marker` asserts the refusal *and* that `closed_senders` still names the sender. The unmet half is the consumer: `analyze.md`'s Step 3 drain loop routes every enumerated row by `kind` and archives it. Because a stream-end marker rides `kind=finding`, filing one produces a spurious "finding" the orchestrator absorbs as an observation, and a `superseded` message is drained as ordinary work. The report declares this as residue; it is still open.

**D4 (paragraph).** The four archive-reading functions were changed together in the single squash commit `51d1c9bc`, so no intermediate foldered-archive-with-flat-allocator state is checkoutable — the plan's atomicity requirement holds. The dual-layout reads are real, not asserted: `next_sequence` scans queue + `archive/` (flat) + `archive/{sender}/`, and `test_next_sequence_takes_the_max_across_flat_and_foldered_twins` pins the max across both. The unmet half of the *Done when* is "the existing files are migrated, and the count moved per sender is reported": that never happened, because the run had no `.plan/` tree, and this checkout has `/home/user/plan-marshall/.plan` but no `local/orchestrator/` under it, so I cannot verify it from the tree either. One nuance on the directory-safety claim: `validate_plan_id` also accepts the `NO_PLAN` sentinel ahead of its regex, so `_foldered_archive_dir` would accept a `NO_PLAN` sender. That value is still a safe directory name (no separator, no traversal), so the guard's purpose is intact — but the report's parenthetical "`validate_plan_id` (`^[a-z][a-z0-9-]*$`)" is not the whole predicate.

## Report accuracy

Checked: every per-deliverable claim, every named symbol, the archive-path shapes, the error-code sets, the atomicity claim, the three sub-agent finding dispositions, the out-of-scope claims, and every stated count.

Contradictions found:

1. **Test counts are understated by one.** `report-01.md:71` states "All 41 tests in the new file pass" and "all 223 inbox tests"; the file is unchanged since the merge (`git log -- test/…/test_inbox_message_state.py` shows only `51d1c9bc`) and carries **42** test functions, all passing, and the four inbox files total **224**. Re-derived today. The "549 plan-orchestrator tests" figure now reads 563, which later plans plausibly explain and which is therefore not counted as a contradiction.
2. **The report ends with four duplicate, unfilled template sections.** `report-01.md:149-163` repeats `## Cost`, `## Contract check (Step 9)`, `## What have we learned (Step 9)`, and `## Residue`, each containing only `_pending_`, after the same four sections were filled at lines 113–147. A reader reaching the end of the document sees the run's own record as unfinished.

Everything else the report claims is confirmed by the tree: `_validate_state_fields` runs after the base checks; `cmd_inbox_amend` preserves `created` and stamps `amended` + `revision` while staying `lifecycle=live`; `cmd_inbox_supersede` writes `lifecycle=superseded` + `superseded_by` and preserves the body; `cmd_inbox_close_stream` files a `finding`-kind marker; the four archive functions are all updated in the one commit; `cmd_inbox_migrate_archive` reports `moved_by_sender`/`moved_total`/`skipped`; the three sub-agent findings are all present as fixed (`cmd_inbox_archive` docstring names `inbox/archive/{sender}/{name}` at `:1202`, `resolve_message_path` documents the foldered-then-flat probe order at `:690-695`, `_resolve_live_message`'s docstring at `:1398-1411` no longer claims to filter on lifecycle and `not_supersedable` exists with its test); and `orchestration-model.md:114` carries the reconciled write-boundary qualifier with all three bolded qualifiers intact.

Not re-derived: the `./pw module-tests plan-marshall` figure of 16287 passed (whole-module run, not attempted here) and the PR-reviewer participation table (GitHub state at the time of the run).

## Out-of-scope compliance

Clean. The landed diff touches ten files. `marketplace/bundles/plan-marshall/skills/manage-lessons/**` is **not** among them — the reference implementation was read and not touched, as required. No carve-out of the scripts-only access rule was added: `amend`/`supersede` are script verbs, and the argument surface still exposes no output path (`_add_inbox_group` docstring, `orchestrator.py:2526-2534`). `archive` was not overloaded as the correction verb — it keeps its consume meaning and the correction verbs are separate. Nothing from the epic-compaction work (generated blocks, settled-narrative relocation, anchor shape) appears in the diff.

One change falls outside the declared "Expected surface": `persona-plan-orchestrator/standards/orchestration-model.md` (one line, the Ledger Write-Boundary "Append-only" qualifier). It is a necessary reconciliation — that line asserted "never edits" about a channel that now has a sanctioned in-place edit — and the report discloses it at line 99. Not counted as an undeclared collateral change. The plan's "Expected surface" named the pre-rename `marshall-orchestrator` skill; the run correctly re-grounded on `plan-orchestrator` and said so.

## Residue carried forward

| Report residue | Still open today? | Evidence |
|---|---|---|
| `analyze` drain does not consume the new state | **Open** | `workflow/analyze.md:57-93` routes purely by `kind`; the strings `live_count`, `closed_senders`, and `lifecycle` do not appear in that file → G3 |
| Real `.plan/` archive migration owed on a local run | **Open, unverifiable here** | `/home/user/plan-marshall/.plan` exists but has no `local/orchestrator/`; nothing in the tree records a migration having run → G4 |
| Plan "Expected surface" named the pre-rename skill | Closed (a plan-authoring note, acted on during the run) | All landed paths are under `plan-orchestrator/` |
| Stale plugin-pin hazard | Open by design, mitigated | The dual-layout reads in `next_sequence`/`resolve_message_path`/`_count_archived` are the stated mitigation and are present |

## What could NOT be verified

- **The physical archive migration and its per-sender counts (D4's second and third clauses).** `.plan/local/orchestrator/` does not exist in this checkout, so there is no archive to inspect and no record of a migration to read. Not asserted as pass; recorded as an open item.
- **The plan's own motivating counts** ("~652 files across three epics, the largest ~428 across 36 senders"). Same reason — they live under `.plan/`. The plan itself flagged them as leads to be re-derived at migration time; the migration has not happened.
- **The whole-module test figure** `16287 passed` and the two claimed xdist-flaky failures. Not re-run (a full `module-tests plan-marshall` was out of proportion to this check); the plan-orchestrator subset is green at 563.
- **PR #1198 reviewer participation** (the 1-of-3 coverage table, the two rate-limit notices). Historical GitHub state, not derivable from the tree.
- **True revision monotonicity across time.** `_validate_state_fields:509-511` is a co-presence check over one file's header; a decrease in `revision` between two versions of the same file is not detectable statelessly and is enforced only by `cmd_inbox_amend`'s `+1` (`:1500`). See G9.

## Adversarial review

**Reviewed by:** an independent agent that did not write this document. Tree state at review: `HEAD = cc926c9b` (docs-only commits since the `0314cc4d` this was verified against; `git log 51d1c9bc..HEAD` over the inbox surface still shows only `5a5446d3` / #1215, re-confirmed with a `lifecycle` grep over its diff returning **0**).

**Checked.** Files opened in full or in the cited region: `_orchestrator_inbox.py` (all 1804 lines re-confirmed by `wc -l`; every function offset in this document re-derived from a `def` listing), `orchestrator.py:2520-2733`, `standards/inbox-envelope.md` (all 153 lines), `SKILL.md` §§ `inbox *`, `workflow/analyze.md`, `workflow/cleanup.md`, `workflow/resume.md`, `persona-plan-orchestrator/standards/orchestration-model.md:108-120`, `test_inbox_channel_contract.py:920-955`, `report-01.md`, `input_validation.py:86-101`, `marketplace_paths.py:73`.

Commands run: `git show --stat 51d1c9bc` (10 files, +1744/-113 — matches); `git log` on `test_inbox_message_state.py` (only `51d1c9bc`); a per-file test-function count at `51d1c9bc` (528 defs across 12 files, 224 in the four inbox files); `pytest -o addopts="" -q` on the new file (**42**), the four inbox files (**224**), and the whole `plan-orchestrator` suite (**563**) — all three figures re-derived, all green.

Functions executed, not merely read — on a scratch harness with `PLAN_BASE_DIR` pointed at a temp dir and `_orchestrator_inbox` imported directly: `cmd_inbox_write`, `cmd_inbox_list`, `cmd_inbox_validate`, `cmd_inbox_close_stream`, `cmd_inbox_amend`. Six mutations applied and reverted, each preceded by `git diff --quiet` (exit 0) and each restored from a byte snapshot taken by this reviewer, never by `git checkout`/`restore`/`stash`; the final `md5sum` matches the pre-mutation snapshot and `git diff --quiet` exits 0.

**Not re-checked.** The `./pw module-tests plan-marshall` figure of 16287 and its two claimed xdist-flaky failures (whole-module run, out of proportion). PR #1198 reviewer participation and the two rate-limit notices (historical GitHub state). The `PLAN-TRUTH-032` ledger row `cleanup.md:106` cites (under `.plan/`, absent). The physical `.plan/local/orchestrator/` archive and its per-sender counts (absent — this is G4). `landing-payload-spec.md` and the `phase-6-finalize` producer surfaces were swept for stale inbox claims but not read in full. The `analyze.md` Step 4/5/5b branch bodies were read only far enough to establish where a mis-routed row lands.

| Item | Original claim | Verdict | Evidence |
|---|---|---|---|
| Verdict | `implemented-with-gaps` | **corrected → `partially-implemented`** | The D4 row itself reads "Implemented? **Partly**"; two of three *Done when* clauses unmet. A partly-implemented deliverable does not support the stronger word |
| G1 | `SKILL.md:242` omits four new rejection codes; guard test pins "the seven old codes" | **upheld, figure corrected** | Section re-read: exactly seven codes, stopping at `filename_sender_mismatch`. All four new codes reachable — `validate_envelope:484-486` returns `_validate_state_fields`' code; executing a `lifecycle=retired` message returned `error: invalid_lifecycle`. But the guard tuple at `:933-943` holds **eight** entries (the seven plus `invalid_message_name`), not seven — corrected in G1 and in § Deliverable-by-deliverable |
| G2 | `cleanup.md` fact 2 asserts no quiescence signal exists | **upheld** | Line 108 verbatim; `git log` on the file shows `#1183` (`91a1c771`) last, no reconciliation since. The gap already offers the honest alternative (narrow the premise rather than drain), so it is not overclaiming |
| G3 | `analyze` drain ignores the new state | **upheld and re-severitied `medium` → `high`** | Whole-file sweep for `lifecycle\|live_count\|closed_senders\|superseded\|stream-end` returns one hit (line 52, "closed lifecycle"). The `kind` table at `:70-76` is exhaustive with no escape hatch, so a `superseded` `kind=landing` message reaches Step 4's **full ship reconciliation** — a `landings/` write and a `queue --transition ... --status shipped`. That is a false ledger write, which the severity rubric puts at `high`. The Fix was also extended: it must amend `analyze.md:225`'s `messages_archived + messages_invalid + messages_archive_failed == messages_scanned` invariant, or the accounting stops closing the moment either new rule fires |
| G4 | Physical migration never ran; unverifiable here | **upheld** | `.plan/` in this checkout holds `local/logs` and `local/marshall-state.toon` only — no `local/orchestrator/`. Nothing in the tree records a migration |
| G5 | `inbox-envelope.md:151` names six of ten verbs | **upheld, count re-derived** | Ten `actions.add_parser` calls at `orchestrator.py:2546-2721`: write, amend, supersede, close-stream, validate, list, archive, migrate-archive, detect, landing-check. Line 151 names six. Each of the ten has a `### inbox {verb}` section in `SKILL.md`, so the added names resolve |
| G6 | argparse group help still describes five verbs | **upheld** | `orchestrator.py:2538-2541` verbatim; the module docstring at `:57-69` and the `_add_inbox_group` docstring were updated and this literal was not |
| G7 | `close-stream` is not an in-place mutation | **upheld and strengthened** | `cmd_inbox_close_stream` calls `compose_envelope` then `allocate_message_path` (`:1645-1653`) — no existing file is opened. Two further defects found in the same bullet: its bolded lead says "**one** sanctioned in-place edit" while its body names three verbs, and `orchestration-model.md:114` already states the correct pair, so the two canonical documents contradict each other. Severity left `low` — it is a doc statement no code reads |
| G8 | Module docstring describes a flat archive write target | **upheld, Fix and Done-when rewritten** | Line 33 verbatim; the real target is `sender_dir / dest_name` at `:1296`. But the original Done-when ("no sentence describes a flat write target") was **unsatisfiable**: `:1298` deliberately keeps a flat destination for an off-shape source so its `os.link` `OSError` still surfaces as `invalid_message_name`. Rewritten to require that carve-out be stated rather than erased |
| G9 | `live_count: 0` + empty `closed_senders` reads as empty while a stuck invalid message sits in the queue | **upheld, re-executed** | Independently reproduced: `{'count': 1, 'live_count': 0, 'closed_senders': [], 'invalid_count': 1}`, row `error: invalid_kind`. Structural at `:1140-1142` — `live_count` filters on `row['valid']`, so an invalid row can never raise it |
| G10 | Report understates counts by one; duplicate `_pending_` tail | **upheld, both halves re-derived** | Line 71 verbatim ("41", "223"); duplicate sections at 149-163 confirmed by heading offsets. `git log` on the test file shows only `51d1c9bc`, and it collected **42** tests at that commit and today; the four inbox files collect **224**. The "549 → 563" figure was correctly *not* counted as a contradiction: the same 12 files carried 528 test *defs* at the merge, so 549 collected (with parametrisation) is consistent |
| G11 | — | **added** | `_add_inbox_group`'s docstring at `orchestrator.py:2523-2524` enumerates nine sub-verbs and omits `landing-check`, which the same function registers at `:2719`. Made stale by `#1215`, not by this plan; recorded because it is the third stale enumeration in that one function |
| G12 | — | **added** | Executed: after `close-stream demo-plan` succeeds, `cmd_inbox_write` for the same sender **succeeds** (`demo-plan-002.md`) and the listing then reports `closed_senders ['demo-plan']` alongside `live_count 1`; a second `close-stream` also succeeds (`demo-plan-003.md`), hidden by `closed_senders`' set dedup. Every surface states the marker as a fact about the future ("will send no more"); nothing enforces it. `medium`, because `analyze.md` reads `closed_senders` nowhere today (G3) — closing G3 without closing this makes it `high` |
| D0 row | clean pass | **upheld** | One enum (`LIFECYCLES:108`), `kind` deliberately not extended (`STREAM_END_KIND:116`), rejection recorded at `report-01.md:37`, stream termination shown to fit at `inbox-envelope.md:101-113`. All four offsets exact |
| D1 row | clean pass | **upheld by mutation** | Restamping `created` while leaving the stamps intact turns exactly `test_should_preserve_created_across_an_amend` RED — the D5(b) control bites in isolation, not only inside a compound mutation |
| D5 row | "non-vacuous, proven" on three of five controls | **upheld and completed** | The original document mutation-proved (a), (b) and (e) only. All five were proven individually here, plus the D4 directory-safety guard: (c) 3 RED, (d) 2 RED, (e) **6** RED across all four inbox files (the original reported 3, having run only `test_inbox_message_state.py`), guard 2 RED |
| "swept the tree, clean" | out-of-scope compliance and stale-restatement sweeps | **upheld, sweep broadened** | Re-swept with wider patterns than the original: `inbox/archive` and `append-only` across `marketplace/`, `doc/`, `test/`; verb enumerations across `doc/developer/`, `.claude/`, `agents/`; inbox references across `phase-6-finalize/`, `plan-retrospective/`, `landing-payload-spec.md`, `templates/plan-spec.md`. The broader sweep found exactly one thing the narrower one missed — G11. `manage-lessons/**` confirmed absent from the ten-file diff |

**Documents corrected.** *verification.md*: verdict `implemented-with-gaps` → `partially-implemented` with the reason stated inline; the D2 paragraph's "seven old codes" corrected to the eight-entry tuple; the D5 row's evidence replaced with the five individually-proven controls plus the D4 guard, and the D5(e) mutation's blast radius corrected from 3 RED to 6 RED. *gaps.md*: open items 10 → 12; G3 re-severitied `medium` → `high` with the `landings/`-write consequence named and its Fix extended to the `analyze.md:225` count invariant; G1's guard-test figure corrected to eight; G5's ten-verb list re-derived and made citable; G7 extended with the internal count contradiction and the `orchestration-model.md:114` conflict; G8's unsatisfiable Done-when rewritten around the deliberate off-shape flat fallback; G9 given its executed payload and the structural cause; G11 and G12 added; three notes added to "Considered and NOT raised"; a `## Refuted during adversarial review` section added recording that **nothing was refuted**, and naming for each gap the specific observation that would have refuted it.

**Residual doubt — what a third reviewer should look at first.**

1. **G12 versus the plan's scope.** The plan's D3 says only "a sender **can** mark its stream ended". A third reviewer may reasonably judge enforcement out of scope and prefer G12's alternative fix (weaken the prose) over its primary one. What is not defensible is leaving the documents asserting a guarantee the code does not make.
2. **G2's disposition is a judgement, not a fact.** Whether per-sender `closed_senders` amounts to epic-wide emission quiescence is genuinely open; the gap deliberately does not decide it. Someone who knows what `PLAN-TRUTH-032` was to supply should decide, and that record is under `.plan/`.
3. **The `analyze.md` branch bodies.** G3's `high` rests on reading the Step 4 routing consequence, not on executing it — `analyze.md` is an LLM workflow with no test harness. A reviewer who can trace an actual drain over a superseded `kind=landing` message would convert that reading into an observation.
4. **The un-swept producer surfaces.** `landing-payload-spec.md` and the `phase-6-finalize` inbox-writing steps were pattern-swept, not read. A statement about message immutability phrased in wording neither sweep used would still be sitting there.
