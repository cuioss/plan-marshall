envelope_version=1
sender_type=plan
sender_id=plan-pr-046
epic=review-apparatus
kind=landing
created=2026-09-13T20:29:34Z

## What landed

plan-pr-046 shipped as #1477 (merged) — CodeRabbit's clean-review participation credit is now gated by a declared content marker, so a pre-review walkthrough comment no longer clears the review barrier.

```landing-facts
schema=landing-facts/1
plan_id=plan-pr-046
epic=review-apparatus
pr=#1477
merge_state=merged
cleanup_owed=false
deliverables_total=5
deliverables_done=5
total_tokens=6765189
total_wall_seconds=477641.0
steps=finalize-step-sync-baseline:done,project:finalize-step-lessons-housekeeping:done,pre-push-quality-gate:done,project:finalize-step-plugin-doctor:done,pre-submission-self-review:done,finalize-step-simplify:done,architecture-refresh:done,push:done,create-pr:done,project:finalize-step-era-stamp-fill:done,ci-verify:done,automatic-review:done,branch-cleanup:done,project:finalize-step-deploy-target:done,project:finalize-step-sync-plugin-cache:done,project:finalize-step-review-retrospective:done,plan-marshall:plan-retrospective:done,lessons-capture:done,finalize-step-preference-emitter:done,record-metrics:done,finalize-step-print-phase-breakdown:done,emit-landing:done
step.record-metrics.total_tokens=6765189
step.record-metrics.total_wall_seconds=477641.0
step.record-metrics.any_phase_missing_end_time=false
step.branch-cleanup.merge_state=merged
step.branch-cleanup.cleanup_owed=false
step.branch-cleanup.work_performed=true
step.create-pr.pr_number=1477
step.finalize-step-sync-baseline.action=rebased
step.finalize-step-sync-baseline.upstream_commit_count=6
```

## Residue

- **The merge commit is `77cb2e251b348ce3e840ac1e565f5426f2d587de`**, landed through the GitHub merge queue (squash). The queue performed the update-and-test against `main` on its side; this run deliberately did NOT rebase locally before the merge, because a rebase would have moved HEAD off `8313358fc` and invalidated both required bots' verified reviewed-commit SHAs at once while CodeRabbit's rate window was spent.

- **`branch-cleanup` did not record a `merge_mechanism` fact.** The step re-entered on an already-merged PR (an earlier orchestrator attempt enqueued it but never wrote a step record), so this run merged nothing itself. The absence of the key is the honest signal, not a gap.

- **The review-versus-gate delta is `excluded`, not zero.** `project:finalize-step-review-retrospective` found the PR's findings carry three different `reviewed_commit_sha` values (`9fd4709d`, `29b86a6d`, `8313358f`), one per loop-back iteration, so no single tree was ever reviewed in full. It recorded `gate_tree_unsubstantiated` with `structural_share: null` rather than deriving a newest-wins SHA no reviewer actually reviewed against. The 7-addressable / 3-structural partition judgment is preserved in the artifact as a judgment about an unmeasurable PR, not as a rate.

- **Two of the three required/optional bots are unmeasurable in the review corpus.** `cuioss-review-bot` (`participated_but_empty`) and `sourcery` (`refused_structural`, 150000 diff-char cap) have no findings-store records, so nothing persists their `review_completeness` classification where a step at `order: 990` can read it. The retrospective declined to upgrade their rows from that separately-observed state. That handoff gap is a producer defect this epic owns.

- **CodeRabbit refused six triggers across six operator-mandated 90-minute waits before a review landed, and each trigger RESET its rate window rather than shortening it** (stated ETA moved outward 20 → 48 → 52 minutes). A recovery that re-triggers when its own claim clock expires therefore spends an attempt to learn nothing and pushes the window further away. Two refusal-detector defects derived from this were carried out as `plan-pr-046-001.md`.

- **`orchestrator inbox` has no read verb** (`amend, archive, close-stream, detect, landing-check, list, migrate-archive, supersede, validate, write`), so a plan cannot inspect its own queued messages to check for duplicates before writing. `inbox validate` resolves existence only and `inbox list` returns headers only, while the scripts-only rule bars a direct `Read`. Every candidate-lesson this run filed relied on evidence-anchoring instead of inspection.

- **`project:finalize-step-deploy-target`'s documented Step 1 command is denied by this repo's own PreToolUse hook.** The skill documents `./pw generate-claude`; hook rule R4 denies any Bash call whose first token is `./pw`, and the redirect it offers is a dead end because `architecture resolve --command generate-claude` returns `Command not found` (a generator alias is not a canonical build command). The step completed by routing through the build executor instead. Either the skill adopts the executor invocation as its documented Step 1, or R4 gains a carve-out for generator aliases.

- **`check-manifest-consistency` graded `branch_cleanup_changes` as fail over no evidence.** At finalize order 995 the plan is already merged, so its diff against `origin/main` is empty — and the aspect reported `diff_available: true` while grading. The sibling `check-outline-vs-shipped` resolved 31 realized paths through the shared resolver in the same run, so the evidence was reachable. This is the normal post-merge state, not an edge case.

- **`permission-prompt-analysis` structurally cannot observe its subject.** It reaches only the reduced transcript, whose reduction keeps operator turns and gate decisions by construction (5 of 1810 messages here); a permission prompt is neither. It was recorded `coverage: not_evaluated` rather than letting an empty list read as a clean zero.

- **Cost.** `single_module + bug_fix` anchors at 1.3M tokens / 90 min. This run spent 6.77M tokens and 5h07m worked — 5.2× the anchor — with finalize alone at 3.28M (48.5%), more than execute's 2.20M. The overrun is entirely the review loop, not the change: `pre-submission-self-review` fired 7× with 6 loop-backs, `pre-push-quality-gate` 5×, `automatic-review` 4×, `ci-verify` 3×.

- **Scope drift worth recording:** 10 undeclared files were modified (threshold 5), all during finalize as the self-review rounds reached contract docs and build files. And `cuioss-review-bot.md` was declared a `write-replace` in deliverable 4 but settled as a deliberate non-edit during execute, with the outline never corrected.
