# Gaps — 310-baseline-reconcile-anchors-on-a-stale-phase-1-sha-and-one-verdict-auto-merges

**Source:** verification.md (same directory)   **Open items:** 8

## G1 — Correct the `baseline-reconcile` command-table row that still names the stored `worktree_sha` anchor

- **Kind:** stale-statement
- **Severity:** medium
- **Where:** `marketplace/bundles/plan-marshall/skills/workflow-integration-git/SKILL.md:184` — the `baseline-reconcile` row of the `git-workflow` command table
- **What is wrong:** The row reads "…fetches `origin/{base_branch}`, lists upstream commits **since the captured `worktree_sha`**, and runs `git merge-tree`…". D1 removed that anchor entirely: `_cmd_baseline_reconcile.py:162 _resolve_merge_base` computes `merge-base(HEAD, origin/{base})` on every call and no code reads `status.metadata.worktree_sha`. The `branch-sync-state` row six lines above (`:178`) *was* updated by the same commit, so this is a miss inside the edited region, not an untouched file.
- **Why it matters:** This table is the authoritative one-line description an agent reads when choosing the verb. It states as current behaviour exactly the defect this plan removed, and it points a reader at a field the script no longer consumes — the same class of false signal the plan exists to close. It also directly falsifies the report's "no stale 'init-time SHA anchor' prose survives".
- **Fix:** Replace "lists upstream commits since the captured `worktree_sha`" with "lists upstream commits since `merge-base(HEAD, origin/{base_branch})`, recomputed per call". While there, add "non-mutating on every classification — never moves the branch ref" to the row (it currently says only "no working-tree mutation", which understates the D2 guarantee).
- **Done when:** `grep -rn "captured \`worktree_sha\`" marketplace/` returns zero hits, and the row names the merge-base anchor.
- **Module/topic:** `plan-marshall:workflow-integration-git` — SKILL.md command table

## G2 — Correct the Step 3d skip-condition row that still says "since phase-1-init"

- **Kind:** stale-statement
- **Severity:** medium
- **Where:** `marketplace/bundles/plan-marshall/skills/phase-2-refine/standards/refine-workflow-detail.md:280` — the "Skip Conditions Summary" table row `| Zero upstream commits since phase-1-init | Fast-path log, no findings |`
- **What is wrong:** The upstream set is now `{merge-base(HEAD, origin/{base})}..origin/{base}`, not "since phase-1-init". Line **234** of this same file was correctly rewritten by the landing commit to name the merge-base and to say "never a SHA captured at plan initialisation" (`git show 60e5fd81 -- …/refine-workflow-detail.md` adds 11 lines, two of which carry `merge-base`, and touches no `phase-1-init` line) — line 280 contradicts it 46 lines later, in the same document.
- **Why it matters:** The two statements disagree about what a zero `upstream_commit_count` means. A reader who takes line 280 will interpret a zero as "nothing landed since the plan started" instead of "the branch is not behind", which is precisely the misreading that produced the plan's observed contradictory verdicts.
- **Fix:** Change the row to `| Zero upstream commits since merge-base(HEAD, origin/{base_branch}) | Fast-path log, no findings |`.
- **Done when:** `grep -rn "upstream commits since phase-1-init" marketplace/` returns zero hits and the row agrees with line 234 of the same file. (Do **not** use the bare pattern `since phase-1-init` as the gate — `phase-5-execute/SKILL.md:270` contains an unrelated, correct sentence ending "…since phase-1-init no longer records it", so the bare pattern can never reach zero.)
- **Module/topic:** `plan-marshall:phase-2-refine` — `standards/refine-workflow-detail.md` Step 3d

## G3 — Document the three new return signals so a consumer is told how to handle them

- **Kind:** omission
- **Severity:** medium
- **Where:** `marketplace/bundles/plan-marshall/skills/workflow-integration-git/scripts/_cmd_baseline_reconcile.py:439` (`head_unresolved`), `:448` (`merge_base_unresolved`), `:553` (`probe_mutated_head`); consumers `marketplace/bundles/plan-marshall/skills/phase-2-refine/standards/refine-workflow-detail.md:275-282`, `marketplace/bundles/plan-marshall/skills/phase-6-finalize/standards/finalize-step-sync-baseline.md:78-96`, and **both** `baseline-reconcile` call sites in `marketplace/bundles/plan-marshall/skills/phase-6-finalize/standards/branch-cleanup.md` — the pre-rebase classifier at `:228-240` and the § "Re-run the classifier against the current head" block at `:510-519`, whose parsed `classification` feeds the pre-merge operator prompt. (The phase-5-execute call site is a separate gap — see G7.)
- **What is wrong:** D1 added a fail-closed skip (`merge_base_unresolved`), D3 added a skip (`head_unresolved`) and a fail-loud error (`probe_mutated_head`). `grep -rn "probe_mutated_head\|merge_base_unresolved\|head_unresolved" marketplace/` returns exactly three hits, all in the script itself (`:439`, `:448`, `:553`) — no SKILL.md, no standards doc mentions any of them. The Step 3d "Skip Conditions Summary" table enumerates skip reasons and lists neither new one. All three finalize consumer blocks instruct only "if the script exits non-zero → STOP and return an error TOON"; both new paths return with the wrapper's normal exit, carrying `status: skipped` or `status: error` in the payload, so that instruction does not reach them, and each block then says "Parse the TOON return for `classification`, …" — a field neither payload contains. **Both halves of that mechanism were executed, not inferred:** (i) `git-workflow.py` prints the payload through `triage_helpers.print_toon`, which always returns 0 (`script-shared/scripts/workflow/triage_helpers.py:145-153`), and `file_ops.safe_main` (`tools-file-ops/scripts/file_ops.py:1664-1698`) exits with that value — running `git-workflow.py branch-sync-state --plan-id definitely-not-a-real-plan` emits `status: error` and exits **0**; (ii) running `cmd_baseline_reconcile` against a worktree with an orphan history returns exactly `{status: skipped, plan_id, reason: merge_base_unresolved, base_branch}` — no `classification`, no `conflict_count`, no `auto_reconcilable`.
- **Why it matters:** D3's whole point is that a regression is "caught at the probe, rather than discovered at the landing". A guard that emits a signal no consumer is instructed to read is caught nowhere: the finalize step will read a missing `classification` from an error payload and take an undefined branch. Same for the `merge_base_unresolved` fail-closed path.
- **Fix:** (a) Add the three reasons to the Step 3d "Skip Conditions Summary" table with their behaviour. (b) In `finalize-step-sync-baseline.md` (before `:84`) and at **both** `branch-cleanup.md` call sites (before `:230` and before `:519`), replace "if the script exits non-zero → STOP" with an explicit `status` branch, because the exit code is always 0: `status: error` (any `error`, `probe_mutated_head` included) → STOP and return the error TOON to the dispatcher; `status: skipped` → force `{decision} = needs_user` and log the `reason`; only `status: success` proceeds to parse `classification`. (c) Add a "**Return**" block to the `baseline-reconcile` section of `workflow-integration-git/SKILL.md` (the canonical-invocation stub at `:862` currently carries no return documentation at all) listing the success fields (`classification`, `auto_reconcilable`, `merge_base_sha`, `merge_base_source`, `upstream_commit_count`, `conflicts[]`, `in_flight_files[]`), every skip reason (`no_remote`, `fetch_failed`, `head_unresolved`, `merge_base_unresolved`, plus the pre-existing activation skips), and the typed errors `probe_mutated_head` / `finding_persist_failed` — the sibling `branch-sync-state` section (`:431-478`) is the shape to copy.
- **Done when:** each of the three tokens appears in at least one authoritative skill document, and all three finalize consumer blocks branch on `status` before they parse `classification`.
- **Module/topic:** `plan-marshall:workflow-integration-git` + `phase-2-refine` / `phase-6-finalize` consumer docs

## G4 — Give the barrier re-fire mapping a test that can actually fail

- **Kind:** vacuous-test
- **Severity:** high — re-severitied from `medium` during adversarial review. This is the rubric's "a guard that passes against the defect it names", demonstrated rather than argued (see the mutation below), and the defect it fails to catch is destructive (re-pushing a merged-and-deleted branch).
- **Where:** `test/plan-marshall/workflow-integration-git/test_git_workflow.py:632-677` — `test_verdict_token_drives_refire_skip_mapping`
- **What is wrong:** The test defines its own oracle three lines above the assertion (`def verdict(state): return 'RE-FIRE' if state == 'ahead' else 'SKIP-OR-DECLINE'`) and then asserts a dict built from that function. The rule it claims to pin lives only in prose — `phase-6-finalize/SKILL.md:698-716` and `standards/push.md:119`, two independent restatements that can already drift from each other — and `branch-sync-state` has no code consumer anywhere (`grep -rn "branch_sync_state" marketplace/ --include=*.py` finds only the definition at `git-workflow.py:1195`, the argparse registration at `:2128-2134`, and two docstring mentions). **Proven by mutation:** the `remote_absent_unverified` branch in `phase-6-finalize/SKILL.md:711-713` was rewritten to "RE-FIRE (re-push the branch)" — i.e. the exact resurrection defect D4 exists to close — and the three `branch-sync-state` tests, this one included, stayed **green** (`3 passed`). The file was byte-restored from a scratchpad snapshot afterwards (md5 match, `git diff --quiet` clean). The non-vacuous half of the test is the four state *tokens*, which `cmd_branch_sync_state` really does produce; only the RE-FIRE/SKIP mapping is self-asserted.
- **Why it matters:** D4 stated "⛔ **A test is the deliverable, not a caveat in a document**" for exactly the destructive-routing half. As built, the destructive-routing half is still guarded only by a document. The regression this closes — resurrecting a merged-and-deleted branch — would return silently.
- **Fix:** Extract the mapping from prose into data the test can read. Either (a) add a small pure helper in `git-workflow.py` (e.g. `push_barrier_action(state) -> 're-fire' | 'skip'`) that `cmd_branch_sync_state` includes in its payload as `barrier_action`, have `phase-6-finalize/SKILL.md` instruct the dispatcher to branch on that field instead of on the state token, and assert the helper's output per state; or (b) if the mapping must stay in prose, have the test parse `phase-6-finalize/SKILL.md`'s item-1 block and assert that neither `remote_absent_*` token appears on a RE-FIRE line. Option (a) is preferable — it removes the prose from the decision path entirely.
- **Done when:** a test exists that fails when the push barrier is made to re-fire on `remote_absent_landed` or `remote_absent_unverified`, without the test also being the thing that defines the mapping.
- **Module/topic:** `plan-marshall:workflow-integration-git` tests + `phase-6-finalize` push barrier

## G5 — Delete the dead `auto_reconcilable == false` disjunct from the two threshold rules

- **Kind:** doc-drift
- **Severity:** low
- **Where:** `marketplace/bundles/plan-marshall/skills/phase-6-finalize/standards/finalize-step-sync-baseline.md:93` and `marketplace/bundles/plan-marshall/skills/phase-6-finalize/standards/branch-cleanup.md:238`
- **What is wrong:** Both rules read `classification == overlap_no_content_conflict AND (auto_reconcilable == false OR {threshold} == no_overlap_only) → needs_user`. Since `_cmd_baseline_reconcile.py:492` sets `auto_reconcilable = classification == 'overlap_no_content_conflict'`, the first disjunct can never be true inside a branch already conditioned on that classification — both docs even say so in a trailing parenthetical ("always reports `auto_reconcilable == true`"). Relatedly, `auto_reconcilable` is now `false` on `no_overlap`, a state that is trivially reconcilable, so the field carries no information the `classification` token does not already carry.
- **Why it matters:** A decision rule with an unreachable branch, annotated as unreachable, invites a reader to believe the classifier can downgrade auto-resolution — it no longer can. It is dead surface in a decision table an agent executes.
- **Fix:** Reduce both rules to `classification == overlap_no_content_conflict AND {threshold} == no_overlap_only → needs_user`, keeping the explanatory sentence. Keep `auto_reconcilable` in the payload (it is already read by the `auto_resolvable` rule one line above in both docs) and add one sentence to the `baseline-reconcile` "**Return**" block that G3(c) creates in `workflow-integration-git/SKILL.md`: "`auto_reconcilable` is derived — it is `true` exactly when `classification == overlap_no_content_conflict`; it is not an independent signal and never downgrades an auto-resolvable overlap."
- **Done when:** neither doc contains `auto_reconcilable == false` as a live condition (`grep -rn "auto_reconcilable == false" marketplace/` returns zero hits), and the SKILL.md return block states the derivation.
- **Module/topic:** `plan-marshall:phase-6-finalize` — sync-baseline / branch-cleanup threshold rules

## G6 — Correct the three factual errors in this plan's own run report

- **Kind:** stale-statement
- **Severity:** low
- **Where:** `doc/plans/truthful-signals/310-baseline-reconcile-anchors-on-a-stale-phase-1-sha-and-one-verdict-auto-merges/report-01.md:3` (header) and the § Build gate paragraph
- **What is wrong:** (a) The header still reads "**PR:** _pending_    **Outcome:** _in progress_" although the change landed as PR #1206 / `60e5fd81`, and the report's own § Contract check names PR #1206 — the document contradicts itself. (b) § Build gate says "three `.py` files" and then enumerates four (`_cmd_baseline_reconcile.py`, `git-workflow.py`, and two test modules); `git show --name-only 60e5fd81` confirms four. (c) § Findings claims "no stale … 'init-time SHA anchor' prose survives", refuted by G1 and G2.
- **Why it matters:** The report is the durable record of this run and is the input to retrospective audits. A count that is off by one and an "outcome: in progress" on a landed plan both mislead a later reader, and the clean-sweep claim is the one an auditor would otherwise trust instead of re-deriving.
- **Fix:** (a) Complete the two unfilled template fields in the header — `**PR:** #1206`, `**Outcome:** merged as 60e5fd81` (these were left as placeholders, so filling them completes the record rather than editing it). (b) Correct the count word in § Build gate: "three `.py` files" → "four `.py` files" (`git show --name-only 60e5fd81` lists `_cmd_baseline_reconcile.py`, `git-workflow.py`, `test_baseline_reconcile.py`, `test_git_workflow.py`; 13 paths in total, 11 of them non-plan). (c) Do **not** strike the "no stale … 'init-time SHA anchor' prose survives" clause — a run report is a record of what the run claimed, and deleting the claim erases the evidence. Append a one-line correction immediately after it instead: "Correction (post-landing verification): two sites survive the sweep — `workflow-integration-git/SKILL.md:184` and `phase-2-refine/standards/refine-workflow-detail.md:280`; see this plan's `gaps.md` G1/G2."
- **Done when:** the report's header carries PR #1206 and the merge commit, § Build gate says "four `.py` files", and the sweep clause carries the correction line naming the two surviving sites.
- **Module/topic:** `doc/plans/truthful-signals/310-…` — run report

## G7 — The phase-5-execute call site parses `conflict_count` with no non-`success` branch

- **Kind:** omission
- **Severity:** medium
- **Where:** `marketplace/bundles/plan-marshall/skills/phase-5-execute/SKILL.md:416-431` (Step 3 items 6-7, parse instruction at `:423`, self-absorption branch at `:425`) and `marketplace/bundles/plan-marshall/skills/phase-5-execute/standards/sync-with-main.md:51-60` (parse instruction at `:58`, self-absorb branch at `:60`)
- **What is wrong:** `baseline-reconcile` has a **third** documented dispatch site beyond the two finalize steps and phase-2-refine Step 3d: phase-5-execute's drift check. Both documents say "Parse `conflict_count`, `upstream_commit_count`, and `upstream_commits` from the returned TOON" and then branch on `conflict_count == 0` → **self-absorb** (write `status.metadata.worktree_sha`/`main_sha`, log, and continue the task loop). Neither document branches on `status` first. D1's new `merge_base_unresolved` and D3's new `head_unresolved` / `probe_mutated_head` payloads carry **no** `conflict_count` at all and are returned at exit code 0 — both verified by execution, see G3. An agent that reads an absent `conflict_count` as falsy takes the self-absorption branch: it records a new baseline and continues the task loop on the strength of a probe that explicitly declined to classify. This call site is absent from the report's § D0 enumeration and from § D2's consumer-doc list, and neither file was touched by the landing commit (`git show --name-only 60e5fd81` lists no `phase-5-execute` path), so the "dependency made explicit" work D2 performed for the finalize consumers never reached it.
- **Why it matters:** This is the one consumer whose non-`success` mishandling is not merely a logged confusion — it writes plan state and proceeds. It converts a fail-closed skip (D1's whole point) into a silent fail-open, which is the defect class the plan exists to remove.
- **Fix:** In both documents, insert an explicit branch immediately before the "Parse `conflict_count`…" instruction: `status: error` → return the structured drift TOON with `error: baseline_drift` (do NOT self-absorb, do NOT enter the task loop); `status: skipped` → same abort, with `display_detail` carrying the `reason`; only `status: success` proceeds to read `conflict_count`. Add a sentence stating that an absent `conflict_count` is never to be treated as zero. Add the corresponding rows to the § "Drift Semantics" table in `sync-with-main.md:114-121`.
- **Done when:** both `phase-5-execute/SKILL.md` and `standards/sync-with-main.md` branch on `status` before reading `conflict_count`, and the `sync-with-main.md` Drift Semantics table has a row for the non-`success` return.
- **Module/topic:** `plan-marshall:phase-5-execute` — Step 3 drift check / `standards/sync-with-main.md`

## G8 — The non-mutation sentence the landing added is broader than what the probe guarantees

- **Kind:** stale-statement
- **Severity:** medium
- **Where:** `marketplace/bundles/plan-marshall/skills/phase-6-finalize/standards/finalize-step-sync-baseline.md:76` — the sentence added by the landing commit: "The probe is a **non-mutating classifier on every classification** — it performs only `fetch + diff + merge-tree`, anchors its ranges on `merge-base(HEAD, origin/{base_branch})` recomputed per call, and never moves the branch ref."
- **What is wrong:** "performs only `fetch + diff + merge-tree`" is false. `cmd_baseline_reconcile` calls `_maybe_auto_update_stale_base_branch` (`_cmd_baseline_reconcile.py:414`) **before** the fetch, on every invocation, ungated by `--no-emit`. When the configured `base_branch` no longer resolves on origin, that helper runs `git ls-remote`, then **persists** a rewritten `base_branch` into `references.json` via `_update_references_base_branch` (`:224-248`) and emits a `decision` log entry (`:283-289`). Verified by execution, not by reading: a `--no-emit` classify-only run against a live clone whose configured base branch does not exist on origin recorded one persisted-write attempt (`[('p', 'main')]`) and returned `status: success, base_branch_updated: True, original_base_branch: gone-branch`. The plan's own **Goal** — "the classifier probe **writes nothing on any path**" — is therefore unmet; D2's narrower done-when ("no classification path moves a ref") *is* met, which is why the deliverable is not filed as unimplemented. The D3 guard cannot catch this either: it compares HEAD before and after (`:434`, `:549-560`) and is blind to a `references.json` write.
- **Why it matters:** This sentence is the safety justification a finalize dispatcher reads before invoking the probe. It says the probe cannot alter plan state; it can — and the field it rewrites (`references.base_branch`) is the rebase target the very next step uses. Shipping a broader non-mutation guarantee than the code provides is the same false-clean-signal class this epic exists to close, and the landing introduced it.
- **Fix:** Rewrite the clause in `finalize-step-sync-baseline.md:76` to enumerate the real side-effect set: "it never moves the branch ref and never touches the working tree; its only persisted write is the stale-`base_branch` auto-update to `references.json` (with its decision-log entry), which fires only when the configured base branch no longer resolves on origin — check `base_branch_updated` / `original_base_branch` in the return." Add the same two fields to the `baseline-reconcile` "**Return**" block G3(c) creates, and state there that `--no-emit` suppresses Q-Gate findings but **not** the `references.json` update.
- **Done when:** no document claims the probe "performs only fetch + diff + merge-tree" or that it writes nothing; the stale-`base_branch` persisted write is named in `finalize-step-sync-baseline.md` and in the `workflow-integration-git/SKILL.md` return block, and `base_branch_updated` / `original_base_branch` are documented return fields.
- **Module/topic:** `plan-marshall:phase-6-finalize` — sync-baseline step doc + `workflow-integration-git` SKILL.md return contract

## Refuted during adversarial review

**None.** Every claim in G1–G6 was re-derived independently and survived. What the review found instead was under-statement, not over-statement — one severity was raised (G4), two instance lists were incomplete (G3's consumer set; the plan's consumer population, now G7), one Done-when gate was unachievable as written (G2), and one new defect was found by executing the probe rather than reading it (G8). The specific re-derivations that could have refuted a gap and did not:

- **G1** — `grep -rn "captured \`worktree_sha\`" marketplace/` returns exactly one hit, `workflow-integration-git/SKILL.md:184`; `git show 60e5fd81 -- …/SKILL.md` shows the `branch-sync-state` row at `:178` edited and the `baseline-reconcile` row at `:184` untouched, so "a miss inside the edited region" is exact.
- **G2** — line 280 is present verbatim; line 234 (not 231) carries the corrected merge-base sentence and was added by the landing.
- **G3** — the three tokens have exactly three occurrences tree-wide, all in the script; the exit-0 and absent-field mechanism was reproduced by running the CLI and the command function.
- **G4** — the mutation described above proves the mapping assertion cannot fail.
- **G5** — `auto_reconcilable == false` occurs at exactly the two cited lines; `_cmd_baseline_reconcile.py:492` makes the disjunct unreachable as claimed.
- **G6** — `git show --name-only 60e5fd81` lists four `.py` files; the report header still carries the two placeholders.
