# 2026-08-25-09-004 — Populate the four context-load columns and run enrich at the metrics close

component: plan-marshall:phase-6-finalize
category: improvement
status: moved-to-epic (OPEN — call-site future work; corpus original retained)
source: corpus lesson 2026-08-25-09-004, copied into finalize-machinery per operator direction
(note: condensed 2026-09-17 — six observations; enrich never ran; third cause found:
session id captured AFTER finalize, so enrich would walk the wrong transcript)

## Context

No four-field cost attribution from either source: dispatch flags optional and never
passed (0/N measured every run); `enrich` never invoked (no scheduled call site; plus
ordering artifact — retrospective measures at 995 before record-metrics at 998; plus
session-capture precondition 14-006). Billing column empty on multi-million-token plans.

## Proposed action

Pass `message.usage` at every record-dispatch-boundary site; invoke enrich at the
metrics close (after capturing session id at finalize entry).

## Epic status

Open future work (metrics theme). Corpus original RETAINED.
