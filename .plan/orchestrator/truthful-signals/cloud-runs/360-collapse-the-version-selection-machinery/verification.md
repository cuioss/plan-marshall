# Verification — 360-collapse-the-version-selection-machinery

**Verified against:** commit `5cea6604a2a934fd6b7567bf44e4118ead017a5a`   **Landed as:** PR #1223, commit `d01edfdfd04e19d572625ad24d47af1b1f73bd3f`   **Verdict:** partially-implemented

*(Verdict corrected from `implemented-with-gaps` during adversarial review — see § Adversarial review. D1's headline action was not performed and the plan's Goal clause "the executor resolves bundle paths at run time rather than freezing them at generation time" is unmet; six of seven deliverables landed.)*

## Method

What was actually done, in order:

- Read `plan.md` and `report-01.md` in full.
- Located the landed commit (`git log --oneline --all --grep '#1223'` → `d01edfdf`, 15 files, +628/−2102) and read its stat and the per-file diffs for `marketplace_bundles.py`, `generate_executor.py`, `_doctor_shared.py`, `data-model.md`, `provisioning-fail-closed-audit.md`, `plan-marshall/SKILL.md`, `test_executor_version_split_regression.py`, `test_generate_executor.py`.
- Checked for later supersession: `git log d01edfdf..HEAD -- <each expected-surface file>`. **Three** later commits touch an expected-surface file (re-derived per file during adversarial review; the original "only one" was wrong): `7cadb986` (#1272) — a cosmetic `_REPO_ROOT = PROJECT_ROOT` swap in `test_marker_free_resolution.py` and `test_generate_executor.py`; `8872700b` (#1263) — a house-style pass over `test_generate_executor.py`; `d2e94b45` (#1236) — 7 added / 3 removed lines in `tools-script-executor/SKILL.md` covering executor refresh after a rebase, none of them marker- or version-selection-related. The four production sources (`generate_executor.py`, `marketplace_bundles.py`, `cache_retention.py`, `_doctor_shared.py`) and `test_executor_version_split_regression.py` / `test_marketplace_bundles.py` are untouched since `d01edfdf`. No deliverable of this plan has been superseded.
- Opened at HEAD, in full or in the relevant regions: `marketplace/bundles/plan-marshall/skills/script-shared/scripts/marketplace_bundles.py` (whole file), `marketplace/bundles/plan-marshall/skills/tools-script-executor/scripts/generate_executor.py` (§ `_CLAUDE_RESOLVER_TEMPLATE`, `cmd_preflight`, `_check_emitted_path_provenance`), `test/plan-marshall/tools-script-executor/test_marker_free_resolution.py` (whole file), `test/plan-marshall/tools-script-executor/test_executor_version_split_regression.py`, `marketplace/bundles/plan-marshall/skills/marshall-steward/scripts/cache_retention.py`, `marketplace/bundles/pm-plugin-development/skills/plugin-doctor/scripts/_plugin_pin_trap.py`, `marketplace/bundles/plan-marshall/skills/tools-script-executor/SKILL.md` §§ "Version-aware bundle-path resolution" / "Self-healing path resolution" / "generate_executor — preflight", `manage-config/standards/data-model.md`, `manage-config/standards/provisioning-fail-closed-audit.md`, `plan-marshall/SKILL.md:108`, and the retired `test_orphan_marker_existence_only.py` at `d01edfdf^`.
- Re-derived the sweeps rather than trusting the report: tree-wide grep for `orphaned_at` (`*.py`, `*.md`, `*.adoc`) and for `structurally impossible`, `saturat`, `retention pin`, `pollution detect`, `sanctioned existence`, `Pin resolution`, and every symbol D4 claims to have deleted (`_detect_multi_version_pollution`, `_retention_pinned_versions`, `_live_version_dirs`, `_carries_skills_tree`, `_mark_superseded_version_dirs`, `_partition_version_dirs`, `live_version_dirs`).
- Executed code rather than reading it where the claim is about a return value:
  - `loader_selected_version` from `_plugin_pin_trap.py` on four marker configurations — it returns the newest-overall dir in every case.
  - The D6(d) detector helper `_writes_marker` against six synthetic write shapes.
  - A glob-coverage comparison of `**/skills/**/scripts/*.py` vs `**/skills/**/scripts/**/*.py` under `marketplace/bundles` (386 vs 412 files; 26 uncovered).
  - The preflight return dict, counted field-by-field (seven).
- Ran tests: `uv run python -m pytest test/plan-marshall/tools-script-executor/ test/plan-marshall/script-shared/test_marketplace_bundles.py test/plan-marshall/marshall-steward/test_cache_retention.py test/pm-plugin-development/plugin-doctor/test_plugin_pin_trap.py -o addopts="" -q` → **503 passed**.
- **Two mutation checks**, each on a file first confirmed clean with `git diff --quiet`, each restored from a byte snapshot taken before the edit (never `git checkout`/`restore`/`stash`), each restored within the same step and re-confirmed clean:
  1. Re-introduced `and not (d / '.orphaned_at').exists()` into `select_live_version_dir`'s candidate filter → `test_saturated_cache_resolves_to_newest_without_degraded_warning` **FAILED** (`got None`). Restored; `git diff --quiet` exit 0.
  2. Re-introduced `and not (version_dir / '.orphaned_at').exists()` into the `_CLAUDE_RESOLVER_TEMPLATE` candidate filter → `test_resolver_ignores_orphan_mark_and_selects_newest_carrying_the_script` and `test_resolver_survives_deletion_of_generation_time_version` both **FAILED**. Restored; `git diff --quiet` exit 0.

  ⇒ The D6 **resolver** guards (a)/(b) are not vacuous: each side of the two-site policy is independently pinned and goes red when the removed behaviour is put back. This says nothing about D6(d), whose population *is* vacuously satisfiable — a third mutation in § Adversarial review demonstrates it.
- No file was mutated that another agent had modified. `git status` showed one unrelated file (`_cmd_baseline_reconcile.py`) already modified by another agent; it was not touched, snapshotted, or restored.

Only two files were written: this one and `gaps.md`.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D0 | GATE: confirm chain + ownership by symbol; establish why-baked | ownership column confirmed from source, both consumer sets enumerated | yes | yes | yes | yes | Every link the report names is confirmable at `d01edfdf^`: sole writer `generate_executor.py:2399` (`(version_dir / '.orphaned_at').write_text(...)`), sole selector read `marketplace_bundles._partition_version_dirs`, mirror read in `_CLAUDE_RESOLVER_TEMPLATE`, comment-only mentions in `_doctor_shared.py` and `cache_retention.py`, out-of-scope reader `_plugin_pin_trap.py:64,554`. Why-baked confirmed at HEAD: `.plan/execute-script.py:196` `SCRIPTS` is an absolute-path map, `:610` `_PYTHONPATH` is baked, and `tools-script-executor/SKILL.md:457-475` documents the four-tier existence-guarded self-heal — the bake is a fast path, not a hard pin. |
| D1 | LEVER A: stop baking absolute version paths; resolve at executor runtime | an executor generated against one version still resolves after that version is deleted | partly | **no — reinterpreted (disclosed)** | yes | yes | Marker removal done: `marketplace_bundles.py:16-58` `select_live_version_dir` is newest-eligible with no marker read and no degraded fallback; `generate_executor.py:477-539` template mirror likewise. `_partition_version_dirs` / `live_version_dirs` absent tree-wide. But **absolute paths are still baked**, so D1's headline action was not performed. Evidence corrected during adversarial review: the local `.plan/execute-script.py:196-198`, `:608-611` shows a baked absolute `SCRIPTS` map and `_PYTHONPATH`, but those are *marketplace-layout* paths under `marketplace/bundles/` and carry no version segment, so that file alone does not demonstrate *version*-pinned baking. The load-bearing evidence is `generate_executor.py:740-753` — `_check_emitted_path_provenance` exists to refuse an executor whose emitted paths span more than one version dir, and "no-ops … when the emitted paths carry no version-dir segment, which is the marketplace layout": in the plugin-cache layout the emitted map is version-dir-anchored by construction. Done-when holds (`test_resolver_survives_deletion_of_generation_time_version`) — and held pre-fix too, as report F3 states. |
| D2 | LEVER C: stop writing the shared marker; state the enforcement-test interaction | no write to the shared field remains under our tree | yes | yes | yes | yes | Tree-wide grep: the only `.orphaned_at` writes outside `doc/plans/` are in three **test fixtures** (`test_cache_retention.py:72`, `test_executor_version_split_regression.py:69,271`, `test_marketplace_bundles.py:36`, `test_marker_free_resolution.py:86,95,112`). No production write. `test_orphan_marker_existence_only.py` deleted, and the retirement is stated in the report (§ D0 last block, § Deliverables D2) and the PR body — not silent. |
| D3 | LEVER B: evaluate keeping ONE version dir | the decision is RECORDED either way | yes | yes | yes | yes | Recorded as **not adopted** with the running-process-PYTHONPATH reason, which the tree substantiates (`.plan/execute-script.py:610` bakes `_PYTHONPATH`). `cache_retention.py` is absent from `git show --stat d01edfdf`, matching "unchanged". |
| D4 | Retire what the levers make dead | no unreachable containment code remains | yes | yes | yes | yes | Tree-wide grep for `_detect_multi_version_pollution`, `_retention_pinned_versions`, `_live_version_dirs`, `_carries_skills_tree`, `_mark_superseded_version_dirs`, `_partition_version_dirs`, `live_version_dirs` → **zero hits** outside `doc/plans/` and `.pyc` caches. Guard 4 retained at `generate_executor.py:740` (`_check_emitted_path_provenance`), still called at `:1428`. |
| D5 | Correct the saturation claim wherever it is stated | no document asserts the refuted guarantee | yes | yes | yes | **no — 3 stale sites** | The refuted guarantee is gone: no `structurally impossible` hit anywhere relates to marker saturation; `data-model.md:507-513` rewritten; `tools-script-executor/SKILL.md:579-625` and `:762-769` rewritten; `provisioning-fail-closed-audit.md:95` rewritten; `plan-marshall/SKILL.md:108` corrected six→seven-field. **But** `test_executor_version_split_regression.py:180,222,226,227` still assert the deleted retention-pin mechanism. |
| D6 | Tests, each verified to FAIL pre-fix | all four pass, each seen red first | yes | (a) reinterpreted, (c) control by construction — both disclosed | yes | **(d) weaker than the guard it replaced, and vacuously satisfiable** | All six tests in `test_marker_free_resolution.py` pass. Mutation 1 reds (b); mutation 2 reds (a) and its survival companion. (c) is a matched negative control, honestly labelled as green both ways. (d) `test_no_production_source_writes_the_shared_marker` passes but its detector `_writes_marker` (`:238-266`) catches only one AST shape, and its population is unasserted — a third mutation (adversarial review) set `_SOURCE_GLOB` to a pattern matching zero files and **all six tests still passed**. See below and gaps G1/G6. |

### D1 — the headline action was not performed; the reinterpretation is disclosed

`plan.md` D1 reads "stop baking absolute version paths into the executor. Resolve bundle script directories **at executor runtime** from a single selector." At HEAD the generated `.plan/execute-script.py` still contains a fully-baked absolute-path `SCRIPTS` map (line 196 onwards) and a baked `_PYTHONPATH` (line 610), so link 3 of the plan's seven-link chain is **still present**. What D1 actually delivered is the removal of marker consultation from an already-runtime fallback resolver.

This is not concealed: report § D0 "why-baked" establishes the bake is a fast path, report finding **F3** states the deviation outright, and the PR body carries a dedicated "Note on D6(a)" saying the same. The plan's own *Done when* for D1 is satisfied. Graded "as documented: no" because the deliverable's stated action differs from what landed, not because the run misrepresented it.

### D5 — three stale restatements of deleted machinery in a test file

`test/plan-marshall/tools-script-executor/test_executor_version_split_regression.py` had its **module** docstring rewritten by this PR to the marker-free model, but three in-file statements were left behind:

- `:180` — `test_all_legs_agree_all_marked` docstring: "no marker carries a currency signal and all three legs **fall back to the retention-pinned NEWEST dir**".
- `:222` — section banner "Case 4: a marker on **the retention-pinned dir** does not suppress it".
- `:226-228` — `test_marker_on_the_retention_pinned_dir_does_not_suppress_it`, docstring "The newest-on-disk dir **is the retention pin**".

`_retention_pinned_versions` was deleted by D4 in this same commit. Both tests pass, but by a different route than they claim: nothing pins anything, and the marker is not read at all, so the assertion holds because eligibility alone drives selection. This is precisely the "fixture reaches the asserted state by a different route than the test claims" shape.

### D6(d) — a strictly narrower guard than the one it replaced

`_writes_marker` (`test_marker_free_resolution.py:238-266`) flags a `write_text`/`write_bytes` call only when the `.orphaned_at` **string literal** appears inside the call's own target expression. Executed against six shapes:

| shape | caught |
|---|---|
| `(version_dir / '.orphaned_at').write_text(...)` — the exact pre-fix shape | **yes** |
| `marker = version_dir / '.orphaned_at'` … `marker.write_text(...)` | no |
| `(version_dir / ORPHAN_MARKER_NAME).write_text(...)` | no |
| `open(version_dir / '.orphaned_at', 'w')` | no |
| `(version_dir / '.orphaned_at').touch()` | no |
| a write **inside** an emitted-code template string constant | no |

The retired `test_orphan_marker_existence_only.py` handled **three** of those misses by design (corrected during adversarial review — the original text said four): `_assigned_alias` (`d01edfdf^:...:338`) / `_check_alias_uses` (`:348`) resolved alias-bound marker paths, `_CONTENT_PARSE_CALLS` covered `open()`, and `_embedded_code_tree` (`:366`) / `_template_label` (`:387`) parsed `_CLAUDE_RESOLVER_TEMPLATE`. It **missed** the named-constant shape and `.touch()` — its walk starts from an `ast.Constant` equal to `.orphaned_at` and widens only through `/` joins (`_marker_path_node`, `:253-264`), so a name-indirected marker never enters it. (`_METADATA_PROBE_ATTRS` covered non-`exists()` *reads*, not writes.) Both claims were established by executing the retired `analyse_source` on the six shapes, not by reading it. The alias-bound shape is not hypothetical: `_plugin_pin_trap.py:554` uses exactly it (`marker = entry / ORPHAN_MARKER_NAME`), and `test_marker_free_resolution.py` itself writes markers as `(version_dir / _MARKER_NAME).write_text(...)` at `:86`, `:95`, `:112`. The template shape matters most, because that constant is the code that ships into every generated executor.

The guard goes red on the one shape it recognises — but it is **vacuously satisfiable**, which the original text got wrong. Mutating `_SOURCE_GLOB` to `'**/skills/**/scripts-NOPE/*.py'` (zero matches) leaves all six tests green: nothing asserts the population is non-empty or reaches the files that mention the marker. The retired test carried that floor explicitly (`TestPopulationIsNonVacuous`, `d01edfdf^:...:514-540`, plus `_population_summary` at `:453` publishing the count in every assertion message) and it was dropped with the file. The report's "re-established, not dropped" is therefore wrong on two axes, not one — shape coverage and population non-vacuity. Recorded as gaps G1 and G6.

## Report accuracy

Checked claim by claim. Contradictions found:

1. **"Its 'no marker write' guarantee is re-established, not dropped, by the new D6(d) test"** (§ Deliverables D2). Contradicted on two axes. (i) The replacement detects one AST shape; the retired test additionally detected alias-bound writes, `open()` writes and writes inside emitted-code template constants (three lost shapes; both detectors miss the named-constant and `.touch()` spellings). (ii) The replacement has no population non-vacuity floor, so it passes over an empty scan — demonstrated by mutation. Evidence: the six-shape execution table above (both detectors executed), the empty-glob mutation, and `d01edfdf^:test/plan-marshall/script-shared/test_orphan_marker_existence_only.py:93`, `:338`, `:348`, `:366`, `:453`, `:514-540`.
2. **"A tree-wide sweep confirms no document asserts the refuted guarantee"** (§ Deliverables D5). Narrowly true for the *saturation* guarantee; false for the machinery it depended on. Three statements naming the deleted retention pin survive at `test_executor_version_split_regression.py:180,222,226-228` — in a file this same commit edited.
3. **"D4 … the selector's degraded fallback, and the now-unused `select_live_version_dir`/`live_version_dirs` imports in `generate_executor.py`"** — `select_live_version_dir` was not made unused, it was **kept and rewritten** in `marketplace_bundles.py` and is imported by `test_executor_version_split_regression.py:28` and `test_marker_free_resolution.py:29`. Only the `generate_executor` re-export was dropped. Wording, not substance.

Verified as accurate, having re-derived each: the seven-link ownership table and both consumer sets (D0); `_mark_superseded_version_dirs` as the sole writer under our tree (`d01edfdf^:generate_executor.py:2399`, the only write hit in the pre-fix marketplace grep); the circularity argument (verbatim at `d01edfdf^:generate_executor.py:557`: `if pair[0] == pinned or not (pair[0] / '.orphaned_at').exists()` — the earlier paraphrase of this line has been replaced with the actual source); Guard 4 retained and marker-independent (`generate_executor.py:740`, called at `:1428`); the five D5 doc sites all corrected in the landed diff; the preflight field-count correction (the return dict at `generate_executor.py:2290-2298` has exactly seven keys, and `_PREFLIGHT_FIELDS` in `test_generate_executor.py:1515` pins the same seven); F1's `SKILL.md` § preflight rewrite (heading `:762`, body `:769`); F2's test-comment rewrite; `cache_retention.py` unchanged as D3 states; the D3 rationale's premise that PYTHONPATH is captured at generation time (`.plan/execute-script.py:610`); the pin-trap detector left untouched.

Not verifiable from this clone, and so neither confirmed nor contradicted: the `./pw verify` figure `19560 passed, 14 skipped` (the tree has moved — #1272 reports 20,329); the wall-clock and branch-commit SHAs (squashed away); the reviewer-participation table and rate-limit bodies (GitHub state, not tree state); the live-machine saturation and marker-deletion observations, which `plan.md` itself labels unreachable from a clone.

## Out-of-scope compliance

Compliant.

- **"NEVER the plugin host's registry file. Read only."** — the landed diff touches nothing outside `marketplace/bundles/`, `test/`, and `doc/plans/`.
- **"Retiring the sibling detector plan … do not pre-emptively retire it."** — `_plugin_pin_trap.py` is absent from the diff and still present at HEAD with its marker reads intact (`:64`, `:554`).
- **"Re-opening the marker ENCODING question."** — not reopened; the change removes the field from our side rather than re-encoding it.

One collateral change, declared: the six-field → seven-field correction at `plan-marshall/SKILL.md:108`. It is a pre-existing defect in a file the PR already edited, is named in the report's Findings and Residue and in the commit body, and re-derives correctly against the actual return. No undeclared collateral change is present in the diff.

## Residue carried forward

| report-01.md residue item | Still open at HEAD? |
|---|---|
| Sibling detector re-scope — `_plugin_pin_trap.py` still reads `.orphaned_at` and still models retention pins / degraded fallback | **Open.** `:64` defines `ORPHAN_MARKER_NAME`, `:554` reads it, and `loader_selected_version` (`:269-289`) documents itself as mirroring a selector model that no longer exists. Executed on four marker configurations it returns newest-overall every time, so it does not currently mis-report the loader — but its `SHAPE_1_SATURATION` GC-exposure axis (`:368-372`) still ranks marker saturation as "repair before the fuse burns" for a resolver that no longer consults the marker. |
| Pre-existing field-count bug fixed in passing | **Closed.** `plan-marshall/SKILL.md:108` says "seven-field"; the return dict has seven keys. |
| "No follow-up owed on this PR's own surface" | **Contradicted** by the three findings above (D6(d) coverage, the three retention-pin statements, and the two smaller `cache_retention.py` restatements). |

## What could NOT be verified

Stated explicitly rather than passed by default:

- The `./pw verify` result quoted in the report (`19560 passed, 14 skipped`). The tree has moved since; the targeted suites relevant to this plan (503 tests) were run and are green, but the whole-tree figure at PR time cannot be reproduced.
- Everything about the live plugin cache: the measured saturation (every version dir marked), the foreign producer's write-and-delete behaviour, and the 66 ms marker-deletion observation. `plan.md` labels all three unreachable from a clone; nothing in the tree confirms or refutes them, and the design argument that rests on them needs no measurement.
- Reviewer participation, rate-limit bodies, auto-merge arming, and CI conclusion for PR #1223. Only the landing itself is confirmable: `d01edfdf` is an ancestor of HEAD on `main`.
- Whether the plugin host's own collector actually reads the marker in an epoch-ms encoding (third-party behaviour, outside this repository).
- The red-first evidence for D6(a)/(b)/(d) as claimed. The pre-fix reds were reproduced by *mutation* against today's code (both mutations went red, so the guards discriminate), not by checking out `d01edfdf^` and running the new tests against it.

## Adversarial review

**Reviewed by:** an independent agent that did not write this document.

**Checked.** Every `high` gap, every clean-pass deliverable row, every "swept, clean" claim, and every re-derivable figure in this document.

- **Re-executed, not re-read.** `_writes_marker` on six synthetic write shapes (result reproduced); `analyse_source` from the retired `test_orphan_marker_existence_only.py`, recovered via `git show d01edfdf^:…` and loaded as a module, on the **same** six shapes — this is what refuted G1's "handled the first four" clause; `loader_selected_version` from `_plugin_pin_trap.py` over **all 14** marked/unmarked configurations of one, two and three version dirs (the document claimed four), zero divergence from `max(dirs, key=_version_key).name`; both glob patterns under `marketplace/bundles` (386 / 412 / 26 uncovered, identical with and without a `__pycache__` filter).
- **Third mutation (new).** `test_marker_free_resolution.py` confirmed clean (`git diff --quiet` exit 0), byte-snapshotted, `_SOURCE_GLOB` set to `'**/skills/**/scripts-NOPE/*.py'` → **all six tests still passed** (`6 passed in 0.15s`). Restored from the snapshot; `git diff --quiet` exit 0. No `git checkout` / `restore` / `stash` was used. No other file was mutated; the several `doc/plans/**` files modified by other agents were not touched.
- **Sweeps re-run with broader patterns than the original.** `-i` case-insensitive tree-wide sweep for `retention.pin|retention_pin|pollution detect|multi-version pollution|degraded fallback|all-orphaned|structurally impossible|saturat` (found only G2's and G3's sites plus unrelated uses of "structurally impossible" and build-queue saturation); a second sweep for `at least one live|always survives|cannot saturate|never saturate|guarantee.*marker|marker.*guarantee|unconditionally pinned|pinned unconditionally` returned no restatement of the refuted guarantee. A no-filter `orphaned_at` sweep over the whole tree (all file types, not only `*.py`/`*.md`/`*.adoc`) confirms D2: every remaining write is a test fixture.
- **Figures re-derived.** `d01edfdf` = 15 files, +628/−2102 ✓. Test run reproduced: **503 passed**. Preflight return dict = seven keys at `generate_executor.py:2290-2298`, `_PREFLIGHT_FIELDS` at `test_generate_executor.py:1515` ✓. `plan-marshall/SKILL.md:108` says "seven-field" ✓. Guard 4 at `:740`, called at `:1428` ✓. Pre-fix sole writer at `d01edfdf^:generate_executor.py:2399` ✓. `select_live_version_dir` at `marketplace_bundles.py:16-58` ✓. Per-file supersession check re-run — the "only one later commit" figure did **not** re-derive.
- **Not re-checked.** The live-machine observations (`plan.md` labels them unreachable from a clone); PR #1223's CI, reviewer participation and rate-limit bodies; the `./pw verify` whole-tree figure; the red-first evidence at `d01edfdf^` (reproduced by mutation against today's code only, as the original states); the pre-fix per-file diffs of `_doctor_shared.py` and `provisioning-fail-closed-audit.md` beyond confirming their HEAD text; the OpenCode resolver template; `test_preflight_pollution.py`'s deletion beyond confirming it in the commit stat.

| Item | Original claim | Verdict | Evidence |
|---|---|---|---|
| G1 | D6(d)'s `_writes_marker` is far narrower than the retired guard; severity `high` | **upheld, rewritten** | Six-shape table reproduced against the current detector. Severity holds: the template-embedded shape ships into every generated executor and passes green. Rewritten because the retired-test comparison was wrong (see R2) and because the detector's own host file writes markers in an undetected spelling at `:86`/`:95`/`:112`. |
| G1 clause "retired test handled the first four by design" | — | **refuted** | Executed `analyse_source`: caught inline, alias-bound, `open()`, template; **missed** named-constant and `.touch()`. Three shapes lost, not four; two never covered. Moved to `gaps.md` § Refuted during adversarial review, R2. |
| G1 citation `d01edfdf^:…:335-345` | — | **refuted, corrected** | `:335` is inside `_record_attribute_use`. `_assigned_alias` `:338`, `_check_alias_uses` `:348`, `_embedded_code_tree` `:366`, `_template_label` `:387`. R3. |
| G1 Fix "port the three helpers verbatim" | — | **rewritten** | A verbatim port closes alias-bound, `open()` and template only. Named-constant and `.touch()` coverage must be written new. |
| G2 | Three retention-pin statements at `:180`, `:222`, `:226-228` | **upheld** | Grep reproduced: exactly four line hits (`:180`, `:222`, `:226`, `:227`) for `retention.pin`, zero for `_retention_pinned_versions` tree-wide. The module docstring at `:14-19` *was* rewritten to the marker-free model in the same commit, sharpening the contrast. Severity `medium` correct — both tests pass and no behaviour is wrong. |
| G3 | Pin-trap loader model and saturation axis are stale; severity `medium` | **upheld, sharpened** | Exhaustive execution (14 configs) replaces the four-config sample. `git log -S` shows the docstring came from #1213 and was accurate then — residue, not a defect introduced by #1223. Line refs corrected `:269-289` → `:269-287`, module docstring `:2-45` → `:2-44`. Two stale test sites named precisely (`test_plugin_pin_trap.py:268-271`, `:274-277`). |
| G3 Fix "simplify … documents newest-eligible selection" | — | **rewritten** | `VersionDir` carries only `name` and `marked` and cannot evaluate eligibility, so the function models newest-**overall**. Writing "newest-eligible" over a newest-overall body would reintroduce the same class of mismatch. The Fix now requires the approximation to be named. |
| G4 | `_SOURCE_GLOB` misses 26 nested modules; severity `low` | **upheld** | 386 / 412 / 26 re-derived by execution; none of the 26 mentions the marker, so the current PASS is truthful and `low` is right. |
| G5(a) | Keep-rule 3 wrongly equated with the resolver's selection | **upheld** | `cache_retention.py:19-20` confirmed verbatim; `select_live_version_dir` is newest-*eligible*; `test_marker_free_resolution.py:135` fixtures the divergent shape. |
| G5(b) | "advisory only" reads as "we still write it" | **re-grounded** | The inference from the word "advisory" alone is weak — `data-model.md` uses it correctly. Re-grounded on what is actually stale: the "none live" live/marked partition wording D4 deleted, and the absence of any statement naming the foreign GC as sole producer. |
| G5 Fix "mirror the correction in `data-model.md:507`" | — | **refuted, removed** | `data-model.md:511` and `:513` already say exactly what G5 asked to be written. Moved to `gaps.md` § Refuted during adversarial review, R1. |
| D0 row (clean pass) | Ownership and why-baked confirmed | **upheld** | Pre-fix sole writer, sole selector read and template mirror all re-confirmed at `d01edfdf^`; `_plugin_pin_trap.py:64`/`:554` confirmed at HEAD; the four-tier self-heal confirmed by reading `resolve_notation` in the generated executor (tier 1 is existence-guarded, tier 3 is the runtime resolver). |
| D2 row (clean pass) | No production write remains | **upheld** | Re-swept with no file-type filter across the whole tree: the only writes are five test fixtures. |
| D3 row (clean pass) | Decision recorded as not-adopted; `cache_retention.py` unchanged | **upheld** | Absent from `git show --stat d01edfdf` and from `git log d01edfdf..HEAD`. |
| D4 row (clean pass) | Seven deleted symbols, zero hits | **upheld** | Re-swept without file-type filters: zero hits outside `doc/plans/`. `test_preflight_pollution.py` (204 lines) also deleted, visible in the commit stat. |
| D1 row | "absolute paths are still baked (`.plan/execute-script.py`)" | **upheld, evidence corrected** | The cited executor's baked paths are marketplace-layout and carry **no** version segment, so it does not by itself show version-pinned baking. `_check_emitted_path_provenance` (`:740-753`) supplies the real proof: it exists to refuse a multi-version emitted set and no-ops only in the marketplace layout. |
| "Method: only one later commit touches any expected-surface file" | — | **refuted, corrected** | Per-file `git log d01edfdf..HEAD` returns `7cadb986`, `8872700b` (both on `test_generate_executor.py`) and `d2e94b45` (on `tools-script-executor/SKILL.md`). The substantive conclusion — no deliverable superseded — survives. |
| "The guard is **not vacuous**" (§ D6(d)) | — | **refuted** | Empty-glob mutation: all six tests green over a zero-file population. New gap **G6**. |
| Verdict `implemented-with-gaps` | — | **corrected → `partially-implemented`** | D1's headline action ("stop baking absolute version paths") was not performed and the Goal's first clause is unmet. The row itself grades D1 "Implemented: partly / As documented: no". |
| **G6 (new)** | — | **added** | D6(d) has no population non-vacuity floor; the retired test had one (`TestPopulationIsNonVacuous`, `:514-540`; `_population_summary`, `:453`) and it was dropped with the file. Demonstrated by mutation. |

**Documents corrected.** `verification.md`: verdict `implemented-with-gaps` → `partially-implemented` with the reason stated inline; the later-supersession count corrected from one to three with each commit named; the D1 evidence re-anchored from `.plan/execute-script.py` to `_check_emitted_path_provenance`; the D6 row and the D6(d) section corrected to record vacuous satisfiability; the retired-test comparison corrected from "first four" to three-lost/two-never-covered, with the correct symbol line numbers; the mutation-summary line narrowed to the resolver guards; report-accuracy item 1 split into two axes; a paraphrase presented as a source quote (`d01edfdf^:generate_executor.py:557`) replaced with the verbatim line; `SKILL.md` line refs `:770` → `:762`/`:769`, `:580-625` → `:579-625`, template `:477-540` → `:477-539`. `gaps.md`: open items 5 → 6; G1 rewritten around an executed two-detector table with a corrected Fix; G3's line refs corrected, evidence made exhaustive, provenance (#1213) added, and the Fix rewritten to forbid a newest-eligible docstring over a newest-overall body; G4 evidence re-derived and cross-linked to G6; G5 narrowed — the `data-model.md` mirror removed, (b) re-grounded; **G6 added**; a `## Refuted during adversarial review` section added carrying R1–R3.

**Residual doubt — what a third reviewer should look at first.**

1. **The verdict call.** `partially-implemented` versus `implemented-with-gaps` turns on whether D1's *Done when* passing outweighs its headline action not landing. The Done-when passes, the plan's Goal clause does not, and D0 arguably licensed the re-scope by finding the bake is a fast path. A reviewer who weighs Done-when above the Goal sentence would keep the original verdict; the reasoning is on the table either way.
2. **G1's `high`.** No marker write exists anywhere in production today, so both G1 and G6 are latent guard weaknesses rather than shipped false signals. `high` rests on the template-embedded shape being the one that reaches every user machine. A reviewer could defensibly rate both `medium`.
3. **The OpenCode resolver template** (`_OPENCODE_RESOLVER_TEMPLATE`, `generate_executor.py:546+`) was not examined for marker consultation or for its own version-selection policy; the D6 suite covers only the Claude target.
4. **Whether `_plugin_pin_trap.py`'s saturation shape should be demoted or deleted.** G3 proposes demotion. If the loader can no longer be affected by the marker at all, the honest move may be to drop `SHAPE_1_SATURATION` from the load-safety oracle entirely and keep it only as a GC-hygiene observation — a judgement this review did not make for the sibling plan's owner.
