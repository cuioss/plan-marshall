id=2026-07-21-15-002
component=plan-marshall:persona-module-tester
category=improvement
status=active
created=2026-07-21

# Fail-closed guard promotion owed to persona-module-tester (deferred post-merge)

## Context

Plan `maven-run-truthful-status` (PR #963, merged) generated lesson `2026-07-21-14-002`: a fail-closed truthfulness guard wired to a constructed/defaulted value (`success_result(exit_code=0)` default, or `dict.get('exit_code', 0)`) is tautologically green and inert in production — a guard MUST be fed the observed value and MUST be proven able to fail with a test.

During finalize, `project:finalize-step-lessons-housekeeping` promoted that lesson's durable rule into `marketplace/bundles/plan-marshall/skills/persona-module-tester/standards/testing-methodology.md` (a new "Prove a Fail-Closed Guard Can Actually Fail" section) and retired the lesson.

## Directive

Re-apply the promotion: add a "Prove a Fail-Closed Guard Can Actually Fail" section to `persona-module-tester/standards/testing-methodology.md` capturing: (1) feed a fail-closed guard the observed value, never a constructor default; (2) prove the guard can fail with a dedicated regression test; (3) watch the `dict.get(key, default)` present-but-`None` trap (the default only applies when the key is absent).

## Why this is owed

The promotion edit could not land: our plan's execution manifest was composed pre-merge and ordered `finalize-step-lessons-housekeeping` AFTER `branch-cleanup` (post-merge), so the promotion edit landed on the main working tree after PR #963 was already merged and its branch deleted — unpushable, and direct-to-main push is forbidden. The concurrent plan `lessons-housekeeping-plugin-doctor-contract` fixed this ordering on main (moving the step into the pre-push settle band). This follow-up lesson is the explicit artifact required by `phase-6-finalize/standards/source-edit-pushability.md` (a post-merge source edit MUST emit a follow-up, never silently revert). The stranded working-tree edit was reverted; re-run this promotion via a normal plan/PR now that the ordering is fixed.
