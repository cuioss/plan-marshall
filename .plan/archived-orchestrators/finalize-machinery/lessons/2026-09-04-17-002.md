# 2026-09-04-17-002 — Recognise a size-cap refusal at FIND time instead of after the operator reads it

component: plan-marshall:automatic-review
category: bug
status: moved-to-epic (OPEN — registry-coverage future work; corpus original retained)
source: corpus lesson 2026-09-04-17-002, copied into finalize-machinery per operator direction

## Context

515-file plan: both size-capped bots refused, NEITHER recognised at FIND time (wordings
hand-registered mid-run); cuioss-review-bot triggered but never answered; operator
closed the gap with a merge authorization (`refusal_structural`), re-granted after a
rebase lapsed it.

## Root cause

Registry matches by literal registration; unseen wording reads as ordinary content, so
completeness classifies the run as covered until a human reads the comment. Two bots in
one run ⇒ registry-coverage problem, not wording drift.

## Proposed action

Treat unmatched comments from registered size-capped bots as REFUSAL CANDIDATES surfaced
for classification; make registration a script verb appending observed wordings (recorded
call, not hand edit in the plan diff).

## Epic status

Open future work. Corpus original RETAINED.
