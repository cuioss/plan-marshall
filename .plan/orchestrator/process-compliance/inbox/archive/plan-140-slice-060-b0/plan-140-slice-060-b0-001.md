envelope_version=1
sender_type=plan
sender_id=plan-140-slice-060-b0
epic=process-compliance
kind=finding
created=2026-09-20T21:27:24Z

## Process-rule gap: no compliant read path for queued orchestrator inbox bodies

Executing PLAN-140 run 3 slice 060 B0 (`/plan-marshall task="implement PLAN-140 run 3 slice 060 B0 ..."`).

Verified via compliant scripts:

- `orchestrator inbox list --slug test-quality` reports `inbox_state: present`, `count: 2`, live messages `plan-140-run-3-001.md` and `plan-140-run-3-002.md` (both `valid: true`).
- `orchestrator inbox list --slug process-compliance` reports `inbox_state: present`, `count: 65`.

Gap: no `manage-*` / `orchestrator inbox` verb exposes the queued message **body** through `.plan/execute-script.py`:

- `inbox list` returns header rows only (name, sender_id, kind, created, lifecycle, revision, consumption, valid, error).
- `inbox validate --slug test-quality --message plan-140-run-3-001.md` returns envelope headers only.
- `inbox read --slug test-quality --plan-id ...` reads only the delivered mailbox `inbox/to/{plan-id}/`, not the queued `inbox/*.md`.
- `manage-files read` resolves only the `plans/{plan-id}/` store, not the `orchestrator/{slug}/inbox/` store.
- `manage-files discover --root <abs> --glob ...` lists paths but returns no content.

Direct `Read`/`cat` of `.plan/local/orchestrator/test-quality/inbox/plan-140-run-3-001.md` and `plan-140-run-3-002.md` would violate the `.plan/ access via scripts only` hard rule, so the D1 diagnosis (`66 vs 55/53`) and the batch map for slice 060 B0 were not recovered compliantly.

Impact: slice 060 B0 scope is known from the task string (`test/plan-marshall/{extension-api,lsp-client,manage-files,manage-logging,manage-providers,platform-runtime,ref-toon-format,script-shared,tools-file-ops,tools-input-validation,tools-permission-doctor,tools-permission-fix,tools-script-executor}/`, mechanical only, per-source fixture hoist + preamble normalization, no splits), but the exact 66-module list and per-batch assignment from `plan-140-run-3-002` could not be confirmed. Proceeding best-effort from the `doctor-marketplace test-conventions` derivation instead.

Request: add a compliant queued-message body read verb (e.g. `orchestrator inbox show --slug S --message M`), or document the sanctioned alternative, so future slices can ground the batch map without a rule breach.
