id=2026-09-04-08-007
component=plan-marshall:phase-5-execute
category=bug
status=active
created=2026-09-04

# Emit an [OUTCOME] line for every completed task, including on re-dispatch

**Carried out of the `review-apparatus` epic inbox** — message `review-packs-become-published-artifacts-005.md`, sender `review-packs-become-published-artifacts`, promoted by the orchestrator drain of 2026-09-04.

## Context

The plan completed 11 tasks. `work.log` carries only 10 `[OUTCOME] (plan-marshall:phase-5-execute) Completed TASK-NNN` lines. TASK-010 ("Separate two direct submits that differ only in explicit --timeout") reached `status: done` with `4/4` steps and emitted no outcome line.

A second, independent check in the same fact extractor — `outcome_for_diffed_tasks` — names the same task under `tasks_with_diff_no_outcome`. So TASK-010 produced a non-empty diff and is not a compliant no-op whose silence would be expected.

## Root cause

This plan re-entered 5-execute four times (`close_count: 4`) and the recorded 5-execute dispatch terminated with `termination_cause: voluntary_checkpoint`. That is the exact condition lesson `2026-05-08-14-001` describes: the outcome line is lost when the agent re-dispatches itself, because the emitting step sits after the checkpoint boundary in the task loop.

The gap is self-concealing. `outcome_pairing` reports `paired: 10` and `unpaired_outcome: []`, which reads as healthy unless the reader compares against the 11 completed tasks rather than against the 10 paired lines.

## Proposed action

Emit the `[OUTCOME]` line before the voluntary-checkpoint yield can be taken, not after the task loop's continuation point, so a re-dispatch cannot swallow it.

Add an end-of-phase reconciliation that compares the `[OUTCOME]` line count against the count of tasks in `status: done` and fails loudly on a shortfall, rather than leaving the discrepancy to be found retrospectively.

## Evidence

- aspect: logging_gap_analysis — `OUTCOME_COVERAGE` expected 11, observed 10
- aspect: log_analysis — `phase5_logging_gaps.outcome_pairing.unpaired_completed: [TASK-010]` and `outcome_for_diffed_tasks.tasks_with_diff_no_outcome: [TASK-010]`, two independent checks naming the same task
- `metrics.toon` — 5-execute `close_count: 4`, `re_entered_phases: 5-execute`
- dispatch boundary — 5-execute row `termination_cause: voluntary_checkpoint`
- Recurrence of lesson 2026-05-08-14-001

## Recurrence — 2026-09-05 (shipped-guards-assume-the-meta-projects-own-layout)

Same shape, same two independent checks, same self-concealing arithmetic — one plan later.

12 tasks reached `status: done`; `work.log` carries 11 `[OUTCOME] Completed TASK-NNN` lines.
`outcome_pairing.unpaired_completed` names **TASK-004**, and `outcome_for_diffed_tasks.
tasks_with_diff_no_outcome` independently names the same task, so it produced a non-empty
diff and is not a compliant no-op.

The concealment recurs verbatim: `outcome_pairing` reports `paired: 11` with
`unpaired_outcome: []`, which reads healthy against the 11 paired lines and only fails
against the 12 completed tasks. The end-of-phase reconciliation this lesson asks for still
does not exist, so the shortfall was again found retrospectively rather than at the phase
boundary.

Conditions matched the stated root cause: 5-execute was re-entered (`close_count: 2`,
`re_entered_phases: 5-execute`) and 3 of its 4 dispatch-boundary rows terminated with
`termination_cause: voluntary_checkpoint` — a 75 % agent-initiated-re-dispatch share.

One addition this run supplies. A sibling gap sits beside it and no rule detects it: the
dispatch audit records **6** `phase-5-execute` `[DISPATCH]` lines against only **3**
entry/re-entry status markers (1 `Starting` + 2 `Re-entering`). `RE_ENTRY_COVERAGE` passes
(2 inferred re-entries, 2 markers) because it clusters the status lines it can see, so the
three dispatches that emitted no marker are outside the population it measures. The
reconciliation this lesson proposes should therefore count `[OUTCOME]` lines against
completed tasks **and** entry markers against `[DISPATCH]` lines — the second comparison is
the one that would have caught the missing three.

Evidence: aspect `log_analysis` `phase5_logging_gaps`; aspect
`execution_context_dispatch_audit` `by_role` (`phase-5-execute` dispatch_lines=6);
`work/metrics-dispatch-boundaries-5-execute.toon`.

## Recurrence — 2026-09-15 (architecture-store-query-truthfulness, PLAN-CIS-049, PR #1489)

⭐⭐ **Third recorded instance, and the largest shortfall yet: 44 tasks done, 40 `[OUTCOME]` lines — FOUR missing** (TASK-024, TASK-025, TASK-026, TASK-041), against one missing in each of the two prior instances. Carried out of the `code-intelligence-substrate` epic inbox (message `architecture-store-query-truthfulness-002.md`) and folded here as a recurrence rather than filed as a new lesson.

The exposing condition is the same and is now quantified: **18 execute dispatches across 3 phase re-entries**, with termination causes `voluntary_checkpoint` 8, `budget_yield` 5, `baseline_drift` 3, `error` 2. ⭐ Note `baseline_drift` — a third yield cause this lesson had not previously named, so the gap is not specific to `voluntary_checkpoint`.

Both independent checks fired again, and this time at `error` severity: `OUTCOME_COVERAGE expected_min 44, observed 40`, and the same four tasks named in **both** `unpaired_completed` and `tasks_with_diff_no_outcome`.

⇒ The proposed action is unchanged and now three-times evidenced: **drive the emission from the status write, not from the envelope's completion path** — or reconcile at execute-phase exit and name the reconciliation as the line's source. The self-concealing arithmetic is unchanged too: `paired: 40` reads healthy against any denominator but the true task count.
