# 2026-09-03-11-003 — Classify executor invalid_invocation rejections from stdout, not stderr alone

component: plan-marshall:plan-retrospective
category: improvement
status: moved-to-epic (OPEN — classifier-channel future work; corpus original retained)
source: corpus lesson 2026-09-03-11-003, copied into finalize-machinery per operator direction
(note: full body retrieved 2026-09-17; condensed here — second failure mode: three
`status: error`-under-exit-0 verbs never entered the population at all, because the log
line records notation + duration and no status)

## Context

`script-failure-analysis` matches stderr against three argparse signatures — but the
executor's pre-flight guard rejects before argparse and diagnoses on stdout, so precise
subtypes never fire and everything buckets residual. Wider: exit-0 error-status calls
are invisible to an exit-keyed classifier.

## Proposed action

Read the stdout block (map guard `reason` → subtype; surface `accepted:`); record child
TOON `status`/`error` in the script-execution.log line; treat `status: error` as failure
regardless of exit code.

## Epic status

Open future work (lessons-pipeline theme, PLAN-05 adjacent). Corpus original RETAINED.
