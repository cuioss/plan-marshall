id=2026-09-18-11-001
component=pm-dev-python:pytest-testing
category=improvement
status=active
created=2026-09-18

# Carve oversized mechanical-split PRs by behaviour cluster into independently-mergeable PRs

# Candidate lesson — slice-040 carve strategy for oversized test-split PRs

Source plan: module-budget-campaign-run-2-slice-040 (epic test-quality).

Observation: a 225-file test-split PR (#1513) exceeded the CodeRabbit 100-file
review cap and could not land. Splitting the same byte-for-byte behaviour-cluster
carves into 9 scoped PRs (#1514, #1515, #1526, #1517-#1522), each independently
CI-green, landed the full footprint with no content change.

Candidate rule: when a mechanical split PR exceeds the reviewer's file cap,
carve by behaviour cluster into independently-mergeable PRs rather than forcing
the monolith through; prove losslessness per carve with fidelity multisets.

Signal provenance: plan-level outcome (9/9 carves merged, 2 oversized closed),
loop_back ceiling 3/3 reached before the pivot.
