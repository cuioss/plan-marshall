# 2026-09-03-19-007 — Batch fragment registration instead of one collect-fragments add per aspect

component: plan-marshall:plan-retrospective
category: improvement
status: moved-to-epic (OPEN — retrospective cost adjacent to PLAN-05; corpus original retained)
source: corpus lesson 2026-09-03-19-007, copied into finalize-machinery per operator direction

## Context

One retrospective registered 17 aspect fragments with 17 separate `collect-fragments
add` calls plus 9 byte-identical capture commands — ~26 Bash calls fully determined by
data the system already holds (`retro_sections.py SECTION_SPEC` + extension-aspect
registry).

## Proposed action

`collect-fragments add-batch --fragment-dir` (derive aspect key from filename);
`run-deterministic-aspects` driver walking SECTION_SPEC + domain rows. Removes hand-typed
key divergence class too.

## Epic status

Open future work adjacent to PLAN-05's retrospective scope. Corpus original RETAINED.
