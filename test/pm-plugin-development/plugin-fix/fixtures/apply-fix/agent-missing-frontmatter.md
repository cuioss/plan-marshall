# Agent Without Frontmatter

This agent file has no YAML frontmatter at all.

## Purpose

Does something useful but is missing required metadata.

## Workflow

1. Step one
2. Step two
3. Step three

## Output

Returns results to the caller.
