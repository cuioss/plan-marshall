[project]
name = "sample-api"
version = "1.0.0"
description = "Depends on sample-core, plus one external distribution."
requires-python = ">=3.11"
dependencies = [
    "sample-core>=1.0.0",
    "requests>=2.0.0",
]
