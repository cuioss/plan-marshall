# Verification — 330-post-run-guard-exempts-every-tracked-plan-file

**Verified against:** commit `2402b02bf5bc64b5ece468b6d2a3e884b5f0b30d`   **Landed as:** PR #1217, commit `77fd11564bacc3953176ded488ebf8ed4d7f1fc1`   **Verdict:** implemented-with-gaps

## Method

What was actually done, in order:

- Read `plan.md` and `report-01.md` in full; extracted the six deliverables, their *Done when* conditions, the out-of-scope boundaries, the claim-label table and the Verification section.
- Located the landed commit (`git log --oneline --all --grep '#1217'` → `77fd1156`, squash-merge) and read its stat and per-file diffs (`git show 77fd1156 -- <path>` for `_invariants.py`, `_plan_parsing.py`, `decision-rules.md`, `SKILL.md`, `worktree-handling.md`, `lessons-capture.md`, `finalize-step-preference-emitter.md`).
- Opened at HEAD: `script-shared/scripts/_plan_state_exemption.py` (whole file), `phase-6-finalize/scripts/post_run_source_guard.py` (whole file), `plan-marshall/scripts/_invariants.py` §§ `_filter_main_dirty_paths` / `_capture_main_dirty_files`, `plan-marshall/scripts/_git_helpers.py::git_dirty_files`, `plan-marshall/scripts/_handshake_commands.py::_check_main_dirty_drift`, `_handshake_store.py` field comments, `manage-execution-manifest/scripts/_manifest_rules.py` (`_apply_code_step_inactive`, `_apply_security_class_inactive`), `manage-execution-manifest/standards/decision-rules.md`, `phase-6-finalize/SKILL.md` item 5f, `plan-retrospective/scripts/_footprint_classification.py`, `extension-api/scripts/_path_attribution_merge.py`.
- Re-derived the D0 premise at HEAD: `git ls-files .plan/` → **13** paths (`.plan/marshal.json`, `.plan/project-architecture/_project.json`, eleven `…/{module}/enriched.json`). Matches the report exactly.
- Re-derived the pre-fix symbol/line citations from the parent commit (`git show 77fd1156^:…`): `_PLAN_STATE_PREFIX` at L91, `filter_tracked_source` at L138 with the prefix drop at L148, `--untracked-files=no` at L179, `_invariants._filter_main_dirty_paths` at L458. Every citation in the report is exact.
- Ran the plan's tests: `uv run python -m pytest test/plan-marshall/script-shared/test_plan_state_exemption.py test/plan-marshall/phase-6-finalize/test_post_run_review_ordering.py -o addopts="" -q` → **38 passed**; `test/plan-marshall/plan-marshall/test_invariants.py` → **117 passed**; `test_phase_handshake_worktree_assertion.py` + `test_worktree_contract_e2e.py` → **31 passed**.
- **Executed** the guard against a purpose-built real repository (tracked `src.md` + tracked `.plan/marshal.json` committed, then `marshal.json` dirtied and an untracked `.plan/local/status.json` written): `_observe_dirty_source` returned `(False, ['.plan/marshal.json'], [], ['.plan/marshal.json'], None)` — the tracked `.plan/` file is reported, the untracked one never enters the population.
- **Mutation check** (highest-risk guard): snapshotted `_plan_state_exemption.py` bytes to the scratchpad (md5 `0abc15a3…`), verified `git diff --quiet` returned 0 first (no concurrent agent edit), then re-introduced the old defect by forcing the retain branch dead (`elif tracked is None or path in tracked:` → `elif False:`). Result: **7 failed, 148 passed** — RED at both confirmed sites (`test_dirty_tracked_plan_state_is_reported`, `test_capture_main_dirty_files_reports_tracked_plan_state`) plus four shared-predicate tests and the mixed-population test. Restored from the saved bytes (md5 re-matched, `git diff --quiet` exit 0). No `git checkout`/`restore`/`stash` used.
- **Executed** a second live probe on the same repo with a tracked, non-ASCII-named `.plan/` file, which exposed a false-clean at site 2 (see G2 in `gaps.md`).
- Beyond-diff sweeps at HEAD: `startswith('.plan/')` / `_PLAN_STATE_PREFIX` / `_BOOKKEEPING_PREFIXES` across all `*.py`; `outside \`.plan/\`` across `marketplace/`, `doc/`, `test/`, `.claude/`; `post_run_review` across all bundle docs; `filter_tracked_source` (removed symbol) across the tree; `affected_files` consumers across bundles.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D0 | Classify the whole exemption population; re-derive tracked `.plan/` set | Every site classified + published; tracked set re-derived | yes | yes | yes | yes | `git ls-files .plan/` → 13 paths at HEAD (report says 13). Pre-fix citations exact (`77fd1156^` L91/L138/L148/L179; `_invariants.py` L458). Classification of #3–#5 independently re-checked: `_path_attribution_merge.py` has `.plan` only inside docstring examples (L92, L359 — no executable prefix drop); the two retrospective checks operate on a committed diff |
| D1 | Shared trackedness predicate at every confirmed site | Every confirmed site uses ONE predicate | yes | yes | yes | yes | `script-shared/scripts/_plan_state_exemption.py:89` `partition_plan_state_exemption`; imported at `post_run_source_guard.py:100` (used at `:201`) and `_invariants.py:640`. Mutation: killing the retain branch turns both sites' positive controls RED |
| D2 | Publish the examined population | `clean: true` distinguishable from a looked-at-nothing pass | partial | yes for site 1 | yes | **no** | `post_run_source_guard.py:222-230` emits `considered_paths`/`exempted_paths`/`offending_paths`; live run returned `considered=['.plan/marshal.json']`. Site 2 (`_invariants._capture_main_dirty_files:679`) returns only the retained list, and `_handshake_store.py:35` persists only `main_dirty_files` — no considered/exempted population (**G3**) |
| D3 | Fix the declared footprint at its freeze point (or teach every consumer) | One of the two implemented and the choice recorded | yes (option B, doc-only) | yes | n/a (no code change) | **no** | `_plan_parsing.py::_extract_affected_files` docstring names the freeze point; `decision-rules.md` § "Declared surface vs. live footprint" states the rule. But the same file's § Pre-Filter `simplify_inactive` (L209-217) and `_manifest_rules.py:298 _apply_code_step_inactive` still drop a step on `affected_files_count == 0` alone (**G4**). The doc's claim about `security_class_inactive` was checked and is true (`_manifest_rules.py:385`) |
| D4 | Decide/document the disposition for a legitimately-dirty tracked `.plan/` file | A stated remedy exists, or a recorded proposal | yes | yes | yes | yes | `phase-6-finalize/SKILL.md:1214` carries the disposition; its order claims re-derived from frontmatter: `architecture-refresh` order 10, `branch-cleanup` 70, `finalize-step-preference-emitter` 992 — all confirmed. Durable remedy recorded as a proposal in report-01.md § "Contract / lifecycle proposal (D4)", which the plan explicitly sanctions |
| D5 | Tests (a)-(d), each seen red pre-fix | All four pass, each seen red first | yes | mostly (see below) | yes | yes for the two confirmed sites | 38 + 117 + 31 tests pass at HEAD. Mutation reproduces RED at both sites. (b) negative controls are green-by-construction — the report says so plainly and the plan's own Verification section frames (b) as a must-stay-green control |

**D2.** Only the post-run guard publishes a population. The layer-D capture — the other site D1 fixed — persists `main_dirty_files` (retained paths only) on the handshake row; an empty list there cannot be told from "everything was exempted". `_capture_main_dirty_files` does distinguish *not applicable* (`None` on an unresolvable root or failed probe) from *clean* (`[]`), so the pass is not wholly opaque, but the exempted population the guard declined to count is invisible. See G3.

**D3.** The choice (option B) is recorded and the freeze point is named by symbol, so the deliverable's literal done-when is met. The gap is in the reach of option B: `decision-rules.md`'s new subsection says a gate "MUST NOT treat the declaration as ground truth for what changed", while two sections further down the same document — and the code it documents — do exactly that for `finalize-step-simplify`. See G4.

**D5(b).** The plan's D5 header says "each verified to FAIL pre-fix" and its done-when says "all four pass, each seen red first"; the plan's own Verification section then frames D5(b) as the control that *guards the over-broad fix*, i.e. one that must stay green. The run took the second reading and said so explicitly. Not counted as a gap — the plan is self-inconsistent here and the report resolved it transparently.

## Report accuracy

Re-derived, and contradicted:

- **"round-3 verdict clean … no remaining stale statement of the old guard/layer-D … behavior in any current-state prose"** — contradicted. `phase-6-finalize/standards/emit-landing.md:216-219` still reads "reports any dirty TRACKED path outside `.plan/` as a non-blocking WARNING plus a finding" and "This step writes only under `.plan/`". That paragraph is the same item-5f restatement family the run *did* fix in `lessons-capture.md` and `finalize-step-preference-emitter.md`, it predates the plan (introduced by `5a5446d3`, PR #1215, an ancestor of `77fd1156`), and the report's own round-3 sentence ("Every surviving 'outside `.plan/`' occurrence is a correct description of the non-`.plan/` tracked-source control or an explicit contrast") does not hold for it. See G1.
- **"Total: six stale-restatement findings … plus one borderline premise — all seven fixed"** — the seven named fixes are all present and truthful in the tree (checked each: `worktree-handling.md:231` and `:27`, `test_post_run_review_ordering.py`, `_handshake_store.py:25`, `_handshake_commands.py:17`, `phase-handshake.md:127`, `lessons-capture.md:24`, `test_invariants.py::test_capture_main_dirty_files_only_untracked_dot_plan_paths_returns_empty_list`, `test_phase_handshake_worktree_assertion.py`, `test_worktree_contract_e2e.py:16`). The count of *fixes* is accurate; the claim of *exhaustive* coverage is not (G1).

No contradiction found in, having checked each: the 13-file tracked set; the pre-fix line/symbol citations (L91/L138/L148/L179, L458); the D0 classification of all six sites; the single-predicate claim; the `considered_paths`/`exempted_paths`/`offending_paths` field set and the explanation that `exempted_paths` is structurally empty at site 1 (`--untracked-files=no` means git has already dropped untracked paths — confirmed by executing the guard); the D4 order numbers (10 / 70 / 992); the D3 statement about `security_class_inactive`'s two-legged gate; the removal of `filter_tracked_source` leaving no dangling consumer anywhere in the tree.

## Out-of-scope compliance

Compliant. The three declared boundaries hold in the landed diff: `gitignore_setup.py` is untouched; nothing about *what or when* architecture-enrich writes changed; no consumer gate was taught to re-derive its own scope (D3 went the documentation route instead, as declared).

Collateral beyond the plan's "Expected surface" list, all of it consequential rather than undeclared drift: the new `script-shared/scripts/_plan_state_exemption.py` (required by D1's shared-predicate mandate), `manage-execution-manifest/standards/decision-rules.md` (the D3 option-B landing place), and the seven sweep-driven doc/comment/test corrections (`worktree-handling.md`, `phase-handshake.md`, `_handshake_store.py`, `_handshake_commands.py`, `lessons-capture.md`, `finalize-step-preference-emitter.md`, three test files outside `test/plan-marshall/phase-6-finalize/**`). Noted, not faulted — though it is itself an instance of the declared-vs-actual footprint gap D3 describes.

## Residue carried forward

- **D4 durable remedy is an open proposal** — still open. `SKILL.md:1214` still records the disposition as advisory and points at the plan report for the three options; no push-path or finding-classification change has landed since.
- **Two external reviewers rate-limited** — a PR-time condition; not re-checkable from the tree and not re-requested (no later commit references it).
- **Local plugin-cache sync owed on a developer machine** — machine-local by construction; correctly not a debt of the cloud run (CLAUDE.md § Standalone Plan Lane).

## What could NOT be verified

- The branch-level commits the report cites (`4d882c5`, `3e415f8`, `ad40b0f`) are absent from this clone — the PR was squash-merged and the branch deleted (`git cat-file -t` finds none of the three). Only the squash `77fd1156` is verifiable.
- The `./pw verify` figure ("19515 passed, 14 skipped") and the per-commit quality-gate claims — not re-run here (full verify is out of proportion to this check); the targeted suites that matter to this plan were run and pass.
- The CI-context and reviewer-participation tables (PR #1217 checks, `coderabbitai` / `sourcery-ai` / `cuioss-review-bot` verdicts, comment ids) — GitHub-side state, not derivable from the tree.
- The motivating live instance ("architecture descriptors dirty on main after a finalize") — machine state under git-ignored `.plan/local/`, unreachable from any clone, as the plan itself flags.
- The token/wall-clock figures in § Cost — self-reported harness data with no tree artifact.
