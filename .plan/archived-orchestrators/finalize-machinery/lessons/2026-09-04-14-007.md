# 2026-09-04-14-007 — references.json carries no plan_creation_sha, so scope_creep_check compares nothing

component: plan-marshall:manage-references
category: bug
status: moved-to-epic (OPEN — baseline-anchor future work; corpus original retained)
source: corpus lesson 2026-09-04-14-007, copied into finalize-machinery per operator direction
(note: full body retrieved 2026-09-17; condensed here — three recurrences, growing cost)

## Context

`scope_creep_check` → `could_not_look`/`no_baseline_sha`: `plan_creation_sha` never
written (though handshake `main_sha` captured). Unread discriminator = fail-open
(couldn't-look reporting as passed). Recurrences: manifest cross-check starved
(4/5 checks skip); operator-authorised boundary widening unquantified — exactly where a
baseline diff was most needed. Adjacent gap: `affected_files` understated (2 vs 3).

## Proposed action

Persist `plan_creation_sha` at 1-init (copy of handshake `main_sha`); render
`could_not_look` visibly; append realized footprint to `affected_files`.

## Epic status

Open future work (references theme). Corpus original RETAINED.
