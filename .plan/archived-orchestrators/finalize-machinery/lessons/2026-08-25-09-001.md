# 2026-08-25-09-001 — Capture the realized footprint at branch-cleanup before the worktree is removed

component: plan-marshall:phase-6-finalize
category: bug
status: moved-to-epic (OPEN — capture-timing future work; corpus original retained)
source: corpus lesson 2026-08-25-09-001, copied into finalize-machinery per operator direction
(note: full body retrieved 2026-09-17; condensed here — three recurrences)

## Context

Five consumers asked for the realized footprint; none could answer — every resolver tier
depends on state branch-cleanup destroys (worktree, branch; squash-merge commit has no
second parent); `references.modified_files` never written, only declared
`affected_files`. Recurrences: hand-derived footprints 3 runs straight; then a mid-
finalize ad-hoc capture (27 paths) invalidated by 3 loop-back HEAD advances — missing 3
landed files, carrying unlanded uv.lock; equal-size-but-disjoint sets (29 vs 29) that
only symmetric-difference comparison (`reconcile-scope`, never called) detects.

## Proposed action

Persist the realized footprint at branch-cleanup (last point before removal); re-run
after every loop-back HEAD advance; gate the merge on empty-symmetric-difference
`reconcile-scope`; add the squash-merge commit as a resolver tier (repairs archived
plans too).

## Epic status

Open future work (branch-cleanup/footprint theme). Corpus original RETAINED.
