id=2026-09-02-13-005
component=plan-marshall:phase-6-finalize
category=improvement
status=active
created=2026-09-02

# [VERIFY] emission is effectively absent across the finalize lane

# [VERIFY] emission is effectively absent across the finalize lane

## Context

The plan's `work.log` carries **one** `[VERIFY]` entry for the whole run, against at least six verification events that demonstrably executed:

- three phase-5 verification steps (`verify:quality-gate`, `verify:module-tests`, `verify:coverage` — all three recorded by `record-step`);
- `pre-push-quality-gate`, `firing_count: 2`;
- `project:finalize-step-plugin-doctor`, `firing_count: 2`;
- `ci-verify`, one firing.

`references/logging-gap-analysis.md` sets `expected_min` for the VERIFY category at 6 and calls `observed / expected_min < 0.5` a warning. Observed here is `0.17`.

By contrast the other categories are healthy or better: `STATUS` 51, `DECISION` 125, `STEP` 44, `SKILL` 34, `DISPATCH` 20, `ARTIFACT` 14, over 329 work entries.

## Root cause

Verification steps record their outcome through `mark-step-done` (`display_detail`) and, for phase-5 steps, through `manage-execution-manifest record-step` — both of which are state writes rather than log emissions. Nothing in the finalize lane emits `[VERIFY]` on a build/gate/doctor run, so the work log has no verification-shaped trail at all.

The gap matters here specifically because verification is the most re-fired work in this lane: `pre-push-quality-gate` and `plugin-doctor` each fired twice, `verify:coverage` was attempted inline, killed at 388s, and re-run at orchestrator tier. Reconstructing that sequence required joining `status.json`, `decision.log`, `execution.toon` and two dispatch-boundary files, because the one log that is meant to narrate it says nothing.

## Proposed action

Emit one `[VERIFY] (plan-marshall:{caller}) {command} -> {verdict}` work-log line at each verification boundary: the phase-5 verification steps, `pre-push-quality-gate`, `ci-verify`, and any project finalize step that runs a gate. Carry the verdict and the re-fire ordinal, so a re-fired gate is legible without opening `status.metadata.phase_steps`.

This is cheap and it is the channel the retrospective's own expected-pattern table already assumes exists.

## Evidence

- aspect: logging_gap_analysis — `VERIFY, expected_min 6, observed 1` (ratio 0.17)
- aspect: log_analysis — `top_tags`: STATUS 51, STEP 44, SKILL 34, DISPATCH 20, ARTIFACT 14; VERIFY absent from the top five
- `status.metadata.phase_steps` — `pre-push-quality-gate` and `project:finalize-step-plugin-doctor` each `firing_count: 2`
- decision `35b0d6` — the `verify:coverage` inline kill and orchestrator-tier re-run, recoverable only from the decision log

## Recurrence — 2026-09-03 (user-language-and-vocabulary, PR #1382)

Second observation. Zero `[VERIFY]` entries in a 538-entry work log; the top five tags are STATUS 93, SKILL 60, STEP 58, ARTIFACT 30, DISPATCH 19. Verification demonstrably ran: three phase-5 verification steps (`verify:quality-gate`, `verify:module-tests`, `verify:coverage`), four `pre-push-quality-gate` firings and three `ci-verify` firings.

Filed by the plan as a batched recurrence observation and folded here by the orchestrator (`operator-ux` epic) rather than raised as a new lesson. Retained by `finalize-step-lessons-housekeeping` on 2026-09-02 as "untouched by this plan. No coverage." — correct at the time, and it reproduced during the same run.

## Recurrence — 2026-09-03 (plan-150-close-the-namespace-conversion, PR #1383)

Third observation, and the first where the surrounding log is otherwise rich — so this is not
a quiet run masking the gap.

Three `[VERIFY]` entries in a 773-entry work log, against 8 deliverables that each declare a
verification command and explicit success criteria, plus three manifest-declared
`phase_5.verification_steps`. Ratio 0.38, below the 0.5 warning floor. The tag census:
`[MANAGE-STATUS]` 198, `[BUILD-SERVER]` 107, `[STATUS]` 67, `[MANAGE-TASKS]` 55,
`[ARTIFACT]` 42, `[SKILL]` 41, `[STEP]` 41, `[DISPATCH]` 19, `[OUTCOME]` 14, `[ATTEMPT]` 13
— and `[VERIFY]` 3, the second-rarest tag after `[BLOCKED]` 1.

Verification unambiguously ran and was green: whole-tree verify passed with 23,739 tests, the
pre-push quality gate was green on both the scoped bundle and the whole tree, and every
deliverable's criteria were met. **The strongest evidence the plan produced is the least
visible in its own log.**

This observation adds a structural root cause the earlier ones did not name: `[VERIFY]`
emission is not wired to the per-deliverable verification path the way `[ARTIFACT]` and
`[OUTCOME]` are wired to task completion. Those two pair exactly — 9 of 9 outcomes paired,
0 unpaired in either direction, and `RE_ENTRY_COVERAGE` pairs 2 of 2 — so the guarded
categories are complete and the unguarded one is not. The fix is therefore a guard, not more
diligence at call sites.

Folded here by the orchestrator (`test-quality` epic) from inbox message
`plan-150-close-the-namespace-conversion-006.md` rather than raised as a new lesson.

## Recurrence — 2026-09-04 (domain-glob-seeding, PR #1406)

Fourth observation, and the first at **exactly zero** in a log this large.

`[VERIFY]` count: **0**, in a 416-entry work log. Tag census: `[STATUS]` 65, `[STEP]` 39,
`[SKILL]` 30, `[ARTIFACT]` 19, `[DISPATCH]` 19 — `[VERIFY]` does not appear at any rank.

Verification ran, repeatedly and expensively:

- three manifest-declared `phase_5.verification_steps` (`verify:quality-gate`,
  `verify:module-tests`, `verify:coverage`);
- `pre-push-quality-gate`, `firing_count: 2` — the second firing triggered by
  `verdict_currency=invalidated` (recorded `4e85bef2` != live `047b723d`), and it gated 10
  bundles plus the whole tree, test-compile and module-tests;
- `project:finalize-step-plugin-doctor`, 37 rules over 14 scoped skill dirs, clean;
- `ci-verify`, all checks green.

The cost is measurable: `pyproject_build` is the plan's largest cost line at 60 calls and
4,077,340 ms — **51.2% of all script time**. Half the run's script time went into work that
emitted not one line on the channel meant to narrate it.

This observation also supplies the **counter-example that makes the guard argument concrete**.
On this plan the guarded categories are again complete — `OUTCOME_COVERAGE` pairs 7 of 7,
`RE_ENTRY_COVERAGE` pairs 2 of 2 — while the unguarded `[VERIFY]` is at zero for the fourth
consecutive run. Four plans, four different shapes of work, one invariant outcome: guarded
categories hold, the unguarded one does not. Diligence at call sites has now been given four
chances and has produced 1, 0, 3 and 0.
