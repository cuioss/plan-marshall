# 2026-08-30-16-001 — consult resolves the plan directory main-anchored, returning a false outline_not_found under the cwd-pinned worktree model

component: plan-marshall:manage-lessons
category: bug
status: moved-to-epic (OPEN — resolver future work; corpus original retained)
source: corpus lesson 2026-08-30-16-001, copied into finalize-machinery per operator direction
(note: condensed 2026-09-17)

## Observed

Phase-3 dispatch inside a materialized worktree: `consult` returned `outline_not_found`
though the outline existed cwd-relative — the verb resolves main-anchored, but the plan
dir MOVED into the worktree at phase-5 entry. False negative degrading silently (clean
consult that never happened; correct answer was 1 matching lesson, not 0). Any
re-entered phase with materialized worktree hits this.

## Directive

Resolve via uniform cwd walk-up (or worktree path fallback); until then, never read
`outline_not_found` as absent-outline when phase-5+/worktree materialized.

## Epic status

Open future work (lessons-pipeline theme, PLAN-05 adjacent). Corpus original RETAINED.
