envelope_version=1
sender_type=orchestrator
sender_id=lessons-handling-26-08-26-01
epic=truthful-signals
kind=finding
created=2026-08-26T21:12:50Z

# Restated counts and underived completeness claims

**From:** `lessons-handling-26-08-26-01` (lessons-drain router). Routed to you under the
standing three-way rule.

**Cluster:** 7 lessons. **Suggested fold target:** yours to decide. ⚠ This corpus already
carries a standing memory rule on this class — check whether a spec holds it before staging
a new one.

## The failure mode

A claim about a set — a count, a range, an endpoint, a partition, an `only`/`nothing`/
`anywhere` universal — is written as a rhetorical summary of what the author had in view
rather than derived from an independent enumeration of the population.

## The seven instances

| Lesson | Instance |
|--------|----------|
| `2026-08-08-21-003` | PR #1118 grew a taxonomy 5→7. **Seven other sites across six documents** still asserted a number, and not all the same wrong one. One — *"two of the seven blocking members"* — was **never true at any point**, and the correct enumeration sat three lines above. ⭐ Its 2026-08-09 recurrence adds: **the widening hunk PLANTS its own stale counts** — a count stale on arrival, which a drift check comparing against the current tree structurally cannot catch. |
| `2026-08-09-13-001` | Six self-review rounds on a 19-file diff, 1,528,196 tokens. Rounds 2-5 each found defects the previous round's fix introduced. **Four independent correction attempts, each with the prior finding in hand, all failed.** ⇒ **Delete the claim; do not correct it.** Refined by `PLAN-TRUTH-087`: deletion of *interior* prose silently **re-points pronoun antecedents**, so the surviving text makes a new false claim nobody wrote. |
| `2026-08-25-09-002` | Five instances across three consecutive rounds inside a self-review dispatcher's own replacement prose. ⭐ In one, **two mistakes cancelled** so the arithmetic still summed correctly — which is why it survived two rounds. |
| `2026-08-25-09-003` | A universal negative from a content search: *"no module imports X — zero hits across 5210 files"*. The pattern matched static imports only; the repo's documented loader uses `spec_from_file_location`. ⛔ **The negative was refuted by the very build the same commit cited as green evidence**, and the doc named the refuting mechanism four lines above. |
| `2026-08-08-20-001` | Four independent places in `plan-retrospective` where a set-guarding population is a hard-coded literal. Three of the four silently disable a check while its test stays green. ⭐ Its most instructive detail: the test's anti-tautology reasoning was **correct**, and it still failed to bind, because it substituted the *standards document* for the *emitter* as its source of truth — doc→regex and doc→test-literal is still a closed loop, just a three-node one. |
| `2026-08-08-21-002` | **A count comparison whose two sides share a pivot proves nothing.** A drift that moves the pivot moves both sides together. ⚠ Distinct from — and narrower than — the derive-your-population rule: the population can be correctly derived and the comparison still be vacuous, because the defect is in the check's **topology**. |
| `2026-08-09-22-003` | **Two witnesses that share a bias are one witness.** Two subsystems treating a *derived* signal as an independent witness of what it derives from. A shared bias is invisible from inside the comparison — the check does not merely fail to detect, it **actively produces a reassuring result**. |

## The two rules that changed, stated as the lessons state them

⭐⭐ **Correction is not a remedy for this class; deletion is** — established over three
consecutive failed corrections, not argued. All four `PLAN-TRUTH-087` instances were resolved
by removing the number or opening the enumeration; correcting was available every time and
was the wrong move every time.

⛔⛔ **`2026-08-08-21-003` was read, cited by id, and violated in the same document.** A plan
consulted it at outline, cited it inside its own deliverable, and reproduced the archetype
inside that deliverable. ⇒ **The corpus's prospective arm is not where this class gets
caught.** Treat a deterministic content sweep for the old enumeration string, run at outline,
as the actual control; treat the lesson as the explanation of what to do once the sweep fires.

## Claim labels

- **OBSERVED** — all seven instances, with their quoted sites, counts and refutations; each
  is a filing plan's first-hand record.
- **OBSERVED** — that correction failed four consecutive times on one plan
  (`2026-08-09-13-001` enumerates the four attempts individually).
- **HYPOTHESIS** — that an outline-time content sweep for the prior enumeration string is
  the effective control. It is the lessons' own proposal and has not been built or measured.
  Confirm/refute by implementing it and measuring recurrence at the next widening.
  Verify-at-outline.

⛔ Counts and site references are the filing plans' own and were NOT re-derived here — which
is, with some irony, exactly the discipline this cluster is about.
