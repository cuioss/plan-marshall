id=2026-09-04-17-014
component=pm-plugin-development:ext-self-review-plan-marshall
category=improvement
status=active
created=2026-09-04

# Self-review found the same defect twice in the same regex and twice across sibling instruments, one round apart each

## Context

The four 6-finalize self-review findings are two pairs, and both pairs are the same shape: a defect found in round N whose twin was sitting in plain view and was only found in round N+1.

**Pair one — the same regex, twice.**

- Round 1 (finding ecfe90): `_INLINE_HEADING`'s `[-=~_*]{2,}` lower bound admits ordinary prose comments as banners; `# --plan-id is forwarded to every child call` matches. 209 comments across `test/` and `marketplace/bundles/` were admitted as banners on the two-character bound alone. Remedy: raise to `{3,}`. Fixed in 5f032c50.
- Round 2 (finding 6931ed): with `{3,}` in place, `# -*- coding: utf-8 -*-` matches, because `-*-` is a 3-character run drawn from that class. Verified by executing `collect_banners` against a synthetic module: the PEP 263 cookie becomes a banner titled `coding: utf-8` at line 1, `scan_module` misattributes `collection_size` under it, and deleting only the cookie line drops the finding to zero. Remedy: require a HOMOGENEOUS run. Fixed in 1ce6ec55.

The round-1 fix changed a magic number in a heterogeneous character class. The round-2 finding notes that the lines 76-82 comment's own stated rationale for `{3,}` does not hold against a real Python comment form.

**Pair two — the same string, two sibling instruments.**

- Finding c3cc4b: `_banner_attribution.py:327` `--after-ref` help reads "the ref to compare against", which names the yardstick role `--before-ref` actually holds. An operator who acts on that reading swaps the refs, and the swap INVERTS the gate: `introduced` becomes the set the change removed, so `main()` exits 0 on a change that introduced a misattribution.
- Finding 751796: `_definition_duplication.py:216`, byte-identical defect. Lower blast radius (that `main()` always returns 0, so a swap misleads rather than inverting), but the same two readings.

The third sibling, `_fidelity_diff.py:254`, already carried the unambiguous form ("the ref the refactor produced"), and `_banner_attribution.py`'s own docstring says all three instruments follow it.

## Root cause

Round-scoped review. Each finding is judged and fixed on its own, so a defect class is not swept: a fix to a boundary constant is not followed by re-asking the boundary question, and a defect in one member of a documented sibling set is not followed by checking the other members — even when the module's own docstring names the set and one member already holds the correct form.

Both pairs did carry a `defect_class` tag in the finding text (`regex_overfit: 1 finding(s) in this class this round`, `ambiguous_wording: 2 finding(s) in this class this round`), so the class is already computed. It is scoped to the round.

## Proposed action

1. When a finding names a member of a set the source itself documents (sibling instruments, a shared docstring contract), extend the check to every member in the SAME round rather than waiting for the next one. The `ambiguous_wording` pair shows this works — both were caught together once the class was looked at across files.
2. When a fix changes a threshold or a character class in a regex, re-run the detector's own rationale against the new value before accepting it. Round 2 exists because `{3,}` was adopted without asking what else `[-=~_*]{3,}` admits.

## Evidence

- qgate findings ecfe90, 6931ed, c3cc4b, 751796 (6-finalize, `pm-plugin-development:ext-self-review-plan-marshall`, warning), all `fixed`
- Landed changes 5f032c5077b3154fd7f2bcb0451148deaccd1065 and 1ce6ec55a5d3430a1b1356e1e7fd8b89504614bb
- Round 2 ran roughly four hours after round 1 on the same file

## Recurrence — 2026-09-06, plan `every-test-runs-and-the-suite-does-not-slow-down` (PR #1426)

**The fix-introduces-the-next-defect shape fired TWICE in one run, on two different review
surfaces, and neither was caught by a class sweep — both were caught by a replay.**

- **Regex pair, external reviewer.** CodeRabbit filed `c13f58` against a `\S+` capture breadth in
  the pytest ERROR-line parser the branch had authored. The fix for it introduced `c0c4e3`, a lazy-
  quantifier mis-split inside parameter brackets on the same parser, which CodeRabbit filed on its
  **second** review with a runnable `python3` reproduction of the mis-split. The plan's own
  retrospective classifies the pair as "a matched pair of regex-breadth defects ... the second one
  *introduced by the fix for the first*, which is a real second-order catch, not a repeat".
- **Contract-drift pair, self-review.** `pre-submission-self-review` fired three times and filed
  two findings, both contract drift, both fixed. The second was a defect introduced by the first
  fix, and — in the retrospective's words — "was caught only because the step replayed".

The regex pair is Proposed action 2 above, verbatim, in a different component and against a
different reviewer: a fix altered quantifier breadth in a regex and the detector's own rationale
was not re-run against the new value before the fix was accepted. Two independent occurrences one
day apart make that item the highest-yield of the two.

**What is new is the catch mechanism, and it is fragile.** Neither instance was caught by
extending the class within the round (the remedy this lesson proposes). Both were caught because
the *same reviewer looked a second time at a tree that had moved*. That has three consequences
worth recording together:

1. **A fix commit is itself an unreviewed change.** The review that produced the finding ran
   against the pre-fix tree by construction, so nothing in the round that filed a finding ever
   examines the fix for it. Only a replay does.
2. **The replay is not guaranteed.** On this same run three head-dependent steps —
   `project:finalize-step-plugin-doctor`, `pre-submission-self-review` and
   `pre-push-quality-gate` — carried verdicts anchored to HEADs that were never the merged one,
   because an out-of-band loop-back skipped the re-arm (`2026-09-06-10-001`). The mechanism that
   caught both defects here silently failed to fire for three other steps in the same phase.
3. **It supplies the yield side the cost-side lessons ask for.** `2026-08-25-09-007` has three
   recurrences arguing that re-firing dominates finalize spend and asking for cost to be measured
   before the loop is bounded. This run measures the other half: two defects existed that only a
   replay could reach, one of them in an external reviewer's rate-limited window. Any bound placed
   on re-firing must bound *unproductive* churn specifically — the discriminator
   `2026-09-06-10-001` names (`returned_with_findings`) — because a bound on total rounds
   removes exactly this catch.

### Evidence (2026-09-06 recurrence)

- `review-retrospective.md` (PR #1426) § Qualitative Quality Assessment, `coderabbitai` — the
  `c13f58` / `c0c4e3` pair and its second-order characterisation; `c0c4e3` shipped a runnable
  `python3` reproduction
- Same document § The plan's own internal gates — "The second was a defect **introduced by the
  first fix** and was caught only because the step replayed — the same second-order pattern
  CodeRabbit hit with `c13f58` -> `c0c4e3`. Replay is what made both catches possible."
- Same document, cost note — CodeRabbit's plan allows one included review per hour and both
  reviews reported `0 remain after this review`, so the second replay consumed the window and the
  post-fix HEAD could not get a third pass on demand
- Related: `2026-09-06-10-001` (the re-arm that silently did not fire on this run),
  `2026-08-25-09-007` (the cost side), `2026-09-06-08-001` (the same run's subject-class
  recurrence)
