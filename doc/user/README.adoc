= User Guide
:nofooter:
:toc: left
:toclevels: 3
:toc-title: Table of Contents
:sectnums:
:source-highlighter: highlight.js

xref:../../README.md[Plan Marshall] » User Guide

Operating Plan Marshall in a target project. If you are working on Plan Marshall itself, see xref:../developer/README.adoc[Developer Guide] instead.

== Pages

* xref:installation.adoc[Installation] — install and update the marketplace via Claude Code's `/plugin` tooling. Snapshot (`dist-claude`) and pinned (`claude/v*`) channels.
* xref:getting-started.adoc[Getting Started] — first-run setup via `/marshall-steward`.
* xref:commands.adoc[Commands] — `/marshall-steward` and `/plan-marshall`.
* xref:configuration.adoc[Configuration] — surface map of every configuration file Plan Marshall touches and what governs each.
* xref:recipes.adoc[Recipes] — the surgical-fix micro-lane fast path for pre-diagnosed fixes: what it skips/keeps, and when (and when not) to use it.
* xref:efforts.adoc[Efforts] — per-role model selection: wizard presets, polymorphic `marshal.json` shape, worked example, troubleshooting.
* xref:terminal-title.adoc[Terminal Title] — VS Code and IntelliJ-specific integration for the per-session status indicator.
* xref:enforcement-hook.adoc[Enforcement Hook] — the optional PreToolUse hook that blocks four hard-rule violation families inside a plan context; how to enable it via `/marshall-steward`.
* xref:parallelism-and-locking.adoc[Parallelism and Locking] — operator guide for running concurrent plans: the build-queue knobs (`max_slots`, `max_retries`, `build.queue.upper_limit_seconds`), the main-anchored `[LOCK]` log, and a stuck-queue FAQ.

== Concept Crossover

User-side workflows depend on a small handful of concepts. When something here references a mechanism or contract, the explanation lives under xref:../concepts/README.adoc[Concepts]:

* xref:../concepts/planning-workflow.adoc[Planning Workflow] for `/plan-marshall` and the six phases.
* xref:../concepts/execution-context.adoc#per-role-model-selection[Execution Context › Per-Role Model Selection] for the per-role model system referenced from xref:efforts.adoc[Efforts].
* xref:../concepts/execution-context.adoc[Execution Context] for why effort changes take effect on the next dispatch without a Claude Code restart.
* xref:../concepts/parallelism-and-locking.adoc[Parallelism and Locking] for the merge mutex and build-queue limiter that serialize concurrent plans, referenced from xref:parallelism-and-locking.adoc[Parallelism and Locking].
