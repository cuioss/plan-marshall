# 2026-08-25-09-010 — compute-footprint diffs against local main, inflating the footprint after an upstream landing

component: plan-marshall:manage-references
category: bug
status: moved-to-epic (OPEN — footprint-base future work; corpus original retained)
source: corpus lesson 2026-08-25-09-010, copied into finalize-machinery per operator direction
(note: full body retrieved 2026-09-17; condensed here)

## Context

`compute-footprint` reported 39 files vs 14 real — 25 extras from an upstream commit
landed mid-flight. `_references_core.resolve_base_ref` falls back through local names
only (`--base-ref` → `base_branch` → `'main'`; never `origin/`); the `...` diff form's
exclusion guarantee holds only while the base is current, and worktree local `main`
goes stale exactly when it matters. The same computation persists as
`realized_footprint`, so the inflation is stored, not just reported.

## Proposed action

Resolve against the remote-tracking ref (fallback local only when absent), or pin the
recorded fork point; report the resolved base ref alongside the count.

## Epic status

Open future work (footprint theme). Corpus original RETAINED.
