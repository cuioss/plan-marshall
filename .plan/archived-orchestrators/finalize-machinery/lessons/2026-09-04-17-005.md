# 2026-09-04-17-005 — Record a change-ledger row per build so build time stops reading as unavailable

component: plan-marshall:build-pyproject
category: improvement
status: moved-to-epic (OPEN — ledger-row future work; corpus original retained)
source: corpus lesson 2026-09-04-17-005, copied into finalize-machinery per operator direction
(note: condensed 2026-09-17)

## Context

155 pyproject_build calls (71% of script time) yet `total_build_seconds: unavailable` —
the wrapper never appends change-ledger rows; the honest answer is the useless one.

## Proposed action

One ledger row per invocation (command, duration, verdict) at the point all three are
known; suspect-zero/killed-vs-error rules then have rows to apply to.

## Epic status

Open future work (build-ledger theme, PLAN-06 adjacent). Corpus original RETAINED.
