{
  "_rationale": {
    "safe_patterns": "Artifacts that are always safe to auto-delete before commit — build outputs, caches, and OS metadata that should never be version-controlled.",
    "uncertain_patterns": "Build output directories that might contain intentional files — require user confirmation before deletion.",
    "skip_dirs": "Directories to skip entirely during traversal — large and never useful to scan for artifacts."
  },
  "safe_patterns": [
    "**/*.class",
    "**/*.pyc",
    "**/__pycache__/**",
    "**/*.egg-info/**",
    "**/.eggs/**",
    "**/*.tsbuildinfo",
    "**/*.temp",
    "**/*.backup*",
    "**/*.orig",
    "**/.DS_Store",
    "**/Thumbs.db",
    "**/*.swp",
    "**/*.swo",
    "**/*.log",
    ".plan/temp/**"
  ],
  "uncertain_patterns": [
    "target/**",
    "build/**",
    "dist/**",
    "out/**",
    ".next/**"
  ],
  "skip_dirs": [
    ".git",
    ".venv",
    "venv",
    "env",
    "node_modules"
  ]
}
