# 2026-09-03-19-006 — phase_handshake verify exits non-zero with empty stderr, so the failure can be classified but not triaged

component: plan-marshall:plan-marshall
category: bug
status: moved-to-epic (OPEN — staged in PLAN-06; corpus original retained until that plan ships)
source: corpus lesson 2026-09-03-19-006, copied into finalize-machinery per operator direction

## Context

`phase_handshake verify` exited 1 twice at the 4-plan boundary; captured stderr empty.
`script-failure-analysis` classifies as `bug / script_internal_error` — body ran and
failed — but the only artifact is the exit code: nothing says which invariant failed or
against which phase. The plan otherwise completed both handshakes cleanly, so the
transient failure goes uninvestigated precisely because it logs nothing.

## Proposed action

On any non-zero exit, write the failing invariant name + observed-vs-expected values to
stderr before returning (both sides already computed; cost is one formatted line).

## Epic status

Staged in PLAN-06 (anchors-and-mutex, deliverable 4). Corpus original RETAINED until
PLAN-06 ships.
