# Landing Analysis: PLAN-PR-024 — Participation credit anchored to the merge candidate

epic: review-apparatus
workstream: WS-01
pr: #1349 (merged, squashed as `8025b2210`)

> Written by `analyze` after corroborating against ground truth. Source: operator paste,
> cross-checked against `git log origin/main`, `git show --stat`, and the plan's own
> `landing` message `-010.md`.

## Ground-Truth Corroboration

| Claim | Verdict | Evidence |
|---|---|---|
| Merged as #1349, squashed `8025b2210` | **corroborated** | `8025b2210` on `origin/main` |
| 6/6 deliverables, 23/23 finalize steps | **corroborated** | landing message `-010`, step list |
| Diff scope | **corroborated, and larger than declared** | 10 files, +2000/−118 |
| Landing message is complete | ⛔ **CONTRADICTED** | `landing-check` → `complete: false`, **all 8 required keys missing** |
| Registry pin gate holds | ⛔ **CONTRADICTED** | executor `0.1.1550` vs registry `installPath 0.1.1544` |

## ⛔ Two live defects this landing surfaced

### 1. The landing message carries NO `landing-facts` block — a regression

`grep -c 'landing-facts'` returns **0**. `missing_keys` is the whole required set of 8, which is the
signature of a **prose-only landing**. This is a *regression*, not a standing gap: PLAN-PR-034's
landing four days earlier (#1344) returned `complete: true` with 0 missing keys, from the same
`emit-landing` step.

⇒ **The drain cannot reconcile machine-readable facts for this plan.** Everything below the outcome
line is narrative only. ⭐ This is precisely **PLAN-PR-028 D2's** subject and it now has a live,
first-party instance to point at — recorded on that spec.

### 2. The registry pin gap re-opened, by SIX versions

`MARSHALL_VERSION = 0.1.1550` (regenerated by this run's `sync-plugin-cache`, which reported
`v0.1.1550`); registry `installPath` is still `0.1.1544`. **`executor == installPath` FAILS.** This is
the known per-landing steady-state leak — sync mints a new cache version and never re-pins the
registry. ⚠ Repair is operator-only. Not asserted as the cause of defect 1; the two are recorded
independently.

## Deliverable Fidelity vs Spec

All 6 shipped as specified. ⭐ **The amendment applied before emit was exercised and held**: D2's
ledger-reader clause had prescribed dropping a key-only row from the map — the non-fix `010 G8` names
— and was corrected pre-emit to carry an invalid-legacy-record state, with the Done-when re-pointed
from an input-equivalence assertion to `participated_stale`. Deliverable 4 ("an unresolvable merge
candidate is undecidable, not stale") is that correction shipped.

## Metrics and Anomalies

- 4,040,890 tokens / 108,070,000 billing-weighted; 5h46m worked / 11h21m wall.
- ⚠ **Billing is 26.7× tokens** — in the normal 12–28× band, unlike PLAN-PR-034's anomalous
  6-finalize row. No new instance of that anomaly here.
- **11.4% of the run went to turns completing no step**, most of them operator restarts of a stall.

## Routing and Merge Behavior

- ⛔⛔ **The in-house self-review found 5 real defects; the entire external reviewer set found 0.**
  `automatic-review` recorded **0 comments, 1 empty / 2 refused** — no bot produced a finding.
- Two of the five are worth the epic's attention:
  - `906944` — a hole in the **merge barrier's own positive-validation enumeration**: a field added to
    a routing paragraph but not to the required-input list, in a document that argues that exact case
    eleven lines below.
  - `5de248` — a live bug: a **lexicographic timestamp compare ranked `-05:00` before `Z`**, so the
    guard fired on comments that post-dated the commit.
- Merged via queue. CI green, zero dispatch.

⭐ **This inverts the epic's usual finding.** The standing evidence (PRs #1336/#1337/#1338/#1343) is
that CodeRabbit catches what pr-agent misses. Here **no external bot caught anything** while in-house
review caught five — including one live bug. n=1, and `2 refused` means two bots never read the diff,
so this is not evidence the bots are useless; it is evidence that **on this PR the reviewer set
contributed nothing and the refusals were the reason**. Recorded as a watch, not a conclusion.

## Reconciliation Actions

- [x] row `status` → `shipped`; `pr`, `landing`, `plan_marshall_plan_id` stamped
- [x] Open Defect: landing-facts regression (folded onto PLAN-PR-028 D2)
- [x] Open Defect: registry pin gap re-opened at 6 versions
- [x] Watch: external reviewer set contributed zero on this PR
- [x] inbox drained; blocks regenerated

## Follow-Ups

- **PLAN-PR-028 `540b` gains a live instance** — the prose-only landing is D2's exact subject.
- **Six defects routed to `truthful-signals`**, none this epic's surface. Sharpest: `e5372d` —
  `detect-artifacts` offered ~108,879 paths as safe-to-delete, **including the running plan's own live
  state**. Nothing broke only because the agent declined to follow its documentation.
- Two self-corrections the run recorded: an omitted `candidates` prompt field, and `branch-cleanup`
  marked done without persisting `realized_footprint`/`merge_commit_sha` — the same defect
  lesson `2026-08-25-09-004` already tracks.
