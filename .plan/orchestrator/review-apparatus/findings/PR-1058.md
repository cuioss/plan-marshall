# Run finding: PR #1058

epic: review-apparatus · analysed 2026-07-30 · evidence: `ci pr comments --pr-number 1058`
(19 comments, 9 unresolved)

## Participation

| Bot | Result | Detail |
|---|---|---|
| CodeRabbit | ✅ reviewed, **2 findings** | "Actionable comments posted: **2**" — one 🟡 Minor / Quick win, one 🟠 **Major** / Heavy lift |
| Sourcery | rate-limited | "weekly rate limit of 500000 diff characters" — **known, NOT an issue** |
| PR-Agent | ✅ result present, **0 findings** | informational Guide: tests present / no security concerns / no major issues detected |

## ⛔ Verdict — STRUCTURAL DEFICIT, ratio 2 : 0, and one was MAJOR

PR-Agent provided a result, satisfying the must-always-provide-a-result rule. But CodeRabbit found two
substantiated findings on the **same diff**, one of them **Major**, where PR-Agent found none. A
baseline existed. **Bug under the comparative rule.**

⭐ **This is the sharpest instance in the corpus**, because PR-Agent's Guide positively asserts
*"No major issues detected"* on a diff where the other reviewer posted a finding it classified as
**Major**. That is not a difference of threshold or taste — it is a direct contradiction of a specific
claim, on the same input.

The two CodeRabbit findings:

1. 🟡 **Sequence-width contract inconsistency** — line 24 promises exactly three digits, but
   `get_next_id()` emits `1000` after sequence 999. Deferred to TASK-4; CodeRabbit acknowledged and
   held the thread open.
2. 🟠 **Major — documented category list hardcoded** rather than derived from `VALID_CATEGORIES`.
   Triaged as out-of-scope (line unmodified by the diff, and already agreeing with runtime
   `LESSON_CATEGORIES`); CodeRabbit **withdrew the finding** on that rationale.

⚠ Note for the back-feed: finding 2 was **withdrawn after triage**, so it is not an accepted finding
and yields no detector. It still counts for the structural-deficit tally, because the deficit rule is
about what the reviewers *produced*, not about what survived triage.

## Posted answers

✅ Complete. Both inline findings answered in-thread and resolved; the CodeRabbit review-summary body
answered ("Meta review-summary comment carrying no independent actionable content — both cited items
are the inline comments already triaged separately"). No `untransmitted`, no unjustified `skipped`.

⚠ Same duplicate-transmit pattern as #1055: the meta-summary disposition appears twice
(`IC_…MS8qBg`, `IC_…MT0zXg`), both replying to the same `comment_id`.

## Could we have found it ourselves?

| Finding | Answer |
|---|---|
| Sequence-width contract: doc says three digits, `get_next_id()` emits four past 999 | **YES** — a documented width/format claim contradicted by the emitting function is diff-local. Close to the existing `_detect_count_prose` / advertised-form family |
| Hardcoded category list vs `VALID_CATEGORIES` (withdrawn) | **n/a — not accepted.** Withdrawn after triage as out-of-scope and already consistent. No detector owed |

**One YES**, and it points at a shape adjacent to detectors that already exist rather than a new class.

## Feeds

- `PLAN-PR-012` — one YES, adjacent to the existing advertised-form/count-prose family.
- `PLAN-PR-004` — ⭐ **the strongest single piece of evidence available**: PR-Agent asserted "No major
  issues detected" while another reviewer posted a **Major** finding on the same diff. This is a
  better-grounded oracle than the planned `/review` on `#1042`, because the contradiction is on
  record rather than to be induced.
- `PLAN-PR-006` — ⚠ **re-aim required**, see `epic.md` decision of 2026-07-30.
