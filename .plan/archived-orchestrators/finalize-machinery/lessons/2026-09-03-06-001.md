# 2026-09-03-06-001 — Report three-valued head_sha evidence instead of a bare head_sha_verified bool

component: plan-marshall:automatic-review
category: bug
status: moved-to-epic (OPEN — review-currency evidence future work; corpus original retained)
source: corpus lesson 2026-09-03-06-001, copied into finalize-machinery per operator direction

## Context

`head_sha_verified` bool carries three conditions: real decline, mid-review progress
marker, HEAD named in prose the matcher cannot parse. PR #1382: coderabbit in-progress
marker + pr-agent prose naming the head both returned false — literal contract would
enter two bots into declined_bots and block on declines that never occurred.

## Proposed action

Three-valued `head_sha_evidence` (verified_field / named_in_body / absent); narrow the
SKILL.md decline arm to `absent`; route `named_in_body` to the proceed path.

## Epic status

Open future work (review-currency theme). Corpus original RETAINED.
