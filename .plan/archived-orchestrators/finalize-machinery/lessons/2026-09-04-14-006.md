# 2026-09-04-14-006 — Capture the session id at finalize entry, not at retrospective order 995

component: plan-marshall:plan-retrospective
category: bug
status: moved-to-epic (OPEN — capture-placement future work; corpus original retained)
source: corpus lesson 2026-09-04-14-006, copied into finalize-machinery per operator direction
(note: condensed 2026-09-17)

## Context

Two-session plan: session_ids held only the phases-1-5 session through all of finalize;
finalize session appended at retrospective Step 1 (order 995) — every finalize consumer
resolving transcripts by id read the wrong session or none (Billing empty on a 5.36M
plan). Escalation: a later run shows finalize entry is still too late — need plan-init
capture, since enrich attributes per-phase windows that elapsed before any late capture.

## Proposed action

Capture at finalize entry (cheap, idempotent) + on every session-restart resume; keep
retrospective capture as backstop only. Generalised: capture at plan-init; where the
runtime cannot supply it that early, make the loss visible at the point of loss.

## Epic status

Open future work (session-identity theme, PLAN-07 adjacent). Corpus original RETAINED.
