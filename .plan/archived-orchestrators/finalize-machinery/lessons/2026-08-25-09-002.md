# 2026-08-25-09-002 — Stamp returned_with_findings when a self-review round loops back with findings

component: plan-marshall:phase-6-finalize
category: bug
status: moved-to-epic (OPEN — outcome-vocabulary future work; corpus original retained)
source: corpus lesson 2026-08-25-09-002, copied into finalize-machinery per operator direction
(note: condensed 2026-09-17 — four instances; fourth PARTIALLY fixed: 4/5 rounds correct,
one residual `failed` branch + denied delta anchor)

## Context

Findings-bearing self-review rounds stamp `outcome: failed` (679K tokens reading as
terminal waste) because `returned_with_findings` keys on `outcome: loop_back` — a
vocabulary the step's own doc prescribes against itself (three sites mandate
`--outcome failed`; SKILL keys the stamp on loop_back). Productive rounds read as waste;
`prior_firings: [failed × n]` cannot tell "found defects" from "could not look".

## Proposed action

Findings-bearing rounds stamp `loop_back` (inline-replay re-fires — what already
happens); keep halting behavior; guard: non-empty-findings round can never record
`failed`.

## Epic status

Open future work. Corpus original RETAINED.
