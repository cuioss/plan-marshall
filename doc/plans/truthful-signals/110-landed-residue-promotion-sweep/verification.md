# Verification — 110-landed-residue-promotion-sweep

**Verified against:** commit `0314cc4d`, re-confirmed at `ac1618f3` (HEAD at adversarial review). Every commit between the two touches only `doc/plans/**`: `git diff --stat 0314cc4d ac1618f3 -- marketplace/ test/ .gitignore` is empty, so the whole subject surface is byte-identical at both.   **Landed as:** PR #1169, commit `894c37d5`   **Verdict:** implemented-with-gaps

## Method

What was actually done:

- Read `plan.md` and `report-01.md` in full.
- Located the landed commit: `git log --oneline -- doc/plans/truthful-signals/110-landed-residue-promotion-sweep/` → `894c37d5` ("chore(extension-api): promote long-build completion-signal residues (plan 110) (#1169)"). Read the full landed diff (`git show 894c37d5`, `git show --stat`): **3 files, 360 insertions, 0 deletions** — the plan move (rename, 0 bytes changed), `report-01.md` (+290), and `marketplace/bundles/plan-marshall/skills/extension-api/standards/build-systems-common.md` (+70). No `*.py`.
- Opened the promoted section at HEAD (`build-systems-common.md:69–197`) and read all three subsections plus the Enforcement note. **Not all of that range is this plan's:** the "three non-green conditions" subsection at :126-184 was added later by `d4ae2e81` (#1193) and `e2b6665b` (#1279). This plan's contribution is the +70 lines of `894c37d5` — the section header, (a), (b) + Enforcement, and (c).
- Verified every factual claim the promoted text makes against source, not prose:
  - executor buffering — `execute-script.py.template:1518–1532`: `subprocess.run(..., capture_output=True)` then `print(result.stdout)` **after** completion. Residue (a)'s mechanism is source-confirmed, not merely a run-observation.
  - the three-way stamp conjunction — `execute-script.py.template:1609` `if _is_build_class_notation(notation, subcommand) and not _mentions_help(script_args):`, with `_is_build_class_notation` at :334, `_mentions_help` at :812, `_BUILD_CLASS_PREFIXES` at :314, `_BUILD_EXECUTING_SUBCOMMANDS = frozenset({'run'})` at :329.
  - the status vocabulary and `unknown` definition — `_derive_build_status` (`execute-script.py.template:430–493`): `exit_code < 0 → killed`; other non-zero → `error`; exit 0 with a wrapper-claimable status → that status; else `unknown`. Matches the promoted wording "`unknown` (exit 0 with no wrapper-claimable status on the payload)" exactly.
  - "only `status == success` is a pass" — `manage-tasks/scripts/_cmd_pre_commit_verify_freshness.py:456–469` (`entry.get('status') == 'success'`).
  - "`classify-outcome` takes the terminated status as an input alongside the row/byte check" — `manage-change-ledger/SKILL.md:218–256`: the verb's signature is `--job-status {completed|killed} --output-bytes N --worktree-sha SHA`. Confirmed, not assumed.
  - `HARNESS_BASH_CEILING_SECONDS = 600` (`tools-file-ops/scripts/constants.py:35`) — the "600000 ms = harness Bash ceiling" claim holds.
  - every cross-link target: `#log-file-handling`, `#timeout-management` (headings at :19 and :7), `../../manage-change-ledger/SKILL.md` § Entry Shapes (:69) and § classify-outcome (:218), `../../build-pyproject/standards/pyproject-impl.md` § "Timeout bound ordering" (:75). All resolve.
- Ran the test the run cites for D3: `UV_HTTP_TIMEOUT=600 uv run python -m pytest test/plan-marshall/tools-script-executor/test_build_class_stamp_discriminator.py -o addopts="" -q` → **30 passed**.
- **Mutation check** (the highest-risk mechanism the promoted rule asserts, even though the plan added no code): saved the template bytes to the scratchpad, removed the `and not _mentions_help(script_args)` conjunct at :1609, re-ran the same test → **6 failed, 24 passed** (`test_help_probe_writes_no_build_row_while_the_bare_verb_still_does`, all four `test_every_help_spelling_…` parameterisations, `test_help_probe_leaves_the_freshness_gate_unable_to_report_fresh`). Restored by writing the saved bytes back (`cp` from scratchpad); md5 matches the pre-mutation value `c2f0e553…`; `git status --porcelain` shows no modification to any tracked file by this verification.
- Executed the plugin-doctor rule the run continuation says it fixed: loaded `_analyze_historical_prose_in_skills.analyze_historical_prose_in_skills(Path('marketplace'))` → **0 findings** whole-tree. The Provenance→Enforcement rewrite is in the landed content (`build-systems-common.md:119`) and passes today.
- Re-derived D0's evidence: `.gitignore` at `894c37d5` lines 46–48 (`.plan/*`, `!.plan/marshal.json`, `!.plan/project-architecture/`); `git ls-files .plan` returns only `marshal.json` and `project-architecture/**`; `git ls-files | grep -i lesson` returns only skill sources — **no lessons corpus is tracked anywhere in git**.
- Swept the tree for stale or competing restatements: `grep -rn "over-inclusive"` (0 hits outside `doc/plans`), `grep -rni background` across the build skills (only the new section), `grep -rn "kind=build"` across all `*.md`, `grep -rn "build-executing|build-class"` across `*.py`/`*.md`, and `grep -rn "build-systems-common"` for reachability.
  **The sweep was not broad enough** — see § Adversarial review. Re-run whole-repo and un-scoped it adds one further `build-systems-common` reader (`doc/concepts/build-management.adoc:46`, project docs rather than a loaded skill) and, more consequentially, two restatement conflicts the `over-inclusive`/`kind=build` patterns could not reach because neither uses that vocabulary: the promoted text's own under-statement of the help conjunct (**G6**) and the collision between residue (c) and the leaf/orchestrator build-tier SSOT in `ref-workflow-architecture/standards/agents.md` + `plan-marshall/workflow/await-long-running.md` (**G7**). A sweep keyed to the phrasing of the primary claim misses restatements phrased any other way; that is the same defect this row records against the run.
- Queried GitHub for the landed PR (`pull_request_read get` / `get_commits` on #1169, `get_commit` on `7a9ee21`) to re-derive the merge state, label, head branch, file count and commit count.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D0 | GATE: is the residue set derivable from git? | run reports a derived set **or** a statement it is not derivable, with method | yes | yes | yes | yes | `report-01.md` § D0 states verdict + 3-step method + population (~24 ids, 0 resolvable). Independently re-derived: `.gitignore:46-48` at `894c37d5`, `git ls-files .plan` = 2 trees, `git ls-files \| grep -i lesson` = skill sources only |
| D1 | Promote the three build/`script-shared` residues, labelled run-observation, caveat intact | all three written into the governing standards | yes | yes | yes | partial | `build-systems-common.md:69` section header; :73-75 "run-observations" label; (a) :77-90; (b) :92-124 + Enforcement :119-124; (c) :186-196. Mechanism claims confirmed against `execute-script.py.template:1518,1609,430`, `_cmd_pre_commit_verify_freshness.py:469`, `constants.py:35`. **Sweep gap:** the doc the promotion cross-links, `manage-change-ledger/SKILL.md:74-83`, still equates the stamp predicate with a **two-way** conjunction → G1. **Reachability gap:** the section lives in a `mode: knowledge` extension-author skill with no path from the agent-facing build-invocation guidance → G2. **Added at adversarial review:** the promoted text itself names only `--help` where the boundary also fires on `-h` → G6, and residue (c)'s unqualified "never background a long build" contradicts the build-tier SSOT that mandates exactly that for an `orchestrator`-tier build → G7 |
| D2 | Promote whatever D0 derived | each derived residue promoted, or recorded already-covered | n/a — correctly halted | yes | yes | yes | Landed diff touches exactly one standards file; nothing was reconstructed. `git show --stat 894c37d5` = 3 files |
| D3 | Parity tests only where a rule has mechanical form; report empty rather than pad | any test added asserts the rule and nothing else; empty is reported | n/a — correctly reported empty | yes | yes | yes | No test in the landed diff. The cited existing test exists and passes (30 passed) and is **non-vacuous**: removing the `_mentions_help` conjunct turns 6 of its tests red |

**D1 — the one deliverable that is not a clean pass.** The promotion itself is accurate and, on residues (a) and (b), better substantiated in the tree than the plan's HYPOTHESIS labels required: (a)'s buffering is not merely observed, it is structural (`capture_output=True` at `execute-script.py.template:1518`, printed at :1530 after return), and (b)'s corrected form matches `_derive_build_status` and the freshness gate term for term. Two things keep it from a clean pass. First, the promotion states a **three-way** conjunction (`build-systems-common.md:110-124`) while the very section it cross-links for the ledger contract — `manage-change-ledger/SKILL.md:74-82` § Entry Shapes — still describes build-class-ness as a **conjunction of two** conditions and names only "a bare `--help` dispatch that carries no subcommand at all" as suppressed. Under that wording `run --help` is build-class and would stamp a row; in source it does not (proved by the mutation run above, where exactly the `run --help` cases went red). The third conjunct landed in `415dcf13` (#1127, 2026-08-09), two days before this promotion, so the drift was already present and reachable when the run performed its "beyond-diff staleness sweep". Second, residues (a) and (c) are addressed to the agent *invoking* a long build, but they were filed in `extension-api`, whose own SKILL.md declares it a `mode: knowledge` reference "load on-demand when creating or modifying extensions". `grep -rn build-systems-common` across `marketplace/` and `.claude/` finds readers only in `extension-api/**` and the four `build-*/standards/*-impl.md` files — none of the agent-facing surfaces (`persona-plan-marshall-agent`, `phase-5-execute`, `execute-task`, `.claude/skills/cloud-plan-lane`, `CLAUDE.md`) points at it. By the plan's own Verification criterion ("a rule the cold reader cannot apply has been filed, not promoted"), the reader who most needs (a) and (c) has no route to them.

## Report accuracy

Re-derived at the moment of writing. **Confirmed accurate:** the D0 method's `.gitignore` citation (line 46 at the landed commit — line 45 today after an unrelated edit) and its `git ls-files ".plan/**"` result; the "three-way conjunction … pinned by `test_build_class_stamp_discriminator.py`" finding F1 (source-confirmed, and the pin is non-vacuous under mutation); the D1 placement rationale (`pyproject-impl.md:3` does point at `build-systems-common.md`, as do `maven-impl.md:3`, `npm-impl.md:3`, `gradle-impl.md:3`); "no `*.py` in the branch diff" (landed diff is 3 `.md` files); the not-already-in-the-standard check (the pre-existing "Build Status Determination" section at :46 does answer a different question); the `skip-bot-review` label and the `chore/landed-residue-promotion-sweep` head branch (GitHub API on #1169); the F3/F4/F5 dispositions are all present in the landed text (`:99-109` carries the known-terminated conditioning and the `classify-outcome` cross-link; the report says "not derivable from git-reachable evidence", not "gone"; the findings claim is scoped to the completed review); and the historical-prose fix (0 findings whole-tree today).

Contradictions and drift found:

1. **"beyond-diff staleness sweep found no false statement in any untouched build/ledger doc"** (Findings § "Step 6 sub-agent, all other categories: CLEAN") — contradicted by `manage-change-ledger/SKILL.md:74-83`, which is a ledger doc, was untouched, and equates the stamp predicate with a two-condition conjunction that source has not implemented since `415dcf13` (2026-08-09). See G1 — and, added at adversarial review, G7, a second untouched-doc conflict the same sweep missed.
2. **Build gate § "no local build was run"** — contradicted by the run's own continuation section, which records "Verified locally with `./pw quality-gate` — `total_issues: 0`" for the historical-prose fix. The Build-gate section was never reconciled with the events appended below it.
3. ~~**"The complete work (all six commits) … is re-published"**~~ — **withdrawn at adversarial review; this is not drift.** The count re-derives (PR #1169 carries **10** commits: `b65401de`, `0b73707d`, `21e4b6f2`, `74122313`, `2238eea4`, `ebccb170`, `ac06cd08`, `ac079c94`, `66d9b886`, `5e55f2bf`), but the report's sentence is a record of the re-issue moment — the six commits then on the `claude/…-11npl8` branch plus `ac06cd08`, the commit that wrote the sentence — not a claim about #1169's final count. The remaining three were appended by the run's own continuation and are described below it. See gaps.md § Refuted.
4. **Contract check (Step 9) rows 2 and 7** state the branch as "harness-assigned `claude/landed-residue-promotion-sweep-11npl8`, kept as-is" and the PR as "#1161". The work landed from `chore/landed-residue-promotion-sweep` as PR #1169 (GitHub API, and the header of the same report). The table was not updated when the run continued.
5. **Commit SHAs `7a9ee21` / `22b740b` / `3f5c936` / `217940b` / `308050c`** are unreachable in a clone (`git cat-file -t` fails for each) — the branches were rebased and the PR squash-merged. They do resolve through the GitHub API (`get_commit 7a9ee21` returns the promotion commit), so this is a citation that a reader cannot follow locally rather than a false claim. The post-rebase equivalents are `0b73707d` (promotion), `74122313` (F2 disambiguation), `b65401de` (plan move).

No other figure in the report was contradicted: the three-residue count, the "one standards file" scope, the 2-of-3 reviewer coverage on #1161, and the D3-empty justification all hold.

## Out-of-scope compliance

Compliant on every declared boundary. The landed diff is 3 files, all `.md`, 360 insertions / 0 deletions: no production behaviour change, no test change, no collateral edit outside the plan directory and the one standards file. The absorbed producerless-`phase-1-init` item is absent from the diff, as required. No retired lesson was revived or reconstructed — D2 wrote nothing.

One disclosed deviation from the lane contract, not from the plan: `CLAUDE.md` § "Standalone Plan Lane" requires a cloud session to keep its harness-assigned `claude/{slug}-{hash}` branch, and the run re-issued the work on `chore/landed-residue-promotion-sweep`. The report records this as operator-directed and explains why (#1161 could not be admitted to the merge queue). It also carries the one `skip-bot-review` label on a `marketplace/bundles/**` change, justified in the PR body by the identical diff already having been reviewed on #1161 — confirmed on the PR (label present, merged).

## Residue carried forward

| Residue declared in report-01.md | State in today's tree |
|---|---|
| "The plan is a strong candidate for closure" — recommend closing plan 110 | **Still open, and not verifiable from the tree.** `doc/plans/truthful-signals/110-landed-residue-promotion-sweep/` still exists with `plan.md` + `report-01.md`; `doc/plans/README.md` defines no closure marker, so closure is an operator action in git-ignored `.plan/` state |
| CLA unsigned (`license/cla` pending) | **Moot.** PR #1169 is `merged: true`, merged by `cuioss-oliver` at 2026-08-11T20:36:12Z |
| "No follow-up production behaviour change is owed" | **Holds.** The D1(b) over-inclusiveness is fixed in source (`execute-script.py.template:1609`) and pinned by a test that goes red under mutation; nothing promoted requires code to change |

## What could NOT be verified

- **The cold reads.** The plan's Verification section requires the Step 6 sub-agent to hand a promoted rule to a cold reader with a violating sample. That transcript is not in the tree; only the report's assertion that all four cold reads passed exists. What is checkable — whether the promoted text states the two D1(b) answers the plan names — does hold (`build-systems-common.md:99` yes for the missing row; :110 the present row is not authoritative).
- **The run observations themselves.** (c)'s asymmetry — "harness-initiated auto-backgrounding preserved the job every time; caller-initiated background execution was killed twice" — is a run observation with no artifact in the tree and is not reproducible on demand; the plan labels it HYPOTHESIS. Only its numeric half (600000 ms = the 600 s harness ceiling) is source-confirmed.
- **Branch-local commits and CI check details.** The pre-rebase SHAs the report cites, the `merge_group` verify failure, and the per-check `verify / conclusion = success` / `verify / verify = skipped` results are not reachable from the clone. The consequence of the merge_group failure *is* verifiable and passes: the historical-prose analyzer reports 0 findings across `marketplace/`.
- **The plan's live-corpus claim** ("the authoring machine held 23 active lessons, none from the dated range") — unverifiable by construction, as the plan itself states.

## Adversarial review

**Reviewed by:** an independent agent that did not write this document.

**Checked.** Every `high` gap: there were none, so instead every row of the deliverable table and every "swept, clean" claim was re-derived from scratch.

- **The landed diff.** `git show --stat 894c37d5` → 3 files, 360 insertions, 0 deletions; `git show 894c37d5 -- .../build-systems-common.md` read in full, so the promoted text is distinguished from the two later commits that also touch that file (`d4ae2e81` #1193, `e2b6665b` #1279 — the "three non-green conditions" subsection at :126-184 is **theirs, not this plan's**, which the original document did not say).
- **Every file:line and symbol citation in § Method**, opened at HEAD: `build-systems-common.md` headings (:7, :19, :46, :69, :77, :92, :126, :186 — all as cited), `execute-script.py.template:314/329/334/430/812/1609`, `_cmd_pre_commit_verify_freshness.py:456-471`, `constants.py:35`, `manage-change-ledger/SKILL.md:69/74-83/218-256`, `pyproject-impl.md:75`, `extension-api/SKILL.md:5,14`. All resolve.
- **Functions executed, not read.** `_derive_build_status` was extracted from the template and run on eight real argument pairs: `(-9, None) → killed`, `(1, {'status':'success'}) → error`, `(0, None) → unknown`, `(0, {'status':'success'}) → success`, `(0, {'status':'timeout'}) → timeout`, `(0, {'status':'unknown'}) → unknown`, `(0, {'foo':1}) → unknown`, `(0, {'status':123}) → unknown` — the promoted wording for `unknown` is confirmed by execution, and `WRAPPER_CLAIMABLE_BUILD_STATUSES` was read from `_ledger_core.py:62` rather than assumed. `_mentions_help` was likewise run on real argv: `run --help`, `run --help=x`, `run -h`, `run -vh` → `True`; `run`, `run --helpful` → `False`. **That last run produced G6** — the promoted text names only `--help`.
- **The mutation was re-applied independently.** `git diff --quiet` on the template returned 0 (clean); bytes saved to the scratchpad (md5 `c2f0e5538ae98e4987633e58556d225c`, matching the figure this document cites); the `and not _mentions_help(script_args)` conjunct removed at :1609; test re-run → **6 failed, 24 passed**, and the six names are exactly the six this document lists. Restored by copying the saved bytes back; md5 matches and `git status --porcelain` is empty.
- **Test and analyzer figures re-derived.** `test_build_class_stamp_discriminator.py` → **30 passed** (unmutated). `analyze_historical_prose_in_skills(Path('marketplace'))` → **0 findings** (run with `ref-toon-format`, `script-shared`, `tools-file-ops`, `tools-marketplace-inventory` and `plugin-doctor` script dirs on `sys.path`).
- **D0's evidence re-derived**: `.gitignore` at `894c37d5` lines 46-48 and at HEAD lines 45-47; `git ls-files .plan` → `marshal.json` + 12 `project-architecture/**` files only; `git ls-files | grep -i lesson` → skill sources only. `git log -S` on both `_mentions_help(script_args)` and the full conjunct line → single hit `415dcf13`, 2026-08-09.
- **GitHub re-queried**: `pull_request_read get` and `get_commits` on #1169 → `merged: true`, merged by `cuioss-oliver` at 2026-08-11T20:36:12Z, `head.ref = chore/landed-residue-promotion-sweep`, labels `[skip-bot-review]`, `changed_files: 3`, `additions: 360`, `commits: 10` with the ten SHAs as listed. The five pre-rebase SHAs were re-tested with `git cat-file -t` — all five fail, as stated.
- **Sweeps re-run broader than the originals**, un-scoped to the whole repo: `over-inclusive` across `*.md`/`*.py`/`*.template`/`*.adoc` (2 hits, both unrelated `plan-retrospective` uses); `build-systems-common` whole-repo (adds `doc/concepts/build-management.adoc:46`); every `*.md`/`*.adoc` file mentioning `kind=build` (13 files) opened at the mentioning line; and a sweep on backgrounding vocabulary (`auto-background`, `self-background`, `backgrounded`) that the original patterns could not reach — **this is what produced G7**, and it also refuted G2's stated rationale.

**Not re-checked:** the cold-read transcripts (absent from the tree, as recorded); the (c) run observation itself; the `merge_group` CI check details; the plan's live-corpus claim; the CodeRabbit review threads on #1161; and whether the historical-prose analyzer is non-vacuous (it was run, not mutated).

| Item | Original claim | Verdict | Evidence |
|---|---|---|---|
| Verdict `implemented-with-gaps` | D0/D2/D3 clean, D1 partial | **upheld** | D2 and D3 are `n/a` because D0's declared stop condition fired, not because they were skipped — the landed diff touches exactly one standards file and no test, which is what "correctly halted / correctly reported empty" predicts. No deliverable is unimplemented, so `partially-implemented` would be wrong. |
| D0 row (clean pass) | residue set not derivable, method + population recorded | **upheld** | `.gitignore` and `git ls-files` re-derived independently at both commits; nothing in git maps a retired lesson id to its rule. |
| D2 row (clean pass) | correctly halted, nothing reconstructed | **upheld** | `git show --stat 894c37d5` = 3 files; no second standards file, no invented rule. |
| D3 row (clean pass) | correctly reported empty; the cited existing test is non-vacuous | **upheld** | No test in the landed diff; 30 passed clean, 6 failed under an independently re-applied mutation, with the same six test names. |
| G1 | `manage-change-ledger/SKILL.md` states a two-way conjunction source has implemented as three-way since `415dcf13` | **upheld, wording sharpened** | Confirmed at :74-83 and at `execute-script.py.template:1609`; `git log -S` confirms `415dcf13` / #1127 / 2026-08-09 as the sole introducer. Sharpened because the two-condition sentence is a *correct* description of `_is_build_class_notation`; the defect is equating that function with the stamp predicate. Line range corrected `:74-82` → `:74-83`. |
| G2 | residues (a)/(c) unreachable from any agent-facing surface | **upheld as an omission, re-severitied medium → low, rationale rewritten** | The reachability half survives a whole-repo sweep (one further reader, `doc/concepts/build-management.adoc:46`, is project docs). But "the audience never meets the rule" is **false**: `await-long-running.md:84` already states the harness kills backgrounded jobs with zero output, :88 already forbids polling, and `agents.md:164-178` already forbids a leaf from backgrounding a build. What is unreachable is the mechanism and the ledger substitute, not the prohibition — so the gap is real and smaller. |
| G3 | (c) and `persona-plan-marshall-agent:75` give opposed framings of auto-backgrounding | **upheld** | Both lines confirmed verbatim. Noted that the two documents prescribe the same *action*, so only the connotation conflicts — which is why `low` is right. |
| G4 | three stale statements in `report-01.md` | **split into G4 + G5, third statement refuted** | Bundling three instances in one row is itself a defect (a finding is recorded per instance) and the compound done-when could be half-satisfied. G4 keeps the § Build gate ↔ § Run continuation contradiction (`:116` vs `:270`); G5 is the contract-check table (`:199`, `:206`). The "all six commits" statement is refuted — see gaps.md § Refuted. |
| G6 | *(new)* the promoted text names only `--help` where `_mentions_help` also fires on `-h`, `-h=`, and `-h` in a cluster | **added** | Produced by executing `_mentions_help`, not by reading it. A defect in the text this plan shipped, not an untouched doc. `low`. |
| G7 | *(new)* residue (c)'s unqualified "never background a long build" contradicts the leaf/orchestrator build-tier SSOT | **added** | `agents.md:164-178` declares itself the single source of truth and states an `orchestrator`-tier build is not runnable in-leaf "(inline OR backgrounded)" while `await-long-running` "is the ONLY component permitted to background a build" (`:78-84`). `medium`, not `high`, because G2 establishes the section has no agent-facing reader today. |

**Documents corrected.** In `verification.md`: the base-commit header now names both commits and the command that shows the subject surface identical at each (the "two intervening commits" figure no longer re-derives — there are 33, all `doc/plans/**`); the § Method sweep bullet now records that the sweep was too narrow and names what a broader one found; drift item 1 gains G7 and the corrected line range; drift item 3 is withdrawn with its reasoning; the D1 table row names G6 and G7. In `gaps.md`: G1 sharpened and line-range-corrected; G2 re-severitied to `low` with a rewritten rationale, a corrected reader set, and a second fix target (`await-long-running.md`); G3 unchanged but for a commit citation; G4 split into G4 and G5; G6 and G7 added; a `## Refuted during adversarial review` section added; **Open items** 4 → 7.

**Residual doubt.** Three places a third reviewer should look first. (1) **G7 versus G2 are in tension by construction** — G7 says a reader of (c) gets a contradictory instruction, G2 says there is no such reader. If the right remedy is to *move* (a) and (c) out of `extension-api` rather than cross-link into it, G2, G3 and G7 collapse into one relocation and the severities should be re-cut together. (2) **The plan required D1's residues to land "in the build / `script-shared` standards specifically"; none of `script-shared` was touched.** This review accepted `extension-api/standards/build-systems-common.md` as the shared build standard (the four `*-impl.md` files do point at it) and did not file a gap, but a reviewer who reads that sentence as binding placement would file one. (3) **The historical-prose analyzer was run, not mutated** — its 0-findings result is the only unproven-non-vacuous figure either document relies on.
