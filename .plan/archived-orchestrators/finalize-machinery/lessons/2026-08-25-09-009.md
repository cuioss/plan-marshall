# 2026-08-25-09-009 — analyze-logs reports build_count 0 against 84 recorded pyproject_build calls

component: plan-marshall:plan-retrospective
category: bug
status: moved-to-epic (OPEN — counter-population future work; corpus original retained)
source: corpus lesson 2026-08-25-09-009, copied into finalize-machinery per operator direction
(note: condensed 2026-09-17 — seven observations; mechanisms separated: daemon path writes
no row; attribution lost to NO_PLAN; direct path 1-row-per-60; equal totals disagreeing
(23 vs 23, union 36))

## Context

`build_count: 0` against 84 daemon-routed calls (48% of script time) — counter matches
only the direct-execution emission shape; confident zero reads as "ran no builds".
Consumers honest (render `unavailable`), but build-minimality facet starves every run.

## Proposed action

Count from a marker the daemon path emits; state which zero (genuine none vs matched
nothing) with the population; write rows on the common path below routing; stamp plan
id at write (explicit unresolvable state, never NO_PLAN default); publish row-vs-call
counts side by side.

## Epic status

Open future work (build-telemetry theme). Corpus original RETAINED.
