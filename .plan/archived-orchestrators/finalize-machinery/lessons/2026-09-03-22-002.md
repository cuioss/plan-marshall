# 2026-09-03-22-002 — Lessons carrying YAML frontmatter are listable but unaddressable by every id-keyed verb

component: plan-marshall:manage-lessons
category: bug
status: moved-to-epic (OPEN — corpus-integrity future work; corpus original retained)
source: corpus lesson 2026-09-03-22-002, copied into finalize-machinery per operator direction
(note: condensed 2026-09-17)

## What happened

Housekeeping tried to retire 17-001; `remove` → not_found for an on-disk, listed
lesson. 10 of 78 lessons carry YAML `---` frontmatter instead of `key=value`: listed
(empty component/category) but unreachable by get/read/remove/update/supersede/
set-body/set-title — no-exit state, accumulating, silently overstating the active
corpus; housekeeping treats the miss as non-fatal. Recurrence: 12 of 87 days later —
write path still open.

## How to apply

Normalise the ten AND close the write path (reject malformed at write, or teach the
reader both forms); preferably make list and id-verbs agree either way.

## Epic status

Open future work (lessons-pipeline theme, PLAN-05 adjacent). Corpus original RETAINED.
