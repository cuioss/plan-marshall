# 2026-09-08-13-004 — Correct the documented --measured-diff-size form; the flag is scalar, not optional-valued

component: plan-marshall:automatic-review
category: bug
status: moved-to-epic (corpus original retired 2026-09-17: completely_covered — same contract as 08-25-09-014, correct at HEAD per PR #1507 run)
source: corpus lesson 2026-09-08-13-004, moved into finalize-machinery per operator direction

## Context

SKILL.md documented `--measured-diff-size` with an empty placeholder and asserted every
list flag is `nargs='?'` — neither holds for this scalar flag; the documented form
rejects outright. Worst documentation-defect kind (no reason to distrust; opaque exit 2).
The general assertion is the more dangerous half (stated once, applied many times).

## Root cause

Invocation block written from neighbouring `nargs='?'` flags and generalised. Nothing
validates documented forms against the live argparse surface (manage-invocation-invalid
checks names, not forms).

## Proposed action

Fix the form; replace the blanket sentence with per-flag truth; consider extending the
`--help`-walk validator to flag forms (value-required vs optional-valued).

## Coverage (finalize-machinery)

Same contract as 08-25-09-014, verified correct at HEAD by the PLAN-02 / PR #1507 run.
Corpus original retired as completely_covered.
