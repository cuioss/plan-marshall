# 2026-09-06-10-001 — An out-of-band loop-back override skips the re-arm the loop-back machinery owns

component: plan-marshall:phase-6-finalize
category: improvement
status: moved-to-epic (OPEN — cost/order future work; corpus original retained)
source: corpus lesson 2026-09-06-10-001, copied into finalize-machinery per operator direction

## Context

At loop-back ceiling 3/3, a genuinely productive fourth cycle proceeded under operator
override — without mutating max_iterations, so it routed around `set-phase` and its
re-arm bookkeeping: three gates carried verdicts anchored to an ancestor HEAD, silently
(`outcome: done` everywhere). The ceiling also miscounts: bounds total loop-backs when
it means to bound unproductive churn (`returned_with_findings` already stamps the
discriminator). Opposite failure to 19-005 on the same surface: re-arming is
all-or-nothing and coupled to the loop-back path rather than to HEAD.

## Proposed action

First-class operator-authorised ceiling extension (recorded grant admitted as a normal
loop-back, so set-phase runs); exclude `returned_with_findings` loop-backs from the
ceiling (or separate budget); pre-merge staleness assertion comparing each completed
head-dependent step's anchor against the merge candidate (catches stale greens however
authorised).

## Epic status

Open future work (WS-01 follow-up). Corpus original RETAINED.
