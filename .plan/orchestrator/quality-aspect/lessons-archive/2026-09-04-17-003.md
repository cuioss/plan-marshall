id=2026-09-04-17-003
component=plan-marshall:phase-3-outline
category=improvement
status=active
created=2026-09-04

# Give a sweep deliverable a declaration form its execution can actually satisfy

# Give a sweep deliverable a declaration form its execution can actually satisfy

## Context

Deliverables 7 (migrate hand-rolled sys.path preambles onto the shared loader) and 8 (convert non-enabled-rule suppressions, close the gate with RUF100) are tree-wide SWEEPS. Both declared their mutation scope as an enumerated outline-time snapshot of the tree. Execution reached 300 files beyond that snapshot: the realized footprint is 521 paths against 221 declared-and-hit, so 57% of what shipped sits outside every deliverable's declared surface.

## Root cause

The outline offers only a path list. A sweep's real scope is a PREDICATE ("every test module carrying a hand-rolled sys.path preamble"), and the set that predicate selects moves between outline time and execute time. Enumerating it produces a declaration that is wrong the moment anything else lands, and the retrospective then reads the difference as scope creep even though no unplanned work occurred.

## Proposed action

Add a predicate-shaped mutation-scope form to the deliverable schema — a glob or a named detector plus the command that enumerates it — so a sweep declares the rule rather than a snapshot of its output. The coverage comparison then re-evaluates the predicate against the realized footprint instead of differencing two lists taken at different times.

## Evidence

- aspect: request_result_alignment — scope_creep 300 files, far above the <5-file acceptable band; recall on declared modification-intent files is 99.1%, so the drift is entirely in the other direction
- aspect: artifact_consistency — references_only[300], affected_files_exact_match status warn
- aspect: outline_vs_shipped — touched_but_unassessed 521 of 521 realized_footprint_paths
- Downstream consequence: the 515-file diff is exactly what both size-capped review bots then refused, so the declaration style and the lost review coverage are causally linked on this plan

## Recurrence — 2026-09-09 (close-the-runtime-slice-parametrization, PR #1455)

Same defect, **opposite direction** — which is the finding. This plan's five survey
deliverables ("Survey, classify and collapse tabular test families in {module}") declared 142
modification-intent files and shipped 97, a recall of 68.3% with 45 declared files never
touched. Scope creep was exactly **zero**: `references_only[0]`, so every realized path fell
inside some deliverable's declared surface.

The cause is the same enumerated snapshot, read the other way. A survey deliverable's real
scope is the predicate "every test module carrying a collapsible tabular family", and a file
surveyed and found to carry none is a **correct no-op** — yet the outline declared all 142
candidates under `Files expected to mutate:` rather than under `Files to survey:`, so every
correct no-op scored as a missed modification. Three of eight deliverables landed `partial`
on coverage alone (D3 59.3%, D4 47.8%, D5 66.7%) while all 12 tasks reached `done` and every
stated success criterion held (collected items 20766 -> 20954, never lowered; skips unchanged
at 8).

The original entry showed an enumerated snapshot going stale outward. This one shows it
being wrong inward at declaration time, on a plan where nothing drifted at all. A path list
cannot express "these are candidates; the predicate decides which mutate", so it mis-grades
in whichever direction the predicate happens to resolve. That is the argument for the
predicate-shaped form above, now evidenced from both sides.

- aspect: request_result_alignment — recall 68.3%, `declared_but_untouched: 45`, `scope_creep` zero
- aspect: artifact_consistency — `affected_files_recall` fail at 68% against a 70% threshold; `outline_only[45]`, `references_only[0]`
- aspect: outline_vs_shipped — `comparison: measured`, `include_unrealised: 0 of 0`, `exclude_violated: 0 of 13`
