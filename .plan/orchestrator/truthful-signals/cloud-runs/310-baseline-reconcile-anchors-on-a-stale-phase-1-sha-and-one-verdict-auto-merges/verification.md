# Verification — 310-baseline-reconcile-anchors-on-a-stale-phase-1-sha-and-one-verdict-auto-merges

**Verified against:** commit `2402b02bf5bc64b5ece468b6d2a3e884b5f0b30d`   **Landed as:** PR #1206, commit `60e5fd81b48949d455413a84975cc39e79475f94`   **Verdict:** implemented-with-gaps

## Method

Read `plan.md` and `report-01.md` in full. Located the landing with `git log --oneline --all --grep '#1206'`, read `git show --stat 60e5fd81` and the per-file diffs for both scripts and all six docs. Read the pre-fix file (`git show 60e5fd81^:…/_cmd_baseline_reconcile.py`) to confirm the plan's asserted-absence hypotheses at source.

Files opened at HEAD:

- `marketplace/bundles/plan-marshall/skills/workflow-integration-git/scripts/_cmd_baseline_reconcile.py` (all 613 lines)
- `marketplace/bundles/plan-marshall/skills/workflow-integration-git/scripts/git-workflow.py` (`cmd_branch_sync_state` and helpers, lines 1152–1285; argparse registration line 2128)
- `marketplace/bundles/plan-marshall/skills/workflow-integration-git/SKILL.md` (command table, `### branch-sync-state`, `### baseline-reconcile`)
- `marketplace/bundles/plan-marshall/skills/phase-2-refine/standards/refine-workflow-detail.md` (Step 3d, lines 205–285) and `phase-2-refine/SKILL.md:129`
- `marketplace/bundles/plan-marshall/skills/phase-6-finalize/standards/finalize-step-sync-baseline.md`, `standards/branch-cleanup.md`, `standards/push.md`, `SKILL.md` (push-barrier re-entry block)
- `test/plan-marshall/workflow-integration-git/test_baseline_reconcile.py`, `test_git_workflow.py`
- `marketplace/bundles/plan-marshall/skills/plan-marshall/scripts/_invariants.py` (`_capture_worktree_sha`, line 704) to check the sibling-field coupling claim

Commands run:

- `uv run python -m pytest test/plan-marshall/workflow-integration-git/test_baseline_reconcile.py -o addopts="" -q` → **25 passed**
- `uv run python -m pytest test/plan-marshall/workflow-integration-git/test_git_workflow.py -o addopts="" -q` → **96 passed** (25 + 96 = 121, matching the report's "121 scoped tests")
- Tree-wide sweeps for `no_remote`, `auto_reconciled`, `merge_failure_paths`, `baseline_drift_reconcile_failed`, `auto_reconcilable`, `merge_commit_sha`, `worktree_sha`, `since phase-1-init`, `since the captured`.

Mutations applied (file byte-snapshot taken to the scratchpad first; `git diff --quiet` confirmed the file was not concurrently modified; restored from the snapshot, and `git diff --quiet` confirmed clean afterwards — no `git checkout`/`restore`/`stash` used):

1. **Anchor mutation** — re-introduced the defect by making `_resolve_merge_base` prefer `status.metadata.worktree_sha` when present. Result: **3 failed / 22 passed** — `test_two_calls_after_reconcile_agree_zero_upstream_no_overlap`, `test_in_flight_set_excludes_files_the_plan_never_touched`, `test_merge_base_recomputed_not_read_from_stored_status` all went RED. The D1/D5(a)/(b)/(c)/(d) tests are non-vacuous.
2. **D3 guard mutation** — replaced the post-probe condition at line 552 with `if False:`. Result: **1 failed / 24 passed** — `test_d3_guard_fires_when_probe_moves_head` went RED. The D3 guard is the only thing that test passes on.

## Deliverable-by-deliverable

| # | Deliverable | Done-when condition | Implemented? | As documented? | Correct? | Complete? | Evidence (file:line / symbol / command + result) |
|---|---|---|---|---|---|---|---|
| D0 | GATE: enumerate anchor readers + verdict side effects | Both directions enumerated, population stated | yes | yes | yes | yes | `report-01.md` § D0. Re-derived: `_resolve_baseline_sha` (pre-fix `_cmd_baseline_reconcile.py:159`) was the only reader of `status.metadata.worktree_sha` as a git SHA; grep over `marketplace/**/*.py` finds no other reader today (`_invariants.py:704 _capture_worktree_sha` computes it live via `git_head`, does not read the field; `manage-change-ledger` / `manage-architecture` hits are `compute_worktree_sha`, a content hash). Only ref-moving side effect pre-fix: `git merge origin/{base} --no-edit` at `60e5fd81^:…:415`. Blast radius correctly reported not knowable (`.plan/` git-ignored). |
| D1 | Anchor on merge-base of HEAD and base, per call | No range computed from a stored SHA | yes | yes | yes | **no** | `_cmd_baseline_reconcile.py:162 _resolve_merge_base`, called at `:443` inside `cmd_baseline_reconcile` (no cache, no module-level memo). Both ranges anchor on it: `:294 _list_upstream_commits` (`{mb}..origin/{base}`) and `:598 _list_in_flight_files` (`{mb}..HEAD`). Fails closed at `:445` with `merge_base_unresolved`. Mutation 1 confirms the tests bind. **Incomplete:** two docs still state the old anchor — see G1, G2. |
| D2 | Probe non-mutating on every path | No classification path moves a ref | yes | yes | yes | **no** | The whole focused-reconcile block is gone; classification at `:475-481` derives from `merge-tree --write-tree` (`:341 _detect_merge_conflicts`) plus the file-set intersection. `auto_reconciled`/`merge_failure_paths`/`baseline_drift_reconcile_failed` are zero-occurrence tree-wide. Re-derived adversarially by **mutation**: re-inserting a real `git merge origin/{base}` on the `overlap_no_content_conflict` branch turns `test_classification_overlap_no_content_conflict_is_non_mutating` and `test_classify_only_never_moves_head_on_every_classification` RED (2 failed / 23 passed) — the no-ref-move guarantee is genuinely pinned. Dependency check: no flow depended on the merge — `finalize-step-sync-baseline.md:30` and `branch-cleanup.md` § "Rebase Branch onto Base" perform the rebase via `worktree-rebase-to`. **Incomplete:** a persisted `references.json` write survives on every path and one consumer sentence denies it — see the D2 note below and G8. |
| D3 | Fail-loud guard: post-probe HEAD assertion | Regression caught at the probe | yes | yes | yes | **no** | `head_before` at `:434`, `head_after` + guard at `:549-560` returning `status: error, error: probe_mutated_head` ahead of the payload and ahead of the persist-failure arm. Mutation 2 confirms the guard is what makes `test_d3_guard_fires_when_probe_moves_head` pass. **Incomplete:** `probe_mutated_head` (and the two new skip reasons) appear in no skill document — see G3. |
| D4 | Verdicts reconcilable; ambiguous verdict must not route to a destructive action | A test pins both | yes | yes | yes | **partly** | Half 1: `test_two_calls_after_reconcile_agree_zero_upstream_no_overlap` (real git fixture, real merge, asserts equal classification + 0 upstream). Half 2: `git-workflow.py:1195 cmd_branch_sync_state` splits the absent-ref case into `remote_absent_landed` (`:1268`, proven by `:1179 _head_contained_in_base`) and `remote_absent_unverified` (`:1277`, the decline). `no_remote` as a *state* is gone tree-wide. **But** the "must not route to a destructive action" half is pinned by `test_verdict_token_drives_refire_skip_mapping`, whose mapping is a lambda defined inside the test — see G4. |
| D5 | Five tests (a–e), each red pre-fix | All five pass, each seen red first | yes | yes | yes | yes | (a)+(d) `test_baseline_reconcile.py:941`; (b) `:992`; (c) `:1027`; (e) `:1060`; plus D3 `:1111`. All 25 pass at HEAD. Red-first independently re-derived for (a),(b),(c) by mutation 1 and for D3 by mutation 2. **(e) corrected by adversarial review** — it was written off as "trivially green by construction"; mutation 3 (re-inserting the removed `git merge` on the `overlap_no_content_conflict` branch) turns it RED, so it is a live guard, not a tautology. Mutation 4 (memoising `_resolve_merge_base` per `(worktree_path, base_branch)`) turns D5(a) and D5(b) RED, so D1's "**per call, not cached**" clause — which no listed test claims to cover — is in fact pinned. |

**D1 — incomplete sweep.** The code is right; two prose sites still state the removed anchor. `marketplace/bundles/plan-marshall/skills/workflow-integration-git/SKILL.md:184` (the `baseline-reconcile` row of the command table) reads "lists upstream commits since the captured `worktree_sha`". The very same table's `branch-sync-state` row six lines above was edited by this commit, so the miss is inside the edited region. `marketplace/bundles/plan-marshall/skills/phase-2-refine/standards/refine-workflow-detail.md:280` reads "Zero upstream commits since phase-1-init" in the Step 3d "Skip Conditions Summary" — again in a file this commit edited (line **234** of the same file was correctly updated to name the merge-base; the "231" in the first draft of this document did not re-derive).

**D3 — undocumented signal.** `probe_mutated_head`, `merge_base_unresolved` and `head_unresolved` exist only in the script and its tests. `workflow-integration-git/SKILL.md`'s `baseline-reconcile` entry documents no return fields or reasons at all; `refine-workflow-detail.md:275-282`'s "Skip Conditions Summary" enumerates the skip reasons and omits both new ones; the finalize consumer blocks only instruct "if the script exits non-zero → STOP", which does not cover a `status: skipped` or a `status: error` returned with exit 0. A guard whose signal no consumer is told to read is only half a fail-loud. **Corrected by adversarial review:** the consumer set here was under-counted. `branch-cleanup.md` dispatches the probe **twice** (`:223` pre-rebase and `:514` § "Re-run the classifier against the current head", the latter feeding the pre-merge operator prompt), and `phase-5-execute` is a **third** call site that this document did not open at all — see gaps.md G7. The exit-0 mechanism was confirmed by running the CLI (`triage_helpers.print_toon` always returns 0; `file_ops.safe_main` exits with that value), and the absent-field consequence by running `cmd_baseline_reconcile` against an orphan history: the payload is exactly `{status, plan_id, reason, base_branch}`.

**D4 — the routing half is untestable as built.** `branch-sync-state` has no code consumer at all (grep over `marketplace/ --include=*.py` finds only the script's own definition at `git-workflow.py:1195`, the argparse registration at `:2128-2134`, and two docstring mentions); the push barrier's re-fire rule lives in `phase-6-finalize/SKILL.md:698-716` prose, restated a second time in `standards/push.md:119`. The test's mapping half therefore asserts a dictionary derived from a lambda declared three lines above it, and would stay green if the SKILL.md prose were edited back to re-firing on a ref-absent state. **The adversarial review stopped saying "would" and did it:** the `remote_absent_unverified` branch at `SKILL.md:711-713` was rewritten to "RE-FIRE (re-push the branch)" and the three `branch-sync-state` tests all stayed green (`3 passed`). That is the rubric's "a guard that passes against the defect it names", so gaps.md G4 is re-severitied `medium` → `high`.

**D2 — one persisted write survives, and one consumer sentence overstates the guarantee.** D2's done-when ("no classification path moves a ref") is met, and no ref-moving path remains. But the plan's **Goal** says the probe "writes nothing on any path", and it still does write: `cmd_baseline_reconcile` calls `_maybe_auto_update_stale_base_branch` at `:414` on every invocation, ungated by `--no-emit`, and on a stale base branch that helper persists a rewritten `base_branch` into `references.json` and emits a decision-log entry. Reproduced by execution, not read: a `--no-emit` run whose configured base branch does not exist on origin recorded a persisted-write attempt and returned `base_branch_updated: True, original_base_branch: gone-branch`. The D3 guard cannot see it — it compares HEAD only. The sentence the landing added at `finalize-step-sync-baseline.md:76` ("it performs only `fetch + diff + merge-tree`") is therefore false in the direction that matters. See gaps.md G8. The deliverable stays `implemented` because its own done-when is ref-scoped; the `Complete?` column is corrected to **no**.

## Report accuracy

Contradictions found:

1. **"no stale … 'init-time SHA anchor' prose survives"** (§ Findings, verification-sub-agent paragraph) — **contradicted**. `workflow-integration-git/SKILL.md:184` and `phase-2-refine/standards/refine-workflow-detail.md:280` both still state the init-time anchor. The sub-agent's sweep, and the report's endorsement of it, are wrong on this point.
2. **"three `.py` files: `_cmd_baseline_reconcile.py`, `git-workflow.py`, and two test modules"** (§ Build gate) — the enumeration names **four** files. Re-derived from `git show --name-only 60e5fd81`: `_cmd_baseline_reconcile.py`, `git-workflow.py`, `test_baseline_reconcile.py`, `test_git_workflow.py`. The count word is wrong; the verdict it supports (build gate takes the full path) is unaffected.
3. **Header fields** — "**PR:** _pending_    **Outcome:** _in progress_" while the same document's § Contract check names PR #1206 and the change landed as `60e5fd81`. Internally inconsistent and now stale.

Checked and found accurate: the D0 enumeration (single git-SHA reader; the other `worktree_sha` matches are a different content-hash concept); "`auto_reconciled`/`merge_failure_paths`/`baseline_drift_reconcile_failed` are zero-occurrence in the bundle" (re-derived: 0 hits each); "no `no_remote` state token survives … except the … probe skip-reason" (re-derived: exactly one hit, `_cmd_baseline_reconcile.py:405`, the no-git-remote skip — line number has shifted from the reported 386 because of a later unrelated commit); "the surviving `merge_commit_sha` hits are the legitimate `references.merge_commit_sha` landing field" (re-derived: all 10 hits are `manage-references` / `plan-retrospective` / `branch-cleanup` landing-field uses); "all 121 scoped tests pass" (re-derived: 25 + 96 = 121, all pass); "the finalize steps already performed their own rebase and never depended on the probe's merge" (confirmed at `finalize-step-sync-baseline.md:30` and `branch-cleanup.md` § Rebase Branch onto Base); the D3-fires claim (re-derived by mutation); PR #1206 (`git log --grep '#1206'` → `60e5fd81`) and the later plan #1210 the Residue section cites (`28b7f7d4`). **Corrected:** the CLI `--help` fix's *content* is present at `git-workflow.py:2128-2133`, but the commit SHA `51e4c55` is **not resolvable in this clone** (`git cat-file -t 51e4c55` → "Not a valid object name") — PR #1206 was squash-merged, so the branch commits do not exist here. The first draft listed that SHA under "checked and found accurate"; only the content was checkable.

## Out-of-scope compliance

Compliant. The landed diff touches 11 non-plan files, all inside the declared Expected surface: the two scripts, their two test modules, and seven consumer docs in `workflow-integration-git`, `phase-2-refine`, `phase-6-finalize`. No collateral change outside those bundles.

- "Changing what the anchor field means for its other consumers" — honoured; `manage-status` is untouched, and D1 removes the read rather than redefining the field (the plan listed `manage-status/**` as expected surface, and the run correctly landed nothing there, explaining why).
- "Hardening the auto-merge rather than removing it" — honoured; the merge block is deleted, not guarded.
- "A general branch-recovery workflow" — honoured; `remote_absent_unverified` declines and no recovery path was designed.

## Residue carried forward

- **"The arm-once-green step is the only remaining action."** — **closed.** PR #1206 landed as `60e5fd81` on `main`; a later plan (#1210) sanctioned the arm-and-hand-off contract change the report proposed under "What have we learned".
- **"No code, test, or doc residue — all deliverables landed in commits on this branch."** — **contradicted by today's tree**: two stale doc statements survive (G1, G2), the new signals are undocumented at four consumer blocks (G3, G7), and the landing itself added one over-broad non-mutation claim (G8).

## What could NOT be verified

- The whole-tree figures `19463 passed, 14 skipped, 0 failed` and the `./pw quality-gate` clean result — the tree has advanced far past the landing, so those numbers are not re-derivable at HEAD, and a whole-tree run was out of proportion for this check. **Figure corrected:** the first draft said "~18 commits"; re-derived, `git rev-list --count 60e5fd81..2402b02b` = **97** (60 touching `marketplace/` or `test/`, 56 touching a `.py` file). The correction strengthens the conclusion rather than changing it.
- The red-first claim for the remaining 7 of the reported 11 pre-fix failures (the `auto_reconcilable` rename updates, the `overlap_no_content_conflict` non-mutating rewrite, and the three branch-sync-state D4 tests). Four were re-derived by mutation; the rest were taken as read.
- The historical blast radius of the auto-merge — genuinely not knowable from this clone, exactly as the report states (`.plan/` is git-ignored and absent).
- CI check-run states on head `51e4c55` and the reviewer-participation table — provider-side facts, not derivable from the tree.
- Whether `status.metadata.worktree_sha` still has any live reader anywhere outside `marketplace/` (the sweep covered the bundle tree and `test/`). **Narrowed by adversarial review:** inside `marketplace/**/*.py` there is now **no** reader of the metadata field at all — the two handshake columns that carry the same names are computed live (`_invariants.py:704 _capture_worktree_sha` calls `git_head(metadata['worktree_path'])`; `:584 _capture_main_sha` reads the main checkout), and every other `worktree_sha` hit is the `compute_worktree_sha` content hash. So the field is now written (by phase-5-execute self-absorption) and read by nothing on this surface. That is **not** filed as a gap: the plan explicitly puts "changing what the anchor field means for its other consumers" out of scope and reserves it to a sibling plan. It is flagged under Residual doubt instead.

## Adversarial review

**Reviewed by:** an independent agent that did not write this document.

**Checked.** Every one of G1–G6 was re-derived from the tree rather than re-read from this document; every "swept, clean" claim was re-run, twice with a broader pattern than the original; every `high`-candidate row was re-tested by mutation. Concretely:

- **Files opened at HEAD that this document had not opened:** `phase-5-execute/SKILL.md` (Step 3, lines 383–441), `phase-5-execute/standards/sync-with-main.md` (whole drift/self-absorption contract), `branch-cleanup.md` § "Re-run the classifier against the current head" (`:500-540`), `workflow-integration-git/SKILL.md` § `branch-sync-state` (`:431-478`) and § `baseline-reconcile` canonical-invocation stub (`:862-870`), `script-shared/scripts/workflow/triage_helpers.py` (`print_toon`, `create_workflow_cli`), `tools-file-ops/scripts/file_ops.py` (`safe_main`), `_invariants.py:584 _capture_main_sha`, `_cmd_baseline_reconcile.py:224-291` (the `references.json` auto-update helpers).
- **Commands run:** both scoped test modules (`25 passed`, `96 passed`, `121 passed` — the 25 + 96 = 121 split re-derived); `git show --name-only 60e5fd81` (13 paths, 11 non-plan, 4 `.py`); `git show 60e5fd81 -- …/SKILL.md` and `… -- …/refine-workflow-detail.md` (to prove the two misses sit inside edited regions); `git cat-file -t` on all three cited SHAs; `git rev-list --count 60e5fd81..2402b02b`; `git log --grep` for #1206 and #1210; sweeps for `no_remote` (1 hit in `marketplace/`), `merge_commit_sha` (10 hits), `auto_reconciled` / `merge_failure_paths` / `baseline_drift_reconcile_failed` (0 hits), `probe_mutated_head|merge_base_unresolved|head_unresolved` (3 hits, all in the script), `auto_reconcilable == false` (2 hits), and a **broader** anchor sweep (`since (the )?(plan|phase-1|init|baseline)|initiali[sz]ation SHA|stored SHA|captured SHA|baseline sha|anchor(ed|s)? on` over all `*.md`) that surfaced no third stale site beyond G1/G2.
- **Functions executed** (not merely read): the `git-workflow` CLI end-to-end (`branch-sync-state --plan-id definitely-not-a-real-plan` → `status: error`, **exit 0** — the mechanism G3 asserts); `cmd_baseline_reconcile` against a purpose-built worktree with an orphan history (→ `status: skipped, reason: merge_base_unresolved`, payload keys exactly `{status, plan_id, reason, base_branch}` — no `classification`, no `conflict_count`); and `cmd_baseline_reconcile --no-emit` against a clone whose configured base branch does not exist on origin (→ one persisted `references.json` write attempt, `base_branch_updated: True`) — the evidence for G8.
- **Mutations applied** (each preceded by `git diff --quiet`, byte-snapshotted to the scratchpad, restored from that snapshot, md5 re-verified, `git diff --quiet` clean afterwards; no `git checkout`/`restore`/`stash`): **(3)** re-inserted the removed real `git merge` on the `overlap_no_content_conflict` branch → 2 failed / 23 passed, refuting "D5(e) is trivially green by construction"; **(4)** memoised `_resolve_merge_base` → 2 failed / 23 passed, showing D1's "per call, not cached" clause is pinned; **(5)** rewrote the `remote_absent_unverified` branch of `phase-6-finalize/SKILL.md` to RE-FIRE → the three `branch-sync-state` tests stayed **green**, proving G4's guard is vacuous.

**Not re-checked.** The whole-tree `19463 passed` / `./pw quality-gate` figures (out of proportion, and 97 commits stale). The red-first claim for the 7 pre-fix failures this document already listed as taken-as-read — mutations 3–5 add three more independently re-derived, leaving 4 still taken as read. Provider-side facts (CI check-run states, the reviewer-participation table). The historical blast radius. `manage-status` internals beyond the field-reader sweep. The 96 tests in `test_git_workflow.py` were run but only the 3 `branch-sync-state` ones were mutation-probed.

| Item | Original claim | Verdict | Evidence |
|---|---|---|---|
| Verdict | `implemented-with-gaps` | **upheld** | No deliverable is unimplemented — D1/D2/D3/D4 all have working code and binding tests; every open item is a doc, test-oracle or record defect. `partially-implemented` would be wrong. |
| G1 | `SKILL.md:184` still names the captured `worktree_sha` | upheld | Exactly one hit tree-wide; `git show 60e5fd81` edits the `:178` row and leaves `:184` untouched, so "a miss inside the edited region" is exact. Severity `medium` correct — a stale sentence, not wrong behaviour. |
| G2 | `refine-workflow-detail.md:280` still says "since phase-1-init" | upheld, **rewritten** | Row present verbatim. Two figures did not re-derive: the corrected line is **234**, not 231, and the gap is **46** lines, not 49. The **Done-when was unachievable** — the bare pattern `since phase-1-init` also matches an unrelated correct sentence at `phase-5-execute/SKILL.md:270`, so it can never reach zero; narrowed to `upstream commits since phase-1-init`. |
| G3 | Three new signals documented nowhere; consumers cannot act on them | upheld, **rewritten** | The 3-hit sweep re-derived exactly. The exit-0 + absent-field mechanism was **executed**, not inferred. The instance list was **incomplete**: `branch-cleanup.md` dispatches the probe twice (`:223`, `:514`), and the second feeds the pre-merge operator prompt. Where/Fix/Done-when extended to all three finalize blocks. Severity `medium` upheld. |
| G4 | The barrier-mapping test asserts its own oracle | upheld, **re-severitied medium → high** | Mutation 5: inverting the documented mapping to the exact resurrection defect leaves the test green. That is the rubric's "guard that passes against the defect it names", and the uncaught regression is destructive. Line range corrected `632-675` → `632-677`; the prose citation widened to `SKILL.md:698-716` plus the second restatement at `push.md:119`. |
| G5 | `auto_reconcilable == false` is a dead disjunct in two rules | upheld | Exactly two hits, at the cited lines; `:492` makes the disjunct unreachable. `low` is right — dead surface, no wrong outcome. **Fix made actionable**: "decide separately whether to drop the field" replaced with a definite instruction, and the Done-when given a grep gate. |
| G6 | Three factual errors in the run report | upheld | All three re-derived (`git show --name-only` → four `.py` files; header placeholders intact; G1/G2 refute the sweep clause). **Fix rewritten** — the original instructed striking a claim the run made; a report is a record, so the correction is now an appended note, while the two *unfilled template placeholders* are simply completed. |
| G7 | *(new)* phase-5-execute parses `conflict_count` with no `status` branch | **added** | A third documented call site this document never opened; not in the report's D0/D2 consumer enumeration; `git show --name-only 60e5fd81` touches no `phase-5-execute` path. Its non-`success` branch self-absorbs and writes plan state, so a declined probe can be read as "no drift". `medium`. |
| G8 | *(new)* the landing's own non-mutation sentence is broader than the code | **added** | `finalize-step-sync-baseline.md:76` — added by this commit — says the probe "performs only `fetch + diff + merge-tree`". Executed counter-example: a `--no-emit` run persists a `references.json` `base_branch` rewrite via `_maybe_auto_update_stale_base_branch` (`:414`), ungated by `--no-emit` and invisible to the HEAD-only D3 guard. The plan's Goal ("writes nothing on any path") is unmet; D2's ref-scoped done-when is met. `medium`. |
| D0 | Enumeration complete, population stated | **re-scoped** | The anchor-reader direction re-derives exactly (one reader pre-fix, none today — including no reader of the metadata field anywhere in `marketplace/**/*.py`). The mutate-on-verdict direction is script-scoped only: the phase-5-execute self-absorption write on the `conflict_count == 0` verdict is a verdict-driven mutation the enumeration does not name (G7). |
| D5(e) | "trivially green post-fix by construction" | **refuted** | Mutation 3 turns it red. It is a live guard. |
| D1 "per call, not cached" | asserted, not test-mapped | **strengthened** | Mutation 4 shows a memoised merge-base turns D5(a) and D5(b) red. |
| "~18 commits since the landing" | figure | **corrected** | 97 (`git rev-list --count 60e5fd81..2402b02b`); 56 touch a `.py` file. |
| "the CLI `--help` fix in `51e4c55`" | listed as verified | **corrected** | `git cat-file -t 51e4c55` → not a valid object in this clone (squash merge). Only the resulting content at `git-workflow.py:2128-2133` is checkable. |

**Documents corrected.** *verification.md*: D2's `Complete?` → **no** with a new D2 note; D5's evidence cell rewritten (mutation 3 + 4); the D3 and D4 notes extended with the missed call sites and the mutation-5 result; three figures corrected (line 231→234, "~18 commits"→97, `51e4c55` unverifiable); the `worktree_sha`-reader item in "What could NOT be verified" resolved; this section appended. The **verdict is unchanged** at `implemented-with-gaps`. *gaps.md*: open-item count 6 → **8**; G2's Done-when replaced (the original gate could never pass) and two line figures corrected; G3's Where/Fix/Done-when extended to the third finalize block and given executed evidence; G4 re-severitied to **high** with mutation evidence and its line range corrected; G5's and G6's Fix text made concretely actionable; **G7** and **G8** added; a `## Refuted during adversarial review` section added, recording that nothing was refuted and naming what was re-derived to reach that.

**Residual doubt — what a third reviewer should look at first.**

1. **G8's blast radius.** The `references.json` `base_branch` rewrite fires on every call including `--no-emit`, at four dispatch sites, and rewrites the rebase target. Nobody has asked whether a *wrong* auto-detected default (e.g. a fork whose `origin/HEAD` points elsewhere) can silently redirect a finalize rebase. That is a behavioural question this review only opened.
2. **`status.metadata.worktree_sha` is now written and read by nothing** on this surface. Out of scope for this plan by its own boundary, but a sibling plan owns the field and should be told the last consumer is gone — a field with a writer and no reader is the next stale-anchor incident waiting to be re-wired.
3. **`conflict_count == 0` is described as "the upstream commits and the worktree's in-flight changes touch disjoint sets of files"** (`phase-5-execute/SKILL.md:416`, `sync-with-main.md:51`). It is not — `conflict_count` comes from `merge-tree`, and `overlap_no_content_conflict` also has zero conflicts with *overlapping* files. This predates the plan and is deliberately not filed as one of its gaps, but it is the same false-equivalence class and sits on the same decision surface.
4. **The four remaining taken-as-read red-first claims** (the two `auto_reconcilable` rename updates and two of the three branch-sync-state D4 tests) were never re-derived by anyone.
