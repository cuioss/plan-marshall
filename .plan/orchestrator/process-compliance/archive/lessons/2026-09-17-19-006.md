# Archived lesson 2026-09-17-19-006 — incorporated into epic process-compliance

> Provenance: moved from `.plan/local/lessons-learned/2026-09-17-19-006.md` at operator
> direction. Epic target: WS-05 / PLAN-05-dispatch-envelopes. Source status at move:
> active. Body below is verbatim.

id=2026-09-17-19-006
component=plan-marshall:plan-marshall
category=improvement
status=active
created=2026-09-17

# verification-feedback loop_back returns must carry loop_back_target

# verification-feedback loop_back returns must carry loop_back_target

The wait-region unified triage (producer=finalize-feedback) returns
`status: loop_back` without a `loop_back_target` field, and item 7c produces no
`phase_steps` record — so neither of the two surfaces the continuation hook reads
carries the granularity decision. The orchestrator had to infer `5-execute` from
`fix_tasks_created > 0` via the granularity invariant.

## Proposal

Require `loop_back_target` on every `verification-feedback` `loop_back` return, the
same validation the `mark-step-done --loop-back-target` contract already enforces for
step records. The inference is correct today and unnecessary.

## Evidence

Plan plan-03-review-currency, unified triage return: `status: loop_back`, 6 findings
triaged, 5 fix tasks, no `loop_back_target` key present. Filed via epic
finalize-machinery inbox (plan-03-review-currency-010).
