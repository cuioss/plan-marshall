# Leaf distribution — the edge TARGET, publishing its name only in setup.cfg.
[metadata]
name = setuptools-core-lib
version = 1.0.0
description = Leaf distribution — depends on nothing internal.

[options]
install_requires =
    typing-extensions>=4.0.0
