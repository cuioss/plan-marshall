# PR #1067 — `feat(extension-api): add derivation-resolver seam with provenance`

epic: review-apparatus · analysed 2026-07-30 · **MERGED** `14:41:54Z`, merge commit `c6b501e6` ·
25 files / 12 commits · evidence: `pulls/1067{,/reviews,/commits}`, `issues/1067/comments` ·
4 issue comments, 12 reviews

> ⚠ **Reported by the operator as "enqueued, watching for it to land". It had already landed** — merged
> `14:41:54Z`. Re-derived at analysis time rather than trusted from the report; the window for anything
> pre-merge was already closed. Same expiry shape as `code-intelligence-substrate-002`, second time today.

## 1. Participation

| Bot | Result | Evidence |
|---|---|---|
| **CodeRabbit** | ✅ **Reviewed — 5 actionable findings** (`COMMENTED`, 13:35:43Z, commit `371854d1`), plus 1 nitpick | `pulls/1067/reviews` |
| **Sourcery** | ⛔ **Refused — diff-size limit** (`COMMENTED`, 11:19:29Z): *"larger than the review limit of 150000 diff characters"* | `pulls/1067/reviews` |
| **PR-Agent** (`cuioss-review-bot`) | ⚠ Guide posted 11:20:37Z, `updated_at` **==** `created_at` — a result, then never touched again | `issues/1067/comments` |

## 2. Verdict

**Scoring: `deficit`, 5 : 0, against a real baseline.** CodeRabbit found 5 genuine defects on the same
diff PR-Agent had already answered; PR-Agent's Guide names none of them. Per `review-practice.md` § 1 the
Guide satisfies must-provide-a-result and is not a defect on its own — **the structural deficit is.**

⭐ **The baseline came from an OPTIONAL bot, and that makes the deficit more pointed, not less.** Config:
`required_bots: "pr-agent"`, `optional_bots: "coderabbit,sourcery"`. So the bot whose silence would have
blocked nothing found five real defects, while the bot the merge gate actually depends on found none.
⛔ **Gating ≠ value** — "optional" means only that its silence never blocks. Nothing here licenses reading
an optional bot's findings as less important; on this PR they were the only findings that existed.

⚠ **But the deficit is confounded and must not be scored naively**: PR-Agent reviewed at 11:20:37Z, and
the branch was **rebased at ~13:10** (every committer date resets to `13:10:10Z`). CodeRabbit reviewed the
*post*-rebase tree. **The two bots did not review the same artifact**, so 5 : 0 overstates the gap. Recorded
as a deficit with the confound named, not as clean and not as a clean 5 : 0.

⭐ **Sourcery's refusal is the SECOND diff-size sighting today** (after `API-Sheriff#133`), verbatim the same
message. Two in one day, both on large PRs ⇒ not a one-off, and correlated with size rather than time.

## 3. Posted answers

✅ **Complete.** Five triage dispositions posted 13:47:58–13:48:07Z, each a thread reply, with CodeRabbit
acknowledging each 13:48:12–13:48:28Z. Plus two batched `## Triage dispositions` PR-level comments
(11:44:23Z, 13:48:09Z). No `untransmitted`, no unjustified `skipped`.

⚠ **The 13:48 CodeRabbit replies are NOT a re-review** — they are replies on `371854d1` threads. Do not
read thread activity on a stale commit as review of a later one.

## 4. Could we have found it ourselves?

Answered for the one finding the evidence settles; the rest need the finding bodies and are owed.

| Finding | Defect | Answer |
|---|---|---|
| `835226` | *"one of those nine"* against a list of eleven | ⭐ **Yes — but by WIDENING, not adding.** `_detect_count_prose` exists and ran; it scans only `SKILL.md` and matches only `operations\|fields\|steps\|rules\|commands`. The finding fell outside both bounds. **This produced the FOURTH answer shape**, now in `review-practice.md` |
| `4f670e` | Hardcoded resolver-count literals — standing lesson `2026-06-25-10-001` | ⛔ **Yes, and worse: the plan's OWN housekeeping had already flagged it as unresolved and it shipped anyway.** Not a detector gap — a **disposition** gap. A flagged-and-unresolved lesson did not block the finalize |
| `3e04a8` | Silent edge-drop ⇒ `status: ok, edge_count: 0, notes: []` | **No — semantic.** Needed reading the merge path and reasoning about what a zero means. The bot's job |
| `8da924` | Truthy non-string id admitted; duplicate ids collapse identity | **No — semantic** (falsiness-vs-type validation intent) |
| `4a4012` | Deferred import outside `try/except ImportError` | ⚠ **Owed.** Plausibly a structural lone-unguarded-call shape; needs the finding body to answer honestly |

⭐ **A defect found while answering the above, in the detector itself**: `_detect_count_prose`'s comment
claims *"``nine checks`` are matched"*, but `checks` is **not** in `_CARDINALITY_NOUNS`. **The count-prose
detector's own documentation is an unverified count claim its code contradicts.** Folded into PLAN-PR-012.

## ⭐ The apparatus finding that outranks the rest

**CodeRabbit's rate-limit window reopened while the plan was blocked on the merge mutex**, and the review
it then gave found 5 real defects — one being the plan reproducing its own target defect.

⛔ **The review was recovered by LUCK.** Had the mutex not blocked, #1067 merged with all five defects and
every signal green.

⇒ A rate-limit refusal is a **deferred review carrying real content**, not an absent one. ⚠ This does not
reopen the retired rate-limit watch — the operator's ruling that rate limiting is expected and is not a bot
malfunction **stands**.

> ⛔⛔ **CORRECTED SAME DAY.** This section first read *"nothing in the apparatus waits for a reopening
> window."* **That was false**, and it was an asserted absence recorded without a `grep` — the
> higher-risk half of the verify-first contract, and the epic's own archetype committed in its own
> findings doc.
>
> **The recovery mechanism is SHIPPED**: `automatic-review`'s opt-in `review_rate_window_await`
> (+ `review_rate_window_timeout_seconds`, default 3600, matched to CodeRabbit's ~hourly reset) claims the
> bot's rate window, paces a bounded wait on the claim's expiry, then **generates** the trigger event —
> and it already splits `awaitable_window` / `hard_quota` / `unknown`.
>
> ⭐ **The true finding is an ACTIVATION question**: `.plan/marshal.json:113` sets
> `"review_rate_window_await": false` (also the default). The recovery was luck **because it is switched
> off**, not because it is missing. Per § 3's third answer shape, ⛔ do NOT compensate with a second
> mechanism. The question to put to the operator is whether to arm it — not what to build.

## ⛔ And the merged tree was reviewed by nobody

Three loop-back commits (`14d4e3dc`, `c4ff2273`, `76c7200b`, 14:03:46–14:04:05Z) fixed CodeRabbit's five
findings. **No review of any kind exists after 13:48:28Z.** The PR merged at 14:41:54Z on `76c7200b`.

⭐ **This is the third sighting of the PLAN-PR-013 shape and the sharpest**: what merged unreviewed was the
**remediation code for a reviewer's own findings** — the highest-risk diff on the PR, written under
loop-back pressure, and systematically the least likely to be reviewed because it arrives after every bot
has had its turn.

## Feeds

- **PLAN-PR-013** — third sighting; the remediation-code observation added to D3.
- **PLAN-PR-008** — the rate-limit-reopening evidence for `refused_awaitable`; second diff-size sighting.
- **PLAN-PR-007** — third rebase sighting; the rebase here is *proven* by the committer-date reset.
- **PLAN-PR-012** — the fourth answer shape (widen, don't add) and the detector's own stale comment.
- **`review-practice.md`** — fourth answer shape recorded.
- **Not ours**: the five code defects belong to the plan's own epic. ⛔ Only the apparatus half is taken —
  the `4f670e` **disposition** gap (a flagged, unresolved lesson shipping anyway) is noted as adjacent and
  is not claimed here.
